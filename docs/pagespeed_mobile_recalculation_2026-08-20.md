# Recalcul Google PageSpeed mobile — 20 août 2026

Le rapport Google PageSpeed mobile a été relancé après propagation de la version `26df751a`, sous l’identifiant `9alkyivs8h`.

La vérification HTTP indépendante confirme que le nouveau bundle `index-DpXUnRsO.js` est distribué par `akademy.neodev.click` et que le logo Anthropic WebP public (716 octets) reçoit désormais `Cache-Control: public, max-age=31536000`.

L’interface Google a généré le rapport, mais son rendu dynamique n’a pas exposé la valeur textuelle du score dans l’extraction navigateur. Le score ne doit donc pas être déclaré amélioré avant une lecture exploitable du résultat Google.
