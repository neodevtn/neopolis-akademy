import fs from "node:fs";
const source=JSON.parse(fs.readFileSync("/home/ubuntu/datacamp_packages/microsoft-copilot-in-word/microsoft-copilot-in-word/COURSE_MANIFEST.json","utf8"));
const path="client/public/data/courses/microsoft_copilot_in_word__01.json",course=JSON.parse(fs.readFileSync(path,"utf8")),removed=new Set(["dc_ch01_act03","dc_ch02_act05","dc_ch02_act06","dc_ch02_act08","dc_ch03_act03"]),byKey=new Map(source.chapters.flatMap(c=>c.activities.map(a=>[`${a.chapter_number}.${a.exercise_number}`,a])));
const rubric=(p,id)=>(p?.match(/<required_elements>\s*([\s\S]*?)\s*<\/required_elements>/i)?.[1]||"").split(/\n(?=\s*\d+\.)/).filter(v=>/^\s*\d+\./.test(v)).map((v,i)=>({id:`${id}_criterion_${i+1}`,label:v.replace(/^\s*\d+\.\s*/,"").split(":")[0].replace(/\*\*/g,"").trim(),description:v.replace(/^\s*\d+\.\s*/,"").replace(/\*\*/g,"").trim(),weight:1}));
for(const lesson of course.lessons){lesson.chapters=lesson.chapters.filter(c=>!removed.has(c.id));for(const chapter of lesson.chapters){const m=chapter.id.match(/^dc_ch(\d+)_act(\d+)$/),a=m&&byKey.get(`${+m[1]}.${+m[2]}`);for(const b of chapter.blocks||[])if(b.type==="cloud_exercise"){const r=rubric(a?.content?.question?.prompt,b.id);if(r.length)Object.assign(b,{rubricCriteria:r,maxScore:r.length,passingScore:r.length,minWords:1,rubricVersion:"datacamp-source-2026-08-28",evaluationPrompt:b.assignment,environmentGuide:{fr:"Utilisez un assistant IA génératif auquel vous avez personnellement accès. Ne partagez ni clé API, ni donnée confidentielle.",en:"Use a generative AI assistant you can personally access. Never share API keys or confidential data."}});}}}
fs.writeFileSync(path,JSON.stringify(course,null,2)+"\n");
console.log(JSON.stringify({activities:course.lessons.flatMap(l=>l.chapters).length,rubricated:course.lessons.flatMap(l=>l.chapters).flatMap(c=>c.blocks||[]).filter(b=>b.type==="cloud_exercise"&&b.rubricCriteria?.length).length}));
