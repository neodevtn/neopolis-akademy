import fs from "node:fs";
const source = JSON.parse(fs.readFileSync("/home/ubuntu/datacamp_packages/ai-for-sales/ai-for-sales/COURSE_MANIFEST.json", "utf8"));
const coursePath = "client/public/data/courses/ai_for_sales__01.json";
const course = JSON.parse(fs.readFileSync(coursePath, "utf8"));
const index = new Map(source.chapters.flatMap(c => c.activities.map(a => [`${a.chapter_number}.${a.exercise_number}`, a])));
const removed = new Set(["dc_ch01_act03", "dc_ch01_act06", "dc_ch02_act05", "dc_ch02_act06"]);
const clean = text => typeof text !== "string" ? text : text.replace(/<br\s*\/?>(?![^<]*>)/gi, "\n").replace(/<\/?(?:p|div|span|ul|ol)>/gi, "\n").replace(/<li>/gi,"- ").replace(/<\/li>/gi,"\n").replace(/<\/?(?:strong|b)>/gi,"**").replace(/<\/?(?:em|i)>/gi,"_").replace(/Microsoft Copilot/gi,"un assistant IA génératif de votre choix").replace(/\bCopilot\b/gi,"l’assistant IA choisi").replace(/\bDataCamp\b/gi,"Neopolis Akademy").replace(/\b\d+\s*XP\b/gi,"").trim();
const deepClean = (v,k="") => typeof v === "string" ? (/(?:id|url|src|path|filename|key|version)$/i.test(k)?v:clean(v)) : Array.isArray(v)?v.map(x=>deepClean(x)):v&&typeof v==="object"?Object.fromEntries(Object.entries(v).map(([key,x])=>[key,deepClean(x,key)])):v;
const rubric = (prompt,prefix) => (prompt?.match(/<required_elements>\s*([\s\S]*?)\s*<\/required_elements>/i)?.[1]||"").split(/\n(?=\s*\d+\.)/).filter(x=>/^\s*\d+\./.test(x)).map((x,i)=>({id:`${prefix}_criterion_${i+1}`,label:x.replace(/^\s*\d+\.\s*/,"").split(":")[0].replace(/\*\*/g,"").trim(),description:x.replace(/^\s*\d+\.\s*/,"").replace(/\*\*/g,"").trim(),weight:1}));
for(const lesson of course.lessons){lesson.chapters=lesson.chapters.filter(c=>!removed.has(c.id));for(const chapter of lesson.chapters){const m=chapter.id.match(/^dc_ch(\d+)_act(\d+)$/);const sourceActivity=m&&index.get(`${+m[1]}.${+m[2]}`);for(const block of chapter.blocks||[]){if(block.type==="cloud_exercise"){const rubricCriteria=rubric(sourceActivity?.content?.question?.prompt,block.id);if(rubricCriteria.length)Object.assign(block,{rubricCriteria,maxScore:rubricCriteria.length,passingScore:rubricCriteria.length,minWords:1,rubricVersion:"datacamp-source-2026-08-28",evaluationPrompt:clean(block.assignment)});block.environmentGuide={fr:"Utilisez un assistant IA génératif auquel vous avez personnellement accès. Ne communiquez ni clé API, ni donnée confidentielle.",en:"Use a generative AI assistant you can personally access. Never include an API key or confidential data."};}Object.assign(block,deepClean(block));}}}
course.exerciseCount=course.lessons.flatMap(l=>l.chapters).filter(c=>c.type==="exercise"||c.type==="quiz").length;
fs.writeFileSync(coursePath,JSON.stringify(course,null,2)+"\n");
console.log(JSON.stringify({activities:course.lessons.flatMap(l=>l.chapters).length,exerciseCount:course.exerciseCount},null,2));
