/* Generated from the protected Architect Foundations exercise corrections.
 * This server-only registry must never be included in learner course payloads. */

export type ArchitectFoundationsCorrection = {
  correction?: unknown;
  rubric?: unknown;
  sampleAnswer?: unknown;
  correctOptionIds?: string[];
};

export const ARCHITECT_FOUNDATIONS_SECURE_CORRECTIONS: Record<string, Record<string, ArchitectFoundationsCorrection>> = {
  "claude_certified_architect_foundations__01": {
    "ex_claude_certified_architect_foundations__01_001": {
      "correction": {
        "en": "1) Evaluate AI suggestions against expertise — concrete criteria\n- Aligns with learning objectives: map each suggestion to a specific objective.\n- Fits the target learner profile: check prerequisites, language level, time available.\n- Pedagogical soundness: matches evidence-based methods (spaced practice, retrieval, worked examples).\n- Cognitive load & sequencing: won’t overload students or skip essential scaffolding.\n- Assessment fit & transfer: supports measurable practice and real-world application.\n- Accessibility & inclusivity: format and examples are culturally and physically inclusive.\n\n2) Decision process — don’t just accept/reject\n- For each suggestion write a one-line verdict plus rationale: “Accept/Adapt/Reject — because…”\n  Example: “Adapt — AI’s project-based module builds transfer but assumes prior Python. Add a preparatory mini-unit on basics.”\n- Record concrete changes you’ll make and why (learning impact, time cost, equity).\n\n3) Use expertise to shape future AI prompts\n- Feed back constraints and outcomes: “Generate a 2-week unit for novice learners with no coding, include scaffolded labs and low-stakes quizzes.”\n- Keep examples of what worked/failed to refine prompts.\n\n4) Recognize and capture unexpected insights\n- Pilot small cohorts; solicit reflection: “What surprised you?” Log novel approaches that improved learning.\n\n5) Diligence & documentation (must-do)\n- Retain final authority: sign-off step before release.\n- Document decisions with a simple template: Item, AI suggestion, Decision (Accept/Adapt/Reject), Rationale, Changes made, Owner, Date.\n- Keep versioned records and a short audit note for student-facing transparency when relevant.",
        "fr": "Critères d’évaluation (actionnables)\n- Alignement avec les objectifs d’apprentissage mesurables (CO: verbes observables, critères d’évaluation).  \n- Adéquation au public cible (pré-requis, niveau, attentes professionnelles).  \n- Charge cognitive & progression (séquencement, fragmentation en micro‑tâches).  \n- Preuve ou sources (références pédagogiques, données d’apprentissage antérieures).  \n- Faisabilité (temps, ressources, accessibilité, éthique).\n\nComment justifier une acceptation/refus\n- Accepter si : alignement clair + amélioration mesurable prévue. Documenter : “Pourquoi” (pédagogie), “Comment” (modifications concrètes), “Métriques” (quizz, taux de complétion).  \n- Rejeter si : conflit avec objectifs, surcomplexité, biais ou risque éthique. Expliquer précisément les défauts et proposer une alternative.  \n- Exemple de réponse concise à l’AI : “Bonne idée pour l’engagement, mais simplifier la terminologie, ajouter un quiz formatif de 5 questions et un exemple concret adapté aux débutants.”\n\nFaçonner de meilleures suggestions futures\n- Fournir dans le prompt : objectifs d’apprentissage, profil apprenant, contraintes (durée, accessibilité), exemples de bonne/pr mauvaise production, rubriques d’évaluation.  \n- Demander des variantes (niveau débutant/intermédiaire) et justifications pédagogiques.\n\nReconnaître et tester les insights inattendus\n- Conserver, prototyper en mini‑module, recueillir feedback pilote et données avant adoption.\n\nDiligence & documentation (template)\n- Champs : Décision | Rationale | Modifs apportées | Preuves | Risques | Date | Responsable.  \n- Exemple : “Inclure scénario client interactif | Augmente application pratique | Ajout d’un guide de 10 min + quiz | Basé sur études X | Risque = temps de production | 2026‑07‑25 | M. Durand.”\n\nRisques concrets à surveiller : sur‑confiance à l’AI, informations inexactes, biais culturels, surcharge cognitive, incompatibilité d’accessibilité, violation de propriété intellectuelle."
      }
    },
    "ex_claude_certified_architect_foundations__01_002": {
      "correction": {
        "en": "1) Start with the big picture (deliverable: 1-page course overview)\n- State 2–4 core learning goals, target learners, time/assessment constraints, and how success will be judged (rubrics/checkpoints).\n\n2) Use the 4Ds as a collaborative thinking routine (not a checklist)\n- Discover: map prior knowledge, relevant concepts, and real-world contexts (create a concept map).\n- Define: translate big-picture goals into 3–6 measurable objectives and key transfer tasks.\n- Develop: brainstorm novel concept connections and student activities that scaffold toward objectives (generate 6–8 activity seeds).\n- Deliver/Deepen: pilot a microlesson, collect rapid feedback, and iterate.\n\n3) Maintain coherence while exploring new ideas (concrete tactics)\n- Anchor every new idea to an objective and an assessment task.\n- Use “boundary objects” (concept maps, exemplar student work, anchor texts) so collaborators share context.\n- Sequence ideas with explicit transitions and scaffolds (pre-activity, guided practice, transfer task).\n- Insert formative checkpoints that probe both fidelity and novelty (quick writes, mini-projects).\n\n4) Build personal competencies through deliberate practice\n- Practice microteaching weekly (5–15 minute lessons), record and self-rate using a short rubric.\n- Keep a reflective log: what connection worked, why, next-step hypothesis.\n- Pair with a peer for targeted feedback cycles (observe, suggest one change, re-run).\n\nConcrete artifacts to produce quickly: 1-page overview, concept map, 3-module pathway with one exemplar lesson, 2 formative assessments, teacher reflection protocol. Focus on enhancement — richer learning and transfer — not just speed.",
        "fr": "Correction modèle — ce que devrait contenir une bonne réponse\n\n1) Cadre 4Ds appliqué (actionnable)\n- Définir les 4Ds comme phases itératives (Discover/Define → Design → Develop → Deploy/Drive) et montrer comment chaque phase devient un point de partenariat cognitif entre expert disciplinaire (contenu, validité) et expert pédagogique (séquençage, activités, évaluation).\n- Exemples concrets de rituels : réunion Discover (30–45 min) pour vue d’ensemble + contraintes, atelier concept-mapping (45–60 min) en Design, prototypes de séquence et tâches d’apprentissage en Develop, pilote micro‑module et collecte de preuves en Deploy.\n\n2) Maintenir la cohérence tout en explorant\n- Méthodes : carte conceptuelle maîtresse (anchor map), alignement explicite Objectifs→Activités→Évaluations, « linking sentences » au début/fin de chaque module.\n- Processus pratique : extraire 3 concepts-ancres par séquence; toute nouvelle idée doit se rattacher à au moins un ancre.\n\n3) Construire et maintenir le contexte\n- Artefact central : document de contexte (1 page) contenant vue d’ensemble, publics cibles, compétences visées, contraintes, hypothèses. Mettre à jour à chaque itération.\n- Techniques : balises temporelles dans le plan de cours, commentaires versionnés, résumé « où en sommes-nous » en tête de chaque module.\n\n4) Développer des compétences personnelles\n- Routines concrètes : journal de réflexion hebdomadaire, 2x/mois séances de teach-back, micro‑objectifs de pratique délibérée (15–30 min/jour).\n- Mesures : progrès via évaluations formatives, auto‑évaluation sur critères précis (clarité des explications, qualité des tâches, feedback).\n\n5) Critères de qualité (mesurables)\n- Amélioration pédagogique démontrée (pré/post, tâches transfer), cohérence (pourcentage d’objectifs alignés), réceptivité des apprenants (feedback qualitatif) et vitesse d’itération (cycles courts).\n\nUne bonne réponse donne ces éléments avec un exemple concret d’application (p. ex. construire une nouvelle séquence sur « résolution de problèmes » : vue d’ensemble → 3 ancres conceptuelles → 3 tâches authentiques → prototype → pilote → révision)."
      }
    },
    "ex_claude_certified_architect_foundations__01_003": {
      "correction": {
        "en": "Suggested 50-minute facilitation plan and expected outputs — focused on enhancement of pedagogy, not just speed.\n\nTiming: Stage 1 (10 min) — Planning approach\n- Clarify course: course title, level, enrollment, modalities, constraints.\n- Specify work needed: redesign module, create week-by-week, generate assessments.\n- Define expertise vs AI help: list teacher-led decisions (values, relationships, grading) vs AI tasks (content scaffolds, drafts, retrieval).\n- Output: one-paragraph scope + responsibility table.\n\nStage 2 (10 min) — Initiating collaboration\n- Share context succinctly (3–4 bullet classroom facts), explain top 2 challenges, and set partnership norms (AI as draft partner; teacher verifies).\n- Example prompt to AI: “Given X course and constraints Y, produce a prioritized list of content gaps and 3 scaffolded lesson starters. Flag uncertain claims.”\n- Output: prioritized challenge list + 3 AI lesson-starters.\n\nStage 3 (20 min) — Build conversation: Description & Discernment\n- Content identification: map core concepts, misconceptions, and prerequisites.\n- Journey mapping: sequence 4–6 learning steps per module with estimated time and formative checks.\n- Learning objectives: write 3–5 SMART objectives per module; align 1 assessment task and rubric criteria to each objective.\n- Use iterative prompts: ask AI to generate alternatives, then request justifications and pedagogical tradeoffs.\n- Output: concept map, sequence chart, objectives + aligned assessment bullets.\n\nStage 4 (10 min) — Documentation & reflection\n- Verify accuracy: cite sources, quick fact-check routine, peer-review checklist.\n- Document decisions and rejections: table with decision, rationale, rejected alternatives and why.\n- Create AI role statement (example): “AI supports drafting, sequencing, and candidate assessments; all materials are teacher-reviewed and adapted to students’ needs; AI must flag uncertainty and provide sources.”\n- Outcome artifact: one-page partnership summary + attachments (objectives, sequence, rejected ideas).\n\nCenter human judgment: keep relational choices, ethical decisions, and final assessments teacher-owned.",
        "fr": "Temps conseillé (50 min) + livrables attendus. Réponse idéale = plan concret, exemples et preuves de contrôle humain.\n\nÉtape 1 — Approche de planification (10 min)\n- Clarifier cours, public, résultats d’apprentissage mesurables (1–3 objectifs SMART).\n- Lister tâches où l’IA apporte valeur pédagogique (ex. feedback formateur, différenciation, modèles d’erreurs) vs tâches purement administratives.\n- Définir expertise requise (enseignant = jugement final ; IA = suggestions, ressources, analyse de données).\nLivrable : tableau tâches ↔ rôle humain/IA.\n\nÉtape 2 — Initier la collaboration (10 min)\n- Script d’ouverture partagé avec l’IA : contexte de la classe, contraintes, défis d’apprentissage.\n- Expliquer défis précis (motivations, lacunes, diversité linguistique).\n- Établir règles : confidentialité, transparence, limites d’intervention.\nLivrable : message contextuel prêt à l’emploi.\n\nÉtape 3 — Construire la conversation (20 min)\n- Identification du contenu clé et séquencement pédagogique (séquence d’activités, points d’évaluation).\n- Cartographie du parcours : activités d’entrée, pratique guidée, feedback, transfert.\n- Objectifs d’évaluation formative alignés sur compétences.\n- Exemples d’outputs IA attendus (ex. questions diagnostiques, rubriques, suggestions de différenciation).\nLivrable : plan de séance + 2 exemples de produits IA (questions, commentaires types).\n\nÉtape 4 — Documentation et réflexion (10 min)\n- Vérifier exactitude factuelle (source-checklist), annoter rejets et pourquoi.\n- Documenter décisions pédagogiques (choix d’une suggestion IA ou refus et justification).\n- Déclaration de rôle IA (exemple) :\n  « L’IA propose exercices et feedbacks ; l’enseignant valide, contextualise et prend la décision pédagogique finale. »\nLivrable : journal de décisions + déclaration de rôle.\n\nRisques et mitigations (intégrés dans chaque étape) :\n- Hallucinations → vérification sources ; limiter usage non référencé.\n- Dépendance → maintien de tâches évaluatives humaines.\n- Biais/inéquité → tester suggestions sur sous-groupes.\n- Confidentialité → anonymiser données étudiantes."
      }
    },
    "ex_claude_certified_architect_foundations__01_004": {
      "correction": {
        "en": "Design for partnership\n1) Define the learning goal and human responsibilities. Specify what judgment, values, relationship-building, and socio-emotional support only teachers will provide (assessment judgments, model thinking, feedback on student identity/voice).\n2) Assign AI a supportive, transparent role. Use AI for time-consuming, low-stakes tasks (generate practice items, summarize student misconceptions, draft feedback options) but require teacher review before use with students.\n3) Co‑design prompts and rubrics. Create reusable prompt templates that include constraints (grade level, cultural sensitivity, evidence-based strategy) and an explicit review checklist for teachers (accuracy, bias, alignment to learning target).\n4) Model and scaffold student use. Teach students how to query AI (show worked examples, ask for sources, critique outputs). Require students to annotate how AI influenced their work and to submit teacher-reviewed revisions.\n5) Build feedback loops. Use quick formative checks: sample AI-generated suggestions, teacher edits, and student responses to iterate prompts and instruction.\n6) Safeguards and equity: require source citations, run bias checks, protect student data, provide alternatives for students who opt out, and monitor accessibility.\nConcrete teacher workflow (example):\n- Goal: Improve thesis writing. Prompt AI: “Create three differentiated thesis-statement scaffolds for a 9th-grade argumentative essay on school uniforms, with evidence prompts and counterargument starters.” Teacher reviews, edits language/values alignment, distributes to students with rubric requiring explicit citation of AI use and one teacher conference.\nSuccess metrics: student metacognition, improved rubric scores, equitable participation, reduced teacher prep time.",
        "fr": "Objectif visé : intégrer l’IA comme partenaire cognitif tout en gardant l’autorité pédagogique, les valeurs et la relation enseignant·e–élève au centre.\n\nActions concrètes\n- Définir rôles clairs : IA = assistant (propositions, analyses, drafts); Enseignant·e = jugement final, adaptation éthique, relation pédagogique.\n- Flux de travail type : objectif d’apprentissage → IA génère ressources, activités différenciées, quiz formatifs → enseignant·e révise, personnalise, valide → mise en classe → collecte de données → IA aide à analyser progrès → enseignant·e ajuste.\n- Exemples d’usage : suggestions de feedback individualisé (brouillon), parcours d’apprentissage adaptatifs, plans de séquence, analyses d’erreurs fréquentes.\n- Prompt modèle (à adapter) : « Pour ce objectif [X], propose 3 activités différenciées (débutant/intermédiaire/avancé), 2 questions diagnostiques et un brouillon de feedback personnalisé pour un élève qui fait [erreur Y]. Indique sources et niveau CECR ou programme. »\n\nRisques concrets et mitigations\n- Sur-dépendance : formation obligatoire + quotas d’usage.\n- Biais/erreurs factuelles : revue humaine systématique + provenance des sources.\n- Vie privée : minimiser données, chiffrement, consentement parentale.\n- Érosion relationnelle : réserver tâches relationnelles (mentorat, entretien) exclusivement aux enseignant·e·s.\n\nMesures d’évaluation\n- Indicateurs : gains d’apprentissage (pre/post), satisfaction enseignant·e/élève, fréquence de corrections humaines, incidents éthiques. Piloter par phases et audits réguliers."
      }
    },
    "ex_claude_certified_architect_foundations__01_005": {
      "correction": {
        "en": "AI Fluency for Educators — model answer\n\nCore idea\nAI Fluency equips teachers to integrate AI into courses so learning is more effective, efficient, ethical, and safe. The course applies the 4D Framework — Delegation, Description, Discernment, Diligence — to concrete classroom practice.\n\nHow each “D” applies (with concrete actions)\n- Delegation: Decide which tasks to give to AI vs. humans. Action: audit course tasks and mark those suitable for AI (e.g., first-draft feedback, quiz generation, rubric-based grading, example creation). Create step-by-step teacher prompts for recurring uses (e.g., “Create three formative quiz items on X aligned to this rubric…”).\n- Description: Require transparency about AI use. Action: ask students to include the prompt they used, the model/version, and a 1–2 sentence rationale for using AI in submissions.\n- Discernment: Teach students and staff to evaluate AI output. Action: provide a fact-check checklist (source verification, cross-referencing, plausibility, bias flags) and classroom exercises where students identify errors and correct AI responses.\n- Diligence: Maintain safety, privacy, and quality. Action: adopt a data-privacy checklist (no PII in prompts), run bias and copyright checks on generated materials, and schedule periodic reviews of models and policies.\n\nWhy this matters (measurable benefits)\n- Matches reality: students already use AI — teaching intentional use reduces cheating and increases learning transfer.\n- Workforce readiness: employers expect AI-harnessing graduates.\n- Institutional risk management: consistent policies reduce legal, privacy, and reputational risks.\n\nImplementation roadmap (practical starter steps)\n1. Map course objectives to AI-appropriate tasks. 2. Draft an AI-use policy and assignment-level instructions. 3. Model prompts in class and require prompt disclosure. 4. Build rubrics that assess higher-order skills AI can’t perform. 5. Monitor outcomes and iterate every term.",
        "fr": "Application concrète du cadre 4D pour l’éducation — correction modèle\n\nDélégation (quoi confier à l’IA / limites)\n- À déléguer : génération de quiz formatifs, synthèses de textes, propositions d’activités différenciées, feedback initial sur brouillons, suggestions de différenciation pédagogique.\n- À ne pas déléguer sans revue humaine : notation sommative finale, décisions disciplinaires, jugements professionnels sur progrès individuel.\n- Action : créer une grille listant tâches + niveau d’autonomie IA (automatique / avec revue / interdite).\n\nDescription (transparence et traçabilité)\n- Obliger la déclaration systématique : modèle/version, prompt utilisé, paramètres, date, rôle de l’outil.\n- Ex. exigence de dépôt : prompt + sortie + sources consultées avant utilisation en évaluation.\n- Action : intégrer un champ “usage IA” dans les soumissions élèves.\n\nDiscernement (évaluation critique des sorties)\n- Enseigner compétences : vérifier sources, repérer hallucinations/biais, croiser informations (≥2 sources fiables), demander justificatifs factuels.\n- Checklist enseignant : exactitude, plausibilité, citation, conformité aux programmes.\n- Activité : exercice où les élèves évaluent et corrigent une sortie IA.\n\nDiligence (sécurité, éthique, conformité)\n- Vérifier confidentialité (consentement, anonymisation, conformité RGPD/Ferpa), SLA fournisseur, risks d’extraction de données.\n- Politique : formation obligatoire pour enseignants, pilotage IT, journalisation des usages, révision annuelle.\n- Rubrique d’évaluation : transparence (20%), exactitude (30%), réflexion critique (30%), éthique/usage responsable (20%).\n\nRisques concrets : hallucinations, biais discriminatoires, fuite de données élèves, surconfiance/paresse cognitive, inégalités d’accès, dépendance fournisseur."
      }
    },
    "ex_claude_certified_architect_foundations__01_006": {
      "correction": {
        "en": "1) Apply the 4D Framework (Discover → Define → Design → Deploy)\n- Discover: run a learner analysis (survey + prior-work audit) to identify gaps and tech access.\n- Define: convert gaps into 3–5 measurable learning outcomes aligned to course vision.\n- Design: create weekly modules that map outcomes → activities → assessments; specify where and why AI is used.\n- Deploy: pilot a module, collect formative data, iterate with classroom observations and student feedback.\n\n2) Use AI for course design & lesson planning (concrete workflow)\n- Prompt example for an LLM: “I teach [course title], enrolls [student profile], 10 weeks. For Week 3 produce a 75‑minute lesson: 3 learning objectives, 3 active-learning activities (one using an AI tool), required materials, formative assessment, and scaffolding for academic integrity.”\n- Use outputs to generate learning-objective/assessment alignment matrix and a reusable lesson template.\n\n3) Create materials that maintain teaching vision & academic integrity\n- Require process artifacts (drafts, annotated AI outputs, reflection logs).\n- Provide explicit AI-use policy on syllabus and assignment pages.\n- Use scaffolded, authentic assessments (projects, portfolios) and randomized prompts to reduce misuse.\n\n4) Lead institutional AI conversations\n- Present pilot data + short policy template to curriculum committee.\n- Offer a 1‑hour faculty workshop with hands-on prompts, rubrics, and an FAQ.\n- Propose an AI governance working group and metrics for impact.\n\n5) Model responsible AI engagement for students\n- Demonstrate prompt design, source-checking, bias checks, and citation of AI outputs in class.\n- Give low-risk practice assignments where students must critique and improve AI-generated drafts.\n\nPrerequisite verification: require certificate from AI Fluency course before enrollment and a brief pre-course skills check (5–10 minute task).",
        "fr": "Correction modèle (objectif : ce que l'étudiant doit livrer)\n\n1) Résultats d’apprentissage mesurables\n- Définir 2–3 objectifs d’apprentissage SMART (ex. « d’ici fin de module, l’étudiant pourra analyser et critiquer 3 sources primaires en utilisant un cadre X »).\n- Indiquer comment l’IA soutient chaque objectif (ex. génération de brouillons, feedback automatisé).\n\n2) Application du 4D (concrètement)\n- Définir : diagnostic des besoins (pré-test, profils apprenants).\n- Concevoir : séquences pédagogiques (activités, évaluations formatives/sommatives).\n- Développer : contenus assistés par IA (ex. fiches, QCM adaptatifs), inclure consignes d’usage de l’IA.\n- Déployer : modalités de déploiement et rétroaction (sprints, débriefings, collecte de données d’apprentissage).\n\n3) Exemple de séance (bref)\n- Thème : critique de sources (90 min). Objectif, activité (analyse en binômes + résumé généré par IA), consigne d’intégrité (annoter toute utilisation d’IA), évaluation (rubrique ci‑dessous).\n\n4) Rubrique (extrait)\n- Compréhension conceptuelle (40%), qualité de l’argumentation (30%), transparence d’usage IA & citations (15%), originalité/éviter paraphrase IA (15%).\n\n5) Modèle de consigne pour étudiant (ex.)\n« Utilisez ChatGPT pour générer un brouillon. Joignez : (a) le prompt exact, (b) les sorties, (c) vos modifications, (d) 2 références vérifiées. »\n\n6) Risques concrets et mitigations\n- Hallucinations → exiger vérification et sources.\n- Plagiat/contournement → tâches à valeur ajoutée (réflexion métacognitive), contrôle par étapes.\n- Biais → diversité de sources + analyse critique.\n- Vie privée/données → interdire données sensibles, utiliser outils institutionnels.\n- Dépendance pédagogique → formation et activités sans IA.\n\n7) Conduite institutionnelle\n- Agenda de réunion : enjeux pédagogiques, exemples de cours, politique d’usage, support formation, critères d’évaluation. Parties prenantes : enseignants, juristes, DPO, étudiants.\n\nUne bonne réponse fournit ces éléments concrets, un exemple de séance, une consigne étudiante réutilisable et des mitigations précises."
      }
    },
    "ex_claude_certified_architect_foundations__01_007": {
      "correction": {
        "en": "Performance Discernment — actively evaluate AI outputs during interaction:\n- Define measurable evaluation criteria up front: accuracy, factuality/hallucination rate, bias disparity across cohorts, relevance, clarity, latency, and safety-rule violations.\n- Build test suites: gold-standard questions, adversarial prompts, edge cases. Run automated checks and periodic human reviews.\n- Use concrete signals: confidence calibration, token-level log probabilities, user thumbs-up/down + free-text corrections, and disagreement detection (model vs. knowledge-base).\n- Actions on failure: label, escalate to human reviewer, correct prompt or model, and add failing case to retraining or prompt-guard lists.\n\nDescription + Discernment (continuous feedback loop):\n- Start: document expected behavior, data sources, limits, and evaluation rubric.\n- During interaction: observe outputs, collect structured feedback, and annotate errors (type, severity, root cause).\n- Close the loop: update prompts/rules, adjust system selection or safety filters, retrain or fine-tune, and redeploy with versioning and changelog.\n- Automate: pipeline for logging → triage → model/prompt update → verification → deployment.\n\nDiligence (responsible AI throughout collaboration)\n- Creation Diligence: run privacy impact/security threat models; prefer on-prem or encrypted APIs for sensitive data; enforce data minimization and retention policies; verify model provenance and licensing.\n- Transparency Diligence: disclose AI assistance clearly (e.g., “This response was generated with assistance from [Model X]”); provide source citations and confidence levels; let users opt out or request human review.\n- Deployment Diligence: assign accountable owner, maintain audit logs, set SLAs and rollback procedures, require human-in-loop for high-risk decisions, run continuous monitoring and incident response plans.\n\nQuick checklist: tests and rubric defined; PIA/threat model done; disclosure visible; human reviewer assigned; logging + feedback loop active.",
        "fr": "Discernement de la performance\n- Mesurer systématiquement : exactitude factuelle (vérifier sources), cohérence logique, exhaustivité, biais détectés, sécurité (ex. génération de contenu dangereux), et confiance/ambiguïté (l’IA signale-t-elle son incertitude?). Utiliser tests unitaires (prompts contrôlés), métriques (taux d’hallucination, précision), et revues humaines régulières.\n\nBoucle description + discernement (feedback continu)\n- Pour chaque interaction, consigner : prompt, réponse, annotations humaines (erreurs, omissions, biais). Transformer ces annotations en actions : ajuster prompt-engineering, changer modèle/paramètres, mettre en place filtres post-traitement, ou réentraîner. Itérer jusqu’à convergence sur critères définis.\n\nDiligence de création (sélection responsable)\n- Critères concrets : conformité RGPD/lois locales, retention minimale des données, chiffrement en transit/au repos, contrôle d’accès RBAC, évaluations de sécurité et tests adversariaux. Choisir modèles adaptés au niveau de sensibilité des données (p.ex. modèle interne pour données sensibles).\n\nDiligence de transparence (divulgation)\n- Indiquer clairement quand une sortie est assistée par l’IA, fournir métadonnées (modèle, version, date), et conserver traces d’édition humaine. Obtenir consentement éclairé si nécessaire.\n\nDiligence de déploiement (responsabilité)\n- Validation humaine obligatoire pour décisions critiques; pipeline de revue, journalisation des décisions, plan d’incident (rollback, notification), et attribution de responsabilité organisationnelle.\n\nRisques concrets : fuite de données, hallucination/mésinformation, biais discriminatoire, dépendance excessive, non-conformité réglementaire.\n\nChecklist rapide : tests & métriques, log complet, politique de confidentialité, divulgation utilisateur, revue humaine obligatoire, plan d’incident, boucle de rétroaction documentée."
      }
    },
    "ex_claude_certified_architect_foundations__01_008": {
      "correction": {
        "en": "Curriculum impact — actionable analysis\n- What AI can reliably automate (examples): routine calculations and data-cleaning; first drafts of essays or code; literature summaries; image segmentation and annotation; standard lab/experiment logging.  \n- Which concepts become more important: problem formulation and framing; model interpretation and limitations; data literacy and bias detection; domain-specific reasoning (proof, diagnosis, design judgment); ethics, provenance, and reproducibility; communication of decisions.  \n- Which concepts become less central (but still taught): memorization of facts, routine arithmetic/manipulation, boilerplate coding patterns, low-level annotation tasks.  \n- How AI changes best practices: require provenance and versioning of AI outputs; teach verification workflows (test-cases, independent replication); move assessment from single right answers to defended processes and synthesis; incorporate AI usage disclosure and evaluation criteria; embed prompt design as a practical skill.\n\nPedagogical impact — concrete actions\n- How AI enhances learning: personalized tutoring (adaptive pacing, targeted hints); rich simulations for unsafe/expensive practice; instant formative feedback on drafts/coding; scaffolding complex tasks.  \n- How AI inhibits learning: enables shortcutting (skip practice), false confidence in unchecked outputs, gaming assignment constraints.  \n- Teacher–student–AI collaboration models: teacher = curriculum designer/assessor and AI-skeptic; AI = formative coach, simulator, feedback engine; student = active investigator using AI as tool. Require students to submit prompt logs, rationale for accepting/rejecting AI output, and a reflection on what they learned.  \n- Effective pedagogical approaches: project-based and competency-based tasks, scaffolded assignments that separate ideation from execution, oral defenses/reflections, adversarial tasks (find AI errors), explicit AI-literacy modules, rubrics that grade process, critique, and provenance.",
        "fr": "Résultat attendu (correction modèle)\n\n1) Ce que l’IA peut automatiser (concret)\n- Recherche documentaire synthétique, génération de brouillons, correction grammaticale de base, tests unitaires, simulation numérique répétitive, annotation de données.\n- Exemple par discipline : maths — génération d’exercices/calculs ; histoire — chronologies et résumés ; médecine — analyses d’imagerie pré-triées ; design — variantes de prototypes.\n\n2) Concepts devenant plus/moins importants\n- Plus importants : pensée critique, validation de sources, interprétation des résultats, éthique, méthodologie expérimentale, maîtrise des prompts et post-édition.\n- Moins prioritaires : exécution mécanique de calculs/routines, mémorisation brute d’informations accessibles via IA.\n\n3) Comment l’IA modifie les meilleures pratiques (actionnable)\n- Réorienter évaluations vers processus : livrables versionnés, journaux réflexifs, épreuves orales et pratiques.\n- Intégrer compétences IA : prompt engineering, évaluation de sortie IA, détection de biais.\n- Mettre en place politiques claires d’usage et traçabilité des contributions IA.\n\n4) Impact pédagogique — amélioration vs inhibition\n- Améliore : tutorat adaptatif, feedback instantané, simulations immersives, différenciation.\n- Inhibe : contournement des apprentissages, dépendance, propagation d’erreurs/biais.\n- Risques concrets : triche invisible, deskilling, biais non détectés, atteintes à la confidentialité, inégalités d’accès.\n\n5) Collaboration enseignant-élève-IA (pratiques efficaces)\n- Rôle IA = assistant: proposer variants, tests, diagnostics ; rôle enseignant = médiateur, validateur, enseignant de métacognition.\n- Exemples d’activités : résolution guidée en classe (IA propose pistes, élève critique), simulations cliniques avec débriefing humain, projets où IA produit artefacts à analyser.\n\nChecklist pour une bonne réponse d’étudiant :\n- Identifier automatisations spécifiques par discipline.\n- Lister concepts à renforcer/atténuer.\n- Proposer modifications concrètes d’évaluation et pédagogies.\n- Dresser plan de mitigation des risques (policy + formation).\n- Donner exemples d’activités et rôles clairs pour enseignant/IA/étudiant."
      }
    },
    "ex_claude_certified_architect_foundations__01_009": {
      "correction": {
        "en": "How AI enhances vs inhibits learning\n- Enhances: provides personalized practice,即时 formative feedback, adaptive scaffolds, simulations for experiential learning, and data-driven remediation.\n- Inhibits/risks: over-reliance on generated answers, superficial/shortcut learning, erosion of metacognitive skills, undetected plagiarism, and biased or incorrect AI outputs that go unchallenged.\n\nBest teacher–student–AI collaborations (concrete roles)\n- Teacher: designs tasks, authenticates assessment, models critical evaluation of AI output, coaches metacognition.\n- Student: formulates queries, critiques AI responses, integrates feedback, documents decisions.\n- AI: offers iterative hints, generates distractors/examples, creates practice problems, simulates interlocutors for debates.\n- Workflow: teacher assigns scaffolded prompts → student queries AI → student annotates/edits AI output → teacher reviews process log + artifact.\n\nEffective pedagogical approaches (actionable)\n- Scaffolded, mastery-based tasks with retrieval practice and interleaving.\n- Inquiry/project-based learning using AI for iteration (not final answers).\n- Teach prompt literacy and source verification as explicit skills.\n- Require iterative drafts with teacher/peer feedback cycles.\n\nAssessment strategies & concrete tools\n- Authentic demonstrations: process portfolios, versioned drafts, annotated AI prompt logs, recorded oral defenses, in-class syntheses, and transfer tasks applied to novel contexts.\n- Shortcut-proof assignments: personalized scenarios, randomized data/specs, staged deliverables (outline → AI-assisted draft with prompts → revised final), hidden test cases, and real-world constraints requiring domain judgment.\n- Value process: grade documented revisions, rationale memos explaining edits, critique of AI errors, and peer review notes.\n- Policy: permit AI use with mandatory disclosure + annotated provenance; assess students’ critical appraisal of AI output (rubric: prompt quality, error detection, substantive revision, justification).\n\nExample assignment template (use immediately)\n- Topic brief: submit (1) outline day 1, (2) AI prompts + AI output log, (3) revised artifact with tracked changes, (4) 3-minute recorded justification of edits and limits found. Graded primarily on critique and revision quality.",
        "fr": "Impact pédagogique — comment l’IA aide ou nuit (concret)\n- Améliore : tutorat adaptatif (ex. exercices à difficulté progressive basés sur erreurs), simulations interactives avec feedback immédiat, feedback automatisé sur productions (grammaire, logique, code).\n- Inhibe : remplace la réflexion par réponses prêtes à l’emploi; entraîne dépendance; propage erreurs/biais non détectés.\n- Risques concrets : surconfiance dans sorties erronées, inégalités d’accès, fuite de données étudiantes, effritement des compétences métacognitives.\n\nMeilleures collaborations enseignant‑élève‑IA (actionnables)\n- Enseignant définit objectifs et critères d’évaluation ; IA fournit pratiques ciblées et feedbacks formatifs.\n- Élève utilise l’IA pour itérer (drafts + prompts) ; rend compte du processus.\n- Session hybride : activité guidée en classe + exercices adaptatifs à la maison + débrief humain.\n\nApproches pédagogiques efficaces\n- Enseignement par objectifs + feedback fréquent ; apprentissage par projet avec artefacts personnels ; activités de métacognition (journal de prompt, justification des choix).\n- Enseigner la littératie des prompts et la validation des sources (fact‑checking).\n\nImpact sur l’évaluation — solutions concrètes\n- Démonstrations authentiques : exposés oraux, vives défenses (viva), portfolios avec journal de production (prompts, versions, timestamps), travaux sur données locales.\n- Devoirs résistants aux raccourcis : tâches qui exigent collecte/observation locale, contraintes temporelles, personnalisation (références à expérience personnelle), livrables intermédiaires obligatoires.\n- Valoriser le processus : rubricage qui note étapes (recherche, plan, itérations), critique des sorties IA, auto‑évaluation.\n- Politiques et pratiques : exigence de déclaration d’usage IA, formation obligatoire aux outils, audits ponctuels des sources, accessibilité équitable.\n\nExemple d’exigence pratique pour un devoir :\n- Soumettre : 1) note de cadrage + données brutes, 2) trois versions avec prompts utilisés, 3) rapport final + réflexion de 300–500 mots sur l’apport/limites de l’IA. Évaluer selon une grille pesant 40% processus, 40% qualité du produit, 20% réflexion critique."
      }
    },
    "ex_claude_certified_architect_foundations__01_011": {
      "correction": {
        "en": "Setup\n- Learning objectives: 1) Define AI fluency and its role in amplifying expertise; 2) Identify when to apply AI tools vs. human judgment; 3) Demonstrate core disciplinary concepts that ground AI use. Assessment level: formative summative (Bloom’s: remember → apply). Academic integrity: honor statement, randomized item order, time limits, plagiarism detection for written items.\n\nQuiz (7 items; varied types). Each item links to objectives; answers + brief explanations follow.\n\nQ1 (MC, recalls Obj 1)\nWhich best defines AI fluency?\nA) Replacing experts B) Using AI to extend expertise C) Coding models only D) Ignoring domain knowledge\nAnswer: B. Explanation: AI fluency emphasizes augmentation, not replacement.\n\nQ2 (True/False, Obj 1)\nAI should be trusted to make high-stakes decisions without human review. False.\nExplanation: High-stakes require human oversight.\n\nQ3 (Short answer, Obj 2)\nList two criteria you’d use to decide between AI vs. human decision-making.\nSample answer: stakes/impact and explainability. Explanation: These map to risk and accountability.\n\nQ4 (Matching, Obj 3)\nMatch disciplinary concept to why it matters for AI (e.g., causality → model misuse prevention).\nAnswers: causality→prevents incorrect inference; measurement validity→ensures reliable inputs.\n\nQ5 (Multiple-select, Obj 2)\nWhen delegating tasks to AI, select appropriate considerations (choose all that apply):\n- Data quality ✓\n- Ethical impact ✓\n- Personal preference ✗\nExplanation: Delegation depends on objective criteria, not preference.\n\nQ6 (Applied short essay, Obj 2+3)\nScenario: AI suggests treatment path conflicting with clinical guidelines. What steps do you take?\nAnswer: Verify data, consult guideline, involve clinician, document rationale. Explanation: Emphasizes disciplinary grounding and human oversight.\n\nQ7 (Problem-solving, Obj 3)\nGiven a dataset with biased sampling, name one mitigation and why it preserves validity.\nAnswer: Re-sampling/weighting to reflect population; preserves external validity.\n\nFinal check actions\n- Verify answer keys and explanations against current literature/standards.\n- Review items for cultural/linguistic bias and ambiguous wording; pilot with diverse learners.\n- Ensure appropriate difficulty via item discrimination statistics after piloting.\n- Accessibility: provide alt text, plain language, keyboard navigation, extended time, screen-reader compatibility.\n- Contingencies: offline PDF, extended window for outages, manual grading fallback, integrity review procedure.",
        "fr": "Exemple de correction modèle pour la Knowledge Check Quiz (7 questions), liée aux objectifs : évaluer compréhension d’AI Fluency, rôle des connaissances disciplinaires, et intégrité académique.\n\nQuestions (types, réponses, brèves explications)\n1. QCM (1 point) — Quel énoncé décrit le mieux « AI Fluency » ? Réponse : B (utiliser l’IA pour amplifier jugement humain). Explication : Met l’accent sur complémentarité, pas remplacement.\n2. Vrai/Faux (1 point) — Les modèles d’IA peuvent remplacer entièrement l’expertise disciplinaire. Réponse : Faux. Explication : Les modèles manquent de contexte et jugement éthique.\n3. Mise en relation (2 points) — Associez : « vérification des sources », « biais culturel », « transparence ». Réponses : vérification→exactitude ; biais→équité ; transparence→traçabilité. Explication : Lie pratiques à risques/attentes.\n4. QCM à mise en situation (2 points) — Un étudiant utilise un résumé d’IA sans attribution; quelle action ? Réponse : C (évaluer, exiger attribution, expliquer politique d’intégrité). Explication : Intégrité + pédagogie corrective.\n5. Question courte (3 points) — Décrivez 2 méthodes pour réduire le biais dans une question de quiz. Réponse attendue : langage neutre, test sur groupes divers, révision par pair. Explication : Concrètes et testables.\n6. Question ouverte à niveau d’analyse (5 points) — Donnez un plan d’évaluation pour une tâche d’IA ouverte (critères, échelle, exemples). Réponse attendue : rubriques claires, critères pour usage d’IA, exemples d’erreurs. Explication : Évalue compétence et jugement.\n7. Item de réflexion rapide (1 point) — Nommer une contingence technique pour quiz en ligne. Réponse : fournir version PDF+option soumission par e‑mail. Explication : mitigation simple.\n\nVérification finale (checklist)\n- Exactitude des réponses vérifiée par expert ; explications concises.\n- Lisibilité & accessibilité : langage simple, alt-text, formats accessibles, temps supplémentaire.\n- Neutralité & biais : phrases pilotées vers groupes divers ; tests pilotes.\n- Difficulté : mélange rappel/compréhension/analyse ; alignement sur objectifs.\n- Intégrité : banques de questions, randomisation, consignes claires sur usage d’IA.\n- Contingences : version offline, procédure de soumission alternative, plan de rattrapage."
      }
    },
    "ex_claude_certified_architect_foundations__01_012": {
      "correction": {
        "en": "Setup\n- Learning objectives: (1) Define AI fluency and its role in augmenting expertise; (2) Explain why disciplinary knowledge remains foundational; (3) Identify human capabilities (judgment, creativity, ethics, relationships) and when to apply them; (4) Apply principles to allocate tasks between humans and AI; (5) Articulate ethical risks and mitigation.\n- Assessment level: comprehension → application (Bloom’s: Understand, Apply, Analyze).\n- Academic integrity: honor code statement, randomized question pool, time limits, require brief written justification for applied questions, use similarity checks and instructor spot-checks.\n\nQuiz (7 varied items — answers + brief explanations)\n\n1) MCQ (Obj.1) — Which best defines “AI fluency”?  \nAnswer: B — ability to use AI tools responsibly to amplify human expertise.  \nExplanation: Focus on amplification and human oversight.\n\n2) True/False (Obj.2) — “AI can replace deep disciplinary knowledge.”  \nAnswer: False.  \nExplanation: AI augments but lacks domain judgment and tacit understanding.\n\n3) Short list (Obj.3) — Name three human capabilities essential when working with AI.  \nAnswer: Judgment, creativity, ethical reasoning (also: interpersonal skills).  \nExplanation: These are uniquely human and required for oversight.\n\n4) Multiple-select scenario (Obj.4) — For drafting a clinical care plan, select appropriate leads: data aggregation (AI), final diagnosis (Human), informed consent discussion (Human), literature scan (AI).  \nAnswer: AI: data aggregation, literature scan. Human: final diagnosis, consent.  \nExplanation: AI speeds evidence synthesis; humans retain responsibility for decisions and patient communication.\n\n5) Matching (Obj.1/2) — Match principle to example (AI Fluency → prompt engineering; Disciplinary knowledge → specialist oversight; Human capabilities → ethics review).  \nAnswers: as listed.  \nExplanation: Shows concrete mappings.\n\n6) Short-answer ethics (Obj.5) — Identify two ethical risks when delegating hiring decisions to AI and mitigation steps.  \nAnswer: Risks: bias in training data → mitigation: fairness audits, diverse training data; lack of transparency → mitigation: explainability tools, human-in-the-loop.  \nExplanation: Specific risks + concrete mitigations.\n\n7) Problem-solving (Obj.4) — Write one-sentence justification for keeping a human in the loop for high-stakes decisions.  \nExpected: Humans provide contextual judgment, moral reasoning, and accountability that AI cannot.  \nExplanation: Tests synthesis.\n\nFinal check\n- Verify factual accuracy and align each question to objectives.\n- Remove ambiguous wording; pilot with 3–5 colleagues and adjust difficulty.\n- Check for cultural/bias issues in scenarios; ensure inclusive language.\n- Calibrate difficulty via item analysis after a pilot (item discrimination, difficulty indices).",
        "fr": "Objectifs d’apprentissage (exemples) :\n- Comprendre les principes clés (niveau Compréhension) — «Décrire les principes AI Fluency».\n- Appliquer des méthodes éthiques (niveau Application) — «Appliquer une checklist d’intégrité pour un cas donné».\n- Évaluer trade-offs techniques/éthiques (niveau Évaluation) — «Comparer trois stratégies et justifier un choix».\n\nApproche d’intégrité académique (précise) :\n- Pools de 30+ questions, randomisation par étudiant, variantes numériques/textuelles.\n- Questions demandant raisonnement/étapes (résultat seul non suffisant).\n- Limite de temps raisonnable + captures métadonnées (sécurité / vie privée équitable).\n- Politique claire (open-book autorisé ou non) et grille d’évaluation explicite.\n\nExemple de quiz (7 questions), réponses et brèves explications :\n1) QCM (concept) — Quel principe décrit AI Fluency ? A: Amplifier l’expertise humaine. Explication: distingue augmentation vs remplacement.\n2) Vrai/Faux — «Les outils IA peuvent remplacer complètement le jugement humain.» Réponse: Faux. Exp: manque créativité/éthique.\n3) Court (procédural) — Citez 2 mesures pour réduire le biais dans un dataset. Rép: équilibrage démographique, audits d’équité. Exp: actions mesurables.\n4) Appariement — Associez: (a) Compréhension (b) Évaluation → (a) Résumer, (b) Comparer/justifier. Exp: verbes Bloom.\n5) Cas pratique (application) — Un étudiant trouve un résultat IA erroné; décrivez 3 étapes à suivre. Rép: vérifier source, recalculer/repérer biais, documenter et signaler.\n6) Calcul / court — Si un modèle a précision 0,9 et biais de 0,2 sur un sous-groupe, quelle action prioritaire ? Rép: auditer/collecter données pour sous-groupe (justification attendue).\n7) Question ouverte (évaluation) — Rédigez 150–200 mots justifiant une politique open-book pour ce module; grille: clarté (30%), preuves (40%), éthique (30%).\n\nVérification finale (checklist actionnable) :\n- Vérifier exactitude scientifique et sources pour chaque item.\n- Relire distracteurs: plausibles mais non trompeurs.\n- Tester lisibilité (niveau langue) et neutralité culturelle.\n- Rechercher ambiguïtés/indices menant à réponse involontaire.\n- Piloter sur 10–20 étudiants, mesurer difficulté et discrimination, ajuster."
      }
    },
    "ex_claude_certified_architect_foundations__01_013": {
      "correction": {
        "en": "Performance Discernment — checklist for judging whether the AI fulfilled the requested role:\n- Accuracy: facts, data, calculations correct; citations present where needed.\n- Relevance & completeness: answered the question, respected scope and constraints.\n- Tone & format: matched role (e.g., professor-level, conversational) and required output style.\n- Safety & ethics: avoided inappropriate content, handled PII properly.\n- Failure detection: no hallucinations, contradictions, or unsupported claims.\n\nDescription–Discernment Loop (iterative steps to apply):\n1. Describe need precisely: role, audience, constraints, required sources, and evaluation criteria.\n2. Generate output.\n3. Evaluate using the Performance Discernment checklist; annotate specific errors or gaps.\n4. Refine description (add constraints, request sources, ask for step-by-step reasoning, correct mistakes).\n5. Repeat until output meets criteria. Log changes and rationale.\n\nDiligence — concrete actions by type:\n- Creation Diligence: select models with privacy/security guarantees; avoid sending PII; verify licensing; run sample prompts to detect bias or unsafe outputs.\n- Transparency Diligence: disclose AI use in reports/emails (e.g., “Section X drafted with model Y”); share model prompts with collaborators/professors as required.\n- Deployment Diligence: verify facts against primary sources; run unit tests or expert review; keep versioned records; take final responsibility for consequences and corrective actions.\n\nExample (brief): draft literature-review paragraph → evaluate citations & accuracy → ask AI to provide primary-source quotes and full citations → verify sources directly → disclose AI assistance in bibliography.",
        "fr": "Performance Discernment — comment l’évaluer (actions concrètes)\n- Vérifier l’alignement sur le rôle demandé : réponse adopte-t-elle le format, le ton et la profondeur exigés ?\n- Tester l’exactitude : valider 3 à 5 faits clés auprès de sources fiables.\n- Mesurer utilité : l’output permet-il d’accomplir la tâche sans travail supplémentaire majeur ?\n- Signaler indicateurs de non-fiabilité : hallucinations, contradictions, omissions importantes.\n\nDescription‑Discernment Loop (processus itératif pratique)\n1. Décrire précisément le besoin (objectif, format, contraintes, exemples attendus).\n2. Évaluer l’output selon critères mesurables (précision, exhaustivité, style).\n3. Raffiner la description en ciblant défauts observés (ex. : “ajoute méthodes et chiffres”, “réduis à 150 mots”).\n4. Répéter jusqu’à satisfaire les critères.\nExemple : initial “Résume ce papier.” → évaluation : manque méthodes → raffinement “Résume en 150 mots : contexte, objectifs, méthodes (2 phrases), résultats chiffrés, limites.”\n\nDiligence — actions par type\n- Creation Diligence : choisir modèles avec politiques de confidentialité claires, minimiser données sensibles, préférer instances privées ou offres on‑premise, consigner sélection et raisons.\n- Transparency Diligence : déclarer l’usage d’IA (qui, quel modèle, quel rôle), inclure note méthodologique dans livrables, obtenir permissions nécessaires (professeurs, clients).\n- Deployment Diligence : vérifier et sourcer chaque assertion importante, ajouter disclaimers sur incertitude, conserver journal des prompts/versions, corriger erreurs et assumer responsabilité finale.\n\nRisques concrets à surveiller : fuite de données sensibles, hallucinations factuelles, biais reproductibles, violation de propriété intellectuelle, dépendance excessive à l’IA."
      }
    },
    "ex_claude_certified_architect_foundations__01_014": {
      "correction": {
        "en": "Skill development plan (marketing manager → customer survey analysis with Excel pivot tables)\n- Learning goal: Be able to ingest survey CSV, create pivot tables to summarize NPS/CSAT, segment by demographics, and produce 3 actionable insights and a one-slide recommendation.\n- Week-by-week practice: Week 1: intro pivot tables (grouping, filters, calculated fields). Week 2: segmentation and cross-tabs. Week 3: deriving metrics (averages, % distributions). Week 4: storytelling and export to slides.\n- Example AI coach prompts:\n  - “Generate a realistic customer survey CSV (200 rows) with columns: respondent_id, age_group, region, product_usage_freq, satisfaction_1–10, would_recommend (yes/no), open_comments.”\n  - “Create 5 practice tasks using that dataset (e.g., show avg satisfaction by region and top 3 low-satisfaction segments).”\n  - “Review this CSV/pivot screenshot and give 3 specific improvements: formula fixes, better grouping, and visualization suggestions.”\n\nAI as roleplay coach and feedback engine\n- Roleplay prompt: “Act as a senior analyst interviewing me: ask 8 scenario-based questions about segmenting low-NPS cohorts and expect me to explain pivot-table steps and interpretation.”\n- Feedback prompt: “Here’s my pivot export (CSV). Point out errors, suggest alternative calculated fields, and give a concise verbal summary I can use in a 60‑second stakeholder update.”\n\nActive learning checks\n- Biweekly mini-project: full pipeline (clean → pivot → 3 insights → slide). Grade rubric: correctness of pivots, clarity of insights, actionable recommendations.\n- Self-check questions: Did I reproduce the analysis without AI help? Can I teach the steps to a colleague?\n\nJob-search rule of thumb and tasks\n- Rule: Use AI for brainstorming, practice, and draft materials; present an authentic, human final product.\n- AI tasks (example prompts): “List 8 transferable skills from a marketing manager to an analytics role”; “Simulate 10 interview questions for a marketing-analytics hybrid role and score my answers”; “Suggest unique cover-letter angles tying survey-analysis projects to business outcomes.”\n- Human responsibilities: verify technical accuracy, state career goals, preserve authentic voice in resumes/cover letters, own final interview responses and decisions.",
        "fr": "Besoin d’apprentissage réécrit (exemple ciblé) :\n« Responsable marketing souhaitant acquérir des compétences en analyse d’enquêtes clients à partir de tableaux croisés dynamiques Excel pour améliorer la segmentation et les recommandations produit. »\n\nAI comme coach — scénarios et opérations concrètes :\n- Scénarios d’entraînement : nettoyage de données (jeu de données X), construction de tableaux croisés (questions A vs B), interprétation des écarts (% vs base).\n- Exemples d’exercices : créer 5 tableaux croisés, résumer 3 insights actionnables, proposer 2 recommandations marketing.\n- Feedback sur échantillons : fournir un échantillon d’analyse (Excel exporté ou CSV) et demander à l’AI un retour structuré (précision des pivot, choix des filtres, hypothèses non testées).\n- Mises en situation (role-play) : simulation de présentation executive de 5 minutes avec objections à gérer.\n\nVérification de l’apprentissage actif (mesurable) :\n- Indicateurs : temps pour produire un tableau croisé, # insights valides identifiés, score sur un quiz de concepts (pré/post), qualité d’une présentation notée selon une grille (clarté, validité, actionnabilité).\n- Rythme : mini-évaluations hebdomadaires + projet final réel (analyse complète d’une enquête) évaluée par pair ou mentor.\n\nCandidatures — mode d’usage AI + prompts exemples :\n- Rôle AI : brainstorming, identification compétences transférables, mock interviews, brouillons de lettres.\n- Prompt brainstorming (ex.) : « À partir de mon CV [insérer], liste 8 compétences transférables vers le poste X et propose 3 formulations pour la lettre de motivation. »\n- Prompt entretien (ex.) : « Joue l’intervieweur pour un poste marketing senior ; pose 10 questions comportementales techniques et fournis feedback sur mes réponses (format: points forts / points à améliorer). »\n\nResponsabilités humaines :\n- Valider l’exactitude factuelle, personnaliser le ton, conserver l’identité professionnelle, finaliser et relire les documents. Risques à surveiller : surconfiance dans l’AI, erreurs factuelles, fuite de données sensibles."
      }
    },
    "ex_claude_certified_architect_foundations__01_015": {
      "correction": {
        "en": "Tip: in 150–200 words include title, years experience, top skills, 2–3 measurable achievements, career goal, preferred roles/industries, work values, constraints, and one short authentic anecdote.\n\nExample background to share with the AI:\nI’m a Product Manager with 6 years’ experience at two SaaS startups and one mid‑size company. I specialize in user research, roadmapping, and data-driven prioritization. Key achievements: led a mobile app launch that grew MAU by 40% in 9 months; reduced onboarding time by 35% through a redesigned tutorial; cut feature development cycle by 25% by implementing lightweight A/B testing. Transferable skills: cross-functional leadership, qualitative & quantitative research, stakeholder communication, metrics-driven decision making. Career goal: move into Senior Product Manager roles in healthcare tech or education technology, focusing on patient/student-facing analytics and privacy-first design. Values: user empathy, measurable impact, and transparent collaboration. Constraints: open to remote or hybrid roles in the US, target compensation $120–140k, prefer 50–60% PM time vs. ops. Anecdote: I convinced engineering to prototype a low-cost experiment after seeing user confusion in a demo; the experiment identified a 12% churn driver and guided a fix. Please identify transferable skills, 5 target roles, unique cover-letter angles, and 8 interview questions to practice.",
        "fr": "Points clés qu’un bon partage de contexte (15 min) doit contenir — checklist actionnable\n- Identité pro courte : poste actuel, années d’expérience, secteur.\n- Objectif clair : postes ciblés, industries, niveau (junior/senior/management).\n- Compétences techniques & outils + niveaux (ex. Python — avancé).\n- Compétences transférables précises (ex. gestion d’équipe, analyse de données).\n- 3 réalisations mesurables (chiffres, impact, ex. réduit coûts de 12 %).\n- Contraintes/préférences : lieu, télétravail, salaire cible, mobilité.\n- Lacunes à combler (compétences ou expériences manquantes).\n- Documents disponibles/à améliorer : CV, LinkedIn, lettre, portfolio (liens).\n- Style/voix souhaités (formel, direct, storytelling) et degré d’IA (brainstorm only / drafts only).\n- Résultat attendu en 15 min (ex. mapping compétences, 3 bullets COSMÉTIQUES, 5 Q d’entretien).\n\nExemple de message à envoyer à l’AI (copier-coller)\nBonjour — voici mon contexte : Je suis Product Manager, 6 ans d’expérience en SaaS B2B. Compétences : gestion produit (strat/roadmap), SQL (intermédiaire), OKR, leadership d’équipes cross‑fonctionnelles. Réalisations : augmenté ARR de 18 % en 12 mois ; réduit churn de 2 pts ; lancé 4 fonctionnalités générant $300k/an. Je vise PM Senior en scale‑up (Europe), salaire cible €80–95k, open remote partiel. Manques : expérience directe en ML produit et metrics growth avancés. J’ai joint mon CV (ou lien). En 15 min, fournissez : 1) carte des compétences transférables avec gaps prioritaires, 2) 3 bullets de CV réécrits (ton direct), 3) 5 questions d’entretien à pratiquer + 2 variantes de réponses STAR. Priorisez authenticité — je finaliserai les documents.  \n\nRisques à noter : éviter phrases génériques, vérifier chiffres/époques, neutraliser langage trop “template” qui alerte les recruteurs."
      }
    },
    "ex_claude_certified_architect_foundations__01_016": {
      "correction": {
        "en": "Role analysis (30s): identify 3–5 core competencies the role requires (e.g., delivering results, cross‑functional leadership, technical/product judgment, stakeholder management, metrics-driven decision making). Use those to craft realistic behavioral and situational questions.\n\nFive realistic interview questions (with follow-ups)\n1) Tell me about a time you led a project from concept to launch.\n   - Follow-ups: target metrics, timeline, trade‑offs, blockers, team size, what you owned.\n2) Describe a hard technical/design decision you made.\n   - Follow-ups: alternatives considered, data used, risks, post‑launch outcome.\n3) How do you prioritize competing requests?\n   - Follow-ups: frameworks you use, example, stakeholder alignment, impact measurement.\n4) Give an example of resolving a conflict with engineering/design/marketing.\n   - Follow-ups: communication steps, outcome, what you’d change.\n5) Describe a failure and what you learned.\n   - Follow-ups: root cause, corrective actions, how you prevented recurrence.\n\nSample Q1 interaction (one question at a time)\nQuestion: Tell me about a time you led a project from concept to launch.\nGood answer (STAR, 60–90s): “Situation: Our churn rose 8% in Q3. Task: I led a cross‑functional team to design a retention flow. Action: I scoped MVP in 3 weeks, A/B tested 2 variants, coordinated engineers, and prioritized fixes using impact/effort. Result: Variant B reduced 30‑day churn by 12% and increased NPS by 6 points within 8 weeks.”\n\nFeedback (content/clarity/relevance)\n- Content: Strong—includes ownership, concrete metric, timeframe. Add exact team size, your title, tools used (A/B platform), and one trade‑off you made.\n- Clarity: Well‑structured STAR; keep each part one sentence. Avoid jargon.\n- Relevance: Tie outcome to business goal (revenue saved or ARR impact).\nCoaching tips: Prepare 3–4 STAR bullets per competency, quantify results, name stakeholders, and practice flexible storytelling rather than memorized scripts.",
        "fr": "Correction modèle — ce qu’une bonne réponse devrait produire\n\n1) Analyse rapide du rôle (1–2 phrases) : skills clés, niveau d’autonomie, principaux deliverables. Ex. « Rôle : chef de produit technique — priorisation roadmap, coordination eng./design, KPIs de croissance. »\n\n2) Cinq questions réalistes (thèmes) — une à la fois, chacune avec relances :\n- Q1: Parlez d’un lancement produit majeur que vous avez dirigé.\n  Relances: Quel était votre rôle exact? Quels KPIs cibliez-vous? Quels obstacles et comment les avez-vous levés?\n- Q2: Donnez un exemple où vous avez pris une décision impopulaire.\n  Relances: Données utilisées? Résultat mesurable?\n- Q3: Décrivez un problème technique complexe résolu.\n  Relances: Options évaluées? Trade-offs?\n- Q4: Racontez un conflit d’équipe et votre intervention.\n  Relances: Méthode de résolution? Leçon retenue?\n- Q5: Pourquoi ce poste et comment vous contribuez au premier trimestre?\n  Relances: Priorités immédiates? Metrics d’impact?\n\n3) Entraînement avec exemples concrets : pour Q1, modèle de réponse courte (STAR) :\n« Situation: produit X stagnait; Tâche: augmenter MAU de 20% en 6 mois; Action: refonte onboarding + A/B tests; Résultat: +28% MAU, +15% rétention. »\n\n4) Retour structuré (contenu/clair/pertinence) — checklist actionnable :\n- Contenu : fournir chiffres, rôle précis, actions reproductibles.\n- Clarté : phrases courtes, terme concret, éviter jargon vague.\n- Pertinence : relier chaque exemple aux responsabilités du poste.\n\n5) Consignes d’usage : poser la question, laisser candidat répondre, relancer 2x, puis donner feedback immédiat et une suggestion d’amélioration. Scorez sur 1–5 : impact chiffré, clarté, alignement poste. Rappel : viser authenticité, pas de script mémorisé."
      }
    },
    "ex_claude_certified_architect_foundations__01_017": {
      "correction": {
        "en": "Five role-focused interview questions (example for a mid-level product manager):\n1) Tell me about a time you led a cross-functional project to launch a feature.\n2) Describe a difficult technical tradeoff you recommended and why.\n3) How do you prioritize competing stakeholder requests?\n4) Give an example when a project missed goals — what did you do?\n5) How do you measure and report feature success?\n\nInteraction plan (one question at a time):\n- Ask Q1; prompt candidate to answer with a specific example.\n- Follow-ups (examples): “What was the goal/metric?” “Who were the stakeholders?” “What concrete actions did you take?” “What was the outcome (numbers)?” “What would you do differently?”\n\nFeedback rubric (after each answer):\n- Content: Use STAR (Situation, Task, Action, Result). Include concrete actions and quantitative outcomes (%, time saved, ARR, user growth).\n- Clarity: Structure answer (30–90 seconds). Avoid vague language; name teams/tools.\n- Relevance: Tie to job description skills (roadmapping, stakeholder mgmt, analytics).\n- Improvement suggestions: Add metric(s), shorten background, emphasize leadership/decision criteria, include a brief “lesson learned” and next steps.\n\nExample brief feedback for Q1:\n- Strengths: Clear goal, named stakeholders.\n- Weaknesses: No metrics; action list is vague.\n- Revision tip: “Launched X feature to reduce churn by 10%; I led roadmap, set OKRs, ran 3 A/B tests; result: churn -12% in 8 weeks.”",
        "fr": "Correction modèle — ce que doit contenir une bonne réponse\n\n1) Analyse du rôle (concrète) : identifier 5 compétences-clés du poste et pourquoi elles génèrent ces questions (ex. management d'équipe → question sur résolution de conflit ; product-market fit → question sur métriques).\n\n2) Liste de 5 questions réalistes (avec justification courte) :\n- Parlez d’un projet où vous avez priorisé des fonctionnalités contradictoires. (évalue priorisation, trade-offs)\n- Décrivez un conflit d’équipe et comment vous l’avez géré. (leadership, communication)\n- Comment mesurez-vous le succès produit ? Donnez un exemple chiffré. (data-driven)\n- Racontez une erreur technique et ce que vous avez appris. (humilité, apprentissage)\n- Comment intégrez-vous l’IA dans vos process métier ? (vision stratégique, maîtrise IA)\n\n3) Interaction “une question à la fois” : modèle d’invite pour l’IA :\n« Tu joues l’intervieweur pour le poste X. Pose Q1, attends la réponse, propose 2 relances adaptées (ex. demander détails chiffrés, rôle précis), puis fournis un feedback structuré sur contenu/clarité/pertinence (2-3 phrases). Passe à Q2 uniquement quand je dis “suivant”. »\n\n4) Exemple d’entraînement (format) :\n- Question : « Parlez d’un projet où… »\n- Réponse candidate (ex.) : « J’ai réduit le backlog de 30 % en 3 mois en… »\n- Feedback modèle : Contenu: bon exemple quantifié ; Clarté: précisez votre rôle exact ; Pertinence: lié à priorisation — mentionnez impact business.\n\n5) Retours attendus : le feedback doit pointer forces/faiblesses actionnables (ajouter chiffres, préciser influence, expliquer décision), suggérer reformulations courtes.\n\n6) Risques concrets : sur-mémorisation de scripts, réponses génériques, fuite d’informations sensibles dans prompts, dépendance à l’IA au lieu de réflexion propre.\n\n7) Comment montrer la maîtrise de l’IA : expliquer brièvement votre workflow IA (outils, prompts, validation humaine) et donner 1 exemple concret d’amélioration métier.\n\nCe format est actionnable, permet pratique itérative et respecte la consigne d’authenticité (éviter scripts)."
      }
    },
    "ex_claude_certified_architect_foundations__01_018": {
      "correction": {
        "en": "How to run a practice session (one question at a time)\n1) Ask a single interview question and give one specific example from your experience.\n2) Get targeted feedback (content, clarity, relevance).\n3) Use a single follow-up to deepen or reshape the example, then iterate.\n\nConcrete example exchange\n- You (Q): “Tell me about a time you led a cross‑functional project.” Then give one 90–120s example: role, goal, specific actions, metrics, outcome.\n- AI (feedback): Content: Add the project’s budget and timeline, name 2 stakeholders, state your exact role. Clarity: Remove jargon (“stakeholder alignment” → “coordinated weekly syncs”). Relevance: Tie outcomes to the job’s top requirement (e.g., reduced time-to-market by 20%). Suggest a 60–90s condensed version for screening calls.\n- You (follow-up): “Shorten to a 60s answer that emphasizes leadership under a tight deadline.” AI returns revised script and two alternative phrasing options.\n\nPractical rules to stay authentic\n- Don’t memorize word-for-word responses; internalize facts and key phrases, then practice natural delivery.\n- Keep personal details (names, exact numbers, decisions) so answers can’t be generic AI outputs.\n- Run variations aloud: change tone, tempo, or emphasis to sound human.\n\nTrack and disclose AI use\n- Log: date, prompt, AI suggestions used (Y/N), which lines were adopted.\n- Disclosure template: “I practiced responses using AI coaching to clarify examples and phrasing; my answers reflect my own experience.” \n\nHighlight AI fluency on your resume\n- Example line: “Skilled in leveraging generative AI for rapid iteration of communications and interview preparation; maintained audit log of AI-assisted edits.”",
        "fr": "Objectif attendu — ce que l’étudiant doit produire :\n- Conduire des sessions de pratique en posant « une question à la fois » et en enchaînant des relances naturelles ; utiliser des exemples concrets (projets, chiffres, situations comportementales) ; demander à l’IA un retour précis sur contenu/clarté/pertinence.\n\nChecklist actionnable (modèle de bonne réponse)\n1. Structure de la session :\n   - Prompt initial à l’IA : “Agis comme recruteur pour un poste X. Pose une question comportementale. J’y réponds brièvement. Puis pose une seule relance adaptée à ma réponse.”\n   - Exemple pratique : répondre à “Parlez-moi d’un projet où vous avez résolu un conflit d’équipe” puis relance : “Que feriez-vous différemment aujourd’hui ?”\n2. Feedback attendu de l’IA (format précis) :\n   - Contenu : exactitude des faits, résultats chiffrés manquants\n   - Clarté : structure (situation, tâche, action, résultat), langage simple\n   - Pertinence : correspondance avec le rôle; suggestions de reformulation\n   - Authenticité : signes de réponse trop scriptée; alternatives plus naturelles\n3. Exemples de corrections concrètes :\n   - Remplacer “J’ai amélioré le process” par “J’ai réduit le délai moyen de livraison de 12 à 8 jours (−33 %) en…”\n   - Proposer 2 formulations courtes pour rester naturel.\n4. Risques à couvrir :\n   - Mémoriser des scripts et paraître artificiel\n   - Révéler des données sensibles dans les prompts\n   - Ne pas déclarer l’assistance IA quand requis\n5. Traçabilité & transparence :\n   - Tenir un journal : date, question, réponse initiale, feedback IA, version finale, usage en entretien.\n   - Règle simple de transparence : mentionner l’aide IA pour CV/lettres ou si l’employeur le demande.\n\nCritère d’évaluation :\n- Réponses ancrées sur des preuves chiffrées, révisées via l’IA pour clarté, restent authentiques et consignées dans un log."
      }
    },
    "ex_claude_certified_architect_foundations__01_019": {
      "correction": {
        "en": "Use AI to prepare — not to replace your voice. Practical steps:\n\n1. Turn experiences into editable notes, not scripts\n   - Extract 3–5 STAR bullets per role (Situation, Task, Action, Result).\n   - Ask AI to shorten/clarify each bullet, then edit aloud until it sounds like you.\n\n2. Practice flexibly, don’t memorize\n   - Generate 10 mock interview prompts and record multiple, varied answers.\n   - Focus on key points and transitions so you can respond naturally to follow-ups.\n\n3. Demonstrate AI fluency as a skill\n   - On résumé or LinkedIn: “Uses AI tools (ChatGPT, Claude) for research, draft refinement, and automation.”\n   - In interviews: describe concrete ways you used AI to save time or improve outcomes.\n\n4. Track and disclose AI assistance appropriately\n   - Keep a simple log: date, prompt, AI output summary, your edits, and sensitivity level.\n     Example: “2026-07-01 — Prompt: refine product manager STAR — Output: 3 bullets — Edits: replaced metrics with Q2 revenue numbers — Sensitivity: non-confidential.”\n   - Disclose when content was co-created (e.g., portfolio copy, cover letters) or if employer policy requires.\n\n5. Mind confidentiality and authenticity\n   - Never feed proprietary/company-sensitive data into public AI services.\n   - Use AI to clarify and polish; ensure final answers reflect your direct experience and judgment.\n\nAuthentication Principle: AI helps make your story clearer — you remain the author.",
        "fr": "Correction modèle — ce que doit contenir une bonne réponse\n\n1) Comportement concret avec l'IA\n- Utiliser l'IA pour générer « talking bullets » (points clés) et variantes de réponses, pas des scripts mot à mot.\n- Pratiquer avec variations de questions et relances pour s'entraîner à répondre de façon authentique et adaptable.\n- Exercice pratique : produire une version courte (30–60s) et une version longue (2–3 min) de la même expérience en style STAR, puis reformuler oralement sans lire.\n\n2) Transparence et traçabilité\n- Tenir un journal simple : date, prompt utilisé, sortie de l'IA, modifications effectuées. Conserver captures ou fichiers.\n- Formulations de divulgation acceptables :\n  - Sur CV/portfolio : « Rédaction et synthèse assistées par IA ; contenu revu et validé par mes soins. »\n  - En entretien : « J’ai utilisé un outil d’IA pour structurer ma réponse, mais l’expérience et les résultats sont bien les miens. »\n\n3) Valoriser la compétence IA\n- Décrire outils et rôle (p.ex. “utilisé pour synthèse, simulation d’entretien, automatisation d’analyses”).\n- Donner exemples mesurables (temps gagné, erreurs évitées).\n\n4) Risques concrets à mentionner\n- Surdépendance / réponses non authentiques\n- Hallucinations factuelles de l’IA\n- Problèmes de confidentialité si données sensibles soumises\n- Impression négative si on ne divulgue pas l’aide reçue\n\nChecklist finale — une bonne réponse couvre : pratiques d’usage concrètes, exemples d’exercices, méthode de traçabilité, phrases de divulgation, façon de présenter la compétence IA, et identification des risques."
      }
    },
    "ex_claude_certified_architect_foundations__01_020": {
      "correction": {
        "en": "Section 1 — Usage Boundaries\n- Appropriate uses\n  - Practice and exploration: generate practice problems, worked examples, alternate explanations to deepen understanding.\n  - Building understanding: summarize concepts, create study plans, produce annotated examples or step-by-step derivations.\n  - Drafting and ideation: brainstorm approaches, outlines, or hypotheses that you will verify and adapt.\n- When to work independently\n  - Formal assessments (exams, timed quizzes, graded problem sets).\n  - Demonstrations of mastery: capstone projects, in-person presentations, or any work meant to prove your individual skill.\n  - Original thinking where authorship/ownership matters (published research, proprietary designs).\n- Professional contexts where AI adds value\n  - Drafting clear emails, meeting summaries, and documentation.\n  - Rapid literature or codebase summaries, generating test cases, refactoring suggestions, and exploratory data analysis prototypes.\n  - Creating templates, checklists, or accessibility improvements.\n- Red lines (absolute no-use)\n  - Never use AI to answer exam questions or complete graded assignments you must do alone.\n  - Never input sensitive, proprietary, or personal data into AI.\n  - Never present unverified AI output as final or original without validation and attribution.\n\nSection 2 — Learning Applications\n- Use AI as a tutor/coach: ask for hints, Socratic prompts, stepwise explanations, error diagnosis, and practice quizzes — avoid requests for final answers.\n- Maintain explainability: before submitting any work influenced by AI, produce a short annotated note: what the AI contributed, what you changed, and a line-by-line explanation showing you can reproduce and justify the result.",
        "fr": "Section 1 — Frontières d’utilisation (règles précises)\n- Quand utiliser l’IA : tâches pratiques (formatage, synthèse de documents, génération d’idées, prototype de code), exploration conceptuelle, révision/feedback sur brouillons. Exigez toujours vérification humaine pour résultats techniques ou factuels.\n- Quand travailler indépendamment : examens/évaluations, rendu final d’un travail académique sans mention d’aide, démonstration de maîtrise (présentations orales), production d’idées originales revendiquées.\n- Contextes professionnels à valeur ajoutée : préparation de rapports/briefs, extraction et structuration de données, support client (réponses draftées + relecture humaine), aide à la rédaction juridique/contractuelle (brouillons contrôlés), revue de code/optimisation, traduction technique, accessibilité (résumés, alt-text).\n- Lignes rouges (ne jamais utiliser IA) : traitement non révisé d’informations médicales/diagnostiques cliniques, décisions juridiques contraignantes sans avocat, données sensibles/PPI/PII non anonymisées, code de sécurité critique déployé sans audit, réponses en contexte d’examen, manipulation d’informations classifiées.\n\nSection 2 — Applications pédagogiques et preuves de compréhension\n- Rôle : tuteur/coach. Procédure exigée : essayer d’abord soi-même, demander à l’IA explications pas seulement réponses, exiger étapes détaillées et sources.\n- Obligation de transparence : documenter prompts et versions, indiquer l’aide IA dans la soumission.\n- Preuve de maîtrise : inclure une section “Je redis en mes mots” expliquant chaque point fourni par l’IA; joindre test ou démonstration où l’étudiant reproduit ou étend la solution.\n- Checklist avant soumission : ai‑usage déclaré, sources vérifiées, explication personnelle complète, données sensibles exclues/anonymisées, relecture humaine et tests (le cas échéant)."
      }
    },
    "ex_claude_certified_architect_foundations__01_021": {
      "correction": {
        "en": "Section 4 — Skill maintenance (practical, actionable)\n- Core skills to practice without AI: mental math/problem solving, domain knowledge recall (key concepts, definitions, formulas), writing clear outlines by hand, coding small functions from memory, presenting/teaching a topic aloud, reading and summarizing primary sources.\n- Regular “AI‑free” work intervals (examples):\n  - Daily: two 45–90 minute AI‑free focus blocks (morning and afternoon).\n  - Weekly: one full “no‑AI” day for deep practice or creative work.\n  - Monthly: one week of reduced AI use (e.g., only for admin tasks).\n- Methods to verify continued learning:\n  - Timed quizzes or flashcards (Anki), tracked scores over time.\n  - Peer reviews and live coding/presentation tests without aids.\n  - Teach‑back sessions where you explain a topic to a colleague.\n  - Maintain a log of tasks completed unaided and compare speed/accuracy metrics.\n- Warning signs\n  - Over‑reliance: frequent inability to start/complete tasks unaided, degraded recall, unquestioning acceptance of AI outputs, reduced creativity.\n  - Under‑utilization: repetitive manual tasks, missed productivity gains, increased time-on-task, burnout.\n  - Remediation: deliberate practice plans, mentorship, rotating AI use, staged reintroduction of tools, and periodic competency tests.\n\nSection 5 — Ethical guidelines (concrete responsibilities)\n- Accuracy and fact‑checking:\n  - Verify claims against primary/authoritative sources before use; cite them.\n  - Keep an audit trail (what source, when checked); note uncertainty levels.\n  - Correct public errors promptly and transparently.\n- Respect for others’ work and ideas:\n  - Attribute sources and follow licenses; never present others’ work as your own.\n  - Obtain permission before using proprietary or private content.\n  - Avoid decontextualizing summaries; preserve original meaning and credit contributors.",
        "fr": "Section 4 — Maintenance des compétences (pratiques concrètes)\n- Compétences à pratiquer sans AI : diagnostic de problèmes, formulation de questions, raisonnement logique, codage à la main, prise de décision éthique, explication pédagogique à un novice.\n- Intervalles réguliers sans AI : 2–3 blocs hebdomadaires de 60–90 min (ou 4×25 min Pomodoro) dédiés à tâches sans AI ; 1 journée complète par mois “no-AI” pour projet intégral. Maintenir 30–50 % du temps sur tâches critiques sans assistance.\n- Méthodes pour vérifier l’apprentissage : tests récurrents (quiz hebdo), spaced repetition (Anki), teach-back (expliquer à pair qui n’a pas utilisé AI), revues de code/documentation en pair-review mensuelle, livrables réels (mini-projets) évalués contre une grille de compétence.\n- Signes de sur-dépendance (risques concrets) : oubli des étapes clés, incapacité à résoudre un problème sans suggestions d’AI, acceptation d’informations sans vérification, hausse d’erreurs factuelles, ralentissement quand l’AI est indisponible. Signes de sous-utilisation : stagnation des performances, temps excessif sur tâches simples, refus d’adopter gains de productivité. Remèdes : quotas “no-AI”, défis augmentant la difficulté, révisions par pairs.\n\nSection 5 — Directives éthiques (procédures)\n- Exactitude & vérification : vérifier toute affirmation clé auprès de sources primaires (articles, normes, docs officiels), cross-check 2+ sources indépendantes, documenter preuves (liens, captures), noter incertitudes et probabilité d’erreur, corriger publiquement toute erreur détectée.\n- Respect du travail d’autrui : systématiquement citer sources (format standard), demander permission pour travaux protégés, reformuler plutôt que copier, utiliser licences compatibles, conserver registre d’attribution, utiliser outils anti-plagiat pour vérification."
      }
    },
    "ex_claude_certified_architect_foundations__01_022": {
      "correction": {
        "en": "Creation date: 2026-07-25. Update plan: review quarterly and after any major tool change; full policy refresh annually or after a significant incident.\n\nWhere this file is saved (redundancy):\n- Cloud: private folder in Google Drive + versioned copy in OneDrive\n- Local: encrypted drive backup (BitLocker/FileVault)\n- LMS: course portfolio area\n- Paper: dated printout in personal binder\n- Access notes: share link with mentor(s); emergency access instructions in encrypted password manager\n\nPractice plan — apply to real scenarios:\n- Weekly micro-practice: pick one interaction (customer email, code review, lesson plan), run through the 4D Framework (Describe, Decide, Do, Debrief), record outcomes.\n- Monthly case studies: 3 workplace scenarios (hiring screening, performance feedback, project planning) simulated with AI assist; measure time saved and quality changes.\n- Metrics: track decisions changed because of AI, verification steps taken, and learning retained.\n\nShare & accountability:\n- Peer study group: biweekly 60-min review meetings; rotate facilitator and present one applied case.\n- Mentor review: monthly check-in to audit examples and policy updates.\n- Public summary: anonymized lessons posted to course forum for feedback.\n\nCourse completion elements (condensed):\n- 4D Framework: Describe context & constraints; Decide on AI role & safety checks; Do the task with AI; Debrief outcomes and document verification steps.\n- AI-augmented learning: use AI to generate explanations, practice items, and feedback; always validate with primary sources and add reflective notes to internalize skills.\n- AI-augmented career thinking: use AI to research roles/skills and draft materials, but preserve authentic voice, disclose AI use where appropriate, and maintain skill-building plan.\n- Personal AI policy: core values (privacy, accuracy, transparency); permitted tools; data handling rules; verification checklist; update cadence and accountability contacts.",
        "fr": "- Date de création et plan de mise à jour : inscrire la date au format ISO (YYYY‑MM‑DD) et une cadence de révision (ex. « revue mensuelle + après changement de modèle/mission »). Ajouter champ « version » et changelog succinct (qui a modifié quoi et pourquoi).\n\n- Enregistrement redondant et accessible : 1) stockage principal chiffré (ex. Google Drive/OneDrive avec 2FA), 2) dépôt versionné (Git/GitHub privé) pour historique de modifications, 3) sauvegarde locale chiffrée ou service S3. Documenter les listes d’accès et procédures de restauration.\n\n- Mise en pratique sur scénarios réels : créer 3–5 scénarios représentatifs (ex. feedback professionnel, rédaction d’un CV, simulation d’entretien) ; définir KPI (temps, qualité, erreurs détectées) ; exécuter, consigner résultats, itérer prompts et règles. Exemple : « Scénario entretien — prompt initial → réponse AI → évaluation par pair → ajustement de prompt. »\n\n- Partage et responsabilisation : organiser sessions de relecture hebdomadaires, nommer un binôme d’accountability, créer canal de feedback anonyme. Maintenir registre des décisions et escalades.\n\nÉléments pédagogiques attendus :\n- 4D (Discover: contexte & valeurs; Define: objectifs & contraintes; Develop: prompts/guardrails; Debrief: évaluation & itération).\n- Approche d’apprentissage augmentée : AI = tuteur/scaffold (plan d’étude, exercices ciblés, vérification par pairs), pas remplacement des évaluations humaines.\n- Réflexion de carrière augmentée : journaling guidé, mapping compétences vs. opportunités, check d’authenticité.\n- Politique personnelle AI (template) : finalité, outils autorisés, données permises/interdites, révision (mensuelle), responsable de conformité."
      }
    },
    "ex_claude_certified_architect_foundations__01_023": {
      "correction": {
        "en": "Pointers for difficult topics (derivative of a function)\n- Concrete clues: think “instant slope” and “rate of change”; sketch secant → tangent; connect units (m/s from m).\n- Key heuristics: use limit definition for intuition, rules (power/product/chain) for speed, and dimensional checks for plausibility.\n\nComprehension testing questions\n- Define the derivative in one sentence.  \n- Compute d/dx[x^3] and interpret physically.  \n- Given s(t)=t^3−6t^2, find v(t) and time(s) when v=0; explain meaning.\n\nCritical feedback from different perspectives\n- Novice: “I don’t see why limits matter; show a graph.”  \n- Peer student: “Your algebra is fine but you forgot chain rule case.”  \n- Instructor: “Explain steps and common pitfalls (e.g., differentiating constants).”  \n- Applied engineer: “Relate derivative to measurable rates and error bounds.”\n\nConcept re-explanation with new examples\n- Intuition: derivative = slope of tangent. Example: water height h(t)=5t^2 → dh/dt=10t (rate of rise).  \n- Alternate view: derivative as best linear approximation near a point.\n\nProgressive practice problems\n- Easy: d/dx[x^2 + 3x].  \n- Medium: d/dx[(x^2)(sin x)] (use product rule).  \n- Hard: find dy/dx for x^2 + y^2 = 25 (implicit differentiation) and interpret.\n\nAct as student prompt for the teacher\n- “I can compute power-rule problems but get stuck interpreting results—can you show two applied examples and common mistakes?”\n\nDiscernment (Learning Check)\n- Can I summarize the idea in 1–2 sentences?  \n- Can I teach it aloud and answer two follow-up questions?  \n- Can I solve one easy, one medium, and one hard problem without hints?",
        "fr": "Correction modèle (ce que doit contenir une bonne réponse)\n\n1) Pistes/indices pour sujets difficiles — fournissez 3 types d’indices actionnables :\n   - Analogie simple (p.ex. pour régularisation L2 : «L2 agit comme une friction qui freine les poids»).\n   - Décomposition en étapes («divisez l’algorithme en forward, loss, backprop, update»).\n   - Indice ciblé pour déboguer (p.ex. «vérifier norme des gradients > 100 → taux d’apprentissage trop élevé»).\n\n2) Questions d’évaluation — 4 niveaux avec exemples :\n   - Rappel : «Qu’est-ce que la régularisation L2 ?»\n   - Application : «Comment ajouter L2 à une fonction de coût ?»\n   - Transfert : «Quand préférer L1 à L2 ?»\n   - Synthèse/critique : «Quels sont les compromis entre régularisation et sous-/sur-apprentissage ?»\n\n3) Retours critiques (perspectives) — exemples de commentaires :\n   - Apprenant : «J’ai compris l’intuition mais pas la mise en œuvre.»\n   - Expert : «Élargir la discussion sur biais-variance et preuves empiriques.»\n   - Pédagogique/novice-friendly : «Ajouter un schéma étape par étape et un exemple chiffré.»\n\n4) Réexplication avec nouvel exemple — court paragraphe réexpliquant le concept via un cas concret (p.ex. entraînement d’un modèle de régression simple avec et sans L2 montrant erreurs sur un jeu de test).\n\n5) Exercices progressifs — 3 tâches :\n   - Basique : implémenter L2 sur une régression linéaire.\n   - Intermédiaire : comparer L1 vs L2 sur jeu synthétique.\n   - Avancé : optimiser hyperparamètre de régularisation et interpréter la courbe validation.\n\n6) Jeu de rôle étudiant — fournir une question-type d’un étudiant et la réponse attendue + erreurs courantes.\n\n7) Discernement — checklist «Puis-je expliquer cela ?» :\n   - Je peux résumer en 2–3 phrases.\n   - Je peux enseigner à un débutant avec un exemple concret.\n   - Je peux répondre à 3 questions fréquentes et corriger une implémentation simple."
      }
    },
    "ex_claude_certified_architect_foundations__01_024": {
      "correction": {
        "en": "Purpose: a compact lesson template that includes multi-perspective feedback, a fresh re-explanation with examples, progressive practice, and a student role-play—plus self-evaluation questions.\n\nCritical feedback (four perspectives) — give specific, actionable comments:\n- Peer teacher: “Make the proof more visual; add a step that links algebra to the diagram.”\n- Novice learner: “I got lost at step 3; add one intermediate sentence and label sides clearly.”\n- Domain expert: “You used a special-case proof; mention generalization and state assumptions.”\n- Stakeholder (e.g., curriculum lead): “Time per activity is too long; condense to 10 minutes and include assessment.”\n\nConcept re-explanation (with new examples)\n- Brief restatement in plain language, then two novel examples. Example (Pythagorean theorem): “In a right triangle, c² = a² + b². Example 1: a 3–4–5 ladder problem — if a ladder reaches 5 ft up and base is 3 ft, the horizontal gap is 4 ft. Example 2 (real-world): TV screen diagonal—compute width from diagonal and height.”\n\nProgressive practice problems (with hints)\n- Level 1 (Foundational): Right triangle with a=6, b=8 → find c. Hint: compute squares.\n- Level 2 (Applied): Ladder 13 ft, base 5 ft → how high does ladder reach? Hint: c² = a²+b².\n- Level 3 (Transfer): Find integer right triangles with perimeter 60. Hint: search Pythagorean triples or set equations.\n\nAct as student (sample role-play)\n- Student: “Why swap a and b matters?” Teacher: “Good question—try computing both; notice c depends only on squares, not order. Which step shows symmetry?”\n\nDiscernment (Learning Evaluation)\n- After practice, answer: Can I teach this? Can I solve without AI? Do I understand why (derive the formula)? Use these to decide next remediation steps.",
        "fr": "Correction modèle — ce que l’étudiant devrait produire\n\n1) Retours critiques (au moins 3 perspectives)\n- Novice : « j’ai du mal à visualiser la preuve » → demander plus d’illustrations pas à pas.\n- Enseignant : vérifier progression pédagogique, vocabulaire précis, points de confusion anticipés.\n- Expert en mathématiques : signaler approximations, cas limites (triangles dégénérés), rigueur de la preuve.\n- Pair : commenter clarté et pédagogie, proposer exercices alternatifs.\n\n2) Nouvelle explication + nouveaux exemples (ex. : théorème de Pythagore)\n- Reformulation claire : « Dans un triangle rectangle, le carré de l’hypoténuse = somme des carrés des côtés adjacents. »\n- Nouveaux exemples concrets : échelle appuyée contre un mur (hauteur² + distance² = longueur²), diagonale d’un rectangle 3×4 → diag = 5, distance entre deux points (x1,y1) et (x2,y2).\n- Brève preuve géométrique numérique et preuve algébrique (coordonnées).\n\n3) Exercices progressifs (avec indication de solution)\n- Niveau 1 (rappel) : calculer l’hypoténuse d’un triangle 6-8 → 10. (méthode : racine carrée)\n- Niveau 2 (compréhension) : démontrer pour rectangle 5×12 : diag = 13 (raisonner avec deux triangles).\n- Niveau 3 (application) : positionner une échelle de 10 m pour atteindre 8 m ; quelle distance au mur ? (solution : 6 m).\n- Niveau 4 (transfert) : trouver la distance entre (−1,2) et (3,−2) ; appliquer formule distance.\n- Niveau 5 (synthèse) : résoudre un problème réel combinant trigonométrie et Pythagore (indiquer démarche étape par étape).\n\n4) Agir comme étudiant pour enseigner\n- Énoncer en « je » : « Je peux expliquer ainsi… », donner une démonstration courte, poser 2 questions de vérification au public, corriger une erreur courante.\n\n5) Critères de discernement (évaluation)\n- Je peux expliquer à quelqu’un d’autre : fournir 1 explication orale de 2 phrases + 1 dessin.\n- Je peux résoudre sans IA : réussir 4/5 exercices sans aide.\n- Je comprends pourquoi : pouvoir donner la preuve et expliquer cas limites."
      }
    },
    "ex_claude_certified_architect_foundations__01_025": {
      "correction": {
        "en": "Goal: configure an AI Learning Partner so reviewers can verify every decision and see how the system adapts to new tasks.\n\n1) Share learning context (example)\n- “Learner: junior product manager, completed Lesson 1 (user research basics). Key gaps: synthesizing interview notes, writing research plans. Preferred pace: 10–15 min micro-sessions.”\n- Why included: clarifies assumed prior knowledge and pace so the AI tailors explanations.\n\n2) Share transcripts (example)\n- Paste Lesson 1 transcript or a short excerpt with timestamps: “00:00–02:30: Definition of open vs closed questions; 02:31–05:00: Note-taking template.”\n- Why: lets AI ground answers in exact material; cite transcript lines in responses.\n\n3) Specify learning vs assignment goals (examples)\n- Learning goal: “Understand and practice synthesizing 5 interview notes into themes; I want step-by-step feedback.”\n- Assignment completion goal: “Produce a 300-word research plan that meets rubric X (clarity, methods, timeline).”\n- Why: tells AI whether to teach concepts (explain, scaffold) or to produce deliverables.\n\n4) Define questioning vs direct-answer approach (examples)\n- Socratic/questioning mode: “Ask 2–3 guiding questions before giving solutions; provide hints, then confirm.”\n- Direct-answer mode: “Provide a complete model answer with annotated reasoning and sources.”\n- Why: controls cognitive load and shows process vs product.\n\n5) Deployment-diligence checklist to submit\n- Configuration text (context + transcript + goals + mode)\n- One sample prompt and AI response\n- Annotated explanation of every element (why chosen, expected effect)\n- Adaptation note: how settings change for novice vs expert and for a new topic\n\nUse the above as a template; include explicit rationale for each choice so graders can verify reasoning and transferability.",
        "fr": "Partie 1 — Correction modèle (configuration du partenaire IA)\n\n1) Contexte d’apprentissage (à fournir au modèle)\n- Brève description (2–3 phrases) : niveau (débutant/intermédiaire), compétences visées (ex. “comprendre les principes X et résoudre exercices A–C”), durée prévue.\n- Contexte pédagogique : format des cours (vidéo + devoirs), critères d’évaluation (rubrique de notation).\n\nExemple system prompt à donner à l’IA :\n\"Tu es un tuteur pédagogique. Contexte : Leçon 1 — notions X,Y,Z pour étudiants débutants. Objectif : aider à terminer les devoirs tout en développant l’autonomie.\"\n\n2) Transcriptions vidéo (format et contenu)\n- Fournir fichiers .txt ou .json avec : timestamps, étiquettes locuteur, transcription fidèle, et un résumé 3–5 lignes.\n- Inclure métadonnées : durée vidéo, chapitres/timecodes, mots-clés.\n- Exemple d’entrée : {start: \"00:02\", end: \"00:45\", speaker: \"Prof\", text: \"...\"}.\n\n3) Objectifs d’apprentissage (SMART)\n- Spécifiques : \"Expliquer 3 principes de X\".\n- Mesurables : \"Résoudre 4/5 problèmes similaires\".\n- Atteignables, Pertinents, Temporels : échéance pour rendu des devoirs.\n- Indiquer niveau attendu (ex. \"réponse écrite 200–300 mots + 2 exemples\").\n\n4) Politique questionnement vs réponse directe (règles concrètes)\n- Par défaut méthode socratique : poser 2 questions clarifiantes, puis 2 indices graduels.\n- Si l’étudiant demande solution complète ou après 3 indices infructueux, fournir solution pas-à-pas.\n- Toujours demander si l’étudiant veut la solution complète ou un autre indice.\n- Exemple de flux :\n  1. Clarifier (2 questions),\n  2. Indice basique,\n  3. Indice avancé,\n  4. Solution complète si demandé.\n\n5) Contrôles et risques (mitigations)\n- Risques : hallucinations, fuite de données personnelles, réponses non conformes au programme.\n- Mitigations : anonymiser transcripts, demander citations (timecode), ajouter validation humaine finale, limiter génération de code/solutions sans vérification.\n\nPoints d’évaluation à inclure : logs de dialogue, indicateurs de progression (scores des devoirs), et retours étudiants."
      }
    },
    "ex_claude_certified_architect_foundations__01_026": {
      "correction": {
        "en": "Learning context (Lesson 1)\n- Concise summary to share: lesson title, 3 learning objectives, assumed prerequisites, and common confusions.\n  Example: \"Lesson 1 — Intro to Probability. Objectives: 1) Define sample space/events; 2) Compute basic probabilities; 3) Use complement rule. Prereqs: basic algebra. Known difficulty: distinguishing independent vs mutually exclusive.\"\n\nVideo transcripts for AI understanding\n- Provide full verbatim transcript with timecodes and speaker labels; if long, include a short excerpt and a link to full text.\n  Example excerpt:\n  [00:01:12] Instructor: \"A sample space lists all possible outcomes. For a fair coin, S = {H, T}.\"\n- Tell the AI how to use it: \"Use the transcript to extract definitions, worked examples, and instructor hints.\"\n\nLearning vs assignment completion goals\n- Learning goal (signal: STUDY_GOAL:LEARN): seek conceptual understanding, step-by-step reasoning, progressive questioning, and error explanations.\n- Assignment completion goal (signal: STUDY_GOAL:FINISH): request concise final answers, formatted submission-ready responses, and minimal explanation.\n- Always include the signal in your prompt header.\n\nQuestioning vs direct answer approach\n- Questioning (STUDY_STYLE:QUESTIONS): AI gives hints, leading questions, next-step prompts; never gives full solution.\n- Direct answer (STUDY_STYLE:ANSWER): AI provides full solution with clear steps and final answer.\n- Example prompt header: \"STUDY_GOAL:LEARN | STUDY_STYLE:QUESTIONS\"\n\nStudy Protocols by Need (prompt templates)\n- Problem-solving: \"Mode: Problem-solving — HINTS ONLY. Ask 3 leading questions escalating from observation to method. Do not reveal final answer.\"\n- Concept review: \"Mode: Concept-review — PROGRESSIVE TESTING. Start with definition Qs, then 2 application problems increasing difficulty, provide corrective feedback.\"\n- Exam prep: \"Mode: Exam-prep — QUIZ+EXPLANATIONS. Give 10 multiple-choice questions; after each answer, explain why wrong choices are incorrect.\"\n- Writing: \"Mode: Writing — ARGUMENT DEVELOPMENT. Use a sequence of Socratic prompts: (1) thesis refinement, (2) evidence selection, (3) counterargument prompts, (4) draft paragraph request.\"\n\nUse these exact headers and templates when interacting with the AI for consistent behavior.",
        "fr": "Contexte d’apprentissage (Leçon 1)\n- Prérequis : notions de base X, Y (liste courte), durée estimée 45–60 min, compétences visées : comprendre définitions clés, appliquer méthode A à un cas simple.\n- Portée : objectifs portés sur notions fondamentales + 1 exercice guidé + devoir court.\n\nTranscriptions des vidéos (pour compréhension par l’AI)\n- Fournir verbatim + version « nettoyée » (normalisée) ; inclure horodatage et étiquettes locuteur.\n- Format conseillé : [00:00:12] Enseignant : phrase. Version nettoyée : « définition de … ».\n- Ajouter métadonnées : thèmes abordés, questions posées, exemples donnés, erreurs courantes mentionnées.\n\nObjectifs d’apprentissage (liés aux devoirs)\n- Objectifs mesurables (SMART) : ex. « À la fin, l’apprenant pourra expliquer X en 2 phrases et résoudre un exercice similaire en ≤15 min avec 80% de réussite ». Chaque devoir doit mapper à 1–2 objectifs précis.\n\nApproche questionnement vs réponse directe\n- Règle : privilégier questionnement socratique quand l’objectif est raisonnement/transfer; réponse directe pour faits ou procédures techniques.\n- Exemples :\n  - Questionnement : « Quelles hypothèses vous font choisir méthode A ? » → guide en 2–3 questions.\n  - Réponse directe : « Étapes pour calculer B : 1…2…3… »\n\nProtocoles d’étude par besoin\n- Résolution de problèmes : fournir uniquement indices progressifs (indice 1: rappel concept; indice 2: stratégie; indice 3: piste de calcul). Ne pas donner solution complète avant 3 indices.\n- Révision des concepts : tests en 3 niveaux (fondamental, appliqué, extension), rétroaction immédiate et cartes mémoire pour erreurs.\n- Préparation à l’examen : quiz chronométré + correction détaillée pour chaque mauvaise réponse (explication + référence à la leçon).\n- Rédaction : process itératif par questionnement — clarifier thèse via Q, construire plan Q-par-Q, développer paragraphes puis réviser via checklist de cohérence et preuves."
      }
    },
    "ex_claude_certified_architect_foundations__01_027": {
      "correction": {
        "en": "Weekly Concept Work Summary — template and prompts (15–30 min)\n\n1) Header (1 line)\n- Week #, date, topic(s) covered.\n\n2) Concise summary (1–3 sentences)\n- Restate the core concept(s) in your own words (no definitions copied verbatim).\n\n3) Evidence of understanding (2–4 bullets)\n- One solved example (steps + result) or one problem you attempted and outcome.\n- Key formulas/relationships or a short diagram/sketch.\n\n4) Specific difficulties & questions (3 items max)\n- Note precise misunderstandings (e.g., “I’m unsure why step 3 cancels x”) and 1–2 targeted questions to investigate.\n\n5) Connections & transfer (1–2 bullets)\n- How this links to prior concepts or real applications; where you might apply it.\n\n6) Action plan (2–3 concrete next steps)\n- What to practice, resources to consult (chapter/lecture/time), and a deadline (e.g., “Rework problems 4–6; 30 min Friday”).\n\n7) Self-assessment (1 line)\n- Confidence rating 1–5 and one reason.\n\nTiming suggestion: 5 min summary + 10–15 min evidence/questions + 5–10 min plan/self-assess. Example entry (brief):\n- Week 4, Linear independence. Summary: Vectors are independent if only trivial combo gives zero. Evidence: Solved 3x3 determinant ≠0 → independent. Difficulty: Interpreting geometric meaning in R4. Next step: Watch lecture segment 12 and solve 2 more problems by Wed. Confidence: 3/5 (need more practice).",
        "fr": "Checklist et modèle que doit contenir un bon résumé hebdomadaire (15–30 min) — visé actionnable :\n\n- Objectifs d’apprentissage de la semaine (1–2 lignes) : quels concepts précis visés (p.ex. dérivée partielle, biais de confirmation).\n- Activités réalisées (liste courte, datée) : exercices résolus, lectures, prompts/tests, durée par activité.\n- Preuves de maîtrise (quantifiées) : scores de quiz, nombre d’exercices réussis, erreurs récurrentes identifiées.\n- Erreurs et difficultés spécifiques : 2–3 items précis (p.ex. confusion entre gradient et dérivée directionnelle), cause probable, stratégie corrective immédiate.\n- Questions ouvertes à résoudre : 1–3 questions claires à prioriser la semaine suivante.\n- Plan d’action pour la semaine prochaine : tâches concrètes, ressources (chapitres / vidéos / prompts), temps estimé, critère d’achèvement (p.ex. obtenir ≥80% au quiz).\n- Réflexion métacognitive (1–3 phrases) : ce qui a aidé/entravé l’apprentissage et ajustement de méthode.\n\nExemple bref :\nObjectif : maîtriser intégration par parties. Activités : 3 exercices (45 min), vidéo explicative (20 min), quiz (7/10). Difficulté : choix de u et dv — erreur 2/3 fois; plan : refaire 10 exercices ciblés, revoir règle heuristique, expliquer le procédé à un pair. Critère : 9/10 au quiz la semaine prochaine. Question ouverte : comment choisir u/dv avec fonctions trigonométriques composées?"
      }
    },
    "ex_claude_certified_architect_foundations__01_028": {
      "correction": {
        "en": "Week 3 — Living Learning Journal\n\nWeekly concept work summary:\n- Focus: argument development through questioning (Socratic questioning, claim-evidence-reasoning) and reading comprehension (summarizing definitions, paraphrasing examples).\n- Activity: practiced converting a textbook section into three claims, generated supporting evidence, and wrote a 150-word paraphrase of the same section.\n\nUnderstanding clarity assessment:\n- Clear: I can form a testable claim and locate two pieces of relevant evidence; I can paraphrase main ideas without copying.\n- Unclear: Differentiating weak vs. strong warrants and when to probe assumptions deeper.\n- Specific questions to resolve: How do I evaluate warrant strength? What probing questions reveal hidden assumptions?\n\nAI learning usage documentation:\n- Prompts used: “List five Socratic questions to test this claim: [insert claim].” and “Paraphrase the following paragraph in plain English.”\n- Outputs: received question lists and 2 paraphrase variants; I selected and refined questions, then tested them in peer discussion.\n- Validation: compared AI paraphrase against original to ensure no meaning loss; flagged any hallucinations and cross-checked with the source.\n- Limitations noted: AI suggested overly general questions; required human judgment to tailor questions to context.\n\nEffective study strategy identification:\n- Combined techniques: spaced retrieval (daily 10-min recall), teach-back (explain claim+evidence to peer), and targeted questioning (use 3 AI-generated Socratic prompts, then refine).\n- Next steps: schedule three 15-min review sessions this week; prepare two example warrants to analyze.",
        "fr": "Structure attendue (remplir chaque point en 15–30 min) :\n\nRésumé hebdomadaire (3–6 phrases)\n- Quels concepts ont été travaillés (titres précis) et quelles tâches accomplies (ex. : résumé d’article, exercice n°3, implémentation de la fonction X).\n- Résultat concret : progrès mesurables (p. ex. 2 problèmes résolus, 1 concept compris).\n\nÉvaluation de la clarté de la compréhension (rubrique brève)\n- Échelle simple : Clair / Partiellement clair / Confus — justifier en 1–2 phrases.\n- Indicateurs : je peux expliquer sans notes; j’ai confondu A et B; j’ai besoin d’exemples pratiques.\n\nDocumentation de l’utilisation de l’IA (format standardisé)\n- Outil/model (ex. Claude 2.1), prompt utilisé, sortie reçue (copier-coller), comment la réponse a été vérifiée/éditée.\n- But de l’IA : génération d’idées, vérification factuelle, correction de code, reformulation.\n- Risques/limites identifiés (biais, erreurs factuelles) et actions prises pour mitigation (vérifier source X, tester code).\n\nIdentification de stratégies d’étude efficaces (actionnable)\n- Méthodes testées (active recall, flashcards, Q/A, plan d’exercices), preuve d’efficacité (ex. : 80% d’exercices corrects après révision).\n- Plan pour la semaine suivante : 2 sessions de 25 min de spaced repetition, 3 problèmes d’application, 1 session de pair-review.\n\nExemples de phrases de journal : „Cette semaine j’ai travaillé sur X…“, „Je suis confiant sur Y parce que…“, „J’ai utilisé Claude pour… et j’ai vérifié en…“.\n\nPoints évalués : précision des concepts, honnêteté sur incertitudes, traçabilité de l’usage d’IA, plan d’action concret."
      }
    },
    "ex_claude_certified_architect_foundations__01_029": {
      "correction": {
        "en": "Purpose: capture how you use AI for learning, identify effective strategies, track practice needs, and document skill development & insights.\n\nWeekly entry (use one line per session or one block per week)\n- Date; Duration; Topic/goal.\n- Activity type: AI-assisted / Independent / Mixed (mark with A/I/M).\n- Methods used: prompts given, resources consulted, exercises completed.\n- Outcome: what you learned (1–3 bullets), confidence level (0–5), time spent per activity.\n- Practice needs: specific drills to repeat next week (e.g., \"10 timed problem sets on topic X\", \"one peer explanation\", \"rewrite summary without AI\").\n- Skill notes: micro-skills to improve (e.g., \"clarify problem statements\", \"apply theorem Y to examples\", \"write concise code functions\").\n\nMonthly synthesis (one entry per month)\n- Pattern summary: 3–5 recurring strengths and weaknesses from weekly data.\n- Strategy evaluation: which AI prompts or independent methods produced highest gains (include examples).\n- Adjustments: explicit changes to study schedule, practice frequency, or prompt wording.\n- Metrics: progress on confidence scores, speed, accuracy.\n\n\"Aha moment\" log (each time)\n- Short description of insight.\n- Trigger: what caused it (AI explanation, independent problem, peer feedback).\n- Concrete application: how you’ll change practice or approach because of it (next 3 actions).\n\nOptional: keep templates as a simple spreadsheet or note app with columns: Date, Topic, A/I/M, Activities, Outcomes, PracticeNeeds, SkillNotes, Aha.",
        "fr": "Objectif du document : guider l’usage de l’IA pour apprendre, définir stratégies d’étude, besoins de pratique et un système de suivi clair.\n\nStratégies d’étude efficaces (actionnables)\n- Récupération active : créer quiz de 10 questions après chaque session; corriger sans aide, puis demander à l’IA explications sur erreurs.\n- Espacement/interleaving : planifier révisions à J+1, J+7, J+30; mélanger sujets connexes dans chaque session.\n- Elaboration & Feynman : résumer un concept en 3 phrases simples puis demander à l’IA d’évaluer clarté.\n- Pratique délibérée : définir micro-objectifs (ex. 5 problèmes ciblés) avec feedback immédiat.\n- Codage/dual coding : schéma + courte explication textuelle, IA génère variantes.\n\nBesoins de pratique (checklist)\n- Identifier 3 compétences fragiles par module.\n- Décomposer en sous-skills testables.\n- Prévoir exercices progressifs + feedback automatisé (IA) + correction par pair.\n\nNotes de développement de compétences (template)\n- Compétence | Niveau initial | Objectif | Métrique (ex. 80% tests) | Ressources | Exos assignés | Échéance\n\nSystème de suivi\n- Fréquence : journaliser chaque session (idéal 3–5/semaine) + synthèse hebdo courte.\n- Entrées mensuelles : tendance des scores, temps total, 3 points faibles récurrents, actions correctives.\n- Notation AI vs indépendant : pour chaque tâche, % aide IA + score qualité 1–5 (critères : compréhension, originalité, erreurs).\n- Documentation « Aha moment » (template) : date, déclencheur, description courte, pourquoi important, comment le reproduire, action suivante.\n\nExemple d’entrée brève : Session 2026-07-20 | 45 min | Sujet X | Méthode : quiz + explication IA | Aide IA 30% | Score 7/10 | Aha : visualisation liée au concept Y -> refaire schéma + 5 ex."
      }
    },
    "ex_claude_certified_architect_foundations__01_030": {
      "correction": {
        "en": "High-level summary\nUse the Delegation–Diligence loop for strategic/ethical decisions about what to automate and how; use the Description–Discernment loop for the tactical, iterative work of building and refining delegated tasks. They run together: strategic choices set constraints for tactical work, while tactical evidence (Diligence) revises strategy.\n\nDelegation (decide what to give to the model)\n- Problem Awareness: define goals, stakeholders, success metrics, legal/privacy constraints, and high‑risk failure modes.\n- Platform Awareness: document model/version, input/output limits, data retention, capabilities, costs, and known biases.\n- Task Delegation: break the problem into specific tasks; write expected input/output schemas, evaluation criteria, and safe fallbacks (e.g., human review triggers).\n\nDiligence (make sure outputs are safe, accurate, and traceable)\n- Creation Diligence: data quality checks, prompt/test-case library, human-in-the-loop for edge cases, and validation suites.\n- Transparency Diligence: record prompts, model versions, decisions, provenance, and user-facing disclosures.\n- Deployment Diligence: monitoring (performance and safety), access control, rollback plans, incident response, and continual retraining/evaluation.\n\nDescription–Discernment loop (tactical iteration)\n- Description: precisely specify examples, constraints, evaluation metrics, and acceptance thresholds.\n- Discernment: run tests, do error analysis, calibrate confidence thresholds, improve prompts/data, repeat.\n\nConcrete risks and mitigations\n- Hallucination → gold‑label tests, fact‑checking, human review.\n- Data leakage → minimize sensitive inputs, apply DP/filters.\n- Bias/unfairness → disaggregated evaluation, bias mitigation, human oversight.\n- Overreliance/model drift → monitoring, alerts, periodic revalidation.\n\nQuick checklist before deployment\n1) Have success/failure metrics and fallbacks? 2) Logged prompts and model versions? 3) Monitoring + incident plan? 4) Human review for high‑risk cases?",
        "fr": "Réponse attendue — correction modèle (points qu’un bon devoir doit couvrir)\n\n1) Vue d’ensemble concise\n- Expliquez le rôle complémentaire des deux boucles : Delegation‑Diligence (décisions stratégiques/éthiques) et Description‑Discernment (itérations tactiques).\n\n2) Délégation — actions concrètes\n- Prise de connaissance du problème : définir objectifs, parties prenantes, risques acceptables, critères de succès mesurables (ex. précision ≥ 95 %, taux d’hallucination < 2%).\n- Connaissance de la plateforme : capabilities/limitations du modèle, quotas, sécurité, stockage de données, SLA.\n- Délégation des tâches : cartographier tâches → niveau d’autonomie (automatisé / assisté / humain), avec justification (ex. extraction de champs simples = automatisé ; décisions juridiques = humain avec résumé AI).\n\n3) Diligence — contrôles précis\n- Creation diligence : jeux de données (provenance, biais, anonymisation), tests unitaires d’exemples adversariaux, red‑teaming.\n- Transparence diligence : documentation (fiche de modèle, data sheet), explications pour utilisateurs, labels “AI‑assisted”.\n- Déploiement diligence : monitoring (erreurs, dérive, latence), alerting, journaux immuables, procédure de rollback, équipe on‑call.\n\n4) Description‑Discernment — boucle itérative\n- Description : produire artefacts (prompts, sorties, métadonnées).\n- Discernment : évaluer contre tests, annoter erreurs, ajuster prompt/ensemble d’entraînement, réitérer.\n- Critères d’arrêt : stabilité métrique sur N itérations, acceptation stakeholder.\n\n5) Risques concrets à lister\n- Hallucinations, biais, fuite de données sensibles, sur‑confiance des utilisateurs, attaques adversariales, dérive en production.\n\n6) Exemples pratiques\n- Fournir une matrice tâches vs contrôle humain + tests d’acceptation et métriques de surveillance.\n\nUne bonne réponse donne cette check‑list opérationnelle et illustre par au moins un exemple concret de délégation + pipeline de diligence."
      }
    },
    "ex_claude_certified_architect_foundations__01_031": {
      "correction": {
        "en": "Learning objective: Teach instructors to co-design high-quality lessons with AI so the partnership produces better teaching than either alone.\n\nExercise 1 — Establish context & explore approaches (30 min)\n- Goal: Choose a target learner, learning objective, assessment, and pedagogical approach.\n- Tasks (20 min): In pairs, pick one learner profile (e.g., intro CS undergrad), state a single learning objective, choose one approach (lecture, inquiry, project), and write a 1-paragraph rationale.\n- AI prompt example to draft: “You are an expert instructional designer. Given learner X and objective Y, suggest 3 teaching approaches with pros/cons and a 10-minute activity for each.”\n- Deliverable (10 min): 1-page lesson context + the AI reply you’d adapt.\n- Quick rubric: clarity of learner/objective, appropriateness of chosen approach, usable AI suggestions.\n\nExercise 2 — Design Delegation–Diligence loop lesson (30 min)\n- Goal: Practice delegating tasks to AI and verifying outputs.\n- Tasks: 1) Break a 15-minute micro-lesson into subtasks (content script, examples, formative question bank). 2) Create explicit prompts for each subtask. 3) Define verification checks (facts, alignment to objective, accessibility).\n- Sample prompt: “Draft 5 multiple-choice checks aligned to learning objective Z; include correct answer and one-sentence distractor rationale.”\n- Deliverable: micro-lesson + prompts + a 5-item verification checklist.\n- Rubric: specificity of delegation, robustness of diligence checks, ability to catch errors.\n\nExercise 3 — Design Description–Discernment loop lesson (30 min)\n- Goal: Iteratively refine explanations and evaluation criteria with AI.\n- Tasks: 1) Have AI produce two alternative explanations for a concept. 2) Create discriminators (readability, misconceptions addressed, scaffolding). 3) Use discriminators to select and refine the best explanation.\n- Sample prompt: “Write two 3-paragraph explanations of concept C: one analogy-based, one example-driven. Then score each on clarity, misconceptions, and scaffolding (1–5).”\n- Deliverable: chosen explanation + discriminator matrix + final refinement.\n- Rubric: quality of discriminators, measurable improvement after refinement, learner-aligned choice.\n\nAssessment & wrap-up (5 min)\n- Peer review of deliverables using the rubrics, instructor feedback, commit one improvement to iterate with AI later.",
        "fr": "Objectif global : À la fin de la session, les apprenants sauront structurer et enseigner deux boucles clés (Delegation–Diligence et Description–Discernment) et animer des exercices pratiques mesurables.\n\nExercice 1 (30 min) — Contexte et approches\n- Objectifs : définir public, contraintes (sécurité, temps, accès ML), résultats d’apprentissage mesurables.\n- Activités : 5 min briefing, 10 min en binômes pour cartographier besoins (template : public, niveau, risques, outils), 10 min partage + choix d’approche (démo-guidée, apprentissage par problème, pair-programming).\n- Livrable : fiche contextuelle (max 200 mots).\n- Critères : adéquation public/activité, risques identifiés, ressources listées.\n\nExercice 2 (30 min) — Leçon : Delegation–Diligence\n- Objectifs : enseigner comment déléguer tâches à l’IA puis vérifier sorties.\n- Activités : 5 min mini-cours, 15 min atelier — donner au modèle une tâche (ex. rédiger une politique), produire 3 itérations ; les étudiants créent une checklist de diligence (sources, plausibilité, tests unitaires, métriques d’erreur) et exécutent 2 vérifications.\n- Exemples concrets : prompt de délégation + checklist (vérifier citations, cohérence chiffres, style).\n- Évaluation : checklist complète, capacité à trouver 2 erreurs injectées, temps de correction.\n\nExercice 3 (30 min) — Leçon : Description–Discernment\n- Objectifs : enseigner description neutre + évaluation critique.\n- Activités : 5 min mini-cours, 20 min pratique — fournir une description technique générée; élèves annotent ambiguïtés, biais, zones d’incertitude, proposent questions de clarification.\n- Livrable : matrice (Description / Indices de confiance / Questions de vérification).\n- Critères : identification d’au moins 3 ambiguïtés, propositions de tests vérifiables.\n\nRisques et mitigation (brefs) : sur-confiance en sorties IA (imposer checklist), leak de données sensibles (anonymiser exemples), manque de transférabilité (donner variantes d’exercices). Matériel : diapos, templates (fiche contexte, checklist diligence, matrice description), jeu d’exemples injectés. Conseils facilitation : utiliser breakout rooms, fournir exemples modèle, corriger en temps réel."
      }
    },
    "ex_claude_certified_architect_foundations__01_032": {
      "correction": {
        "en": "Overall goal: prepare three 30‑minute practical exercises that build teachers’ ability to design lessons that use model “loops” (Delegation→Diligence; Description→Discernment). Each exercise includes objective, materials, stepwise activity, deliverable, and assessment rubric.\n\nExercise 1 — Establish context & explore approaches (30 min)\n- Objective: Choose learner profile, mode, and 3 candidate teaching approaches; justify selection.\n- Materials: learner persona sheet, whiteboard, timer.\n- Steps (30): 5 min select persona (e.g., university writing class); 10 min list 3 approaches (direct instruction, guided discovery, project-based) with 2 pros/cons each; 10 min pick best approach and outline lesson goal + success criteria; 5 min share & get peer feedback.\n- Deliverable: 1‑paragraph rationale + 3‑line lesson goal.\n- Assessment: clarity of persona, alignment of approach to goals, concise rationale.\n\nExercise 2 — Design a Delegation→Diligence loop lesson (30 min)\n- Objective: Teach how to delegate tasks to a model and verify outputs.\n- Materials: sample prompts, verification checklist.\n- Steps: 5 min state target skill; 10 min draft a 10‑minute lesson: (2 min explain delegation principles, 4 min demo prompt + model response, 4 min student practice with verification); 10 min create a verification checklist (source checking, counterexamples, unit tests) and two test inputs; 5 min peer review.\n- Deliverable: lesson plan + 1 exemplar prompt + verification checklist.\n- Assessment: presence of explicit delegation instructions, concrete diligence checks, test cases.\n\nExercise 3 — Design a Description→Discernment loop lesson (30 min)\n- Objective: Teach students to elicit descriptive outputs, then evaluate/compare them.\n- Materials: sample texts/images, evaluation rubric.\n- Steps: 5 min learning objective; 10 min draft 10‑minute lesson: (2 min framing, 3 min demo prompt to generate descriptions, 5 min guided comparison using rubric); 10 min write a 4‑criteria rubric (accuracy, completeness, bias, clarity) and two comparison tasks; 5 min peer feedback.\n- Deliverable: lesson plan + rubric + sample prompts.\n- Assessment: quality of rubric, alignment of description task to discernment criteria, feasible classroom timing.",
        "fr": "But attendu (résultat final) : pour chaque exercice, un livrable clair (1 page max) contenant objectifs d'apprentissage, public-cible, déroulé minute par minute, activité(s) et critères d'évaluation. Ci‑dessous la correction modèle que l’étudiant doit viser.\n\nExercice 1 — Établir le contexte et explorer les approches (30 min)\n- Objectif : définir public, prérequis, contraintes et choisir 2 approches pédagogiques justifiées (ex. microteaching + pratique guidée).\n- Livrable attendu : 1) Profil élève (niveau, motivations, risques), 2) 2 approches comparées (méthode, durée, avantages, limites), 3) séquence minute par minute (ex. 5’ accroche, 10’ exposé, 10’ activité, 5’ feedback).\n- Critères : adéquation public/objectifs, justification pédagogique, plan temporel réaliste.\n\nExercice 2 — Leçon boucle Délégation–Diligence (30 min)\n- Définition : déléguer une tâche (ex. rédiger un résumé) + définir contrôles systématiques pour vérifier qualité/fiabilité.\n- Livrable : objectif, tâche déléguée, prompt modèle, checklist de diligence (source-check, plausibilité, biais, format), activité d’évaluation.\n- Exemple de prompt : « Rédige un résumé factuel (150–200 mots) de ce texte. Liste 3 sources vérifiables et cite les phrases d’origine. »\n- Vérifications : checklist à cocher, exemple de correction, seuils d’acceptation.\n- Risques concrets : sur-confiance, hallucinations, perte d’apprentissage actif. Contre-mesures : exigence de sources, tâches de vérification par pairs.\n\nExercice 3 — Leçon boucle Description–Discernement (30 min)\n- Définition : entraînement à décrire observables puis inférer/évaluer interprétations.\n- Livrable : activité en 3 étapes (observation guidée, production descriptive, discussion critique des inférences), grilles d’évaluation (exactitude descriptive, justification des inférences).\n- Exemples d’activités : photo → décrire (10’), proposer 3 interprétations distinctes avec preuves (10’), feedback structuré (10’).\n- Risques : confirmation bias, interprétations non étayées. Exiger preuves textuelles/visuelles et alternatives.\n\nRubrique d’évaluation commune : clarté des objectifs (20%), adéquation des activités au temps (20%), qualité des prompts/checklists (30%), gestion des risques & rétroaction (30%)."
      }
    },
    "ex_claude_certified_architect_foundations__01_033": {
      "correction": {
        "en": "Working with this system revealed several intertwined ethical dilemmas, a clearer sense of responsibility, some unexpected operational challenges, and concrete next steps.\n\nEthical dilemmas\n- Bias vs. utility: balancing model performance with disparate impacts on user groups; tradeoffs between optimizing for average accuracy and protecting vulnerable populations.\n- Privacy vs. personalization: using data to improve results while minimizing sensitive data collection and retention.\n- Transparency vs. misuse: how much to reveal about inner workings without enabling harmful exploitation.\n\nResponsibility understanding\n- Shared accountability: product, legal, ML, and ops teams must jointly own outcomes; assign clear RACI (who’s Responsible, Accountable, Consulted, Informed).\n- Proactive duty: obligation to run pre-deployment risk assessments, document decisions, and maintain incident response plans.\n\nSurprising challenges\n- Edge-case failures were more common than expected; small input changes produced outsized harms.\n- Data lineage gaps made root-cause analysis slow.\n- Incentive misalignment: short-term metrics encouraged risky shortcuts.\n\nFuture improvements (concrete)\n- Institute mandatory bias audits and privacy impact assessments before release.\n- Add continuous monitoring (performance, fairness metrics, anomaly detection) with alerting.\n- Improve documentation: model cards, decision logs, data provenance.\n- Regular cross-functional red-teaming and user feedback loops to surface unexpected harms.\n\nThese steps make ethical tradeoffs explicit and operationalize responsibility.",
        "fr": "Une bonne réflexion doit être concrète, nuancée et orientée vers l’action. Points clés à couvrir :\n\n1) Dilemmes éthiques précis\n- Biais et discrimination (ex. scores de décision qui défavorisent des groupes).\n- Vie privée et ré-identification (données d’entraînement sensibles).\n- Usage malveillant (désinformation, automatisation d’attaques).\n- Transparence vs propriété intellectuelle (expliquer sans révéler secret commercial).\n- Impact économique (perte d’emploi, concentration de pouvoir).\n\n2) Répartition claire de la responsabilité\n- Concepteurs/ingénieurs : choix d’architecture, tests et documentation.\n- Product managers / dirigeants : arbitrages de mise en marché et KPI.\n- Organisation (compliance) : audits, politiques d’accès, DPIA.\n- Régulateurs / société civile : normes et surveillance publique.\n- Utilisateurs finaux : utilisation responsable et signalement d’incidents.\n\n3) Défis surprenants rencontrés\n- Comportements émergents non anticipés à grande échelle.\n- Dépendances sur jeux de données opaques et licences floues.\n- Effets de distribution (petites injustices cumulatives).\n- Difficulté à corréler incidents réels et métriques de test.\n\n4) Améliorations concrètes et plan d’action\n- Implémenter model cards, datasheets et logs de décision.\n- Red-teamings réguliers + tests adversariaux automatisés.\n- Monitorings en production (alerts sur dérive de distribution, taux d’erreur par groupe).\n- Mesures de mitigation : differential privacy, filtrage, human-in-the-loop pour décisions sensibles.\n- Gouvernance : comité d’éthique, processus d’escalade, revue avant déploiement, audits tiers.\n- KPIs et métriques : scores de fairness, incidents/1k utilisateurs, temps de réponse aux incidents.\n\nConclure par trade-offs identifiés (sécurité vs utilité) et par un calendrier concret de mise en œuvre et de revue continue."
      }
    },
    "ex_ai_fluency_intro_reflection": {
      "correction": {
        "en": "Your reflection is complete once you have addressed the three questions with specific examples.",
        "fr": "Votre réflexion est terminée lorsque vous avez répondu aux trois questions avec des exemples précis."
      }
    },
    "ex_ai_fluency_certificate_completion": {
      "correction": {
        "en": "**Correct answer: b.** It combines Discernment (checking the result against the goal and evidence) with Diligence (accountability and transparent attribution). **a** treats generated text as self-validating. **c** removes the human judgment that remains necessary for responsible delegation. **d** weakens Description because useful AI collaboration depends on relevant context and constraints.",
        "fr": "**Bonne réponse : b.** Elle associe le Discernment (vérifier le résultat au regard de l’objectif et des éléments probants) et la Diligence (responsabilité et attribution transparente). **a** traite un texte généré comme auto-validant. **c** retire le jugement humain nécessaire à une délégation responsable. **d** affaiblit la Description, car une collaboration utile avec l’IA dépend d’un contexte et de contraintes pertinents."
      },
      "correctOptionIds": [
        "b"
      ]
    }
  },
  "claude_certified_architect_foundations__02": {
    "ex_claude_certified_architect_foundations__02_001": {
      "correction": {
        "en": "System prompt structure\n- First line: role assignment (e.g., \"You are a patient math tutor.\")\n- Following lines: concrete behavioral instructions (how to reason, tone, constraints), e.g. \"Give hints, never full solutions; ask guiding questions; adapt to student's level; use encouraging tone.\"\n\nKey principle\n- System prompts shape response approach (style, constraints, strategy), not content facts. The same user question will be answered differently depending on role/behavior instructions.\n\nTechnical implementation (Python)\nsystem_prompt = \"\"\"You are a patient math tutor.\nGive stepwise hints and ask guiding questions that lead the student to the solution. Do NOT provide full solutions. Adapt language to the student's level and encourage independent thinking.\"\"\"\nuser_input = \"How do I solve x^2 - 5x + 6 = 0?\"\n\nparams = {\n    \"model\": \"claude-2.1\",\n    \"input\": user_input,\n}\nif system_prompt is not None:\n    params[\"system\"] = system_prompt\n\n# call the API with unpacking; if system_prompt is None it is omitted\nresponse = client.create(**params)\n\nUse-case example behavior\n- Given the quadratic question, the tutor should: ask what methods the student knows (factoring/quadratic formula), suggest checking factors of 6 that sum to 5, and prompt the student to try factoring before offering the final factorization only if the student requests it.",
        "fr": "Structure (attendue)\n- Première ligne = rôle: e.g. \"You are a patient math tutor.\"\n- Lignes suivantes = instructions comportementales spécifiques: ton, niveau de détail, interdictions (pas donner la solution complète), méthode pédagogique.\n\nPrincipe clé\n- Le prompt système oriente la manière de répondre (approche), pas le contenu factuel. La même question aura un style et un niveau d'aide différents selon le rôle.\n\nImplémentation technique (exemple Python)\n- Construire params puis ajouter conditionnellement la clé system pour éviter d'envoyer None:\n```python\nparams = {\n    \"model\": \"claude-2.1\",\n    \"prompt\": user_prompt,\n    \"temperature\": 0.2,\n}\nif system_prompt:  # n'ajoute pas la clé si None ou vide\n    params[\"system\"] = system_prompt\n\nresponse = client.create(**params)\n```\n- Raison : certaines APIs traitent None comme null explicite; exclure la clé reproduit le comportement \"no prompt système\".\n\nExemple d'utilisation (Math tutor)\n- system_prompt exemple:\n  \"You are a patient math tutor. Provide stepwise hints and leading questions to help the student reason; do not give the final answer. If the student asks for the solution, first ask why they want it and offer another hint.\"\n- user_prompt exemple: \"Solve x^2 - 5x + 6 = 0.\"\n- assistant attendu (hint-only): \"Commencez par factoriser. Quels deux nombres se multiplient pour donner 6 et s'additionnent pour donner -5? Essayez de placer ces nombres comme facteurs.\"\n\nPoints de validation qu'une bonne réponse couvre\n- rôle clair + règles comportementales, code robuste excluant system quand None, et un exemple montrant différence de sortie (indices vs solution complète)."
      }
    },
    "ex_claude_certified_architect_foundations__02_002": {
      "correction": {
        "en": "Temperature controls randomness in sampling by rescaling the model’s logits before token selection: logits are divided by temperature, then converted to probabilities. Lower temperature sharpens the distribution (more weight to the highest-probability token); higher temperature flattens it (increases chance of lower-probability tokens).\n\nText generation pipeline (concise): input → tokenize → model computes logits → divide logits by temperature → softmax → sample next token → repeat.\n\nConcrete effects\n- Temperature = 0 (or near 0): effectively deterministic sampling — always choose highest-probability token (use for translations, factual extraction, code generation requiring consistency).\n- Temperature 0.2–0.5: conservative but slightly varied — useful for summarization, instruction-following where some fluency variation is acceptable.\n- Temperature 0.6–1.0: creative, diverse outputs — use for brainstorming, creative writing, dialogue variety.\n- >1.0: highly unpredictable; rarely useful for production.\n\nPractical guidelines\n- Start with 0–0.2 for accuracy-critical tasks; 0.7–1.0 for creative tasks.\n- Test several temperatures and compare outputs.\n- Combine temperature with top-p (nucleus) or top-k sampling to control tail risk.\n- For reproducibility, fix the random seed when sampling.\n- Monitor for hallucinations at higher temperatures and validate critical facts.",
        "fr": "Correction modèle :\n\nDéfinition courte et processus\n- Température (0–1) ajuste l’aléa lors du choix du token suivant : tokenisation → distribution de probas → échantillonnage selon la température → itération.\n- À température 0 on prend l’argmax (déterministe). À température plus élevée on amplifie les queues de distribution (plus de choix rares).\n\nPlages pratiques et exemples concrets\n- 0.0 : sortie déterministe — idéal pour extraction de données, génération de code, tests automatisés.\n- 0.0–0.2 : très conservateur — réponses factuelles cohérentes, faible variation.\n- 0.2–0.5 : contrôle ± créativité — bonnes pour résumés, reformulations, Q&A.\n- 0.6–0.8 : créatif — stories, brainstorming, variantes de style.\n- 0.9–1.0 : très aléatoire — exploration créative, génération d’idées inattendues.\n\nRisques concrets et mitigations\n- Hallucinations et incohérences : baissez la température, ajoutez contraintes de prompt, vérification factuelle automatique.\n- Non-répétabilité des tests : utilisez temp = 0 pour reproductibilité ou faites plusieurs tirages et vote majoritaire.\n- Sorties inappropriées : restreignez le prompt, post-filtre, combinez avec top-p (nucleus) ou règles de sécurité.\n\nBonnes pratiques actionnables\n- Pour tâches factuelles : temperature 0–0.2 + exemples few-shot.\n- Pour créativité contrôlée : 0.4–0.6 + générer 3 variantes, puis sélectionner.\n- Combinez température avec top_p (ex. top_p = 0.9) pour mieux contrôler diversité.\n- Mesurez qualitativement (cohérence) et quantitativement (exactitude) sur un échantillon avant déploiement."
      }
    },
    "ex_claude_certified_architect_foundations__02_003": {
      "correction": {
        "en": "Essential elements of a good response-streaming design\n\nHow it works (concrete event schema)\n- Events: response.initial (ack), response.delta (text chunk), response.done (final), response.error.\n- Fields: request_id, chunk_id (monotonic), text, is_final (bool), token_index (optional), elapsed_ms.\n- Server: send response.initial quickly after receiving user message; forward each delta as Claude emits it; send response.done when finished.\n\nFrontend behavior (actionable)\n- Append text chunks in order by chunk_id. Render a typing caret while stream active.\n- Smooth UI: animate short chunks (20–80 ms gaps) to mimic typing; aggregate very small chunks to reduce reflow.\n- Respect user scroll: auto-scroll only if user is at bottom.\n- Offer Stop button to cancel the server stream; on cancel, send a termination signal to server and show partial result with “incomplete” badge.\n- On reconnect, resume by sending last received token_index or request full final result.\n\nOperational considerations & trade-offs\n- Transport: use SSE or WebSocket (SSE for simple one-way); gRPC/HTTP2 for high-throughput.\n- Chunk sizing: balance latency vs rendering overhead (e.g., 32–256 bytes/chunk).\n- Backpressure: limit queue sizes; drop or batch incoming chunks if frontend is overwhelmed.\n- Metrics: measure time-to-first-byte, time-to-last-byte, average chunk latency.\n\nRisks & mitigations\n- Partial/unsafe outputs shown before moderation — run content filter server-side on each chunk or block display until safety checks pass.\n- Duplicate/out-of-order chunks — require chunk_id ordering and deduplication.\n- Network drops — support resume or fetch final transcript.\n\nThis yields immediate perceived responsiveness while preserving correctness and safety.",
        "fr": "Correction modèle (ce qu’un bon élève doit produire)\n\nApproche correcte (résumé)\n- Implémenter un proxy serveur qui appelle l’API Claude en mode streaming, transmet immédiatement un ACK (0-length) puis relaie chaque \"delta\" reçu au client via SSE ou WebSocket pour affichage incrémental.\n\nImplémentation minimale (exemple fonctionnel)\n- Serveur (Node/Express + fetch avec body as stream):\nconst express = require('express');\napp.get('/stream', async (req, res) => {\n  res.writeHead(200, {'Content-Type':'text/event-stream','Cache-Control':'no-cache'});\n  const claudeResp = await fetch('https://api.claude/stream', { method:'POST', body: JSON.stringify(req.query), headers:{'Authorization':'Bearer ...'}});\n  // forward chunks as they arrive\n  claudeResp.body.on('data', chunk => res.write(`data: ${chunk.toString()}\\n\\n`));\n  claudeResp.body.on('end', ()=> res.write('data: [DONE]\\n\\n'));\n});\n- Frontend (EventSource):\nconst es = new EventSource('/stream?msg=bonjour');\nes.onmessage = e => { if (e.data==='[DONE]') es.close(); else appendText(e.data); };\n\nPoints techniques à couvrir\n- Backpressure et buffering: limiter taille des chunks, concaténation côté client pour éviter artefacts UI.\n- Ordonnancement/IDempotence: inclure IDs de fragment pour détecter duplications ou réordonnements.\n- Finalisation: envoyer un signal [DONE] + checksum/totalTokens pour assurer cohérence.\n- Sécurité: filtrage PII côté serveur, validation input, rate-limiting, authentication.\n- Reprise: mécanisme de reconnection et resume à partir d’un offset/lastToken.\n\nRisques concrets et mitigations\n- Hallucinations partielles : ne pas présenter le texte comme définitif avant [DONE]; marquer \"en cours\".\n- Fuite de données sensibles : appliquer redaction et logging minimal.\n- Overload/DoS : quotas, pool de connexions, circuit breaker.\n- Incohérences d’UI : déduplication, ordering par fragment ID.\n\nTests et métriques\n- Mesurer TTFB, Time-to-first-token, tokens/sec, taux d’erreur, % reconnections. Test de charge et UX (simuler latence, pertes de paquets)."
      }
    },
    "ex_claude_certified_architect_foundations__02_004": {
      "correction": {
        "en": "def generate_concise_summary(source_text: str, max_tokens: int = 200, prefer_bullets: bool = False) -> str:\n    \"\"\"\n    Create a concise summary of source_text.\n    - source_text: non-empty string (<= 20_000 characters recommended)\n    - max_tokens: positive int <= 2048\n    - prefer_bullets: boolean for bullet-point output\n    \n    Raises ValueError with actionable messages for invalid inputs.\n    \"\"\"\n    # Input validation\n    if not isinstance(source_text, str):\n        raise ValueError(\"source_text must be a string. If you passed a file path, read the file into a string first.\")\n    if not source_text.strip():\n        raise ValueError(\"source_text is empty or whitespace. Provide a non-empty text to summarize.\")\n    if not isinstance(max_tokens, int):\n        raise ValueError(\"max_tokens must be an integer. For example: max_tokens=150\")\n    if max_tokens <= 0:\n        raise ValueError(\"max_tokens must be > 0. Increase it to allow some output, e.g., max_tokens=150\")\n    if max_tokens > 2048:\n        raise ValueError(\"max_tokens is too large (recommend <= 2048). Reduce max_tokens or split the source_text into parts.\")\n    if not isinstance(prefer_bullets, bool):\n        raise ValueError(\"prefer_bullets must be a boolean: True or False.\")\n    \n    # Lightweight summarization (placeholder for a model call)\n    sentences = [s.strip() for s in source_text.split('.') if s.strip()]\n    if not sentences:\n        raise ValueError(\"Could not parse sentences from source_text. Provide clearer sentence punctuation.\")\n    # Select top sentences up to max_tokens (approximate by length)\n    summary_parts = []\n    char_budget = max_tokens * 4  # heuristic: ~4 chars/token\n    for sent in sentences:\n        if sum(len(p) for p in summary_parts) + len(sent) + 2 <= char_budget:\n            summary_parts.append(sent)\n        else:\n            break\n    if prefer_bullets:\n        return '\\n'.join(f\"- {p}\" for p in summary_parts)\n    return ' '.join(summary_parts)\n\n# Example of error -> corrected call:\n# If Claude sees: ValueError(\"max_tokens must be > 0...\"), it can retry with max_tokens=150.",
        "fr": "class InputValidationError(Exception):\n    \"\"\"Exception levée pour les entrées invalides avec indication de champ et suggestion.\"\"\"\n    def __init__(self, field: str, message: str, hint: str = \"\"):\n        self.field = field\n        self.message = message\n        self.hint = hint\n        super().__init__(f\"{field}: {message} — Suggestion: {hint}\")\n\ndef validate_and_prepare_request(prompt: str,\n                                 temperature: float = 0.7,\n                                 max_tokens: int = 512,\n                                 model_name: str = \"claude-2\") -> dict:\n    # Validation descriptive et explicite\n    if not isinstance(prompt, str):\n        raise InputValidationError(\"prompt\",\n                                   f\"Type invalide (attendu str, obtenu {type(prompt).__name__}).\",\n                                   \"Fournir un texte. Exemple: 'Explique la théorie...'\")\n    prompt_len = len(prompt.strip())\n    if prompt_len == 0:\n        raise InputValidationError(\"prompt\",\n                                   \"Le prompt est vide.\",\n                                   \"Fournir une question ou instruction non vide (>=1 caractère).\")\n    if prompt_len > 10000:\n        raise InputValidationError(\"prompt\",\n                                   f\"Prompt trop long ({prompt_len} caractères).\",\n                                   \"Réduire à <=10000 caractères ou résumer le texte.\")\n    if not isinstance(temperature, (float, int)):\n        raise InputValidationError(\"temperature\",\n                                   f\"Type invalide (attendu float, obtenu {type(temperature).__name__}).\",\n                                   \"Utiliser un nombre entre 0.0 et 1.0, ex: 0.5\")\n    if not (0.0 <= float(temperature) <= 1.0):\n        raise InputValidationError(\"temperature\",\n                                   f\"Valeur hors plage ({temperature}).\",\n                                   \"Choisir une valeur dans [0.0, 1.0], ex: 0.7\")\n    if not isinstance(max_tokens, int):\n        raise InputValidationError(\"max_tokens\",\n                                   f\"Type invalide (attendu int, obtenu {type(max_tokens).__name__}).\",\n                                   \"Utiliser un entier positif, ex: 256\")\n    if not (1 <= max_tokens <= 100000):\n        raise InputValidationError(\"max_tokens\",\n                                   f\"Valeur hors plage ({max_tokens}).\",\n                                   \"Choisir un entier entre 1 et 100000, ex: 512\")\n    allowed_models = {\"claude-2\", \"claude-2.1\", \"claude-instant\"}\n    if model_name not in allowed_models:\n        raise InputValidationError(\"model_name\",\n                                   f\"Modèle inconnu ('{model_name}').\",\n                                   f\"Choisir parmi: {sorted(allowed_models)}\")\n\n    # Préparer le payload (exemple fonctionnel, substituer par appel API réel)\n    payload = {\n        \"model\": model_name,\n        \"input\": prompt,\n        \"temperature\": float(temperature),\n        \"max_tokens\": max_tokens\n    }\n    return payload\n\n# Exemple d'utilisation et gestion d'erreurs pour permettre à Claude de réessayer\ntry:\n    request_payload = validate_and_prepare_request(\"\", temperature=1.5, max_tokens=0)\nexcept InputValidationError as e:\n    # Le message est explicite pour permettre une correction et réessai\n    print(str(e))  # Visible par Claude / l'utilisateur : contient champ, erreur et suggestion\nelse:\n    # Ici on appellerait l'API avec request_payload\n    print(\"Payload prêt:\", request_payload)"
      }
    },
    "ex_claude_certified_architect_foundations__02_005": {
      "correction": {
        "en": "```python\nfrom datetime import datetime\n\ndef get_current_datetime_string(date_format: str = \"%Y%m%d %H:%M:%S\") -> str:\n    \"\"\"\n    Return the current date/time formatted according to date_format.\n    Args:\n        date_format: non-empty str compatible with datetime.strftime\n                     (e.g. \"%Y-%m-%d %H:%M:%S\").\n    Raises:\n        TypeError: if date_format is not a str.\n        ValueError: if date_format is empty or invalid.\n    \"\"\"\n    # Validate type\n    if not isinstance(date_format, str):\n        raise TypeError(\"date_format must be a string, e.g. '%Y-%m-%d %H:%M:%S'\")\n\n    # Validate non-empty (immediate failure for invalid input)\n    if date_format.strip() == \"\":\n        raise ValueError(\"date_format cannot be empty. Use a format like '%Y-%m-%d %H:%M:%S'.\")\n\n    # Attempt to format; catch and re-raise with actionable message\n    try:\n        return datetime.now().strftime(date_format)\n    except Exception as exc:\n        raise ValueError(\n            f\"Invalid date_format '{date_format}': {exc}. \"\n            \"Use valid strftime directives (e.g. '%Y' for year, '%m' for month).\"\n        ) from exc\n\n# Example usage:\n# print(get_current_datetime_string(\"%Y-%m-%d %H:%M:%S\"))\n```",
        "fr": "Principes clés : noms explicites, validation immédiate (TypeError/ValueError), messages d’erreur actionnables (indiquer la valeur reçue et le format attendu). Exemple fonctionnel réutilisable :\n\nfrom datetime import datetime\n\ndef _ensure_non_empty_str(name: str, value):\n    if not isinstance(value, str):\n        raise TypeError(f\"{name} must be a str, got {type(value).__name__}\")\n    if value.strip() == \"\":\n        raise ValueError(f\"{name} cannot be empty or whitespace\")\n\ndef get_current_datetime(date_format: str = \"%Y%m%d %H:%M:%S\") -> str:\n    _ensure_non_empty_str(\"date_format\", date_format)\n    try:\n        return datetime.now().strftime(date_format)\n    except Exception as exc:\n        # strftime raises ValueError on bad format codes in some implementations\n        raise ValueError(\n            f\"Invalid date_format '{date_format}'. Use strftime format tokens like '%Y-%m-%d %H:%M:%S'.\"\n        ) from exc\n\ndef parse_date(date_str: str, date_format: str = \"%Y-%m-%d\") -> datetime:\n    _ensure_non_empty_str(\"date_str\", date_str)\n    _ensure_non_empty_str(\"date_format\", date_format)\n    try:\n        return datetime.strptime(date_str, date_format)\n    except ValueError as exc:\n        raise ValueError(\n            f\"Cannot parse date_str='{date_str}' with date_format='{date_format}'. \"\n            f\"Expected example: {datetime.now().strftime(date_format)}\"\n        ) from exc\n\nPoints qu’une bonne réponse couvrirait :\n- vérifications de type et contenu (non vide) ;\n- messages indiquant la valeur reçue et le format attendu ;\n- levée immédiate d’erreurs spécifiques (TypeError vs ValueError) ;\n- tests unitaires montrant cas valides et erreurs attendues."
      }
    },
    "ex_claude_certified_architect_foundations__02_006": {
      "correction": {
        "en": "Provide a deterministic loop that calls Claude repeatedly, detects structured tool requests in the assistant reply, executes the named tool from a registry, appends the tool result to the conversation, and repeats until no tool request is present. Key points:\n- Require Claude to emit tool calls as a single JSON object: {\"tool_use\": {\"name\": \"...\", \"input\": {...}}}.\n- Parse that JSON from the assistant reply (robust to whitespace).\n- Maintain a max-iteration guard to avoid infinite loops.\n- Append tool results as assistant messages so Claude can continue reasoning.\n\nExample Python implementation (adapt to your Claude client):\n\n```python\nimport json, re, datetime\n\n# Example tool implementations\ndef get_current_datetime(_input):\n    return {\"now\": datetime.datetime.utcnow().isoformat() + \"Z\"}\n\ndef add_duration_to_datetime(input):\n    base = datetime.datetime.fromisoformat(input[\"datetime\"].replace(\"Z\",\"\"))\n    result = base + datetime.timedelta(days=int(input.get(\"days\",0)))\n    return {\"result\": result.isoformat() + \"Z\"}\n\nTOOLS = {\n    \"get_current_datetime\": get_current_datetime,\n    \"add_duration_to_datetime\": add_duration_to_datetime,\n}\n\ndef call_claude(messages):\n    # Replace with actual API call; returns string assistant reply.\n    # For testing, you can simulate expected JSON tool requests.\n    raise NotImplementedError\n\ndef extract_tool_request(text):\n    m = re.search(r'(\\{.*\"tool_use\".*?\\})', text, re.DOTALL)\n    if not m: return None\n    try:\n        obj = json.loads(m.group(1))\n        return obj.get(\"tool_use\")\n    except json.JSONDecodeError:\n        return None\n\ndef run_conversation(initial_messages, max_rounds=10):\n    messages = initial_messages[:]\n    for _ in range(max_rounds):\n        assistant_text = call_claude(messages)\n        messages.append({\"role\":\"assistant\",\"content\":assistant_text})\n        tool_req = extract_tool_request(assistant_text)\n        if not tool_req:\n            return assistant_text  # final answer\n        name = tool_req[\"name\"]\n        inp = tool_req.get(\"input\", {})\n        if name not in TOOLS:\n            messages.append({\"role\":\"system\",\"content\":f\"Tool '{name}' not found.\"})\n            return \"Error: unknown tool\"\n        result = TOOLS[name](inp)\n        messages.append({\"role\":\"tool\",\"name\":name,\"content\":json.dumps(result)})\n    return \"Error: max rounds exceeded\"\n```\n\nThis pattern ensures reliable tool chaining, explicit tool-call formatting, safe parsing, and a loop exit strategy.",
        "fr": "Correction modèle (français) — Exécution et sécurisation d’un chaînage d’outils multi‑tour\n\nComportement attendu\n- Format de message : chaque réponse assistant peut inclure un champ structured tool_use = {\"name\": \"tool_name\", \"input\": {...}}. Si absent, la réponse est finale.\n- run_conversation boucle : appeler Claude → si tool_use présent, exécuter l’outil correspondant → injecter le résultat comme nouveau message assistant/tool_result → relancer Claude → stopper quand plus de tool_use.\n\nExemple fonctionnel (Python simplifié)\n\ndef run_conversation(messages, tools, call_claude, max_iterations=10):\n    \"\"\"\n    messages: list of {\"role\": \"user\"|\"assistant\", \"content\": str}\n    tools: dict name->callable(tool_input)->str\n    call_claude: callable(messages)->assistant_response dict possibly with 'tool_use'\n    \"\"\"\n    for i in range(max_iterations):\n        resp = call_claude(messages)\n        messages.append({\"role\": \"assistant\", \"content\": resp.get(\"content\", \"\")})\n        tool_request = resp.get(\"tool_use\")\n        if not tool_request:\n            return messages[-1][\"content\"]\n        name = tool_request.get(\"name\")\n        inp = tool_request.get(\"input\", {})\n        if name not in tools:\n            raise ValueError(f\"Unknown tool: {name}\")\n        try:\n            result = tools[name](inp)\n        except Exception as e:\n            messages.append({\"role\": \"tool\", \"name\": name, \"content\": f\"ERROR: {e}\"})\n            return f\"Tool {name} failed: {e}\"\n        messages.append({\"role\": \"tool\", \"name\": name, \"content\": result})\n    raise RuntimeError(\"Max iterations reached; possible infinite tool loop\")\n\nPoints de correction attendus\n- Validation stricte du nom d’outil et des inputs (schémas JSON).\n- Limite d’itérations, timeout et logging d’audit.\n- Gestion d’erreurs : erreurs tools ajoutées à la conversation et renvoyées proprement.\n- Sanitation des résultats avant réinjection (échapper code, retirer secrets).\n- Demander confirmation utilisateur pour actions sensibles (ex. suppression, accès privé).\n- Tests unitaires simulant tool failures et boucles infinies.\n\nRisques concrets et mitigations\n- Boucle infinie → max_iterations + détection de répétitions d’état.\n- Injection via input/outputs → valider et échapper.\n- Fuite de données sensibles → masque/filtre des résultats, politique d’autorisation.\n- Ressources/DoS → quotas, timeouts, circuit breaker.\n- Erreurs d’outils tiers → backoff, retries limitées, message d’erreur clair à l’utilisateur."
      }
    },
    "ex_claude_certified_architect_foundations__02_007": {
      "correction": {
        "en": "JSON schema (batch tool):\n{\n  \"name\": \"batch\",\n  \"description\": \"Run multiple tool invocations in parallel\",\n  \"parameters\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"invocations\": {\n        \"type\": \"array\",\n        \"items\": {\n          \"type\": \"object\",\n          \"properties\": {\n            \"tool\": { \"type\": \"string\" },\n            \"args\": { \"type\": \"string\", \"description\": \"JSON-encoded arguments\" }\n          },\n          \"required\": [\"tool\",\"args\"]\n        }\n      }\n    },\n    \"required\": [\"invocations\"]\n  }\n}\n\nNode.js example implementation (working):\nconst tools = {\n  translate: async ({text, to}) => `TRANSLATED(${to}): ${text}`,\n  sum: async ({numbers}) => numbers.reduce((a,b)=>a+b,0),\n  fetchUrl: async ({url}) => { /* fetch and return body */ return `fetched:${url}`; }\n};\n\nasync function run_batch(invocations) {\n  const promises = invocations.map(async (inv, idx) => {\n    const name = inv.tool;\n    const fn = tools[name];\n    if (!fn) return { index: idx, tool: name, success: false, error: 'Unknown tool' };\n    let args;\n    try {\n      args = JSON.parse(inv.args);\n    } catch (err) {\n      return { index: idx, tool: name, success: false, error: 'Invalid JSON args', details: err.message };\n    }\n    try {\n      const result = await fn(args);\n      return { index: idx, tool: name, success: true, result };\n    } catch (err) {\n      return { index: idx, tool: name, success: false, error: err.message };\n    }\n  });\n  return Promise.all(promises); // runs all invocations in parallel\n}\n\nUsage example:\nconst invocations = [\n  { tool: \"translate\", args: JSON.stringify({text:\"hello\",to:\"es\"}) },\n  { tool: \"sum\", args: JSON.stringify({numbers:[1,2,3]}) }\n];\nrun_batch(invocations).then(results => console.log(results));\n\nNotes:\n- Use Promise.all for parallel execution.\n- Return per-invocation status (success/error) so failures don’t abort other runs.\n- Validate unknown tools and JSON parse errors explicitly.",
        "fr": "Schéma Batch Tool (paramètre invocations)\n{\n  \"name\": \"BatchTool\",\n  \"description\": \"Exécute plusieurs outils en parallèle\",\n  \"parameters\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"invocations\": {\n        \"type\": \"array\",\n        \"items\": {\n          \"type\": \"object\",\n          \"properties\": {\n            \"tool\": { \"type\": \"string\" },\n            \"args\": { \"type\": \"object\" }   // arguments déjà en JSON\n          },\n          \"required\": [\"tool\",\"args\"]\n        }\n      }\n    },\n    \"required\": [\"invocations\"]\n  }\n}\n\nExemple d'appel attendu\n{\n  \"invocations\": [\n    { \"tool\": \"search\", \"args\": {\"q\":\"Claude batch\"} },\n    { \"tool\": \"summarize\", \"args\": {\"text\":\"...\"} }\n  ]\n}\n\nrun_batch — implémentation Python (asyncio)\n```python\nimport asyncio\nimport json\n\n# mapping des noms d'outils vers fonctions async\nTOOL_DISPATCH = {\n    \"search\": async_search,\n    \"summarize\": async_summarize,\n    # ajouter d'autres outils\n}\n\nasync def run_single(inv):\n    name = inv.get(\"tool\")\n    args = inv.get(\"args\", {})\n    if name not in TOOL_DISPATCH:\n        return {\"tool\": name, \"status\":\"error\", \"error\":\"unknown_tool\"}\n    try:\n        # validation/normalisation d'args si besoin\n        result = await TOOL_DISPATCH[name](**args)\n        return {\"tool\": name, \"status\":\"ok\", \"result\": result}\n    except Exception as e:\n        return {\"tool\": name, \"status\":\"error\", \"error\": str(e)}\n\nasync def run_batch(invocations):\n    tasks = [asyncio.create_task(run_single(inv)) for inv in invocations]\n    return await asyncio.gather(*tasks)\n\n# Exemple d'utilisation\n# results = asyncio.run(run_batch(payload[\"invocations\"]))\n```\n\nPoints clés d’une bonne implémentation\n- Valider schéma d’entrée (outil existant, args type object)\n- Exécuter en parallèle (asyncio/futures) pour gains de latence\n- Retour structuré par invocation (status, result/error)\n- Timeout/limite de concurrence et journalisation des erreurs\n- Ne pas permettre l'exécution d'outils non autorisés (whitelist)"
      }
    },
    "ex_claude_certified_architect_foundations__02_008": {
      "correction": {
        "en": "Best-practice format for highlighting citations with full source attribution (domain, title, URL, quoted text). Use this consistently and restrict sources to NIH.gov when providing medical/exercise advice.\n\n1) Display format (single citation)\n- Domain: nih.gov\n- Title: Exercise and Physical Activity (example)\n- URL: https://www.nia.nih.gov/health/exercise-physical-activity\n- Quoted text: \"Regular physical activity can help prevent... and improve quality of life.\"\n\n2) Inline usage example (short claim + citation)\n- Claim: Regular aerobic exercise reduces risk of chronic disease.[Domain: nih.gov; Title: Exercise and Physical Activity; URL: https://www.nia.nih.gov/health/exercise-physical-activity; Quote: \"Regular physical activity can help prevent...\"]\n\n3) Multiple citations (numbered)\n1. nih.gov — Exercise and Physical Activity — https://www.nia.nih.gov/health/exercise-physical-activity — \"Regular physical activity can help prevent...\"\n2. nih.gov — Benefits of Physical Activity — https://www.nih.gov/health/benefits-physical-activity — \"Physical activity improves cardiovascular health...\"\n\n4) Implementation rules (actionable)\n- Always include domain, exact page title, full URL, and verbatim quoted text in quotation marks.\n- If paraphrasing, still cite the source and indicate paraphrase (e.g., [paraphrased from nih.gov: Title, URL]).\n- Restrict search/filtering to site:nih.gov when gathering sources (e.g., Google: site:nih.gov exercise guidelines).\n- Include retrieval date for pages that may change: (Retrieved YYYY-MM-DD).\n- If recommending clinical action, add: \"Consult a clinician\" and cite NIH clinical guideline pages.\n\nThis ensures traceability, scientific backing, and clarity between quoted evidence and your summary.",
        "fr": "Format recommandé (remplaçable selon rendu) :\n1) Mettre le texte cité entre guillemets et/ou dans un bloc visuel distinct.\n2) Immédiatement sous la citation, fournir une attribution structurée : Domaine · Titre · URL · Date d’accès (et section/numéro de paragraphe si utile).\n\nModèle :\n\"…texte cité exact…\"  \nnih.gov · Titre exact de la page · https://www.nih.gov/chemin/vers-la-page · consulté le JJ/MM/AAAA\n\nExemple (remplacer par l’extrait exact de la page) :\n\"Les adultes doivent pratiquer au moins 150 minutes d'activité aérobique d'intensité modérée par semaine.\"  \nnih.gov · Physical Activity Guidelines for Americans · https://www.nih.gov/physical-activity-guidelines · consulté le 12/07/2026\n\nChecklist qu’une bonne réponse couvre :\n- Citation mot à mot et délimitée (guillemets/bloc).\n- Domaine précis (nih.gov) et titre exact de la page/document.\n- URL complète fonctionnelle et date d’accès.\n- Indication de la section/paragraphes si long document.\n- Vérifier la date de mise à jour et préférer pages officielles/guidelines.\n- Ne pas extraire hors-contexte ; résumer/paraphraser avec attribution si besoin.\n\nRisques concrets à signaler :\n- Citation inexacte ou sortie de son contexte ; informations erronées.\n- Page obsolète (recommandations mises à jour).\n- Lien mort ou URL interne changeante.\n- Surconfiance en une seule source sans vérifier consensus."
      }
    },
    "ex_claude_certified_architect_foundations__02_009": {
      "correction": {
        "en": "Step 3 — Normalization\n- Ensure embeddings are unit-length: v_norm = v / ||v||. Many embedding APIs return normalized vectors automatically; if not, normalize before storing and querying.\n\nStep 4 — Vector Database Storage\n- Store (embedding, id, chunk_text, metadata). Use a DB optimized for vector ops (FAISS, Annoy, Pinecone, Milvus).\n- Index with the same metric you’ll query (cosine). Keep chunk size ~200–1000 tokens and include source pointers.\n\nStep 5 — Query Processing\n- Convert the user question into an embedding with the same model and normalization procedure to ensure comparable vectors.\n\nStep 6 — Similarity Search\n- Compute cosine similarity: cosine(a,b) = (a · b) / (||a|| ||b||). If vectors are normalized, cosine(a,b) = a · b.\n- Cosine distance = 1 − cosine(a,b).\n- Query example (pseudo):\n  db.query(vector = q_emb, top_k = 5, metric = \"cosine\")\n\nStep 7 — Prompt Assembly\n- Retrieve top-K chunks, rank by similarity score.\n- Build a prompt template:\n  \"Use ONLY the following context to answer the question. Context: [chunk1] [chunk2] ... Question: [user question]\"\n- Provide source citations and ask the LLM to state if the answer is not supported by the context.\n\nPractical tips\n- Always use the same embedding model/version for indexing and querying.\n- Store similarity scores and provenance for filtering and explainability.",
        "fr": "Correction modèle (objectif : pipeline de recherche sémantique correct et robuste)\n\nValidation des étapes\n- Étape 3 : Préciser la normalisation L2 : v := v / ||v||2. Vérifier si l’API renvoie déjà des embeddings normalisés ; sinon normaliser côté stockage/requête.\n- Étape 4 : Choisir une vector DB adaptée (FAISS/Milvus/Qdrant/Pinecone/Weaviate). Index recommandés : HNSW (bonne latence/qualité), IVF+PQ (très grand scale). Stocker aussi métadonnées, id, texte brut et checksum.\n- Étape 5 : Utiliser strictement le même modèle et pré-traitement (tokenisation, suppression d’espace) pour la requête. Vérifier la dimension.\n- Étape 6 : Similarité cosinus : si vecteurs normalisés, cosine(v,q)=dot(v,q). Sinon configurer la DB pour metric=\"cosine\" ou normaliser à la volée. Choisir k, seuil min_score, et options d’approx (ef_search, nprobe) selon tradeoff latence/qualité. Considérer recherche hybride (BM25 + vector).\n- Étape 7 : Prompt assembly : inclure top-k extraits triés par score, ajouter provenance (id, position), dédupliquer overlap, respecter token budget (truncation/summary). Optionnel : reranker avec cross-encoder pour meilleure précision.\n\nRisques concrets et mitigations\n- Mismatch de modèle/dimension → vérifier checksum/dim avant requête.\n- Vecteurs non normalisés → fausses distances ; forcer L2.\n- Documents obsolètes → mise à jour/invalidation d’embeddings.\n- Hallucinations → inclure provenance et score, utiliser reranker ou threshold.\n\nPoints mesurables : recall@k, MRR, latence p99, index size."
      }
    },
    "ex_claude_certified_architect_foundations__02_010": {
      "correction": {
        "en": "1) Query embedding (Step 5)\n- Encode the user question with the same embedding model and preprocessing used for the corpus (same tokenizer, lowercasing/trimming if used).\n- Normalize the embedding to unit length to make cosine similarity a simple dot product:\n  embedding_q = model.encode(query)\n  embedding_q /= np.linalg.norm(embedding_q)\n\n2) Similarity search (Step 6)\n- Compute cosine similarity: cosine_similarity(a,b) = (a · b) / (||a|| ||b||). If vectors are normalized, similarity = a · b.\n- Cosine distance = 1 - cosine_similarity (closer to 0 = more similar).\n- Use your vector DB (FAISS, Annoy, Pinecone, etc.) to retrieve top-k matches by descending cosine similarity (or ascending cosine distance). Example (NumPy):\n  sims = embeddings_matrix @ embedding_q           # if embeddings_matrix rows are normalized\n  top_idx = np.argsort(-sims)[:k]\n- Apply filters: minimum similarity threshold (e.g., >=0.75), timestamp/metadata filters, deduplicate overlapping chunks.\n\n3) Prompt assembly (Step 7)\n- Select 3–8 most relevant chunks (fit model context window). For each chunk, include source attribution and a 1-2 sentence provenance line.\n- Construct prompt: system instructions -> retrieved context chunks (clearly labeled) -> user question. Example structure:\n  [SYSTEM] You are an expert assistant...\n  [SOURCE 1] <text chunk> (source: docA, score: 0.89)\n  ...\n  [USER] <original question>\n- Include an instruction to the LLM to cite sources, avoid hallucination, and state when info is insufficient.\n\nOperational tips\n- Store normalized embeddings to speed retrieval.\n- Monitor similarity distribution and tune k/threshold to balance recall vs. noise.\n- Watch token limits; consider chunk summarization or reranking by LLM if many candidates.",
        "fr": "Correction modèle :\n\nÉtapes concrètes :\n1) Conversion en embedding (étape 5) :\n- Utiliser strictement le même modèle et même prétraitement que pour la base (tokenisation, minuscules, arrêt éventuel).\n- Normaliser l’embedding (L2) : e = v / ||v||.\n\n2) Recherche par similarité (étape 6) :\n- Si vous stockez des vecteurs non normalisés, normalisez-les au stockage ou normalisez à la requête et utilisez IndexFlatIP (produit scalaire) pour obtenir cosine similarity.\n- Récupérer top_k (p.ex. k=5) puis calculer cosine_similarity = dot(e_query, e_doc). Cosine_distance = 1 - cosine_similarity.\n- Appliquer un seuil minimal (p.ex. sim >= 0.70) pour filtrer les résultats peu pertinents.\n- Optionnel : reranker les top_k avec un modèle cross-encoder si disponible.\n\n3) Assemblage du prompt (étape 7) :\n- Inclure uniquement extraits tronqués à la longueur de tokens utile (p.ex. 200-400 tokens par extrait).\n- Fournir attribution : [source_id, score].\n- Donner instruction explicite au LLM : \"Utilise uniquement les informations listées ci‑dessous. Si la réponse n’est pas dans les sources, dis 'Je n’ai pas l’information'.\" Cela réduit les hallucinations.\n- Respecter la fenêtre contextuelle du LLM ; concaténer question + extraits + instructions.\n\nExemple Python (FAISS + numpy) :\n```python\nimport numpy as np, faiss\n# embeddings_db: (N, D) déjà normalisés\nindex = faiss.IndexFlatIP(D); index.add(embeddings_db)\nq = normalize(get_embedding(query))  # shape (D,)\nD_scores, I = index.search(np.expand_dims(q,0), k)\nscores = D_scores[0]; ids = I[0]\ndistances = 1 - scores\n```\n\nPoints clés qu’une bonne réponse couvre : normalisation, choix de k et seuil, gestion des tokens, attribution des sources, mitigation des hallucinations, reranking optionnel."
      }
    },
    "ex_claude_certified_architect_foundations__02_011": {
      "correction": {
        "en": "Example implementation and workflow (concise, actionable):\n\n1) Populate vector store\n- Use zip(chunks, embeddings) to iterate and store both embedding and original text metadata.\n\nPython example:\n```python\nimport numpy as np\n\nclass VectorIndex:\n    def __init__(self):\n        self.ids = []\n        self.vectors = []   # list of normalized np arrays\n        self.meta = []      # list of dicts, e.g. {\"content\": chunk}\n\n    def add_vector(self, embedding, metadata, id=None):\n        vec = np.array(embedding, dtype=float)\n        vec = vec / np.linalg.norm(vec)  # normalize for cosine\n        self.ids.append(id or len(self.ids))\n        self.vectors.append(vec)\n        self.meta.append(metadata)\n\n    def search(self, query_embedding, k=2):\n        q = np.array(query_embedding, dtype=float)\n        q = q / np.linalg.norm(q)\n        sims = np.dot(np.stack(self.vectors), q)   # cosine similarity\n        # return as cosine distance = 1 - similarity\n        distances = 1.0 - sims\n        idx = np.argsort(distances)[:k]\n        return [{\"id\": self.ids[i], \"metadata\": self.meta[i], \"distance\": float(distances[i])} for i in idx]\n```\n\n2) Store chunks:\n```python\nfor chunk, emb in zip(chunks, embeddings):\n    store.add_vector(emb, {\"content\": chunk})\n```\n\n3) Query processing + search:\n- Generate user_embedding = get_embedding(\"what did software engineering department do last year\")\n- results = store.search(user_embedding, k=2)\n\nExample returned results (ordered by increasing distance):\n- {\"distance\": 0.71, \"metadata\": {\"content\": \"Section Two ...\"}}\n- {\"distance\": 0.72, \"metadata\": {\"content\": \"Methodology ...\"}}\n\nNotes:\n- We normalized vectors so higher cosine similarity -> lower distance (1 - sim).\n- Storing original \"content\" in metadata enables meaningful retrieval and answer synthesis.",
        "fr": "Exemple de correction modèle (Python-like) — implémentation concise des étapes 3–5 :\n\n```python\nimport math\nfrom heapq import nlargest\n\nclass VectorIndex:\n    def __init__(self):\n        self.vectors = []  # list of (id, embedding, metadata)\n\n    def add_vector(self, embedding, metadata):\n        # normaliser à l'insertion pour faciliter les comparaisons cosinus\n        norm = math.sqrt(sum(x*x for x in embedding)) or 1.0\n        emb_norm = [x / norm for x in embedding]\n        self.vectors.append((len(self.vectors), emb_norm, metadata))\n\n    def _cosine(self, a, b):\n        return sum(x*y for x,y in zip(a,b))\n\n    def search(self, query_embedding, k=2):\n        # normaliser requête\n        norm = math.sqrt(sum(x*x for x in query_embedding)) or 1.0\n        qn = [x / norm for x in query_embedding]\n        scores = [(self._cosine(qn, emb), meta) for (_id, emb, meta) in self.vectors]\n        topk = nlargest(k, scores, key=lambda t: t[0])\n        return topk  # list of (similarity, metadata)\n\n# --- Étape 3 : peupler l'index\nindex = VectorIndex()\nfor chunk, emb in zip(chunks, chunk_embeddings):\n    index.add_vector(emb, {\"content\": chunk, \"source\": \"document_X\"})\n\n# --- Étape 4 : embedding de la requête\nuser_embedding = embed(\"ce que le département d'ingénierie logicielle a fait l'année dernière ?\")\n\n# --- Étape 5 : similarité et résultat attendu\nresults = index.search(user_embedding, k=2)\n# Ex : results -> [(0.72, {'content': 'section méthodologie', ...}),\n#                   (0.71, {'content': 'section deux', ...})]\n```\n\nPoints clés attendus dans la réponse :\n- utilisation de zip(chunks, embeddings) pour assurer alignement ;\n- conservation du texte original dans metadata (clé \"content\") ;\n- normalisation des vecteurs pour cosinus ;\n- search renvoie top-k avec scores de similarité (ex. 0.72, 0.71)."
      }
    },
    "ex_claude_certified_architect_foundations__02_012": {
      "correction": {
        "en": "Steps\n1. Clone the tools list, clone the last tool schema, add a cache_control field set to { type: \"ephemeral\" }, and replace the last tool in the cloned list. This avoids mutating original tool schemas.\n2. Replace the plain system prompt string with a text-block dictionary that includes cache_control: { type: \"ephemeral\" }.\n3. Construct the request with the modified tools first, then the wrapped system prompt, then the messages — preserving cache order: tools → system prompt → messages.\n4. Send the single request; both cache breakpoints (tool and system prompt) are applied.\n\nWorking JavaScript example\nconst originalTools = [...]; // existing tool schemas (do not modify)\nconst messages = [...]; // user/assistant messages\n\n// 1. clone tools and add cache_control to last tool\nconst clonedTools = JSON.parse(JSON.stringify(originalTools));\nconst lastIndex = clonedTools.length - 1;\nclonedTools[lastIndex] = {\n  ...clonedTools[lastIndex],\n  cache_control: { type: \"ephemeral\" }\n};\n\n// 2. wrap system prompt in text block with cache_control\nconst systemPromptWrapped = {\n  role: \"system\",\n  content: [{\n    type: \"text\",\n    text: \"System instructions here...\",\n    cache_control: { type: \"ephemeral\" }\n  }]\n};\n\n// 3. build request preserving cache order\nconst requestPayload = {\n  tools: clonedTools,\n  messages: [systemPromptWrapped, ...messages]\n};\n\n// send request to chat API with requestPayload\n\nNotes\n- Use structuredClone or deep copy to avoid mutating originals.\n- You may set multiple cache breakpoints in a single request (tools and system prompt).",
        "fr": "Objectif atteint : activer la mise en cache par défaut pour les outils et les system prompts sans muter les schémas originaux, avec points de cache multiples et ordre de concaténation outils → prompt système → messages.\n\nExemple (Python) — modifications concrètes :\n```python\nimport copy\nimport json\nfrom typing import List, Dict\n\ndef prepare_tools_with_cache(tools: List[Dict]) -> List[Dict]:\n    tools_copy = copy.deepcopy(tools)                   # ne pas modifier l'original\n    if tools_copy:\n        last_tool = copy.deepcopy(tools_copy[-1])      # cloner le dernier schéma\n        # ajouter le champ cache_control\n        last_tool['cache_control'] = 'ephemeral'\n        tools_copy[-1] = last_tool                     # écraser dans la copie\n    return tools_copy\n\ndef wrap_system_prompt(system_prompt: str) -> Dict:\n    # envelopper dans un bloc text avec cache_control\n    return {\"type\": \"text\", \"text\": system_prompt, \"cache_control\": \"ephemeral\"}\n\ndef build_cache_key(tools: List[Dict], system_block: Dict, messages: List[Dict]) -> str:\n    # respecter l'ordre: outils -> prompt système -> messages\n    parts = [\n        json.dumps(tools, sort_keys=True),\n        json.dumps(system_block, sort_keys=True),\n        json.dumps(messages, sort_keys=True)\n    ]\n    return \"|\".join(parts)\n\ndef chat(tools, system_prompt, messages, enable_cache_tools=True, enable_cache_system=True):\n    if enable_cache_tools:\n        tools = prepare_tools_with_cache(tools)\n    system_block = wrap_system_prompt(system_prompt) if enable_cache_system else {\"type\":\"text\",\"text\":system_prompt}\n    cache_key = build_cache_key(tools, system_block, messages)\n    # utiliser cache_key pour lookups/stockage\n    # ... logique d'appel du modèle ...\n    return {\"cache_key\": cache_key, \"tools\": tools, \"system_block\": system_block}\n```\n\nBonnes pratiques:\n- Toujours deep-copy avant modification pour éviter effets de bord.\n- Utiliser le champ cache_control sur des blocs/objets précis pour multiples breakpoints.\n- Documenter la durée et la sémantique de \"ephemeral\".\n- Valider la sérialisation stable (sort_keys=True) pour cache_key reproductible."
      }
    },
    "ex_claude_certified_architect_foundations__02_013": {
      "correction": {
        "en": "Checklist and actionable test script for the “Connect → Tools → Run → Verify” workflow\n\n1. Setup and connect\n- Confirm server URL, port, and environment (dev/staging). Use test credentials and note server version/build.\n- Verify network reachability (ping/health endpoint) and authentication success.\n\n2. Navigate and select tool\n- Open Tools panel; confirm tool name/ID visible. Prefer stable identifiers (API endpoint or internal tool ID) if UI changes frequently.\n\n3. Input parameters\n- Use representative test data and a small immutable dataset seed. Include:\n  - Valid nominal case\n  - Edge values (max length, zero, null)\n  - Invalid values (wrong types, malformed IDs)\n- Record exact parameter values.\n\n4. Run tool and capture outputs\n- Click Run and capture:\n  - UI feedback (spinner, success/failure toast)\n  - Response payload/status codes\n  - Server logs and timestamps\n  - Response time\n\n5. Verify results (acceptance criteria)\n- For reads: returned fields match expected values for given ID.\n- For edits: state change persisted (read back and verify), timestamps or version increments as expected.\n- For chains: after edit, perform read to confirm change; after delete, confirm non-existence.\n- Confirm no unexpected side effects (other records unchanged).\n\n6. Failure handling and reporting\n- Capture screenshots, console logs, server trace. Reproduce steps and include minimal repro data.\n- Classify failures: validation error, server error (5xx), wrong data, UI mismatch.\n- Suggest rollback/reset steps or seed re-run.\n\nTips for evolving UI\n- Rely on backend endpoints or test harness when UI elements move.\n- Keep test cases idempotent and include cleanup.\n\nExample quick test: Document tool\n1) Read doc ID=123 → expect title=\"Intro\", author=\"A\", status=200.\n2) Edit doc ID=123 set title=\"Intro v2\" → run → read back → expect title=\"Intro v2\" and version+1.",
        "fr": "Ce que doit contenir une bonne correction (actionnable, vérifiable)\n\n1) Plan de test étape par étape\n- Préconditions : URL du serveur, compte test, jeu de données isolé, version UI.\n- Étapes manuelles : se connecter (capturer token), accéder à la page Outils, sélectionner l’outil, renseigner paramètres, cliquer Lancer, vérifier sortie et logs côté serveur.\n- Critères d’acceptation : réponse HTTP 200 + payload attendu, message d’erreur clair en cas d’échec, état du document cohérent après opérations en chaîne.\n\n2) Cas de test représentatifs (exemples)\n- Lecture simple : id_document valid → expect contenu, code 200.\n- Édition atomique : edit (field X → valeur Y) → relire → valeur prise en compte.\n- Chaine : lire → éditer → lire → vérifier delta attendu.\n- Paramètres invalides : id inexistant → 404 ou code d’erreur explicite.\n- Concurrence minimale : deux edits simultanés → vérifier gestion des conflits.\n\n3) Exemple d’invocation manuelle (curl)\n- Authentification : curl -X POST /login -d '{\"user\":\"u\",\"pass\":\"p\"}' → token\n- Appel outil : curl -H \"Authorization: Bearer $TOKEN\" -X POST /tools/doc/read -d '{\"docId\":\"abc\"}'\n- Réponses attendues (succès) : {\"status\":\"ok\",\"data\":{...}}\n- Réponse d’erreur : {\"status\":\"error\",\"code\":\"NOT_FOUND\",\"message\":\"Document introuvable\"}\n\n4) Points opérationnels et risques\n- Risques concrets : fuites de données de test, conditions de course sur edits, incohérences causées par UI obsolète, tokens expirés, validations côté client manquantes.\n- Mesures : utiliser jeux de données isolés, activer logging détaillé, capturer request/response, tests de régression après modifications UI.\n\n5) Automatisation recommandée\n- Écrire scripts Postman/newman ou tests end-to-end légers (pytest + requests) pour les cas critiques (lecture/édition/chaine)."
      }
    },
    "ex_claude_certified_architect_foundations__02_014": {
      "correction": {
        "en": "Implementation outline and concrete example for the producer–evaluator loop\n\n1) High-level loop\n- Producer: Claude generates/edits CADQuery script for the part.\n- Renderer: run CADQuery to build a solid, export mesh, render to an image (pyrender / trimesh / headless OpenGL).\n- Evaluator: compare rendered image to target using quantitative metrics (SSIM, MSE, edge overlap) and qualitative guidance from Claude.\n- Stop when metrics meet thresholds or max iterations reached; otherwise generate structured feedback and repeat.\n\n2) Concrete Python-style example (conceptual)\n- Dependencies: cadquery, trimesh/pyrender, pillow, scikit-image, requests (Claude API).\n- Comparison metrics: SSIM >= 0.92 and mean squared error (MSE) <= threshold; also compare silhouette IoU and key-dimension tolerances.\n\nPseudocode:\n```\nfor iter in range(max_iters):\n    # Producer: get CADQuery script from Claude (or local generator)\n    cad_script = claude_generate(prompt, previous_feedback)\n    solid = run_cadquery(cad_script)          # returns mesh\n    rendered = render_mesh_to_image(solid)    # consistent camera & lighting\n    metrics = compare_images(rendered, target)  # SSIM, MSE, IoU, edge-diff\n\n    if metrics['ssim'] >= 0.92 and metrics['mse'] <= 200 and metrics['iou'] >= 0.9:\n        accept_and_save(solid, rendered, metrics); break\n\n    # Evaluator: build structured feedback\n    feedback_prompt = format_feedback_prompt(metrics, rendered, target,\n                                             missing_features=[\"holes\",\"fillets\"],\n                                             dimension_errors=estimate_dim_diffs(solid,target))\n    feedback = claude_request(feedback_prompt)\n    previous_feedback = feedback\n```\n\n3) Feedback prompt example to Claude (send images/metrics):\n- “Compare the rendered image and target. Report 3 prioritized, actionable edits to the CAD script (exact features, dimensions to change, tolerances) to reduce the following metric gaps: SSIM X→target, silhouette IoU Y. Provide code snippets for CADQuery edits and suggested camera/lighting adjustments.”\n\n4) Engineering notes\n- Keep camera, lighting, background deterministic across iterations.\n- Log each iteration (script, images, metrics, Claude outputs).\n- Safety: validate CADQuery outputs before rendering; limit iteration budget.\n- Termination: metric threshold, max iterations, or no improvement for N consecutive iterations.",
        "fr": "Points clés d’une correction modèle — implémentation de l’evaluator–optimizer (français)\n\n1) Préparer un pipeline déterministe\n- Génération (producer) : script CADQuery qui produit un modèle paramétrique exportable (STEP/OBJ). Fixer seed, éclairage et caméra.\n- Rendu : exporter vers trimesh/pyrender ou Blender pour obtenir une image 2D comparable.\n\n2) Comparaison (evaluator) — mesures concrètes\n- Alignement préalable : estimer homographie/translation ou centre-of-mass pour aligner rendu et cible.\n- Metrics proposées : MSE, PSNR, SSIM (>0.95 cible), LPIPS (perceptual), IoU sur mask d’objet, comparaison de contours (Canny) et distance de Hausdorff.\n- Seuils d’acceptation explicites (ex. SSIM ≥ 0.95 ET IoU ≥ 0.98).\n\n3) Boucle d’optimisation & feedback actionable\n- Calculer deltas signifiants — ex. «épaisseur +0.4 mm», «rotation -2°», «offset x +1.2mm».\n- Format de feedback structuré : {metric: value, param_deltas: {name: delta}, suggestion: texte}.\n- Contrôles : max_iter=10, tolérance progressive, early stopping si stagnation (>3 itérations sans amélioration).\n\n4) Robustesse et risques\n- Risques : non-identifiabilité des paramètres, overfitting sur artefacts d’éclairage, rendus non reproductibles, minima locaux.\n- Mitigations : multi-vues, régularisation des paramètres, perturbations de rendu (augmentation).\n\n5) Exemple minimal (pseudo-Python)\nfrom cadquery import Workplane\n# build model -> export OBJ, render via pyrender -> image\nfor i in range(max_iter):\n    model = build_model(params)\n    render_img = render(model, camera)\n    score, mask = compare_images(target, render_img)  # retourne SSIM, IoU,...\n    if score['SSIM']>=0.95 and score['IoU']>=0.98: break\n    params = update_params(params, score)  # rule-based ou optimizer\n\nUne bonne réponse décrit ces composants, fournit seuils concrets, format de feedback et un exemple de boucle exécutable."
      }
    },
    "ex_claude_certified_architect_foundations__02_015": {
      "correction": {
        "en": "Step 1 — Send the original, constraint-heavy prompt and accept the first pass.\nExample initial prompt (user → model):\n\"Write a 400-word market analysis of renewable energy adoption in Southeast Asia. Do not mention AI or artificial intelligence. Use a professional, formal tone. No emojis, no informal phrases (e.g., 'hey', 'you know'), and no self-references (e.g., 'as an AI'). Include three actionable recommendations and cite one reputable source.\"\n\nStep 2 — Send a concise, targeted follow-up that lists the actual violations and requests a rewrite.\nExample follow-up (user → model):\n\"You violated three constraints in the previous output: (1) You used the phrase 'as an AI'; (2) You included an emoji in paragraph 2; (3) Tone shifted informal in paragraph 3. Please rewrite the entire article correcting these violations. Keep the structure, length (~400 words), the three recommendations, and the cited source. Produce only the corrected article (no extra commentary). After the article, add a 3-line compliance checklist confirming: 'No mention of AI', 'No emojis', 'Professional tone maintained.' If any constraint conflicts with content, explicitly state which and why.\"\n\nBest practices:\n- Point to exact phrases/paragraphs that broke rules.\n- Ask for a full rewrite rather than incremental edits to avoid missed spots.\n- Require a short compliance checklist to verify constraints.\n- If constraints are ordered, state priority (which to keep if conflicts arise).",
        "fr": "Étapes concrètes et prompt exemples\n\n1) Envoyer le prompt initial (acceptation d’une sortie imparfaite).\n- Exemple : « Rédige un e-mail professionnel (200–250 mots) sans mentionner ‘intelligence artificielle’ ni ‘IA’, sans émojis, ton formel, pas d’appel à l’action commercial direct. »\n\n2) Suivi ciblé sur violations observées — format à utiliser chaque fois.\n- Exemple de prompt de correction :\n  « Tu as produit un texte pour mon prompt précédent. Violations constatées :\n   1) Mention de “IA”/“intelligence artificielle” dans le 2e paragraphe.\n   2) Utilisation d’un émoji en fin de phrase.\n   3) Ton trop familier au début.\n  Réécris le texte initial en respectant strictement ces contraintes. Avant la réécriture, fournis une checklist binaire (Respectée: Oui/Non) pour chaque contrainte. Si une contrainte ne peut être respectée, explique brièvement pourquoi. Réponds uniquement par la checklist puis le texte final, sans autres commentaires. »\n\nValidation automatisée et rapide\n- Vérifier automatiquement : regex pour motifs (“IA|intelligence artificielle”), détection d’émojis (Unicode ranges), longueur, présence de formules familières (tu, salut).\n- Demander au modèle une auto-vérification finale : « Confirme en 1 phrase que toutes les contraintes sont respectées. »\n\nRisques concrets et mitigations\n- Risque : suppression superficielle (synonymes contournant contrainte). Mitigation : lister interdits exacts + regex.\n- Risque : perte d’information après réécriture. Mitigation : demander « conserve le sens et les faits initiaux ».\n- Risque : va-et-vient long. Mitigation : regrouper les violations par ordre d’importance et exiger checklist binaire.\n\nPoints clés d’une bonne réponse\n- Prompt de suivi cite violations exactes (copier-coller idéal).\n- Checklist binaire claire avant le texte.\n- Réécriture fidèle au sens + conformité stricte.\n- Méthode de validation (regex/auto-check)."
      }
    },
    "ex_claude_certified_architect_foundations__02_016": {
      "correction": {
        "en": "Goal: reliably inspect environment after every action so captions align with video/dialogue and errors are caught early.\n\nConcrete workflow (bash + tools)\n1) Extract audio\n- ffmpeg -i input.mp4 -ar 16000 -ac 1 audio.wav\n\n2) Generate timestamped captions with whisper.cpp\n- ./main -m models/ggml-large.bin -f audio.wav -osrt -otxt\n  -> produces captions.srt and captions.txt\n\n3) Quick smoke-check (exists/non-empty, exit code)\n- [ -s captions.srt ] || { echo \"STT failed\"; exit 1; }\n\n4) Visual verification: burn captions into video and review\n- ffmpeg -i input.mp4 -vf \"subtitles=captions.srt:force_style='Fontsize=24'\" review_burned.mp4\n\n5) Automated per-cue alignment check\n- For each SRT cue (start,end):\n  a) compute midpoint T = (start+end)/2\n  b) extract single frame: ffmpeg -ss T -i input.mp4 -frames:v 1 frame_N.png\n  c) OCR the frame: tesseract frame_N.png stdout > ocr_N.txt\n  d) compare normalized OCR text to cue text (string similarity, e.g., Python difflib ratio)\n  e) if similarity < threshold (e.g., 0.6), extract audio clip ffmpeg -i audio.wav -ss start -to end clip.wav and re-run ./main on clip.wav to confirm or flag for manual review\n\n6) Environment inspection best practices\n- Always read existing captions file before overwriting: cat captions.srt\n- Take screenshots after UI actions (upload, align, save) via headless browser or scrot to verify UI state\n- Log tool outputs, exit codes, and hashes of input files so agent can reason about changes\n- On mismatches, retry with higher-quality model, different acoustic preprocessing, or escalate for manual review\n\nThis gives deterministic checks (file presence, similarity thresholds, re-run on segments) and visual confirmation (burned review + screenshots) so the agent can reason about progress and handle errors.",
        "fr": "Correction modèle (points clés et exemples concrets)\n\nObjectif : démontrer comment un agent « inspecte » l’environnement après chaque action pour valider l’état et corriger les erreurs.\n\nÉtapes concrètes\n- Captures d’écran après action GUI :\n  - Exemple Puppeteer (Node.js) : \n    const browser = await puppeteer.launch(); const page = await browser.newPage();\n    await page.goto(url); await page.click(sel); await page.screenshot({path:'after_click.png'});\n    await browser.close();\n  - Ou bash (Linux) : scrot after_click.png ou gnome-screenshot -f after_click.png\n- Vérifier fichiers avant édition :\n  - Lire avant d’écrire : cat fichier.txt || python -c \"print(open('fichier.txt').read())\"\n  - Créer sauvegarde : cp fichier.txt fichier.txt.bak\n  - Appliquer diff après modification : git diff -- fichier.txt\n- Sous-titrage avec whisper.cpp (bash) :\n  - ./main -m models/ggml-small.bin -f input.wav -osrt output.srt\n  - Valider timestamps : parsez output.srt et vérifiez chevauchements, durée totale vs durée audio.\n- Contrôles automatiques (checklist d’inspection) :\n  - Confirmer action produite l’effet attendu (screenshot + DOM/textual diff)\n  - Vérifier absence d’erreurs (codes de sortie, logs)\n  - Valider invariants (timestamps monotones, tailles de fichiers, permissions)\n  - Rollback disponible (sauvegarde, git)\n  - Timeout et retry si l’état attendu n’apparaît pas\n\nRisques concrets à mentionner\n- Captures hors contexte (fenêtre masquée) -> fausses validations\n- Décalages temporaires (race conditions) -> sous-titres désynchronisés\n- Écrasement de fichiers sans sauvegarde -> perte de données\n- Permissions/format de modèle whisper incompatible -> échec d’extraction\n\nCe que doit contenir une bonne réponse : snippets fonctionnels (comme ci‑dessus), étapes d’inspection après chaque action, validations automatisées et plans de rollback."
      }
    }
  },
  "claude_certified_architect_foundations__03": {
    "ex_claude_certified_architect_foundations__03_001": {
      "correction": {
        "en": "Implementation checklist and example\n\n1) Validation & conditional inclusion\n- Treat system prompt as optional but never pass None into the API. Trim whitespace and treat empty string as absent.\n- Only include the system message in the request when a non-empty value exists.\n\n2) Make prompts configurable\n- Store system prompts outside code: configuration file, database, or environment variables keyed by role/context (e.g., \"billing_agent_v1\").\n- Support template variables ({{user_name}}) and prompt versioning.\n\n3) Safeguards\n- Don’t store secrets in prompts. Audit changes, log which prompt key was used, and write unit tests that assert the system message is present/absent as expected.\n\n4) Result / UX\n- Conditional, configurable system prompts let you swap behaviors per role or scenario without code changes, enabling guided, role-specific experiences.\n\nWorking example (Python):\n```python\nimport os, requests\n\ndef get_system_prompt(role):\n    # e.g., load from config or env\n    return os.getenv(f\"SYSTEM_PROMPT_{role.upper()}\", \"\").strip()\n\ndef build_request(role, user_prompt):\n    messages = []\n    system = get_system_prompt(role)\n    if system:                        # include only when non-empty\n        messages.append({\"role\":\"system\",\"content\":system})\n    messages.append({\"role\":\"user\",\"content\":user_prompt})\n    return {\"model\":\"claude-2\", \"messages\":messages}\n\n# usage\nreq = build_request(\"billing_agent\", \"How do I update my payment method?\")\nresp = requests.post(\"https://api.example.com/v1/chat\", json=req).json()\n```\n\nTests: assert that messages includes system when prompt exists and omits it when empty.",
        "fr": "Correction modèle (actionnable)\n\nImplémentation technique (pas-à-pas)\n1. Charger le system_prompt depuis une source configurable (fichier JSON/YAML, variables d'environnement, DB, UI d'administration). Ne pas hardcoder.\n2. Valider et normaliser: system_prompt = (value or \"\").strip(); si vide, considérer comme absent.\n3. Construire le payload messages en ajoutant la message système uniquement si system_prompt != \"\".\n4. Envoyer l'appel API avec la liste de messages résultante.\n\nExemple Python (fonctionnel)\n```python\ndef call_model(user_prompt, config):\n    system_prompt = (config.get(\"system_prompt\") or \"\").strip()\n    messages = []\n    if system_prompt:\n        messages.append({\"role\": \"system\", \"content\": system_prompt})\n    messages.append({\"role\": \"user\", \"content\": user_prompt})\n    return api_client.create(messages=messages)  # remplacez par client réel\n```\n\nBonnes pratiques de configuration\n- Variables par environnement / par cas d’usage (ex: \"onboarding_assistant\", \"legal_reviewer\").\n- Versionner chaque prompt et stocker métadonnées (auteur, date, usage).\n- Permettre templates avec placeholders et rendu sécurisé (ex: Jinja + échapper variables).\n\nTests et validations\n- Tests unitaires: cas avec prompt absent, vide, très long.\n- Contrôles runtime: longueur max, blacklist d’instructions dangereuses.\n\nRisques concrets et mitigations\n- Erreur API si None inclus → valider et filtrer.\n- Injection via prompts configurables → restreindre éditeurs, échapper/filtrer variables.\n- Prompts obsolètes ou contradictoires → contrôles de version et revue humaine.\n\nRésultat attendu\n- Flux adaptatif : l’agent devient un cadre guidé (rôle/tonalité) quand system_prompt présent, sinon comportement par défaut."
      }
    },
    "ex_claude_certified_architect_foundations__03_002": {
      "correction": {
        "en": "Make system prompts configurable and parameterized so the same model can be reused across roles, domains, and workflows.\n\nExample templated system prompt (with placeholders):\n\"You are {role} for {product}. Your goal: {goal}. Tone: {tone}. Allowed actions: {tools}. Required constraints: {constraints}. When user intent is unclear, ask up to 3 clarifying questions. Provide step-by-step instructions and cite sources when available.\"\n\nTwo concrete instantiations:\n- Customer support: role=“Support Agent”, product=“Acme SaaS”, goal=“resolve billing issues quickly”, tone=“empathetic, concise”, tools=“ticket_create, kb_search”, constraints=“no legal advice, do not expose PII”.\n- Legal assistant: role=“Legal Analyst”, product=“Contract Review”, goal=“identify risk clauses”, tone=“formal, precise”, tools=“document_upload, clause_highlight”, constraints=“add disclaimers; flag ambiguous clauses”.\n\nSmall Python example (template substitution):\ntemplate = \"You are {role} for {product}. Goal: {goal}. Tone: {tone}.\"\nconfig = {\"role\":\"Support Agent\",\"product\":\"Acme SaaS\",\"goal\":\"resolve billing issues\",\"tone\":\"empathetic\"}\nsystem_prompt = template.format(**config)\n\nBest practices:\n- Store prompts as versioned config (DB or Git); track changes and rollout.\n- Parameterize roles, tone, tools, and constraints; validate inputs before substitution.\n- Include guardrails (safety, privacy, legal); enforce length limits.\n- Test prompts with representative queries; collect feedback & metrics.\n- Allow runtime overrides and localization; log prompt usage for audits.\n\nResult: configurable prompts convert static Q&A into guided, role-aware interactions that ask clarifying questions, follow workflow steps, call tools, and apply domain constraints for consistent, reusable behavior.",
        "fr": "Pourquoi configurable: sépare le contenu métier de l’implémentation, permet réutilisation, tests A/B, localisation et évolution sans déploiement de code.\n\nImplémentation pratique (étapes actionnables)\n- Définir un schéma de template (id, version, slots, contraintes, métadonnées).\n- Stocker dans une config centralisée (DB/feature flags) avec contrôle d’accès et historique.\n- Fournir un renderer sûr qui remplace/échappe les variables et valide longueur/contraintes.\n- Prévoir profils par rôle/contexte (support, résumé, tutoriel) et fallback par défaut.\n- Versionner et fournir tests unitaires + scénarios d’intégration et métriques (cohérence, taux de réutilisation).\n\nExemple (template + rendu simple)\nTemplate JSON:\n{\"id\":\"agent_support_v1\",\"template\":\"Tu es un assistant {{role}} pour {{company}}. Ton ton: {{tone}}. Contrainte: {{constraints}}. Contexte: {{context}}\"}\nRendu (pseudo-JS):\nconst t=template.replace('{{role}}','agent').replace('{{company}}','Acme').replace('{{tone}}','empathetic').replace('{{constraints}}','pas de conseils médicaux').replace('{{context}}',userContext);\n\nRisques si codé en dur\n- Duplication, incohérences entre cas d’usage\n- Difficulté de mise à jour et rollback\n- Fuites d’informations sensibles dans le code\n- Tests et audits impossibles ou coûteux\n\nChecklist qu’une bonne réponse doit couvrir\n- schéma/versioning, stockage et contrôle d’accès, escaping/validation, profils de rôle, tests et monitoring, procédures de rollback."
      }
    },
    "ex_claude_certified_architect_foundations__03_003": {
      "correction": {
        "en": "Temperature controls how the model samples tokens from the probability distribution it assigns during generation. At temperature = 0 the model behaves deterministically: it always picks the highest-probability token. Increasing temperature flattens the distribution, boosting the chance of lower-probability tokens and producing more varied, creative, or unexpected outputs.\n\nKey effects and guidance\n- Temperature = 0: deterministic, consistent, minimal creativity. Use for data extraction, factual Q&A, templates, code generation, or test-suite comparisons.\n- Temperature 0.1–0.4: very reliable with slight variance. Good for precise summarization, instruction-following, editing.\n- Temperature 0.5–0.8: balanced creativity and coherence. Good for brainstorming, marketing copy, first drafts.\n- Temperature 0.9–1.0: high creativity, higher risk of incoherence and factual errors. Use for playful or highly novel writing.\n\nRisks and practical tips\n- Higher temperature increases hallucination and inconsistency; verify facts when using >0.5.\n- For reproducibility, use temperature = 0 or fix a sampling seed if available.\n- Temperature interacts with other sampling settings (top_p, max tokens) and prompt quality—tune together.\n\nQuick illustration (prompt: “Article title about AI safety”)\n- temp 0: “Ensuring Safe AI Deployment”\n- temp 0.7: “Building Trustworthy AI: Practical Steps for Safe Deployment”\n- temp 1.0: “Guardrails for Machine Minds: Rethinking Safety in an Uncertain Future”",
        "fr": "Correction modèle — points qu’un étudiant doit couvrir précisément :\n\n- Définition claire : la température est un paramètre décimal [0,1] qui re-scale la distribution de probabilité des tokens avant échantillonnage (softmax/T^−1). Température = 0 → comportement « greedy » déterministe ; température élevée → plus d’aléa.\n- Processus : tokenization → modèle calcule logits → softmax ajusté par 1/temperature → échantillonnage (ou argmax si temp = 0).\n- Effets concrets et valeurs recommandées :\n  - 0 (ou 0.0–0.01) : extraction de faits, requêtes API, génération reproductible.\n  - 0.1–0.3 : tâches factuelles / résumés courts (moins de paraphrases, faible hallucination).\n  - 0.4–0.6 : rédaction fluide, réponses créatives contrôlées.\n  - 0.7–1.0 : créativité élevée, variations et reformulations.\n- Bonnes pratiques actionnables :\n  - Pour réduire hallucinations, baisser la température ET utiliser top-p (≈0.9) ou top-k (≈40).\n  - Tester plusieurs valeurs et comparer sorties (ex. 3–5 itérations par réglage).\n  - Temp=0 est greddy, pas un softmax strict ; vérifier répétition ou blocage lexical.\n- Risques concrets : température trop haute → hallucinations, incohérences, sorties offensantes ; trop basse → phrases robotiques, perte de diversité.\n- Remarque technique : la sensibilité peut varier selon le modèle — calibrer empiriquement."
      }
    },
    "ex_claude_certified_architect_foundations__03_004": {
      "correction": {
        "en": "Key idea\n- Stream events from the model to deliver text progressively instead of waiting for the full response. React to event types and forward text deltas to the client immediately so the UI shows incremental text.\n\nServer-side: event loop (example in Python-style pseudo-code)\n- Call streaming API: iterator = client.messages.create(..., stream=True)\n- For each event:\n  - message_start: allocate message_id, send initial ack to client\n  - content_block_start: create content buffer/role\n  - content_block_delta: append delta.text to buffer; immediately push {\"type\":\"chunk\",\"id\":message_id,\"text\":delta} to frontend (WebSocket/SSE)\n  - content_block_stop: mark block finished\n  - message_delta: metadata updates (e.g., tokens, confidence) — forward if useful\n  - message_stop: finalize, send {\"type\":\"done\",\"id\":message_id}\n- Handle errors/timeouts: forward error code and partial text; support client cancel which calls model.cancel()\n\nMinimal Python-like handler (pseudo)\nfor ev in client.messages.create(..., stream=True):\n    if ev.type == \"message_start\":\n        ws.send(json.dumps({\"type\":\"start\",\"id\":id}))\n    elif ev.type == \"content_block_delta\":\n        ws.send(json.dumps({\"type\":\"chunk\",\"id\":id,\"text\":ev.delta}))\n    elif ev.type == \"message_stop\":\n        ws.send(json.dumps({\"type\":\"done\",\"id\":id}))\n\nFrontend behavior\n- Immediately render chunks as they arrive; preserve whitespace and incremental punctuation (append to a DOM text node).\n- Show a typing indicator until first chunk; smooth small updates to avoid flicker.\n- Merge tiny deltas (e.g., <50 chars) before re-render to reduce reflow.\n- Support cancel, reconnect, and finalization.\n\nPractical concerns\n- Buffering/backpressure: limit queue size; drop or pause model source if client slow.\n- Safety: run moderation on final text or on chunks if needed; avoid exposing unvetted content.\n- UX: keep consistent cursor/scroll behavior, show partial answers clearly labeled as “streaming.”",
        "fr": "Objectif attendu — correction modèle (concis, actionnable) :\n\nProcédure d’implémentation\n- Initialiser l’état à la réception de message_start (ID conversation, horodatage).\n- Sur content_block_start, ouvrir un buffer pour ce bloc (métadonnées : rôle, format).\n- Sur chaque content_block_delta, concaténer le delta au buffer et pousser immédiatement au frontend (débit contrôlé, flush toutes N ms ou après X caractères).\n- Sur content_block_stop, marquer ce bloc comme complet et exécuter post-traitements locaux (HTML-escape, entités).\n- Sur message_delta, mettre à jour le statut global et afficher méta-incréments (scores, cursor).\n- Sur message_stop, finaliser la réponse : vérifier intégrité, exécuter filtres de sécurité/modération, stocker transcript.\n\nGestion des erreurs et UX\n- Timeout réseau / interruption : conserver l’état, informer l’utilisateur, permettre tentative de reprise ou recharge.\n- Annulation utilisateur : envoyer cancel au modèle (si supporté), fermer iterator, et rollback/metadata.\n- Dédoublonnage : éviter d’afficher deux fois des deltas en gardant un index de dernier_offset appliqué.\n- Backpressure : buffer côté serveur si frontend lent, tronquer ou prioriser texte critique.\n- Sécurité : n’afficher pas de PII tant que la réponse n’est pas validée; exécuter filtres asynchrones sur deltas sensibles.\n\nExemple Python minimal\nclient = ... \nit = client.messages.create(model=\"claude-2\", messages=msgs, stream=True)\nbuffers = {}\nfor event in it:\n    t = event[\"type\"]\n    if t == \"message_start\": buffers[event[\"id\"]] = {\"text\":\"\"}\n    elif t == \"content_block_delta\":\n        buffers[event[\"id\"]][\"text\"] += event[\"delta\"]\n        send_to_frontend(event[\"id\"], event[\"delta\"])\n    elif t == \"content_block_stop\":\n        finalize_block(event[\"id\"])\n    elif t == \"message_stop\":\n        verify_and_store(buffers.pop(event[\"id\"]))\n    elif event.get(\"error\"):\n        handle_error(event[\"error\"])\n\nPoints d’évaluation clés pour l’exercice : gestion fine des événements, robustesse (timeouts/annulation), affichage incrémental fluide, sécurité des données, et post-traitement fiable."
      }
    },
    "ex_claude_certified_architect_foundations__03_005": {
      "correction": {
        "en": "Use case summary\n- When you need strictly-structured machine-readable output (e.g., JSON) and must remove Claude's incidental newlines, code fences, or brief natural-language commentary before parsing.\n\nConcrete implementation (Python)\n- Steps: strip whitespace, remove Markdown fences/backticks, extract the first JSON object/array, fix common minor formatting errors, then json.loads() and validate.\n\nExample:\n```\nimport re, json\n\ndef clean_and_parse(text):\n    # remove common markdown code fences and language tags\n    text = re.sub(r'```(?:json)?\\n', '', text).replace('```', '').strip()\n    # extract the first {...} or [...] block (dot matches newline)\n    m = re.search(r'(\\{.*?\\}|\\[.*?\\])', text, flags=re.S)\n    if not m:\n        raise ValueError(\"No JSON block found\")\n    payload = m.group(1).strip()\n    # fix trailing commas like {\"a\":1,}\n    payload = re.sub(r',\\s*([}\\]])', r'\\1', payload)\n    return json.loads(payload)\n\n# usage\nresponse_text = model_response['text']  # from Claude\ndata = clean_and_parse(response_text)\n```\n\nPractical tips\n- Instruct the model explicitly: \"Output only valid JSON, no commentary or code fences.\"\n- Add schema validation (jsonschema or pydantic) after json.loads().\n- On parse failure, log the raw output, surface it to a human or retry with a clearer prompt.",
        "fr": "Correction modèle (FR)\n\nExplication courte\n- text.strip() supprime uniquement espaces et sauts de ligne en début/fin. Utile pour enlever padding.\n- json.loads() décode une chaîne JSON valide en objet Python. Nécessite JSON syntaxiquement correct (guillemets doubles, pas de virgule finale, true/false/null exacts).\n- En pratique on combine nettoyage léger + extraction + tentative de réparation avant de parser.\n\nExemple Python robuste (fonctionnelle)\n```python\nimport re, json\n\ndef parse_model_json(output: str):\n    s = output.strip()\n    # essai direct\n    try:\n        return json.loads(s)\n    except json.JSONDecodeError:\n        pass\n    # extraire premier bloc JSON (objet ou liste) non-gourmand\n    m = re.search(r'(\\{.*?\\}|\\[.*?\\])', s, re.DOTALL)\n    if not m:\n        raise ValueError(\"Aucun bloc JSON détecté\")\n    candidate = m.group(1)\n    # retirer virgules finales avant } ou ]\n    candidate = re.sub(r',\\s*(?=[}\\]])', '', candidate)\n    # remplacer guillemets simples convertibles (prudent)\n    candidate = re.sub(r\"(?<=[:\\[,]\\s*)'([^']*)'(?=\\s*[,\\]}])\", r'\"\\1\"', candidate)\n    try:\n        return json.loads(candidate)\n    except json.JSONDecodeError as e:\n        raise ValueError(f\"JSON invalide après nettoyage: {e.msg}\")\n```\n\nCas d'utilisation\n- Toute sortie attendue strictement structurée (API, ingestion DB, tests automatiques).\n\nRisques concrets\n- JSON partiel ou texte ajouté avant/après → échec de parsing.\n- Virgules finales, guillemets simples, ou caractères d’échappement invalides.\n- Perte d’informations utiles si on supprime le commentaire du modèle.\n- Injectation de contenu malicieux dans champs non validés.\n\nBonnes pratiques finales\n- Demander explicitement «Output ONLY valid JSON».\n- Valider le résultat avec jsonschema.\n- Logger la sortie brute pour audit et fallback humain."
      }
    },
    "ex_claude_certified_architect_foundations__03_006": {
      "correction": {
        "en": "Key idea: measure prompt performance with an automated, repeatable evaluation pipeline before any engineering or deployment work. Follow this concrete workflow.\n\n1. Build test suites\n- Representative dataset: in-domain examples, edge cases, adversarial prompts, user variations.\n- Golden labels: expected outputs or high-quality rubrics for subjective tasks.\n- Safety/bias items and recovery cases.\n\n2. Define objective metrics & thresholds\n- Task metrics: accuracy/F1, ROUGE/BLEU, factuality score (automated fact-checker), instruction-following rate.\n- Safety metrics: % harmful/off-policy outputs, toxic word count.\n- UX metrics: average helpfulness (1–5 human rating), latency.\n- Example acceptance criteria: accuracy ≥ 90%, hallucination ≤ 5%, safety violations = 0.\n\n3. Automated evaluation pipeline\n- Run many prompts across model versions and random seeds.\n- Apply automatic scorers and aggregate results with confidence intervals.\n- Include human evaluation on a stratified sample for subjective metrics.\n\n4. Iterate on prompt\n- Analyze failure modes (systematic errors, edge-case patterns).\n- Try targeted fixes: clearer instruction, constrained format, few-shot exemplars, temperature/pen settings, chain-of-thought removal/addition.\n- Re-run tests; require statistical improvement before moving forward.\n\n5. Pre-production safeguards\n- Shadow/canary deployments, real-time monitoring, rollback triggers, and ongoing evaluation on live traffic.\n\nThis ensures objective, repeatable decisions and avoids the traps of untested or ad-hoc prompt deployment.",
        "fr": "Ce que doit contenir une bonne réponse (correction modèle) — concise et actionnable :\n\n1) Objectif et critères d'acceptation\n- Définir métriques claires avant ingénierie : pertinence/relevance (embedding cosine ≥ 0.8), exactitude/fact-check (taux d'assertions vérifiées ≥ 95%), taux d’hallucination acceptable < 2%, toxicité < seuil (ex. score Detoxify proche de 0), latence/throughput opérationnels.\n\n2) Pipeline d’évaluation reproductible\n- Jeu de test représentatif : cas typiques, cas limites, adversarial, données réelles, et sandboxing pour données sensibles.\n- Évaluations automatiques : similarité sémantique (cosine embeddings), fact-checker automatique, métriques de style/longueur, tests unitaires d’instructions.\n- Évaluations humaines : rubriques annotées (fidélité, utilité, sécurité), minimum 3 annotateurs, agreement (Cohen’s kappa ≥ 0.6).\n- Intégration CI : exécution automatique des tests à chaque itération de prompt + reports.\n\n3) Itération contrôlée\n- Mesurer baseline → formuler hypothèse → modifier prompt → A/B ou tests en shadow → n’accepter que si métriques surpassent le baseline et respectent critères de sécurité.\n- Déploiement progressif (canary), monitoring post-déploiement (alertes sur dérive, logs pour réexamen).\n\n4) Risques concrets si on saute l’évaluation\n- Hallucinations factuelles, biais non détectés, fuite de données, dommages réputationnels, non-conformité réglementaire, coûts d’intervention élevés.\n\nPoints attendus dans la réponse de l’étudiant : définition de métriques, construction du jeu de test, combinaison auto + humain, protocole d’itération (A/B, canary), seuils d’acceptation, et plan de monitoring post-déploiement."
      }
    },
    "ex_claude_certified_architect_foundations__03_007": {
      "correction": {
        "en": "Step 5 — Iterate & Optimize (concrete, actionable workflow)\n\n1. Analyze failures\n- Collect low-scoring Q/A pairs and categorize error types: factual errors, hallucinations, incorrect format, missing constraints, verbosity, unsafe content.\n- Count frequency per category to prioritize fixes.\n\n2. Make targeted edits\n- For factual/hallucination: instruct “If you’re unsure, say ‘I don’t know’ or list uncertainty and cite sources if available.”\n- For formatting: add an explicit output schema (e.g., “Reply in JSON with fields: answer, confidence (0–1), sources[]”).\n- For brevity/verbosity: add “Limit answer to N words / X bullet points.”\n- For style/expertise: prepend role/system instruction: “You are an expert X with Y years’ experience.”\n\nExample improvement:\nOriginal: \"Please answer the user's question [user_input]\"\nRevised: \"You are an expert product manager. Answer [user_input] in ≤120 words, include a short 3-step action plan, and label any assumptions.\"\n\n3. Create prompt variants\n- Generate 3–5 variants addressing different hypotheses (clarity, examples, constraints).\n- Optionally vary model params (temperature, max tokens).\n\n4. Re-evaluate\n- Re-run the same evaluation dataset across variants.\n- Compute per-variant average score and per-category error rates.\n- A/B test on held-out inputs if possible.\n\n5. Update dataset & rubric\n- Add edge/adversarial cases discovered in failures.\n- Refine grader rubric to reduce subjectivity (concrete criteria per score).\n\n6. Stopping conditions & logging\n- Stop when average score meets target (e.g., ≥9/10) or no meaningful improvement after 3 iterations.\n- Log prompt versions, changes, metrics, and rationale for reproducibility.\n\nAutomate: script steps 2–4, store results in a CSV, and visualize trends to guide decisions.",
        "fr": "Correction modèle — flux de travail d’évaluation itératif (5 étapes)\n\n1) Ébauche initiale (exemple concret)\nPrompt template :\n\"Tu es un assistant expert. Réponds à la question de l'utilisateur [user_input] en 3 paragraphes : 1) réponse synthétique, 2) explication détaillée, 3) sources et étapes actionnables. Sois concis, neutre, et cite les hypothèses si nécessaire.\"\n\n2) Jeu de données d'évaluation (3+ exemples)\n- Input A : \"Comment réduire le temps de chargement d'une page web React ?\"\n- Input B : \"Explique la différence entre inflation et déflation pour un non-spécialiste.\"\n- Input C : \"Rédige un e-mail professionnel refusant une proposition commerciale sans heurter le contact.\"\nInclure cas simples, ambigus, et adversariaux/edge-cases.\n\n3) Exécution\nAutomatiser : injecter chaque input dans le template, fixer des paramètres (p.ex. top_p/temperature faibles pour cohérence), batcher les requêtes, versionner les prompts et paramètres.\n\n4) Évaluation (système de notation actionnable)\nCritères (échelle 1–10) : Exactitude (40%), Clarté (25%), Complétude (20%), Format/Style (10%), Sécurité/Conformité (5%).\nScore final = somme pondérée. Calculer moyenne et écart-type ; identifier inputs à faible score pour diagnostic. Seuil d’acceptation par défaut : moyenne ≥ 8/10.\n\n5) Itération & contrôle qualité\nActions si score <8 : ajouter exemples few-shot, préciser contraintes, réordonner instructions, restreindre hallucinations (demander sources), ou simplifier l’instruction. A/B tester variantes (au moins 100 exemples si possible), conserver logs, utiliser tests unitaires pour régressions, répéter cycles jusqu’à convergence. Risques à surveiller : sur-ajustement au jeu de test, biais de données, incohérence d’évaluateurs humains."
      }
    },
    "ex_claude_certified_architect_foundations__03_008": {
      "correction": {
        "en": "Step 3 — Prompt execution (practical steps)\n- Load dataset (CSV/JSON) into a DataFrame. Create a prompt template string with placeholders.\n- For each row, format template with the row’s fields to produce a complete prompt.\n- Send prompts in batches to Claude, capture response text + metadata (prompt_id, runtime, model version, raw tokens, timestamp).\n- Example (Python-like pseudocode):\n  ```\n  import pandas as pd\n  from time import sleep\n\n  df = pd.read_csv(\"inputs.csv\")\n  def call_claude(prompt): return client.predict(prompt)  # replace with your client\n\n  outputs = []\n  for i,row in df.iterrows():\n      full_prompt = template.format(**row.to_dict())\n      resp = call_claude(full_prompt)\n      outputs.append({\"id\": row.id, \"prompt\": full_prompt, \"response\": resp})\n      sleep(0.2)\n  ```\n\nStep 4 — Grading (concrete rubric & implementation)\n- Define a 1–10 rubric with explicit criteria and weights (example: correctness 40%, relevance 25%, completeness 20%, clarity 15%).\n- Use an automated grader (another Claude prompt that grades responses given the rubric) or a human labeler.\n- Grade example grader prompt snippet:\n  \"Rate the assistant's RESPONSE to USER_PROMPT on a 1–10 scale using rubric: correctness(40%), relevance(25%), completeness(20%), clarity(15%). Return JSON {score, breakdown, comments}.\"\n- Compute overall metric = mean(score_i). Also compute median, std, and per-criterion averages.\n\nStep 5 — Iteration (how to change & decide)\n- Log changes and version prompts. Try one variable per iteration (temperature, instruction phrasing, examples).\n- Run A/B: split dataset or use paired evaluation; compare mean scores. Use t-test or bootstrap to check significance; require e.g., +0.5 mean improvement and p<0.05 or consistent improvement across folds to accept change.\n- Stopping criteria: marginal improvement < threshold, or max iterations reached.\n- Scale: prototype in Jupyter, then parallelize calls, add retry/backoff, store results in DB, and integrate CI for regression testing.",
        "fr": "Étape 3 — Exécution\n- Implémenter une boucle qui assemble le prompt complet pour chaque entrée (template + variables), en fixant systématiquement les paramètres du modèle (model, temperature, max_tokens).  \n- Gérer la mise en lot, la ré-essai et les limites de débit (backoff exponentiel). Journaliser prompts, métadonnées (seed, paramètres), réponse brute et horodatage pour traçabilité.\n\nExemple Python (pseudocode minimal) :\n```\nfor item in dataset:\n    prompt = template.format(**item)\n    resp = client.generate(model=\"claude-2.1\", prompt=prompt, temperature=0.0)\n    save_record(item_id=item[\"id\"], prompt=prompt, response=resp.text)\n```\n\nÉtape 4 — Notation\n- Utiliser une grille de notation multi-critères (ex. précision, pertinence, exhaustivité, clarté, sécurité), chaque critère 1–10. Exemple de pondération : précision 0.4, pertinence 0.25, exhaustivité 0.2, clarté 0.1, sécurité veto.  \n- Calculer score par paire : score = sum(critère_score * poids). Moyenne et écart-type sur l’ensemble. Fournir aussi distribution (histogramme) et proportion au-dessus d’un seuil.  \n- Pour robustesse : 2 évaluateurs humains par exemple + adjudication en cas de >2 points d’écart, ou utiliser bootstrap pour intervalle de confiance.\n\nÉtape 5 — Itération\n- Modifier une variable à la fois (prompt wording, exemples few-shot, instructions de format, temperature). Conserver un jeu témoin (control) pour comparaison.  \n- Exécuter N≥100 (idéal ≥300) exemples par version; comparer moyennes + test statistique (t-test/bootstrapping) pour significativité.  \n- Prioriser modifications à fort impact : clarifier l’intention, ajouter exemples négatifs/positifs, contraindre format de sortie.  \n- Automatiser tracking (versioning des prompts, métriques) et arrêter quand amélioration marginale < seuil défini."
      }
    },
    "ex_claude_certified_architect_foundations__03_009": {
      "correction": {
        "en": "Use explicit, descriptive tag names that reveal the content’s role and scope. Prefer specific nouns (and consistent casing) over generic terms.\n\nConcrete rules\n- Use descriptive, scoped names: sales_records, athlete_profile, billing_address — not records or data.\n- Keep a consistent convention (e.g., snake_case) and pluralize collections: sales_records vs sales_record_item.\n- Include category or provenance if helpful: external_input, user_prompt, system_instructions.\n- Separate content types: <code>…</code> vs <doc>…</doc> vs <example>…</example>.\n\nExample (XML-style)\n<sales_records>\n  <metadata source=\"crm\" date=\"2026-07-01\"/>\n  <record>\n    <customer_id>123</customer_id>\n    <amount>250.00</amount>\n  </record>\n</sales_records>\n\nUse cases\n- Separate code from documentation: <code> and <explanation>.\n- Athlete info: <athlete_profile>, <medical_history>, <performance_logs>.\n- Mixed content prompts: tag each piece so the model knows what to act on vs ignore.\n\nBenefits\n- Removes ambiguity about each text block’s purpose.\n- Helps smaller/simpler models parse structure and apply the right behavior.\n- Reduces prompt engineering mistakes when multiple content types appear.\n\nBest practices\n- Tag even short inputs (e.g., <user_input>) to clarify intent.\n- Keep tags concise but informative; avoid nesting deeper than needed.\n- Document tag set in a brief schema or legend included in prompts.",
        "fr": "Points clés d’une bonne réponse — spécifique et actionnable :\n\n1. Nommage descriptif et cohérent\n- Préférer des noms contextuels (sales_records, sales_record, customer, product, date, amount; athletes, athlete, stats, event) plutôt que data/records vagues.\n- Utiliser snake_case ou camelCase de manière consistante. Limiter la profondeur à ~3 niveaux quand possible.\n\n2. Séparation claire du code / documentation / données\n- Marquer explicitement les sections : <code language=\"python\">…</code>, <doc>…</doc>, <external_input source=\"csv\">…</external_input>.\n- Ajouter attributs pour métadonnées: <sales_record id=\"R123\" currency=\"EUR\" date=\"2026-07-25\">.\n\n3. Marquage des informations d’athlète et ventes (exemples)\n- Athlète :\n  <athletes>\n    <athlete id=\"A001\" country=\"FRA\">\n      <name>Marie Dupont</name>\n      <sport>athletics</sport>\n      <stats><event name=\"100m\" medal=\"silver\"/></stats>\n    </athlete>\n  </athletes>\n- Ventes :\n  <sales_records version=\"1.0\">\n    <sales_record id=\"S001\"><customer>ACME</customer><product>Widget</product><amount currency=\"EUR\">199.99</amount></sales_record>\n  </sales_records>\n\n4. Bonnes pratiques techniques\n- Toujours fermer les balises, indiquer la version/schema (version, schemaLocation).\n- Marquer l’origine des entrées externes (<external_input source=\"user/csv/api\">).\n- Indiquer unités et langue (currency, lang).\n- Eviter les noms ambigus et la sur-nesting; préférez attributs pour métadonnées.\n\nCes éléments permettent au modèle (surtout les plus simples) de comprendre le rôle de chaque bloc et de produire des sorties plus fiables."
      }
    },
    "ex_claude_certified_architect_foundations__03_010": {
      "correction": {
        "en": "Benefits — concise summary:\n- Removes ambiguity by explicitly labeling each text segment’s role (e.g., <user_input>, <constraints>, <context>).\n- Helps smaller/less capable models parse prompt structure, improving reliability and output quality.\n- Makes prompts easier to read, maintain, and reuse across tasks or teams.\n\nBest practices — actionable guidance:\n1. Define a small, consistent tag set (e.g., <context>, <input>, <goal>, <constraints>, <format>) and document meanings.\n2. Tag every content chunk, even short ones, so the model knows whether text is external input vs. an instruction.\n3. Use human-readable names, shallow nesting, and close tags correctly to avoid parsing confusion.\n4. Keep instructions separate from labelled data: include a clear instruction block that references tags (e.g., “Use <input> and produce output matching <format>”).\n5. Validate examples and iterate: test with simple models first and refine tags if outputs show misinterpretation.\n\nExample:\n<context>Customer account history shows late payments.</context>\n<input>Customer requests a refund for order #1234.</input>\n<constraints>Be concise, polite, ≤2 sentences.</constraints>\n<format>JSON: {\"response\":\"...\",\"action\":\"...\"}</format>\n\nUse this pattern to reduce ambiguity and increase model comprehension and consistency.",
        "fr": "Correction modèle (exemples et bonnes pratiques)\n\nAvantages concrets\n- Réduit l’ambiguïté en séparant rôle/instructions/contenu/format attendu.\n- Aide les modèles simples à repérer des segments (instruction vs données).\n- Améliore la qualité des réponses et la reproductibilité des prompts.\n\nBonnes pratiques actionnables\n1. Définir un petit jeu de balises constantes : <instruction>, <input>, <metadata>, <constraints>, <output_format>.\n2. Placer les balises dans un ordre fixe et documenté (ex. instruction → input → constraints → output_format).\n3. Pour courtes sections, toujours taguer (même une ligne) : <input>...</input> évite l’ambiguïté.\n4. Indiquer explicitement au modèle d’obéir aux balises (« Répond uniquement dans <output>…</output> »).\n5. Échapper les caractères spéciaux dans l’input ou valider/saniter les inputs externes.\n6. Utiliser des contraintes machine‑lisibles (ex. <max_sentences>2</max_sentences>) pour contrôler la forme.\n\nRisques et mitigations\n- Risque : le modèle ignore les balises → mitigation : instruction explicite et exemples.\n- Risque : injection via contenu tagué → mitigation : saniter/valider l’input avant inclusion.\n- Risque : balises inconsistantes → mitigation : choisir des noms simples et un schéma partagé.\n\nExemple de prompt (fonctionnel)\n<instruction>Résume le texte entre <input> en deux phrases.</instruction>\n<input>Le modèle doit apprendre à utiliser des balises XML pour structurer les prompts.</input>\n<constraints><max_sentences>2</max_sentences></constraints>\n<output_format><tag>summary</tag></output_format>\nRépond uniquement dans la balise <summary>...</summary>."
      }
    },
    "ex_claude_certified_architect_foundations__03_011": {
      "correction": {
        "en": "Benefits recap: clear, specific error messages let Claude identify the exact problem (\"date format cannot be empty\"), choose a corrective action, and retry with corrected parameters. To enable this, validate early, raise deterministic errors, and expose structured error text or JSON the model can parse.\n\nExample implementation and retry pattern:\n\n```\nfrom datetime import datetime\n\ndef get_current_datetime(date_format=\"%Y%m%d %H:%M:%S\"):\n    if not date_format:\n        # deterministic, parseable error message\n        raise ValueError(\"date format cannot be empty\")\n    return datetime.now().strftime(date_format)\n\ndef call_with_retry(date_format, max_retries=1):\n    attempt = 0\n    while True:\n        try:\n            return {\"success\": True, \"result\": get_current_datetime(date_format)}\n        except ValueError as e:\n            attempt += 1\n            msg = str(e)\n            # Expose structured error to the LLM/tool caller\n            error_payload = {\"success\": False, \"error\": {\"code\": \"EMPTY_FORMAT\", \"message\": msg}}\n            # Example automated correction based on exact message\n            if attempt <= max_retries and \"date format cannot be empty\" in msg:\n                date_format = \"%Y%m%d %H:%M:%S\"  # corrected value\n                continue\n            return error_payload\n```\n\nBest practices: use concise, consistent messages and optional error codes; return structured error objects; document corrective suggestions; implement bounded retry policies and logging so Claude can safely retry with corrected inputs.",
        "fr": "Le snippet donné illustre le principe, mais voici une correction modèle opérationnelle et des bonnes pratiques pour rendre les erreurs exploitables par Claude.\n\nPoints clés à couvrir\n- Valider rigoureusement (None, chaîne vide, espaces).\n- Lever des erreurs explicites et machine-parseables (code + message).\n- Ne pas divulguer d’informations sensibles dans les messages d’erreur.\n- Fournir un fallback ou des suggestions de correction (ex. format par défaut).\n- Exposer des erreurs structurées (JSON) pour permettre des réessais automatisés.\n- Protéger contre boucles de réessai infinies (max attempts/backoff).\n\nExemple de fonction améliorée (Python) :\n```\nfrom datetime import datetime\n\nclass InvalidParameterError(ValueError):\n    def __init__(self, code, message):\n        super().__init__(message)\n        self.code = code\n\ndef get_current_datetime(date_format=\"%Y%m%d %H:%M:%S\"):\n    if date_format is None or not str(date_format).strip():\n        raise InvalidParameterError(\"EMPTY_FORMAT\", \"date_format cannot be empty\")\n    try:\n        return datetime.now().strftime(date_format)\n    except Exception as e:\n        # fournir un code d'erreur clair pour le retry\n        raise InvalidParameterError(\"INVALID_FORMAT\", f\"invalid date_format: {e}\")\n```\n\nExemple d'utilisation côté assistant (logique de retry)\n- Si code == \"EMPTY_FORMAT\" => remplacer par format par défaut ou demander un format précis.\n- Si code == \"INVALID_FORMAT\" => renvoyer quelques formats valides et réessayer (max 3 tentatives).\n\nRisques concrets\n- Messages non structurés empêchent parsing automatique.\n- Révélations d’implémentation dans les erreurs.\n- Boucles de réessai infinies sans limites."
      }
    },
    "ex_claude_certified_architect_foundations__03_012": {
      "correction": {
        "en": "import { ToolParam } from \"anthropic.types\";\n\nexport const summarize_article_schema: ToolParam = {\n  type: \"object\",\n  title: \"summarize_article_schema\",\n  description: \"Summarize a web article into a compact, readable output tailored to user needs. Use the URL to fetch the article, then return a summary in the requested format and length. This tool is intended for downstream display or inclusion in reports; it should not hallucinate content not present in the source. Prefer conservative outputs when the source is ambiguous or truncated.\",\n  properties: {\n    url: {\n      type: \"string\",\n      description: \"The canonical HTTP(S) URL of the article to summarize. Provide a fully-qualified URL (including scheme) so the tool can fetch the exact resource; do not pass HTML or raw text in this field. If the article is behind authentication, note that the fetch will fail unless the tool is proxied with credentials. Use this to point the summarizer to the authoritative source.\"\n    },\n    format: {\n      type: \"string\",\n      enum: [\"concise\", \"bullet_points\", \"detailed\"],\n      description: \"Desired output style for the summary. 'concise' returns a short paragraph (1–3 sentences), 'bullet_points' returns 3–8 key bullets, and 'detailed' returns a multi-paragraph executive summary (4–8 sentences). Choose the style that best fits downstream consumption (e.g., UI card vs. report). If omitted, the tool defaults to 'concise'.\"\n    },\n    max_length: {\n      type: \"integer\",\n      minimum: 50,\n      maximum: 2000,\n      default: 250,\n      description: \"Approximate maximum token/character length for the generated summary. The tool will try to respect this limit while preserving coherent sentences. Use higher values for thorough summaries and lower values for compact displays. Values outside the range will be clamped to the min/max.\"\n    },\n    include_source_excerpt: {\n      type: \"boolean\",\n      default: false,\n      description: \"When true, include a short (1–2 sentence) excerpt from the article that supports the summary. This helps users verify the summary against the source and reduces hallucination risk. Only include text that the fetch returned; do not fabricate excerpts. Useful for transparency in sensitive contexts.\"\n    }\n  },\n  required: [\"url\"],\n  additionalProperties: false\n};",
        "fr": "Exemple de correction modèle (TypeScript/JSON schema + prompt à Claude) — respecte les conventions demandées et descriptions de 3–4 phrases :\n\nimport { ToolParam } from \"anthropic.types\"\n\nexport const create_invoice_schema: ToolParam = {\n  name: \"create_invoice\",\n  description: \"Outil pour créer et valider une facture dans le système comptable. Il prend les métadonnées de la facture et les lignes d'articles, applique des règles de validation (montant non négatif, date au format ISO) et renvoie un objet prêt à persister. Utiliser cet outil uniquement après authentification et vérification des droits. Fournit des erreurs structurées en cas d'échec de validation.\",\n  type: \"object\",\n  properties: {\n    invoice_id: {\n      type: \"string\",\n      description: \"Identifiant unique de la facture (UUID recommandé). Doit être généré côté client ou backend avant appel et permet de dédupliquer les requêtes en cas de réessai. Exemples: 'inv_123e4567-e89b-12d3-a456-426614174000'.\"\n    },\n    amount: {\n      type: \"number\",\n      minimum: 0,\n      description: \"Montant total de la facture en unité monétaire minorée (ex. cents). Doit être la somme pondérée des lignes d'articles et correspondre à la devise fournie. Valeurs négatives interdites.\"\n    },\n    currency: {\n      type: \"string\",\n      description: \"Code ISO 4217 de la devise (ex. 'EUR', 'USD'). Utilisé pour validation et conversion éventuelle; doit être supportée par le système comptable. Si absent, le système applique la devise par défaut du client.\"\n    },\n    due_date: {\n      type: \"string\",\n      format: \"date\",\n      description: \"Date d'échéance au format ISO 8601 (YYYY-MM-DD). Sert pour calculer les relances et pénalités; ne peut être antérieure à la date d'émission. Valider le fuseau si nécessaire.\"\n    },\n    items: {\n      type: \"array\",\n      items: {\n        type: \"object\",\n        properties: {\n          description: { type: \"string\", description: \"Description de l'article ou service (courte). Fournir détails suffisants pour la facturation et l'audit.\" },\n          quantity: { type: \"number\", minimum: 0.01, description: \"Quantité facturée; valeurs décimales autorisées pour services/poids.\" },\n          unit_price: { type: \"number\", minimum: 0, description: \"Prix unitaire en même unité monétaire que amount.\" }\n        },\n        required: [\"description\", \"quantity\", \"unit_price\"]\n      },\n      description: \"Liste des lignes de facture détaillées. Doit couvrir tous les éléments contribuant au montant total.\"\n    }\n  },\n  required: [\"invoice_id\", \"amount\", \"currency\", \"due_date\", \"items\"]\n}\n\nPrompt recommandé pour demander à Claude de générer le schéma et joindre la doc Anthropic :\n\"Écris le JSON Schema complet pour la fonction create_invoice en suivant la convention function_name_schema, fournis des descriptions de 3–4 phrases pour l'outil et chaque champ, et encapsule le résultat dans le type ToolParam. Joins également un bref rappel des meilleures pratiques d'usage des outils Anthropic (validation, limites de taille, gestion d'erreurs).\"\n\nChecklist qu'une bonne réponse couvre :\n- Nommage: function_name_schema et function_name cohérents.\n- Schéma JSON complet et réaliste avec types, formats, contraintes (minimum, format).\n- Descriptions de 3–4 phrases pour l'outil et chaque argument.\n- Encapsulation dans ToolParam importé pour sécurité de type.\n- Champs required corrects et règles de validation.\n- Prompt à Claude incluant demande d'attacher la doc Anthropic et tests d'exemple."
      }
    },
    "ex_claude_certified_architect_foundations__03_013": {
      "correction": {
        "en": "Batch tool — concise implementation plan and examples\n\n1) Batch tool interface (JSON Schema)\n- Add one tool to the assistant’s tools list:\n  - name: \"batch\"\n  - description: \"Call multiple tools in parallel; returns array of results.\"\n  - parameters (JSON Schema):\n    {\n      \"type\": \"object\",\n      \"properties\": {\n        \"invocations\": {\n          \"type\": \"array\",\n          \"items\": {\n            \"type\": \"object\",\n            \"properties\": {\n              \"id\": { \"type\": \"string\" },                 // client-provided id to map results\n              \"tool\": { \"type\": \"string\" },               // tool name to call\n              \"arguments\": { \"type\": \"object\" }           // arguments for that tool\n            },\n            \"required\": [\"tool\",\"arguments\"]\n          },\n          \"minItems\": 1\n        }\n      },\n      \"required\": [\"invocations\"]\n    }\n\n2) Example assistant tool call (what Claude should emit)\n- Assistant calls the batch tool once with multiple invocations:\n  {\n    \"tool\": \"batch\",\n    \"arguments\": {\n      \"invocations\": [\n        { \"id\": \"t1\", \"tool\": \"translate\", \"arguments\": {\"text\":\"Hello\",\"to\":\"es\"} },\n        { \"id\": \"t2\", \"tool\": \"summarize\", \"arguments\": {\"text\":\"Long document...\",\"length\":\"short\"} }\n      ]\n    }\n  }\n\n3) Backend handling behavior (required)\n- Validate each invocation.tool exists and arguments match that tool's schema.\n- Execute invocations concurrently (thread pool or async tasks), observe per-tool timeouts and rate limits.\n- Return a results array preserving the invocation ids:\n  [\n    {\"id\":\"t1\",\"tool\":\"translate\",\"status\":\"ok\",\"result\":{\"text\":\"Hola\"}},\n    {\"id\":\"t2\",\"tool\":\"summarize\",\"status\":\"ok\",\"result\":{\"summary\":\"...\"}}\n  ]\n- For partial failures include status:\"error\" and an error message per item.\n\n4) Integration notes / best practices\n- Enforce per-invocation timeouts and global timeout to avoid blocking the assistant.\n- Validate and sanitize arguments server-side to avoid injection.\n- Rate-limit and queue if too many concurrent invocations.\n- Preserve deterministic ordering via ids; let assistant reference results by id in follow-ups.\n\nThis lets Claude issue one tool call (batch) and get multiple tool results in a single assistant response round.",
        "fr": "Spécification et correction modèle — Batch Tool\n\nBut : implémenter un outil \"batch\" qui accepte \"invocations\": liste {tool: string, args: object} et exécute les outils en parallèle pour réduire les allers-retours.\n\nPoints clés à couvrir\n- Enregistrement : ajouter \"batch\" à la liste d'outils exposés.\n- Contrat d'entrée : invocations: [{ id?: string, tool: string, args: {...}, timeout_ms?: int }].\n- Contrat de sortie : [{ id?: string, tool: string, status: \"ok\"|\"error\"|\"timeout\", result?: any, error?: {code,message}, duration_ms }].\n- Validation : vérifier que chaque \"tool\" existe et que \"args\" correspond au schéma attendu.\n- Concurrence & timeouts : exécution concurrente (async/threads) avec timeout par invocation.\n- Robustesse : gérer échecs partiels, retenter selon stratégie, fournir erreurs explicites sans bloquer les autres invocations.\n- Sécurité & quotas : vérifier autorisations par outil, appliquer quotas et limites de payload.\n- Idempotence : accepter id/op-token pour éviter effets secondaires en cas de retry.\n- Logging & traçabilité : tracer id, durée, statut; ne renvoyer que données autorisées.\n\nExemple (Python asyncio minimal)\nasync def batch_tool(invocations):\n    results=[]\n    async def run(inv):\n        tool_fn = TOOLS.get(inv['tool'])\n        if not tool_fn: return {'id':inv.get('id'),'tool':inv['tool'],'status':'error','error':{'code':'NOT_FOUND'}}\n        try:\n            return {'id':inv.get('id'),'tool':inv['tool'],'status':'ok','result': await asyncio.wait_for(tool_fn(inv.get('args',{})), timeout=inv.get('timeout_ms',5)/1000)}\n        except asyncio.TimeoutError:\n            return {'id':inv.get('id'),'tool':inv['tool'],'status':'timeout','duration_ms':inv.get('timeout_ms')}\n        except Exception as e:\n            return {'id':inv.get('id'),'tool':inv['tool'],'status':'error','error':{'message':str(e)}}\n    tasks=[run(inv) for inv in invocations]\n    return await asyncio.gather(*tasks)\n\nExemple d'utilisation par l'Assistant : appeler uniquement tool=\"batch\" avec invocations regroupées; traiter la sortie structurée pour poursuivre la conversation."
      }
    },
    "ex_claude_certified_architect_foundations__03_014": {
      "correction": {
        "en": "Disadvantage: systems that use engineered pipelines, retrieval, tool integration, or fine-tuning require a more complex setup than prompt-only methods—more components, infrastructure, and maintenance.\n\nWhen to use which\n- Prompt-only: choose for prototypes, simple Q&A, one-off content, fast iteration, or when latency/cost must be minimal.\n- Complex setup: choose for multi-step workflows, grounded answers (RAG), tool use (calendars, DBs), strict accuracy, personalization, auditability, or high-throughput production.\n\nConcrete risks of the more complex approach\n- Engineering overhead and longer time-to-market\n- Higher hosting, compute, and data costs\n- Increased latency from extra steps (retrieval, API calls)\n- More failure modes and harder debugging\n- Data leakage or privacy exposure across components\n- Drift and maintenance burden (index updates, model refreshes)\n\nMitigations and practical steps\n- Start small: add one capability at a time (e.g., retrieval first, then tool calls)\n- Modular design and clear interfaces between components\n- Automated tests, end-to-end monitoring, and observability (latency, accuracy, errors)\n- Cost/performance benchmarking and fallbacks to prompt-only behavior\n- Secure data flows, access controls, and redaction for sensitive inputs\n- CI/CD for indices and model artifacts; scheduled re-indexing and validation\n\nDecision checklist\n- Is accuracy or grounding essential? Use complex setup.\n- Is speed, simplicity, or experimentation more important? Use prompt-only.",
        "fr": "Correction modèle — Réponse attendue\n\nObservation synthétique\n- Oui : principal inconvénient = mise en place plus complexe que des méthodes purement basées sur des prompts. Explication attendue : complexité d’ingénierie (indexation, embeddings, recherche vectorielle, pipeline de concaténation, reranking), coûts et opérations (hébergement de DB vectorielle, mise à jour des index, monitoring), latence et risque d’erreurs d’intégration.\n\nRisques concrets à lister\n- Hallucinations si documents non pertinents sont récupérés.\n- Biais ou fuite d’information si corpus mal nettoyé.\n- Dégradation de performance (latence, coût) à grande échelle.\n- Problèmes de fraîcheur des données (index obsolète).\n- Complexité d’évaluation et de debugging (difficulté à isoler causes).\n\nMitigations pratiques et étapes actionnables\n- Construire un MVP : débuter par prompts affinés, puis activer RAG sur un sous-ensemble.\n- Choisir un vector store managé (ex. Pinecone, Milvus géré) et mesurer coût/latence.\n- Normaliser/segmenter le texte (chunking, métadonnées) et filtrer le corpus.\n- Utiliser un composant de reranking ou un seuil de similarité pour refuser réponses peu pertinentes.\n- Mettre en place logs et métriques (latence, taux de rappel, précision, taux d’hallucination) et tests A/B.\n- Plan de mise à jour des index (pipeline ETL automatisé) et contrôle d’accès.\n\nQuand choisir chaque technique\n- Prompt-only : cas simples, règles, petit domaine, faible besoin de données externes.\n- RAG : grande base documentaire, besoin de réponses factuelles/à jour, diversité du contenu.\n\nChecklist qu’une bonne réponse couvrirait\n- Détailler les sources de complexité, risques concrets, stratégies de mitigation, critères de choix, et étapes pratiques pour déployer et maintenir une solution RAG."
      }
    },
    "ex_claude_certified_architect_foundations__03_015": {
      "correction": {
        "en": "Recommended UI behavior and sourcing rules (concise, actionable):\n\nAnswer rendering\n- Render the model’s synthesized answer as a single plain-text block at the top (no rich formatting inside the paragraph) so users read the conclusion immediately.\n- Inline citation anchors in the text like “[1]” that jump to the corresponding source card.\n\nSearch-result / citation blocks (separate, visually highlighted)\n- Show each source as a highlighted card (light border or shaded background) below the answer.\n- Each card contains, in this order:\n  1. Source badge: domain (example.com) and trust indicator (e.g., “Official”, “Peer-reviewed”, “Major news”).\n  2. Title as a clickable link (opens URL).\n  3. Full URL and retrieval timestamp (e.g., Retrieved 2026-07-25).\n  4. Short quoted excerpt (≤200 chars) — the exact text the assistant relied on, in quotation marks.\n  5. Metadata: author, publication date, DOI/arXiv number if applicable, paywall flag.\n- Number cards to match inline anchors ([1], [2], …).\n\nAuthoritative-source policy (concrete rules)\n- Use an allowlist for current events and specialist domains: established news orgs, government agencies, major academic journals, professional bodies, preprint servers with context.\n- Require at least one primary/official source (if available) and one independent corroborating source for factual claims.\n- Prefer newer timestamps for fast-moving topics; show “last-updated” and confidence level.\n\nFail-safe behavior\n- If no authoritative source found, explicitly state uncertainty and show the best-available sources with a “Low confidence — corroboration needed” tag.\n\nThis layout makes provenance transparent and supports fact-checking for current events and specialized knowledge.",
        "fr": "Critère de sélection des sources (actionnable)\n- Prioriser sources primaires et officielles (communiqués gouvernementaux, publications scientifiques, bases de données publiques) puis médias reconnus à long terme.  \n- Exiger date de publication/maj < 30 jours pour « current events ».  \n- Vérifier indépendance (éviter agrégateurs sans attribution) et présence d’auteurs/affiliations.  \n- Corroborer chaque affirmation clé avec ≥2 sources indépendantes.\n\nGabarit d’affichage UI (précis)\n- Réponse principale : bloc de texte brut, synthèse claire (2–4 phrases) avec incertitude explicitée.  \n- Références en ligne : bloc distinct visuellement (encadré), listées 1–5 résultats. Pour chaque résultat afficher : domaine, titre, URL cliquable, date, extrait cité (≤200 caractères) et label de crédibilité (ex. « officiel », « pair-reviewed », « presse reconnue »).  \n- Citations inline : utilisez [1], [2] dans le texte renvoyant aux blocs de résultat.  \n- Indiquer quelle phrase est soutenue par quelle source (ex. « Selon [1] … »).\n\nExemple de citation inline\n- Synthèse : « L’événement X a eu lieu le 10/07/2026 [1][2]. »  \n- Résultats :  \n  [1] domaine.com — Titre — URL — 10/07/2026 — “Extrait cité…”\n\nPoints que doit couvrir une bonne réponse\n- Source selection justifiée, dates, degré de corroboration, limitations/potentiel de biais, notes méthodologiques pour vérification.\n\nRisques concrets et atténuations\n- Risques : info périmée, biais d’échantillonnage (mêmes sources), paywalls, désinformation sophistiquée.  \n- Atténuations : horodatage, montrer sources multiples, marquer paywalls, signaler faible corroboration."
      }
    },
    "ex_claude_certified_architect_foundations__03_016": {
      "correction": {
        "en": "Recommended approach and concrete decisions to implement a reliable RAG system\n\n1) Preprocessing / chunking (practical defaults)\n- Chunk size: 500–1,000 tokens (≈300–700 words) per chunk for document QA; reduce to 200–400 tokens for very technical/narrow facts.\n- Overlap: 20–30% overlap between adjacent chunks to preserve context across boundaries.\n- Boundary rules: split on paragraph/heading/sentence boundaries; avoid splitting sentences.\n- Metadata: attach source ID, section heading, position, URL, last-modified timestamp.\n\n2) Retrieval architecture (concrete stack)\n- Indexing: embed chunks with a semantic embedding model (e.g., OpenAI embeddings, Mistral/CLIP-like local model).\n- Store: use vector DB (FAISS, Milvus, Pinecone) + optional inverted index for BM25 hybrid.\n- Retrieval: hybrid search = BM25 for lexical match + dense vector similarity for semantics; default top_k = 5–10.\n\n3) Prompting and answer generation\n- Compose prompt using only top_k chunks plus a short provenance block (source IDs).\n- Provide explicit system instruction: “Answer only from the provided excerpts; if not present, say ‘Insufficient information.’”\n- Optionally re-rank candidates with a cross-encoder to improve precision before generation.\n\n4) Evaluation & iteration (metrics and experiments)\n- Metrics: retrieval recall@k, end-to-end exact-match/F1 for QA, hallucination rate, latency/cost per query.\n- Experiments: sweep chunk sizes (200/500/1000 tokens), overlap (0/20/30%), top_k (3/5/10), embedding model variants; measure tradeoffs.\n- Acceptance criteria: retrieval recall@5 ≥ 90% for target queries and hallucination < 5%.\n\n5) Risks & mitigations (concrete)\n- Missing context across chunks → use overlap and re-ranker or allow model to request more context.\n- Incomplete or contradictory info → surface provenance and confidence; enforce “don’t guess.”\n- Cost/latency → smaller chunks + caching + tune top_k.\n- Privacy/staleness → redact PII pre-indexing; implement refresh pipeline.\n\nKey challenge: treat chunking/search as tunable: run systematic A/B tests on real queries, pick the chunk size/overlap/top_k that optimizes recall vs cost for your use case.",
        "fr": "Étape-par-étape opérationnelle (ce qu’une bonne réponse doit contenir)\n\n1) Stratégie de découpage\n- Choisir taille cible : 200–600 tokens par chunk (≈150–450 mots), recouvrement 50–100 tokens (10–20%) pour préserver le contexte.\n- Deux modes : découpage par structure (headings/paragraphes) si documents bien structurés ; découpage fixe si non-structuré.\n- Sauvegarder métadonnées : doc_id, heading, offset, longueur.\n\n2) Indexation & recherche\n- Calculer embeddings pour chaque chunk (modèle dense), stocker dans vector DB (FAISS/Pinecone/Milvus).\n- Utiliser similarité cosinus; hybrid search = BM25 (sparse) + dense pour robustesse sur termes précis et sémantique.\n- Paramètres initiaux : top_k_dense = 50, rerank_cross_encoder → sélectionner top_k_answer = 3–5.\n\n3) Assemblage du prompt\n- Inclure uniquement les top_k_answer chunks pertinents, avec leur provenance (doc_id, chunk_id, offset).\n- Prompt template : instruction system → question utilisateur → \"Utilise uniquement les extraits ci‑dessous; cite les sources\" → extraits numérotés.\n- Respecter la contrainte tokens du LLM ; tronquer si nécessaire en gardant diversité de sources.\n\n4) Détection et stratégies de secours\n- Définir seuil de similarité (ex. cosine < 0.6 → demander clarification ou élargir recherche).\n- Si réponses contradictoires entre chunks, demander vérification ou retourner les options avec provenance.\n\n5) Évaluation & itération\n- Mesures : recall@k, MRR, précision factuelle (F1 sur QA), latence & coût.\n- Tests : cas standards, questions exigeant contexte large, entrées adversariales.\n- Optimisation : ajuster taille/chunk overlap, hybrid vs dense, reranker, k.\n\nRisques concrets à adresser : pertes de contexte (trop petit chunk), hallucinations si sources insuffisantes, fuite de PII dans extraits, coût lié à large top_k, latence du reranker."
      }
    },
    "ex_claude_certified_architect_foundations__03_017": {
      "correction": {
        "en": "Step 4 — Vector DB storage (practical):\n- Store for each document: id, unit-normalized embedding (L2 norm = 1), source pointer (doc id, chunk range, URL), and metadata (timestamp, provenance).\n- Use a vector DB optimized for ANN + metadata filtering (e.g., Milvus, FAISS, Pinecone, Qdrant).\n- Build an ANN index (HNSW is common). Tune M/efConstruction for build-time/accuracy tradeoff. Persist raw embeddings separately if you need exact re-ranking.\n\nStep 5 — Query processing (must-match):\n- Use the same embedding model and identical text preprocessing (tokenization, lowercasing, chunking) used for source docs.\n- Normalize the query embedding to unit length (so cosine similarity = dot product).\n- Batch embeddings for throughput if handling many concurrent queries.\n\nStep 6 — Similarity search & ranking (practical):\n- Run ANN top_k (e.g., 50–200) with efSearch tuned for recall/latency.\n- Re-rank returned candidates by exact cosine similarity computed on stored (or raw) embeddings.\n- Apply a similarity cutoff (example heuristics: cosine ≥ 0.7 likely relevant; 0.5–0.7 borderline; <0.5 likely irrelevant) and use top-N (e.g., top 3–10) for downstream tasks.\n\nCosine similarity / distance (formula + example):\n- cosine(u,v) = (u · v) / (||u|| ||v||). With unit vectors, cosine = u · v.\n- cosine distance = 1 − cosine.\n- Example: u=[0.6,0.8], v=[0.0,1.0]. cosine = (0.6*0+0.8*1)=0.8; distance=0.2.\n\nOperational notes:\n- Recompute embeddings if you change models. Store version metadata. Monitor recall and latency, and use filtering by metadata to narrow search.",
        "fr": "Points qu’une bonne réponse doit couvrir (actionnable et spécifique)\n\n- Stockage\n  - Stocker les embeddings normalisés (vecteurs unitaires) pour que cosine(u,v) = dot(u,v).\n  - Format: float32, colonnes fixes, conserver métadonnées (document id, offset, source, timestamp).\n  - Choix DB: FAISS (on‑prem, configurable), Milvus/Weaviate/Pinecone (managed). Utiliser HNSW/IVF+PQ selon latence/stockage.\n\n- Ingestion et normalisation\n  - À l’ingestion : extraire texte → chunking cohérent → embed avec le modèle source → normaliser L2 (v/||v||) → stocker.\n  - Conserver version du modèle et dimension pour détection d’incompatibilités.\n\n- Recherche et re‑ranking\n  - Requête : embed + normaliser.\n  - Récupérer top‑K via index approximatif (e.g., HNSW) puis re‑classer par cosine exact sur ces K.\n  - Choisir K >= 50 si ré‑ranking, sinon K faible pour latence stricte.\n\n- Mesures et seuils pratiques\n  - Cosine similarity: 1 = identique. Règles empiriques : >0.8 très pertinente, 0.6–0.8 potentiellement utile, <0.5 généralement non pertinente (à valider par domaine).\n  - Cosine distance = 1 − cosine. Utiliser distance <0.2 pour filtre rapide si besoin.\n\n- Risques concrets\n  - Stocker embeddings non normalisés → métriques erronées.\n  - Mismatch de modèle/dimension → erreurs d’insertion/recherche.\n  - Index non reconstruit après mises à jour → réponses obsolètes.\n  - Fuite de données sensibles via métadonnées.\n  - Biais ou mauvaise couverture des documents → récupération non pertinente.\n\n- Validation/ops\n  - Tests: recall@k, MRR, latence P95, coût stockage.\n  - Automatiser rebuild d’index après bulk updates et monitorer drift d’embeddings.\n\nExemple Python (normalisation et distance)\n  v = emb / np.linalg.norm(emb)\n  q = q_emb / np.linalg.norm(q_emb)\n  cosine = np.dot(q, v)\n  cosine_distance = 1 - cosine"
      }
    },
    "ex_claude_certified_architect_foundations__03_018": {
      "correction": {
        "en": "Example implementation (Python-like pseudocode):\n\n# 1) Create index and populate\nstore = VectorIndex()                 # sample vector DB class\nfor i, chunk in enumerate(text_chunks):\n    emb = generate_embedding(chunk)   # same encoder used for queries\n    meta = {\n        \"text\": chunk,                # store original text for retrieval\n        \"source\": doc_id,\n        \"chunk_id\": i\n    }\n    store.add_vector(emb, metadata=meta)\n\n# 2) Query processing & similarity search\nuser_embedding = generate_embedding(user_question)\nresults = store.search(user_embedding, num_results=5)   # returns items with .distance and .metadata\n\n# 3) Use results (cosine distance: lower = more similar)\nfor r in results:\n    print(\"distance:\", r.distance)\n    print(\"text:\", r.metadata[\"text\"])\n    print(\"source:\", r.metadata.get(\"source\"))\n\nKey technical points to implement:\n- Always store the original text (and source/chunk id) in metadata — embeddings alone aren’t human-readable.\n- Use the same generate_embedding model for both documents and queries to ensure compatibility.\n- Interpret returned distances as cosine distance (lower means more similar).\n- Return multiple top chunks (e.g., 3–10) to provide comprehensive context for downstream tasks (summarization, QA).\n- Ensure the VectorIndex exposes add_vector(embedding, metadata) and search(embedding, num_results) returning distance+metadata for each hit.",
        "fr": "Correction modèle (extrait concis et immédiatement réutilisable)\n\nRésumé : générez des embeddings cohérents pour chaque chunk, insérez-les avec un identifiant et un dictionnaire de métadonnées contenant le texte d’origine, puis recherchez par embedding de requête et retournez plusieurs chunks triés par distance cosinus (plus faible = plus similaire).\n\nExemple Python minimal (fonctionnel conceptuel) :\ndef normalize(v):\n    import numpy as np\n    return v / (np.linalg.norm(v) + 1e-12)\n\nclass VectorIndex:\n    def __init__(self):\n        self.vectors = {}  # id -> vector (numpy)\n        self.meta = {}     # id -> metadata\n    def add_vector(self, id, vector, metadata):\n        self.vectors[id] = normalize(vector)\n        self.meta[id] = metadata\n    def search(self, query_vector, k=5):\n        import numpy as np\n        q = normalize(query_vector)\n        items = []\n        for id, v in self.vectors.items():\n            # cosine distance = 1 - cosine_similarity\n            dist = 1.0 - float(np.dot(q, v))\n            items.append((id, dist, self.meta[id]))\n        items.sort(key=lambda x: x[1])  # plus petit = plus similaire\n        return items[:k]\n\n# Usage\n# 1) generate_embedding doit être la même fonction / modèle partout\n# 2) pour chaque chunk:\n#    emb = generate_embedding(chunk_text)\n#    store.add_vector(chunk_id, emb, {\"text\": chunk_text})\n# 3) pour la requête:\n#    q_emb = generate_embedding(user_question)\n#    results = store.search(q_emb, num_results)\n\nPoints clés à couvrir dans une bonne réponse :\n- Toujours utiliser le même modèle/fonction generate_embedding (cohérence).\n- Stocker embedding + metadata{\"text\": original_chunk, \"source\":..., \"offset\":...}.\n- Normaliser les vecteurs pour cosinus fiable.\n- Renvoyer plusieurs chunks et inclure la distance cosinus.\n- Gérer batching, vérification dimensionnelle et seuils de similarité (ex : dist < 0.25)."
      }
    },
    "ex_claude_certified_architect_foundations__03_019": {
      "correction": {
        "en": "Trade-offs (concise)\n- Benefit: LLM re-ranking improves semantic accuracy — e.g., query “engineering team” correctly prioritizes a “software engineering” doc over “cybersecurity” because the LLM understands intent and roles.\n- Cost: adds latency, API cost, and potential nondeterminism.\n- When to use: high-precision needs (complex queries, ambiguous language, few candidates). Avoid for ultra-low-latency or very high-throughput systems unless mitigations applied.\n\nPractical mitigations\n- Prefilter with a fast vector or BM25 search (k = 50–200) before LLM re-rank.\n- Use a small deterministic model or temperature=0 and short context window.\n- Limit candidate count and minimize document length (summaries).\n- Batch and parallelize re-rank calls; cache results for repeated queries.\n- Return compact, machine-parseable output to reduce token usage.\n\nPrompt template (fill placeholders)\nUser question: {QUESTION}\nCandidate documents (id + short text): \n1. {ID1} — {SHORT_TEXT1}\n2. {ID2} — {SHORT_TEXT2}\n...\nInstruction: Rank the candidate documents by relevance to the user question. Return exactly N documents in decreasing relevance order. Output only a numbered list of document ids with a brief (1–2 sentence) justification and a confidence score 0.00–1.00. Use temperature 0.\n\nConcrete example\nUser question: “engineering team hiring process”\nDocs:\n1 — Software engineering: hiring pipeline, interview rubrics...\n2 — Cybersecurity operations: threat monitoring...\n3 — HR: general hiring policy...\nReturn top 2.\n\nExpected output:\n1) 1 — Highly relevant: describes software engineering hiring pipeline and rubrics (0.92)\n2) 3 — Relevant: general hiring policy applicable to teams (0.55)",
        "fr": "Exemple de correction modèle (prompt ré‑ranker + bonnes pratiques)\n\nPrompt (à envoyer au LLM, temperature=0, max_tokens raisonnable) :\n\"Rôle : vous êtes un ré‑ranker qui juge la pertinence de documents fournis par rapport à une requête utilisateur.\nEntrées :\n1) Requête : « {user_query} »\n2) Documents candidats : liste JSON [{id: string, title: string, text: string}, ...] (inclure max 1000 tokens par doc, tronquer sinon)\nInstruction :\n- Ne vous basez que sur les documents fournis.\n- Pour chaque document, calculez une note de pertinence 0–100 relative à la requête.\n- Retournez les N documents les plus pertinents en ordre décroissant (N={N}).\nFormat de sortie (strict JSON) :\n[{ \"id\": \"...\", \"score\": 0-100, \"justification\": \"une phrase concise expliquant pourquoi ce doc est pertinent\" }]\nNe pas ajouter de texte hors JSON.\"\n\nExemples d'améliorations opérationnelles\n- Préfiltrer par similarité d'embeddings (top 50) avant appel LLM pour réduire latence/coût.\n- Mettre en cache les reranks pour requêtes fréquentes.\n- Limiter la taille des documents ou fournir des extraits.\nRisques concrets et mitigations\n- Hallucination : obliger \"Ne vous basez que sur les documents fournis\".\n- Fuite de données privées : filtrer/supprimer données sensibles avant envoi.\n- Coût/latence : préfiltre embeddings, batcher appels et limiter N.\n- Incohérence : temperature=0 et format JSON strict pour résultats déterministes."
      }
    },
    "ex_claude_certified_architect_foundations__03_020": {
      "correction": {
        "en": "Example method + message template (wildfire risk assessment)\n\n1) One-shot example (show first):  \n- Image A (annotated example): “Example: 60 trees/ha, nearest access 25 m, roof overhang 0.3 m → computed subscores: Trees 80, Access 10, Overhang 0 → Weighted sum = 80*0.5 + 10*0.3 + 0*0.2 = 44 → Final risk = 44/100 (Medium).”\n\n2) Target image (follow immediately):  \n- Image B (target to evaluate)\n\n3) Step-by-step analysis instructions to the model (alternate after images):  \n- Identify and mark objects: trees, roads, roof edges.  \n- Count trees by systematic grid-sampling or automated segmentation; report trees/ha.  \n- Measure distance from building centroid to nearest road (meters).  \n- Measure roof overhang (meters).  \n- Verify counts with second method (e.g., manual point count vs segmentation) and report differences.  \n- Compute subscores using these mappings:\n  - Tree density subscore (0–100): <30 trees/ha = 0; 30–60 linear to 50; >60 → 100.  \n  - Emergency access subscore: <30 m = 0; 30–100 m linear to 50; >100 m = 100.  \n  - Roof overhang subscore: <0.5 m = 0; 0.5–1.5 m = 50; >1.5 m = 100.  \n- Weights: Trees 50%, Access 30%, Overhang 20%. Output weighted sum 0–100 and short justification.\n\n4) Validation step: re-count using alternate method, compare ± tolerance (e.g., ±10% acceptable). If outside, flag “low confidence” and explain discrepancy.\n\nKey takeaway: Provide the example image+answer first, then the target image, and require explicit verification steps — image accuracy depends on precise prompt engineering, not just resolution.",
        "fr": "Correction modèle — ce que doit contenir une bonne réponse\n\nMéthode pas à pas (processus concret)\n1. Identifier et annoter les objets clés sur l’image : toitures, arbres, routes d’accès, cours d’eau, lignes électriques, végétation sèche.\n2. Mesurer/count : estimer la densité arborescente par quadrant (ex. % couverture par 10m²), compter obstacles >1 m près du bâtiment.\n3. Vérification croisée : refaire le comptage avec une grille différente ou méthode de segmentation (superpixels vs. détection d’objets).\n4. Agréger en scores normalisés (0–5) par facteur (densité, accès, débord de toit, combustible proche).\n5. Produire sortie structurée : table, score total (pondéré), et intervalle de confiance + images annotées.\n\nStructure du message (exemple de template utilisateur — alterner image/texte)\n- Image d’exemple (pièce jointe)\n- Texte : « Exemple : cette image → densité arbres=60% (score 4), accès=voie large (score 1). »\n- Image cible (pièce jointe)\n- Texte : « Instructions : appliquez la même méthode et renvoyez : (1) annotations, (2) scores [densité, accès, débord, combustible], (3) score global et confiance. »\n\nOne-shot prompting (exemple concret)\n« Voici un exemple annoté + résultat. Maintenant analyse l’image jointe en suivant exactement les mêmes définitions de score et format de sortie. »\n\nRisques concrets à signaler\n- Arbres à <3 m du toit, voies d’accès obstruées, absence de points d’eau, câbles électriques bas, pente favorisant la propagation, accumulation de débris sur toit.\n\nÀ retenir\n- Spécifier définitions de score, unités et format de sortie ; fournir un exemple annoté ; vérifier par méthode alternative. Prompt engineering = définitions + exemples + format strict."
      }
    },
    "ex_claude_certified_architect_foundations__03_021": {
      "correction": {
        "en": "Summary of rules and actionable guidance\n\n- Activation: caching is not automatic. To enable a cache checkpoint you must add an explicit cache breakpoint in a message block. Do not rely on implicit caching.\n\n- Text block format: always use longhand text blocks to attach cache metadata. Example:\n  { \"type\": \"text\", \"text\": \"Welcome, user.\", \"cache_control\": { \"breakpoint\": true } }\n  Do not use the shorthand string form \"Welcome, user.\" when you need cache control.\n\n- Cache scope: the cache stores ALL content up to and including the block that contains the breakpoint. Any content that appears after that breakpoint (later blocks/messages) is not cached by that checkpoint.\n\n- Content identity rule: follow-up requests can reuse cached content only if the content up to the breakpoint is byte-for-byte identical to the original (including whitespace and punctuation). If any difference exists, the cache is invalidated for that checkpoint. Recommendation: if inputs might vary (dates, tokens, whitespace), canonicalize them before checking identity.\n\n- Breakpoint locations: you may place breakpoints in:\n  - text blocks (use longhand)\n  - image blocks\n  - tool invocations (tool use)\n  - tool results\n  - tool schemas\n  - system prompts\n\nPractical checklist before enabling a breakpoint:\n1. Ensure the block is stable and should be reused.\n2. Use longhand format for text; include cache_control.breakpoint: true.\n3. Confirm follow-up requests will supply identical content up to the breakpoint or implement canonicalization.\n4. Avoid putting frequently changing data before the breakpoint.",
        "fr": "Rèponse modèle (précise et actionnable)\n\n1) Règle générale\n- Le cache n’est pas activé par défaut. Il devient effectif uniquement lorsqu’un bloc de message contient explicitement un cache breakpoint (champ cache_control activé). Le contenu mis en cache comprend tout jusqu’à CE point d’arrêt inclus ; le contenu après n’est pas mis en cache.\n\n2) Format exigé (exemple concret)\n- Utiliser le format longhand pour tout bloc texte :\n{ \"type\": \"text\", \"text\": \"Résumé du client …\", \"cache_control\": { \"enabled\": true, \"cache_id\": \"chat-123\", \"created_by\": \"assistant_v1\" } }\n\n3) Règle d’identité du contenu\n- Toute requête de suivi réutilisant ce cache doit avoir un contenu byte‑à‑byte identique jusqu’au point d’arrêt. Sinon invalider le cache. Implémentation recommandée : calculer et stocker un hash (SHA256) du concaténé des blocs jusqu’au breakpoint ; comparer sur requête suivante.\n\n4) Emplacements valides pour point d’arrêt\n- Blocs texte (ex. message assistant), blocs image, invocation d’outil, résultat d’outil, schéma d’outil, prompts système. Un breakpoint peut être posé sur n’importe lequel.\n\n5) Tests/Checklist de validation\n- Vérifier qu’un cache n’existe pas sans cache_control.enabled=true.\n- Vérifier que le cache contient uniquement les blocs jusqu’au breakpoint inclus.\n- Tester invalidation : modifier un caractère avant breakpoint → cache non réutilisé.\n- Tester réutilisation : identique jusqu’au breakpoint → cache utilisé.\n\n6) Risques concrets et mitigations\n- Cache poisoning (injection de contenu) → signer/hash + validation d’origine.\n- Données sensibles exposées → refuser cache pour champs marqués PII.\n- Stale responses après outils externes → inclure TTL et invalidation par événement.\n\nRéponse attendue : fournir le longhand JSON pour chaque bloc avec cache_control, stocker hash du contenu jusqu’au breakpoint, et tests décrits."
      }
    },
    "ex_claude_certified_architect_foundations__03_022": {
      "correction": {
        "en": "Steps to implement safe caching for tools and system prompt:\n\n1. Tools: copy list first, then clone/modify only the last tool\n- Rationale: avoids mutating original tool schema and restricts cache control to the last tool.\n- Example (Python):\n  ```\n  import copy\n\n  tools_orig = [ {...}, {...}, {\"name\":\"last_tool\",\"schema\": {...}} ]\n  tools_copy = copy.deepcopy(tools_orig)\n  last_tool = copy.deepcopy(tools_copy[-1])\n  last_tool['cache_control'] = {\"type\": \"ephemeral\"}\n  tools_copy[-1] = last_tool\n  # use tools_copy when sending to the model\n  ```\n\n2. System prompt: convert string → list with a single text block that includes cache_control\n- Required format:\n  ```\n  system_content = \"You are an assistant...\"\n  system_blocks = [\n    {\"type\": \"text\", \"text\": system_content, \"cache_control\": {\"type\": \"ephemeral\"}}\n  ]\n  ```\n- Use this list (system_blocks) instead of a plain system string when populating cache-aware fields.\n\n3. Cache application order\n- Apply caching in this order: tools → system prompt → messages.\n- Meaning: set cache_control on tools first, then the system prompt, then any message-level cache_control.\n\nNotes and best practices:\n- Always deep-copy original objects before modifying.\n- Restrict cache_control to only the intended element (last tool, system text block).\n- Validate final payload structure before sending.",
        "fr": "Étapes concrètes et exemple fonctionnel (JavaScript) :\n\n1) Protéger les schémas originaux : cloner en profondeur la liste d'outils (structuredClone ou JSON parse/stringify). Ne modifiez jamais l'array/objets originaux.\n2) Ne rajouter le champ cache_control que sur le DERNIER outil cloné, type \"ephemeral\".\n3) Convertir la chaîne système en une liste contenant un bloc {\"type\":\"text\", \"text\": system_content, \"cache_control\": {\"type\":\"ephemeral\"}}.\n4) Respecter l’ordre de cache : outils → prompt système → messages.\n\nExemple (Node-compatible) :\nconst originalTools = [...]; // votre liste d'outils\nconst systemContent = \"Contenu du prompt système\";\nconst messages = [...]; // messages existants\n\n// Deep clone (fallback si structuredClone absent)\nconst toolsCopy = (typeof structuredClone === \"function\") ?\n  structuredClone(originalTools) :\n  JSON.parse(JSON.stringify(originalTools));\n\nif (toolsCopy.length > 0) {\n  const lastIdx = toolsCopy.length - 1;\n  toolsCopy[lastIdx] = { ...toolsCopy[lastIdx], cache_control: { type: \"ephemeral\" } };\n}\n\n// Prompt système -> liste avec cache_control\nconst cachedSystemPrompt = [{ type: \"text\", text: systemContent, cache_control: { type: \"ephemeral\" } }];\n\n// Assemblage final dans l'ordre requis\nconst cachePayload = {\n  tools: toolsCopy,\n  system_prompt: cachedSystemPrompt,\n  messages: messages // ne pas modifier ici si on veut préserver originaux\n};\n\nBonnes pratiques supplémentaires : vérifier outils non vides (sinon ne rien ajouter), utiliser cloneDeep (lodash) pour structures complexes, et éviter Object.assign() in-place sur objets originaux."
      }
    },
    "ex_claude_certified_architect_foundations__03_023": {
      "correction": {
        "en": "Producer/evaluator loop (concise implementation plan + runnable Python skeleton and prompt examples)\n\nCore loop (high-level)\n- Producer: ask Claude to produce a CADQuery Python script that builds the part from the target photo.\n- Run CADQuery to produce STEP/STL.\n- Render the STEP/STL to a PNG.\n- Evaluator: compute image similarity (SSIM) and ask Claude to compare render vs original; produce feedback.\n- If SSIM < threshold or Claude flags inaccuracies, send feedback to Producer and iterate; else output STEP.\n\nMinimal runnable skeleton (replace placeholders with your Claude client and install cadquery, trimesh, pyrender, scikit-image):\n\nimport cadquery as cq\nimport trimesh, pyrender\nfrom skimage.metrics import structural_similarity as ssim\nfrom PIL import Image\nimport numpy as np\nimport io\n\ndef ask_claude(prompt): \n    # Replace with your Claude API call and return text response\n    return claude_client.generate(prompt)\n\ndef produce_cad_script(target_image_path, feedback=\"\"):\n    prompt = f\"\"\"You are a CADQuery expert. Given this image: {target_image_path}\nProduce a self-contained CADQuery Python script that builds the part. Return only code.\nFeedback: {feedback}\"\"\"\n    return ask_claude(prompt)\n\ndef run_cadquery(script_code, out_stl=\"model.stl\"):\n    # Execute produced code; expected to create 'result' (cq.Workplane/Shape)\n    loc = {}\n    exec(script_code, {\"cq\": cq}, loc)\n    result = loc.get(\"result\")\n    if result is None: raise RuntimeError(\"Script must set variable 'result'\")\n    cq.exporters.export(result, out_stl)\n    return out_stl\n\ndef render_stl(stl_path, out_png=\"render.png\", size=(512,512)):\n    mesh = trimesh.load(stl_path)\n    scene = pyrender.Scene.from_trimesh_scene(trimesh.Scene(mesh))\n    r = pyrender.OffscreenRenderer(*size)\n    color, _ = r.render(scene)\n    Image.fromarray(color).save(out_png)\n    return out_png\n\ndef compare_images(imgA, imgB):\n    A = np.array(Image.open(imgA).convert(\"L\"))\n    B = np.array(Image.open(imgB).convert(\"L\").resize(A.shape[::-1]))\n    score, diff = ssim(A, B, full=True)\n    return float(score), (diff*255).astype(np.uint8)\n\n# Loop\ntarget = \"target.jpg\"\nfeedback = \"\"\nfor i in range(6):\n    script = produce_cad_script(target, feedback)\n    stl = run_cadquery(script)\n    render = render_stl(stl)\n    score, diff = compare_images(target, render)\n    if score > 0.90:\n        print(\"Accept. Output:\", stl); break\n    # Ask Claude to compare and give concrete revision instructions\n    eval_prompt = f\"\"\"Compare target image {target} and render {render}.\nSSIM={score:.3f}. List concrete differences (dimensions, missing features, proportions), and produce specific edit instructions for the CADQuery script (line-level if possible).\"\"\"\n    feedback = ask_claude(eval_prompt)\n\nExample prompts\n- Producer prompt: \"Return only a CADQuery script that constructs the object visible in this image. Use measurable dimensions where possible; parametrize key dims.\"\n- Evaluator prompt: \"Compare images; list 3 concrete mismatches (e.g., 'hole diameter too small by ~20%', 'fillet missing on top edge'), and propose exact code edits.\"\n\nNotes / practical tips\n- Ensure the produced script assigns the final shape to variable result.\n- Use deterministic camera/view for rendering so comparisons are consistent.\n- Combine automated SSIM threshold with Claude’s textual feedback for robust acceptance.",
        "fr": "Correction modèle (objectif : boucle producer-evaluator avec rendu image et comparaison)\n\n1) Structure générale\n- Producer: fonction Python qui prend un vecteur de paramètres, construit le modèle CAD (cadquery), génère un rendu PNG à angle/illumination fixes.\n- Evaluator: compare le PNG rendu à l'image cible (automatiquement via métriques + Claude pour évaluation sémantique). Retourne un objet feedback structuré (diff_type, localisation, magnitude, suggestion_paramètres).\n- Boucle: tant que score < seuil et iterations < max_iter, appeler Producer avec paramètres mis à jour selon feedback.\n\n2) Exemple minimal (Python)\n```\nfrom cadquery import Workplane\nimport numpy as np\nfrom skimage.metrics import structural_similarity as ssim\nfrom PIL import Image\n\ndef build(params):\n    w = Workplane(\"XY\").box(params['x'], params['y'], params['z'])\n    w.val().exportStl('out.stl')  # ou pipeline de rendu\n    return 'out.png'\n\ndef compare(imgA_path, imgB_path):\n    A = np.array(Image.open(imgA_path).convert('L'))\n    B = np.array(Image.open(imgB_path).convert('L'))\n    score, diff = ssim(A, B, full=True)\n    return score, (diff*255).astype('uint8')\n\nparams = {'x':10,'y':20,'z':5}\ntarget = 'target.png'\nfor it in range(50):\n    out = build(params)\n    score, diff_img = compare(out, target)\n    if score > 0.98: break\n    # feedback heuristique: ajuster param selon zones de différence\n    # ex: si diff centroid à droite -> augmenter x\n    centroid = np.array(np.where(diff_img>128)).mean(axis=1)\n    if centroid[1] > A.shape[1]/2: params['x'] += 0.5\n    else: params['x'] -= 0.5\n```\n\n3) Intégration Claude pour feedback textuel\n- Prompt template: \"Compare ces images (base64) : donne 1) liste des différences visibles (forme, taille, position, trou manquant), 2) suggestions paramétriques précises (ex: augmenter x de 0.5 mm), 3) zone prioritaire à corriger.\"\n- Claude renvoie feedback structuré qui alimente l'update rule.\n\n4) Critères d'arrêt et logs\n- Score SSIM >= 0.98 OU aucune amélioration après N itérations OU divergences de paramètres.\n- Log: params, score, feedback, timestamp, diff image.\n\n5) Risques concrets et mitigations\n- Fausses différences dues à caméra/pose/illumination -> fixer paramètres de rendu identiques.\n- Overfitting au rendu (non physique) -> valider sur vues supplémentaires.\n- Boucle non convergente -> ajouter optimiseur (Bayesian, CMA-ES) et limites sur paramètres."
      }
    },
    "ex_claude_certified_architect_foundations__03_024": {
      "correction": {
        "en": "Best practice: accept the imperfect first pass, then send a concise, narrowly scoped correction prompt that references the exact violations and limits edits to only what you want fixed.\n\nHow to do it (stepwise):\n1. Run the complex prompt and save the AI’s output.\n2. Identify specific violations (e.g., tone too casual; length >200 words; missing bullet for feature X; uses passive voice).\n3. Send a focused follow-up that: (a) quotes or references the original output lines to change, (b) lists only the constraints to enforce, (c) explicitly forbids unrelated changes, and (d) requests format (diff, revised paragraph, or line numbers) and a one-sentence rationale for each change.\n\nFollow-up prompt template:\n\"Using the text below, fix only these issues: [list violations]. Do not change anything else. Show the corrected paragraph(s) only and include a one-line note describing each correction. Original text: [paste].\"\n\nConcrete example:\nOriginal excerpt: \"Our app might help families save money. It is really easy to use and could support many countries.\"\nFollow-up: \"Fix only factual certainty and remove vague claim 'many countries'. Keep length and tone. Provide corrected sentence(s) and one-line notes.\"\nCorrected output: \"Our app can help families reduce expenses by automating bill tracking. It is designed for English-speaking markets initially.\" Notes: \"Replaced 'might' with 'can' (more definite language supported by data). Removed 'many countries' and specified scope.\"\n\nWhen to use: when AI repeatedly misses specific constraints. This reduces cognitive load and yields more reliable fixes.",
        "fr": "Objectif : montrer comment accepter une sortie imparfaite puis demander une correction ciblée.\n\nÉtapes concrètes\n1) Accepter la sortie initiale et repérer précisément les violations (numéroter et citer extraits).  \n2) Envoyer un prompt de suivi qui : a) inclut la sortie précédente entre balises, b) liste exactement les violations à corriger, c) demande uniquement les corrections (pas de réécriture complète), d) exige format de réponse minimal (texte corrigé + courtes annotations).  \n3) Valider automatiquement ou manuellement les corrections.\n\nExemples\n- Prompt initial (résumé) : \"Rédige une fiche produit de 150–200 mots, sans adjectifs superlatifs, en voix active, et avec mesures en SI.\"  \n- Sortie AI (extrait) : contient adjectifs superlatifs, phrases passives, unités impériales.\n\nPrompt de suivi (à copier) :\n\"Voici la sortie précédente entre <<< >>>. Corrige uniquement les violations listées ci‑dessous sans modifier le reste du texte. 1) Supprime tous les adjectifs superlatifs (ex. 'le meilleur'). 2) Convertis les unités impériales en SI. 3) Remplace les phrases passives indiquées par [PASSIF] par une voix active. Réponds uniquement en fournissant (a) le texte corrigé et (b) pour chaque changement, une note courte entre crochets expliquant la modification.\"\n\nExemple de sortie corrigée :\n- Texte corrigé + annotations concises (ex. \"[modif: '5 ft' → '1,52 m']\").\n\nChecklist pour une réponse excellente\n- Identification précise des violations + extrait cité.  \n- Prompt de suivi clair, limité aux corrections.  \n- Format de sortie restreint (texte corrigé + annotations).  \n- Méthode de validation proposée.\n\nRisques et atténuations\n- Risque d'altérer le sens : exiger que l'IA signale chaque changement.  \n- Risque d'erreurs de conversion/grammaire : automatiser vérifications (unités, longueur de phrase).  \n- Risque de boucles itératives : limiter le nombre d'itérations et conserver version initiale."
      }
    },
    "ex_claude_certified_architect_foundations__03_025": {
      "correction": {
        "en": "Design the agent to (1) ask concise follow-ups when needed and (2) expose a small set of abstract, composable tools rather than many single-purpose ones.\n\nToolset (concrete, minimal)\n- generate_image(prompt, size, style) — produce cover art, frames, thumbnails.\n- text_to_speech(text, voice, lang) — produce narration audio files.\n- bash_ffmpeg(script) — run shell/FFmpeg to combine images/audio, add transitions, encode formats.\n- post_media(file, platform, caption, metadata) — upload and schedule to platforms.\n- get_user_input(question_id, prompt, schema) — structured follow-up question and validation.\n\nHow the agent asks for missing info\n- Ask targeted, minimal questions: “Which platform (TikTok/IG/YouTube)?”, “Aspect ratio?”, “Do you want a cover for approval first?”, “Preferred voice: female/male/neutral?” Use get_user_input to validate.\n\nExample simple workflow (one prompt: “Create a 30s IG reel from this script”)\n1. Ask for aspect ratio and voice if missing.\n2. text_to_speech(script, chosen_voice) → audio.mp3\n3. generate_image(cover_prompt, 1080x1920) → image.png\n4. bash_ffmpeg(combine image sequence + audio → reel.mp4)\n5. post_media(reel.mp4, Instagram, caption)\n\nExample approval-first workflow\n1. generate_image(cover_prompt) → sample_cover.png (return to user for approval)\n2. on approval: proceed with TTS + FFmpeg + post.\n\nWhy this is better\n- Few abstract tools compose into many workflows, reducing complexity and enabling interactive clarification without exploding tool count.",
        "fr": "Objectif de la correction : concevoir un agent modulaire capable de demander des informations utilisateur et de combiner peu d’outils abstraits pour couvrir des flux de travail simples et complexes.\n\nOutils abstraits recommandés (interface minimale et retours) :\n- run_cmd(command: string) -> {stdout, stderr, code, artifacts[]} (sandboxé, timeout, limits).\n- generate_image(prompt: string, size: string) -> {image_id, url, thumbnail}.\n- text_to_speech(text: string, voice: string) -> {audio_id, url, duration}.\n- post_media(media_ids: [id], platform: string, metadata: dict) -> {post_url, status}.\n- ask_user(question: string, choices?: [string], required?: bool) -> {response}.\n\nFlux d’exemple — “créer une vidéo avec image de couverture pour approbation” :\n1) ask_user: précisions (durée, style, voix, texte de voix-off, vouloir approbation ?).\n2) generate_image(prompt) → proposer miniature; ask_user(“Approuvez ?”) si demandé.\n3) text_to_speech(voice, script) → obtenir audio.\n4) run_cmd(FFmpeg assemble: inputs image/audio, templates) → artifact vidéo.\n5) post_media(video_id, platform, metadata).\n\nBonnes pratiques opérationnelles :\n- Valider et normaliser les entrées (dates, formats, tailles).\n- Demander uniquement les informations nécessaires, utiliser choix pour réduire ambiguïté.\n- Idempotence et journalisation (replay/retry sûr).\n- Sécurité : sandboxer run_cmd, limiter commandes et tailles, vérifier médias uploadés.\n- Gestion d’erreurs claire : codes d’erreur, messages d’action (recommencer, modifier, annuler).\n- Option approbation humaine avant actions irréversibles.\n\nUne bonne réponse décrit ces interfaces, illustre un flux concret et couvre validation, sécurité et gestion d’erreurs."
      }
    },
    "ex_claude_certified_architect_foundations__03_026": {
      "correction": {
        "en": "Before action — inspect\n- List files and metadata: ls -l, ffprobe -v error -show_entries format=duration,sample_rate,channels -of default=noprint_wrappers=1:nokey=1 video.mp4\n- Check existing captions/subtitles and formats (SRT/VTT), language, and speaker layout.\n\nRun Whisper CPP to generate timestamped captions\n- Example (assumes whisper.cpp binary called ./main and model at models/ggml-base.bin):\n  ./main -m models/ggml-base.bin -f audio.wav --language en --task transcribe --output-srt\n- If working from a video, extract audio first:\n  ffmpeg -y -i video.mp4 -vn -acodec pcm_s16le -ar 16000 -ac 1 audio.wav\n\nPost-action verification (concrete checks)\n1. File presence & basic sanity:\n   - [ -f audio.srt ] && echo \"SRT exists\"\n   - wc -l audio.srt\n2. Duration alignment:\n   - audio_dur=$(ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 audio.wav)\n   - last_end=$(grep -- \"-->\" audio.srt | tail -1 | awk -F' --> ' '{print $2}' | awk -F'[:,]' '{print $1*3600+$2*60+$3\".\"$4}')\n   - python - <<PY\n     import sys\n     ad=float(sys.argv[1]); le=float(sys.argv[2])\n     print(\"OK\" if le<=ad+0.5 else \"MISALIGNED\", ad, le)\n     PY \"$audio_dur\" \"$last_end\"\n   - Threshold 0.5s tolerates small rounding; flag otherwise.\n3. Visual verification (screenshot frames with captions burned in):\n   - Burn subtitles into a temp copy: ffmpeg -i video.mp4 -vf subtitles=audio.srt -c:v libx264 -c:a copy out_burn.mp4\n   - Grab frames at sample times: ffmpeg -ss 00:00:05 -i out_burn.mp4 -frames:v 1 frame_5s.png\n   - Repeat for multiple timestamps to confirm placement and legibility.\n4. Check overlapping/empty cues:\n   - grep -- \"-->\" audio.srt | awk -F' --> ' '{print $1,$2}' | awk '{print $2-$1}' # or parse and verify increasing times\n5. Language/quality spot-check:\n   - Listen to 10–20s snippets at random timestamps and compare text.\n\nAutomation: wrap the above into a bash script that runs pre-checks, runs whisper, then runs the verification steps and exits nonzero on failures so an agent can decide to retry or escalate.\n\nRisks to watch: wrong model/language, sample-rate mismatch, truncated audio, caption file encoding issues, subtitle time format mismatches — include explicit checks above to catch them.",
        "fr": "Correction modèle — inspection de l’environnement\n\nPoints clés qu’une bonne réponse doit couvrir (actionnables)\n- Quand inspecter : lire l’état avant une action (précondition) et immédiatement après (postcondition). Documenter horodatage et source.\n- Modalités d’inspection selon contexte : capture d’écran/DOM pour UI, lecture de fichiers pour code, frames et pistes audio pour vidéo, logs et sorties d’API pour backend.\n- Méthode d’évaluation : comparaisons ciblées (hash/quelques pixels/DOM diff/grep), non comparaison brute, pour détecter changements pertinents.\n- Intégration au flux agent : boucle action → attendre événement/latence → inspection → mise à jour de la croyance interne → décision suivante.\n- Mécanismes de robustesse : retries, timeouts, détection de race conditions, validation des hypothèses.\n\nRisques concrets à mentionner\n- Données obsolètes / états intercalaires (race conditions)\n- Coût en latence et bande passante (captures fréquentes)\n- Fuites d’informations sensibles via captures/logs\n- Mauvaise interprétation (OCR/ASR erreurs)\n\nExemples pratiques\n- Selenium (screenshot après clic) :\n  driver.find_element(...).click()\n  time.sleep(0.5)\n  driver.save_screenshot(\"after_click.png\")\n- Lecture de fichier avant modification (Python) :\n  with open(\"config.py\",\"r\",encoding=\"utf-8\") as f: content=f.read()\n- Génération de sous-titres horodatés (Whisper CPP + ffmpeg) :\n  ./main -m models/ggml-base.bin -f audio.wav --output-srt output.srt\n  ffmpeg -i input.mp4 -i output.srt -c copy -c:s mov_text output_with_subs.mp4\n\nChecklist finale pour l’évaluation\n- A-t-il décrit quand/quoi/Comment inspecter ? Oui/Non\n- A-t-il géré latence/retries/validation ? Oui/Non\n- Les risques sont-ils identifiés et atténués ? Oui/No"
      }
    }
  },
  "claude_certified_architect_foundations__04": {
    "ex_claude_certified_architect_foundations__04_001": {
      "correction": {
        "en": "How to use Rewind\n- Create checkpoints automatically (every user prompt). To open: focus the prompt, press Escape twice on an empty prompt.\n- Menu options:\n  - Restore code and conversation: fully revert to that checkpoint.\n  - Restore conversation only: keep current code, revert the chat.\n  - Restore code only: revert files/state, keep the chat.\n- Summarize helpers:\n  - Summarize from here: collapse everything AFTER the chosen checkpoint — use for side conversations you don’t want to expand the transcript.\n  - Summarize up to here: collapse everything BEFORE the checkpoint — use after long setup phases to reduce context while keeping essential state.\n\nPractical tips\n- Create an explicit checkpoint before big refactors or risky experiments.\n- Use “restore code only” to undo file changes without losing discussion context.\n- Use summarization to keep the transcript succinct so Claude doesn’t need to reprocess long tangents.\n\nAutonomous /goal usage\n- Set a persistent completion condition with /goal, e.g.:\n  /goal all tests in src/billing pass, and the type checker reports zero errors\n- Cancel with:\n  /goal clear\n- Important constraint: the fast evaluator only reads the chat transcript. Make goals verifiable from Claude’s outputs (not internal IDE state). For example, require explicit output lines:\n  - “TESTS: 34 passed, 0 failed”\n  - “TYPE CHECKER: 0 errors”\n- Craft goals to request machine-checkable evidence (test run output, linter/type-check summaries, or explicit status lines) so the evaluator can confirm success.",
        "fr": "Ce que doit contenir une correction modèle (actionnable, vérifiable) :\n\n1. Utilisation du rewind\n- Indiquer explicitement la façon d’ouvrir : double-taper Échap sur une invite vide.\n- Démontrer les 3 options et leurs effets par exemple :\n  - Restaurer code + conversation -> montre fichiers/texte restaurés.\n  - Restaurer seulement conversation -> montre historique restauré.\n  - Restaurer seulement code -> montre arborescence/fichiers restaurés.\n- Cas d’usage concrets :\n  - \"Summarize from here\" : compresser une discussion latérale avant de revenir au fil principal.\n  - \"Summarize up to here\" : compresser longues phases de configuration.\n\n2. /goal autonome — formulation et démonstration\n- Exemples précis et vérifiables :\n  - /goal all tests in src/billing pass, and the type checker reports zero errors\n  - /goal Update README with example CLI usage and show example output\n- Montrer la boucle : définir /goal, laisser Claude itérer, inclure dans la transcription les sorties de tests, logs, diffs, ou le contenu final pour vérification.\n- Indiquer /goal clear pour annuler.\n\n3. Contraintes et bonnes pratiques\n- Condition : l’évaluateur lit seulement la transcription → toutes les preuves doivent être dans la sortie (résultats de tests, sorties de linter, diff de fichiers).\n- Rédiger des objectifs mesurables et non ambigus (pas \"améliorer\", mais \"tous les tests X passent\").\n- Après summarization, vérifier que l’information essentielle n’a pas été perdue.\n\n4. Risques concrets et mitigations\n- Perte d’informations importantes lors de la compression → mitigation : sauvegarder un checkpoint avant summarizing.\n- Objectifs vagues non vérifiables → mitigation : inclure commandes et sorties attendues.\n- Restaurations incorrectes confondant versions → mitigation : nommer checkpoints et préciser timestamp/contexte."
      }
    },
    "ex_claude_certified_architect_foundations__04_002": {
      "correction": {
        "en": "Key features — what to use and how\n- Checkpoints: every user prompt auto-creates one. Double-tap Escape on an empty prompt to open the rewind menu.\n  - Restore options: “restore code and conversation”, “restore conversation only”, “restore code only” — pick the smallest restore you need to avoid losing useful context.\n- Summaries:\n  - “Summarize from here” — compress everything after a chosen checkpoint. Use for side conversations you don’t want to carry forward.\n  - “Summarize up to here” — compress the long setup history before a point to shrink context while keeping essentials.\n\nAutonomous features — practical rules and examples\n- /goal: set a completion condition for Claude to keep working across turns. Make goals explicit and checkable by model output because the evaluator only reads the transcript.\n  - Good example: /goal all tests in src/billing pass and the TypeScript checker reports zero errors\n  - Cancel: /goal clear\n  - Do: require test output lines or a clear “ALL TESTS PASSED” summary so the evaluator can verify from the transcript.\n- Loop: run a prompt on an interval between turns (fixed or self-paced). Use for polling CI, deployments, or long-running checks. Escape stops the loop.\n\nRisks & mitigations\n- Risk: evaluator can’t see external artifacts → false negatives. Mitigate by printing test/CI results in the transcript.\n- Risk: runaway loops or token exhaustion. Mitigate by using conservative intervals, explicit stop commands (/goal clear or Escape), and strict goal conditions.\n- Risk: accidental sensitive data broadcast. Avoid putting secrets in loop prompts or checkpoints.\n\nQuick workflow tip: set a precise /goal, run a short loop to poll CI output, and compress irrelevant threads with “Summarize from/up to here” to keep context small and focused.",
        "fr": "Points clés qu’une bonne réponse doit couvrir (comportement attendu, critères de validation et risques/mitigations) :\n\nComportement et critères de validation\n- Point de contrôle : chaque prompt utilisateur crée un checkpoint visible (ID, horodatage). Test : envoyer 3 prompts -> vérifier 3 checkpoints dans la barre/trace.\n- Rembobinage : double appui Échap sur un champ de prompt vide ouvre le menu. Test E2E : focus sur le prompt vide, double-Échap -> menu visible.\n- Options de restauration : trois actions distinctes — restaurer code+conversation, conversation seule, code seul. Test : modifier code/conversation, restaurer chaque option et vérifier l’état exact restauré.\n- Résumer : \n  - \"Résumer à partir d’ici\" compresse tout ce qui suit le checkpoint en un seul message résumé; les messages originaux deviennent masqués mais accessibles pour annulation.\n  - \"Résumer jusqu’ici\" compresse tout ce qui précède. Test : exécuter puis demander d’afficher le contenu résumé + annuler.\n- /goal : accepte une condition vérifiable (ex. \"/goal all tests in src/billing pass\"). Claude continue à itérer jusqu’à ce que la sortie contienne la preuve (ligne \"All tests passed\" ou rapport de test). /goal clear annule. Critère : la transcription doit contenir l’affirmation vérifiable.\n- Boucle autonome : exécute un prompt périodique (intervalle fixe ou adaptatif), chaque itération écrit un message avec ID et timestamp; Esc interrompt. Test : lancer boucle, vérifier plusieurs itérations, presser Échap -> aucune itération supplémentaire.\n\nRisques concrets et mitigations\n- Perte de données lors de restauration → garder snapshots immuables et bouton \"Annuler\".\n- Conditions /goal non vérifiables → valider format (doit contenir output attendu) et refuser sinon.\n- Boucle infinie/resource leak → limiter itérations, timers supervisés, bouton d’arrêt global.\n- Découverte UX/Accessibilité → indiquer raccourcis, prise en charge clavier-only et a11y labels.\n\nExemples vérifiables\n- Transcript doit inclure lignes comme \"Checkpoint #4 created\", \"Restored: conversation only\", \"Summary starting at #3: …\", \"All tests in src/billing passed\", \"Loop iteration 5 @ 2026-07-25T12:00Z\"."
      }
    },
    "ex_claude_certified_architect_foundations__04_003": {
      "correction": {
        "en": "Short answer: CLAUDE.md is the right surface for human-readable guidance (style, preferences, high-level constraints) — not for hard enforcement. Put mandatory, safety-critical, or action-blocking rules in code (hooks / managed policies) so they actually stop behavior.\n\nConcrete guidance (what goes where)\n- Managed policy (org-wide): mandatory, non-negotiable rules that always apply (e.g., “no sharing of PII”, “must use SSO access”). These override everything.\n- PreToolUse hook (enforcement): hard operational guards that prevent specific actions (e.g., “never push to main”, “block external network calls”). Hooks run and reject attempts.\n- CLAUDE.md (project/user surfaces): concise guidance, conventions, examples, and trade-offs. Use for tone, preferred workflows, brief guardrails — keep it short and prioritized.\n- User memory: personal preferences and opt-outs (e.g., “I prefer concise answers”, accessibility needs).\n- Project memory: shared team context (roadmap, code-style decisions).\n- Conversation/session memory: ephemeral state for the current interaction.\n\nExample PreToolUse pseudocode to block pushing to main\nif action == \"git.push\" and target_branch == \"main\":\n    reject(\"Push to main is blocked; create a PR against develop\")\nelse:\n    allow()\n\nBest practices\n- Keep CLAUDE.md < few hundred words and prioritized.\n- Put enforcement in hooks/managed policies; use CLAUDE.md for clarification and examples.",
        "fr": "Non — CLAUDE.md n’est pas la bonne « surface » pour règles strictes et contraintes bloquantes. CLAUDE.md sert d’instructions/guide lisible par Claude ; il devient peu fiable quand il est long ou contradictoire. Les règles impératives (ex. « ne jamais pousser vers main ») doivent être implémentées en code exécutable (hooks ou politiques gérées).\n\nActionnable — où placer quoi\n- Politique gérée (org-wide, priorité maximale) : règles de conformité, sécurité, contrôle d’accès. Ex. interdiction d’exposer des secrets, obligations de chiffrement.\n- Hooks (PreToolUse, Validation, etc.) : enforcement technique. Ex. PreToolUse bloque toute commande git push vers main.\n- Projet (mémoire projet) : conventions d’équipe (branch naming, reviewers obligatoires).\n- Utilisateur (préfs) : préférences personnelles non contraignantes.\n\nRisques concrets si tout reste dans CLAUDE.md\n- Règles critiques ignorées ou contradictoires\n- Comportement non déterministe selon l’ordre des lignes\n- Dérive des pratiques hors audit\n- Tests et monitoring impossibles\n\nExemple PreToolUse (pseudocode)\nif tool == \"git\" and command == \"push\" and target_branch == \"main\":\n    abort(\"Push vers main interdit. Créez une PR.\")\nelse:\n    allow()\n\nBonnes pratiques\n- Garder CLAUDE.md court : max 10 lignes, séparant \"suggestions\" vs \"obligations\".\n- Documenter priorité : politique gérée > hooks > projet > utilisateur > CLAUDE.md.\n- Tester les hooks, auditer les politiques et versionner les changements."
      }
    },
    "ex_claude_certified_architect_foundations__04_004": {
      "correction": {
        "en": "Implementation plan and examples a student should aim for:\n\n1) Make tests the gate (hook behavior)\n- Stop-hook: install a Stop hook script that runs the test suite before allowing a turn to end. If tests fail, exit 2 so the agent receives the failure as a hard signal and retries/fixes automatically.\n- PostToolUse-hook: run lint/type-check after every edit/tool action to fail fast on typing or style problems.\n\nExample Stop hook (stop_hook.sh):\n#!/bin/bash\nset -e\n# run unit tests and linters\npytest -q || exit 2\nnpm run lint || exit 2\n# success\nexit 0\n\nInstall this as the agent Stop hook. Exit codes: 0 = pass, 1 = runtime error, 2 = test/lint failure intended to be shown to the agent.\n\n2) Hook semantics and agent behavior\n- Always run hooks on every turn (enforce via orchestration). Do not make pass/fail conditional on prompts.\n- On exit code 2 the agent reads the failure output, creates an unprompted fix iteration, re-runs hooks until green, or refuses to finalize the edit.\n\n3) Cold second opinion (subagent review)\n- Spawn a fresh session/subagent with no conversation history.\n- Feed it only the changed files + commit diff + failing test logs.\n- Ask it to:\n  a) run tests and linters locally,\n  b) perform static/security checks,\n  c) produce independent comments and a minimal patch.\n- Have CI enforce that at least one cold-review pass is green before merging.\n\n4) Extra safeguards\n- Record hook outputs in CI logs and include them in the failure payload so the agent has full context.\n- Enforce timeout and resource limits to avoid infinite retry loops.",
        "fr": "Objectif clair : vérifier réellement l’exécution des tests à chaque exécution et empêcher la fin d’un tour si les tests échouent. Correction modèle (actionnable).\n\n1) Hook Stop (exécute tests avant de terminer)\n- Script d’exemple (stop_hook.sh) :\n  #!/bin/bash\n  set -e\n  pytest -q || { echo \"TESTS FAILED\"; exit 2; }\n  mypy . || { echo \"TYPE CHECK FAILED\"; exit 2; }\n  flake8 . || { echo \"LINT FAILED\"; exit 2; }\n  exit 0\n- Comportement : hook appelé avant fin de tour. Code de sortie 2 = échec signalé à Claude ; il doit relire/corriger.\n\n2) PostToolUse hook (après chaque modification)\n- Script d’exemple (post_tool_hook.sh) :\n  #!/bin/bash\n  set -e\n  files=$(git diff --name-only HEAD~1 -- '*.py')\n  [ -z \"$files\" ] && exit 0\n  mypy $files || { echo \"TYPE CHECK FAILED\"; exit 2; }\n  pytest --maxfail=1 $files || { echo \"TESTS FAILED\"; exit 2; }\n  exit 0\n- Exécuter lint/typecheck/tests ciblés sur fichiers modifiés pour rapidité.\n\n3) Garantie d’exécution à chaque fois\n- Hook mis en place côté infrastructure (obligatoire, ne peut être contourné).\n- Exporter logs/artéfacts (stdout/stderr, code de sortie) pour audit.\n\n4) Second avis \"à froid\"\n- Lancer un subagent/session sans mémoire ou un conteneur propre (ex. docker run --rm -v $PWD:/w -w /w python:3.11 bash -c \"./stop_hook.sh\").\n- Fournir uniquement le code et les résultats des tests au reviewer. Demander : tests non exécutés, exceptions masquées, dépendances manquantes, flakiness, tests non couverts.\n\nPoints d’attention pratiques : rendre les hooks idempotents, timeouts raisonnables, rejeter tout exit !=0/2 comme erreur interne, consigner les preuves (logs, sortie pytest --junitxml)."
      }
    },
    "ex_claude_certified_architect_foundations__04_005": {
      "correction": {
        "en": "How to use Rewind (step‑by‑step)  \n1. Make sure the text prompt is empty. Double‑tap Escape to open the Rewind menu.  \n2. In the menu choose the checkpoint you want to return to (each user prompt creates one).  \n3. Pick an action:\n   - Restore code and conversation — roll back both the chat history and any code/files to the checkpoint state.  \n   - Restore conversation — roll back only the chat while keeping current code/files.  \n4. Confirm the restore. The session will revert to the chosen checkpoint; you can then continue from that point.\n\nWhen to use each option (concrete examples)  \n- Restore code and conversation: use when Claude executed or generated code that produced wrong outputs (bugs, corrupted files) and you want the entire environment and chat reverted. Example: Claude ran a refactor that broke tests and then kept giving bad fixes.  \n- Restore conversation: use when the chat has gone off‑topic or misunderstood the task but you want to keep manual edits or files you just saved. Example: you manually updated a script but the assistant’s messages are no longer helpful.\n\nPractical tips  \n- Intentionally submit a short “checkpoint” user prompt before major changes so you can rewind to that exact point.  \n- Be aware restores discard everything after the checkpoint; save any important local edits before rewinding.",
        "fr": "Étapes concrètes\n- Créez un checkpoint volontaire en envoyant un message utilisateur (même court : « checkpoint »). Chaque prompt utilisateur crée automatiquement un checkpoint.\n- Pour ouvrir Rewind : placez le focus dans la zone de prompt, videz-la, puis appuyez deux fois sur Échap (Esc). Le menu de rembobinage s’ouvre.\n- Dans le menu, sélectionnez le checkpoint souhaité (vérifiez l’aperçu ou l’horodatage si disponible).\n- Choisissez l’option :\n  - « Restaurer le code et la conversation » : restaure l’état complet (éditeur de code + historique du chat).\n  - « Restaurer la conversation » : restaure uniquement le chat, en conservant l’état actuel du code.\n- Confirmez la restauration. Si vous n’êtes pas sûr, copiez d’abord le code actuel dans un fichier/clipboard.\n\nQuand utiliser chaque option\n- Restaurer le code+conversation : quand le modèle a bifurqué et que vous voulez revenir à un état exécutable cohérent.\n- Restaurer la conversation seulement : quand vous voulez revenir au fil du dialogue mais conserver des modifications de code locales non souhaitées à perdre.\n\nRisques et bonnes pratiques\n- Risques : perte d’éditions non sauvegardées, restauration inattendue, incohérences avec actions externes (les appels API ou effets hors de l’interface ne sont pas annulés).\n- Sauvegardez le code avant restauration, créez checkpoints fréquents, et documentez les actions externes pour éviter effets secondaires."
      }
    },
    "ex_claude_certified_architect_foundations__04_006": {
      "correction": {
        "en": "Restore code — roll back just the files\n- What it does: reverts the repository files to the state captured at a chosen checkpoint without changing the conversation or metadata.\n- When to use: you tested changes that broke the build or you want to revert implementation but keep the discussion history.\n- How to use: create a checkpoint before risky edits; when needed, select that checkpoint → “Restore code”; verify with tests/quick lint. Note: review diff after restore to confirm only intended files changed.\n\nSummarize from here (compress everything after the checkpoint)\n- What it does: collapses all messages after the checkpoint into a short summary to free tokens and speed future context lookups.\n- When to use: you had a long side conversation or iterative debugging you don’t need verbatim.\n- How to use: set checkpoint, run “Summarize from here” with instructions like “Produce a 5-bullet summary preserving action items and code snippets.”\n\nSummarize up to here (compress everything before the checkpoint)\n- What it does: compresses the earlier setup/context while keeping later, implementation-focused messages intact.\n- When to use: long project setup or requirements phase you want summarized so the model can focus on implementation details.\n- How to use: pick the checkpoint at the transition point; run “Summarize up to here” and specify retention rules (e.g., keep API specs and function signatures).\n\nLet Claude run more autonomously (goal + loop)\n- Pattern: give a clear goal, a short loop procedure, evaluation criteria, and hard limits.\n- Example prompt snippet:\n  - Goal: “Deliver a working CLI tool that parses X and outputs Y.”\n  - Loop: “1) Propose next task; 2) implement changes; 3) run tests; 4) report results. Repeat until tests pass or 10 iterations.”\n  - Eval: “Stop when all tests pass or no progress for 2 iterations.”\n- Safeguards: set iteration caps, require confirmations before destructive actions, and checkpoint regularly.\n\nRisks & mitigations\n- Risk: summaries lose critical detail — mitigation: preserve key code blocks and tests in the summary.\n- Risk: autonomous loops run away or make unsafe changes — mitigation: iteration limits, mandatory checkpoints, and human approvals for high‑risk actions.\n- Risk: restore/code drift confusion — mitigation: tag checkpoints with clear names and verify diffs after restore.",
        "fr": "Correction modèle (objectif : flux de travail avec checkpoints, résumés et restauration de fichiers)\n\n1) Marqueurs de checkpoint (pratique)\n- Insérer des marqueurs explicites dans la conversation ou le log de session : [CHECKPOINT: nom|hash|timestamp].\n- Exemple : [CHECKPOINT: design-api|2026-07-25T10:00Z]\n\n2) Commandes pour restaurer uniquement des fichiers\n- Restaurer fichiers à partir du dernier commit : git restore path/to/file\n- Restaurer fichiers à partir d’un commit spécifique : git restore --source=<commit_hash> path/to/file\n- Alternative (anciennes versions Git) : git checkout <commit_hash> -- path/to/file\n- Vérification : git status && git diff --staged path/to/file\n\n3) Prompts modèles pour résumés\n- \"Résumer à partir d'ici\": « Résume tout le texte après [CHECKPOINT: X] en 5–8 points, conserver décisions, tâches et dépendances; supprimer exemples et digressions. »\n- \"Résumer jusqu’ici\": « Résume tout le texte avant [CHECKPOINT: X] en 5–8 points, inclure changements de code, raisons et décisions ouvertes. »\n\n4) Loop autonome (goal + loop) — pseudocode\n- goal = \"livrer feature X\"\n- loop: pour i in 1..N:\n  - observe() — lire état du repo, tâches ouvertes, derniers checkpoints\n  - plan() — produire 1–3 actions atomiques (tests, commit, résumé)\n  - act() — exécuter actions en mode sandbox ou proposer PR\n  - reflect() — enregistrer résultat, créer checkpoint si significatif\n  - stop si goal atteint ou requiert validation humaine\n\n5) Risques et mitigations\n- Risques : perte de données (restauration erronée), résumés perdant détails critiques, dérive d’objectif, modifications non autorisées.\n- Mitigations : gardes-fous (mode read-only par défaut), confirmations humaines pour actions destructrices, tests automatisés avant commit, conserver backups/branches.\n\nUne bonne réponse fournira les marqueurs, commandes Git exactes, prompts de résumé réutilisables, pseudocode de loop et règles de sécurité."
      }
    },
    "ex_claude_certified_architect_foundations__04_007": {
      "correction": {
        "en": "Use checkpoints to manage context and switch between hands-on and autonomous modes.\n\nSummarize from here\n- Purpose: compress everything after a checkpoint (e.g., a side conversation) so the model “forgets” detailed chatter but retains a short summary.\n- When to use: you’ve diverged into a side thread you no longer need in full detail.\n- Example instruction: “Summarize from here — produce a 3–6 bullet summary of all messages after this point and replace them with that summary.”\n\nSummarize up to here\n- Purpose: compress everything before a checkpoint so long setup/requirements don’t consume context while keeping subsequent implementation intact.\n- When to use: after finishing a long planning/setup phase and starting implementation.\n- Example instruction: “Summarize up to here — create a compact 5–8 bullet digest of everything prior to this point; keep later messages unchanged.”\n\nLet Claude run more autonomously (Goal + Loop)\n- Goal: give a concise goal, success criteria, constraints, and permitted actions. Example: “Goal: produce a tested Python script to ingest CSV and output cleaned JSON. Success: tests pass locally. Constraints: no external services, max 300 lines.”\n- Loop: define iterative cycle the model should follow: Plan → Act → Observe → Evaluate → Repeat. Example loop prompt: “You may act autonomously up to 10 iterations. Each iteration: (1) propose next action, (2) execute, (3) report results and artifacts, (4) check success criteria. If a decision has major risk, ask for confirmation.”\n- Best practices: set iteration limits, checkpoints for summarization, explicit abort conditions, and periodic human review.",
        "fr": "But de l’exercice : expliquer clairement quand et comment utiliser les commandes de checkpointing (“Résume jusqu’ici”, “Résume à partir d’ici”) et comment rendre Claude plus autonome via “goal” et “loop”. Correction modèle :\n\n- Définitions concrètes\n  - “Résume jusqu’ici” : compresser tout le contenu antérieur en 4–8 points (décisions, paramètres, dépendances, éléments ouverts) pour libérer du contexte tout en conservant les parties d’implémentation intactes.\n  - “Résume à partir d’ici” : synthétiser tout ce qui suit un checkpoint pour réduire la verbosité d’une conversation parallèle ou d’une longue phase d’exécution.\n\n- Exemples de prompts (utilisables tels quels)\n  - Résume jusqu’ici : «Résume jusqu’ici en 6 bullets : décisions, paramètres, hypothèses, tâches restantes, points de risque, références.»\n  - Résume à partir d’ici : «À partir de ce point, résume en 4 bullets les actions réalisées, outputs produits, erreurs rencontrées et prochaines étapes.»\n\n- Rendre autonome (goal + loop)\n  - Patron : «Goal: [objectif mesurable]. Loop: 1) Plan la prochaine action ; 2) Exécute ; 3) Vérifie (tests/logs) ; 4) Si objectif non atteint, répète.»\n  - Exemple : «Goal: livrer un script X fonctionnel. Loop: plan → coder une fonction → lancer tests unitaires → enregistrer résultats → itérer.»\n\n- Risques concrets et mitigations\n  - Perte d’informations critiques : conservez archives brutes et identifiants de checkpoint.\n  - Erreurs de résumé / hallucinations : demander sources/segments originaux et validation humaine.\n  - Dérive d’état pendant la boucle : ajouter tests automatiques et seuils d’arrêt.\n\n- Bonnes pratiques opérationnelles\n  - Numéroter checkpoints, inclure métadonnées (horodatage, auteur).\n  - Exiger résumés concis + verbatim des décisions sensibles.\n  - Automatiser vérifications post-loop avant fusionner changements."
      }
    },
    "ex_claude_certified_architect_foundations__04_008": {
      "correction": {
        "en": "Concise, actionable checklist and examples to aim for:\n\n1) Read and iterate the plan\n- Read the full plan before running. Mark exact edits and ask Claude to \"insert X here\" so revisions are explicit and fast.\n\n2) Use Compact safely\n- Command: /compact then immediately add explicit instructions like: \"Keep files A, B and the API-change notes; remove debug chats.\" \n- Risk: important context may be deleted — mitigate by naming what must be preserved.\n\n3) Rewind for course corrections\n- Open rewind: double-tap Escape on an empty prompt. Options: restore code, conversation, or both; \"summarize from here\" to compress later turns; \"summarize up to here\" to compress setup.\n- Use restore instead of prompting out of a wrong path.\n\n4) Set Goals for autonomous runs\n- Command example: /goal All tests in src/billing pass and the type checker reports zero errors\n- Cancel: /goal clear\n- Constraint: evaluator reads only the transcript — ensure the completion condition is verifiable from Claude’s outputs (e.g., include test logs in the transcript).\n\n5) Use Loop to poll externals\n- Run a prompt on an interval to fetch CI/deploy status and act on changes. Press Escape to stop.\n\n6) Parallelize safely with work trees\n- Use separate work trees per session to avoid file conflicts. Create .work_tree_include at repo root to copy required ignored files (e.g., .env). Clean work trees auto-remove on exit.\n\nRecommended sequence\n- Scope work → /compact with preservation instructions → set /goal if outcome is clear → run /loop for CI if needed → use work trees for parallel agents → use rewind/checkpoints to recover.\n\nTop risks and mitigations\n- Lost context from compaction: explicitly list preserved items.\n- Invalid goal checks: make success verifiable in transcript.\n- File conflicts: always use work trees and .work_tree_include.",
        "fr": "Points clés à couvrir (correction modèle)\n\n1) Planifier et itérer\n- Toujours fournir un plan structuré (objectifs, étapes, livrables, points de contrôle). Ex : étapes “1. config CI, 2. corriger failing tests X, 3. revue PR”.\n- Itérez sur le plan plutôt que d’espérer : demandez à Claude d’insérer/modifier une section précise (p.ex. “Ajoute une sous-étape après 2 pour mocker l’API Y”).\n\n2) Utiliser compact sans perdre l’essentiel\n- Commande : compact puis ajoutez immédiatement la consigne de résumé (ex. \"Compact — résume en conservant uniquement les changements d’API et les tickets référencés\").\n- Risque : suppression d’informations critiques → Mitigation : dans la même invite, lister explicitement ce qui doit être conservé (tests, endpoints, tickets).\n\n3) Rewind (retour à un point de contrôle)\n- Rappel pratique : touchez deux fois Échap sur une invite vide pour ouvrir le menu rewind.\n- Chaque invite crée un checkpoint ; options : restaurer code/conversation/both, “résumer à partir d’ici” ou “résumer jusqu’ici”.\n- Usage conseillé : créer checkpoints avant gros changements ou refactorings.\n\n4) Goal et Loop\n- Exemple Goal : /goal \"Tous les tests dans source billing passent et le vérificateur de types ne signale aucune erreur\"\n- Annuler : /goal clear\n- Contrainte : l’évaluateur ne voit que la transcription → s’assurer que les preuves (sorties de test, logs) sont dans la sortie.\n- Loop : utiliser pour polling CI/déploiement, appuyer Échap pour arrêter ; surveiller risques de side-effects ou boucles infinies.\n\n5) Work trees pour travail parallèle\n- Créez work trees séparés pour éviter conflits sur fichiers.\n- Fichier .worktree à la racine liste fichiers à copier/ignorer (ex. .env.local, config/private.json).\n- Risque : divergence de config entre arbres → synchroniser explicitement les changements validés.\n\nChecklist de vérification finale\n- Le plan contient critères de succès vérifiables.\n- Compact a des instructions précises de ce qui doit être gardé.\n- Checkpoint (rewind) créé avant opérations risquées.\n- Goal défini quand la condition est vérifiable par la sortie.\n- Loop configuré avec intervalle et stop condition.\n- Work trees configurés et fichiers sensibles exclus/partagés correctement."
      }
    },
    "ex_claude_certified_architect_foundations__04_009": {
      "correction": {
        "en": "Example 1 — redact-and-run command interceptor (Node-style pseudocode)\nfunction onBeforeExecute(call) {\n  // call.input is the raw command string\n  const secretRegex = /\\bsk_live_[A-Za-z0-9_-]{20,}\\b/g;\n  const matches = call.input.match(secretRegex);\n  if (!matches) return { action: \"continue\" };\n\n  // Replace secrets with placeholder before the command executes\n  const sanitized = call.input.replace(secretRegex, \"[REDACTED_SECRET]\");\n  // Optionally record an audit event (but do NOT store secret itself)\n  auditLog({ event: \"secret_redacted\", count: matches.length, time: Date.now() });\n\n  // Return updatedInput so the command runs with the redacted text\n  return { action: \"continue\", updatedInput: sanitized };\n}\n\nKey implementation notes:\n- Use a conservative regex tuned to expected secret formats.\n- Do not persist the original secret anywhere; if you must audit, log only metadata (counts, timestamp, rule id).\n- Test with edge cases (multiple secrets, surrounding punctuation, encoded forms).\n\nExample 2 — SessionStart hook to rehydrate context after compaction\nfunction onSessionStart(session) {\n  // session.compactedMessages is the compacted transcript\n  const fileNames = extractFileNames(session.compactedMessages); // e.g., parse filenames/paths\n  if (fileNames.length === 0) return { action: \"continue\" };\n\n  const summary = `Working files: ${fileNames.join(\", \")}. Last actions: ${summarizeRecentActions(session.compactedMessages, 2)}.`;\n  // Inject a short system message so the model resumes with that context\n  return { action: \"continue\", updatedInput: summary };\n}\n\nNotes:\n- Keep the summary short (1–3 sentences) so it fits into compacted context.\n- Make the summary factual: file names, last edits, outstanding tasks.\n- Validate that the injected summary preserves privacy and contains no raw secrets.",
        "fr": "Hook: masquer une clé en entrée (updatedInput)\n- Détecter la clé dès la lecture (regex strict): /(sk_live_[A-Za-z0-9\\-_]{20,})/g\n- Remplacer par placeholder et renvoyer updatedInput pour permettre l’exécution.\n\nExemple (pseudo-JS):\nconst matches = input.match(/(sk_live_[A-Za-z0-9\\-_]{20,})/g);\nif (matches) {\n  // Ne pas logguer la clé en clair : stocker uniquement un haché pour audit\n  storeAudit({hash: sha256(matches[0]), type: 'secret-leaked', time: Date.now()});\n  const safeInput = input.replace(/(sk_live_[A-Za-z0-9\\-_]{20,})/g, '<REDACTED_KEY>');\n  return { action: 'updatedInput', updatedInput: safeInput };\n}\nreturn { action: 'allow' };\n\nBonnes pratiques opérationnelles:\n- Appliquer regex la plus stricte possible pour réduire faux positifs.\n- Ne jamais écrire la valeur complète du secret dans les logs ou le contexte.\n- Conserver un haché pour traçabilité et rotation des clés si nécessaire.\n- Tester sur exemples non sensibles avant déploiement.\n\nHook: SessionStart après compaction (préserver l’état)\n- Matcher: événement SessionStart déclenché après compact.\n- Extraire métadonnées (noms de fichiers, timestamps, short snippets).\n- Générer un \"short summary\" (3–6 lignes) et injecter dans le contexte via updatedContext/updatedInput.\n\nExemple de résumé retourné:\n{\n  action: 'updatedContext',\n  contextPatch: \"Résumé des fichiers: main.py (logic auth), secret.txt (contenu masqué), notes.md (10 entrées); dernier edit: 2026-07-20. Points clés: token rotated, endpoint /process.\"\n}\n\nBonnes pratiques:\n- Limiter la taille du résumé (p.ex. < 500 caractères).\n- Ne pas réintroduire de secrets dans le contexte résumé.\n- Valider que Claude utilise bien ce résumé (tests de reprise d’état)."
      }
    }
  },
  "claude_certified_architect_foundations__05": {
    "ex_claude_certified_architect_foundations__05_001": {
      "correction": {
        "en": "What makes a great Cowork task (concise checklist + template + example)\n\nCore elements to include (be specific and actionable)\n- Objective: one-sentence outcome you want completed (e.g., “Produce a 2‑page competitor analysis for X.”)\n- Deliverables: exact files/formats (PDF, spreadsheet, slide deck, CSV).\n- Context & inputs: links, uploaded files, existing research, target audience, tone.\n- Tools & data access: which integrated tools to use (web search, company CRM, Google Drive, code execution).\n- Success criteria: measurable checks (word count, chart types, required sections, citation format).\n- Constraints & assumptions: budget, legal/privacy limits, style guide, deadlines.\n- Plan/steps you expect (optional): high‑level steps Claude should run autonomously.\n- Review rules: whether to ask clarifying Qs or proceed autonomously; number of revision rounds.\n\nOne‑line template you can paste into Cowork\nObjective: [what to produce]. Deliverables: [files/formats]. Context: [short background + links/files]. Tools: [list]. Success criteria: [measurable checks]. Constraints: [limits]. Deadline: [when]. Clarify before starting? [yes/no].\n\nWorked example\nObjective: Produce a 2‑page competitor analysis (PDF) + one‑slide summary (PPTX).\nDeliverables: PDF (max 800 words), PPTX slide, data CSV of features compared.\nContext: See attached product spec + list of 5 competitors (links). Audience: VP Product, concise executive tone.\nTools: web search, company CRM, spreadsheet tool.\nSuccess criteria: comparison table of 6 features, 3 recommendations, 3 cited sources per competitor, PDF ≤2 pages.\nConstraints: No proprietary customer data; use public sources only.\nDeadline: 48 hours. Clarify before starting? Only if a competitor link is missing.\n\nUse this structure to delegate end‑to‑end work rather than ask for a one‑off answer.",
        "fr": "Correction modèle — Que signifie « Cowork = déléguer, pas seulement discuter » et comment rédiger une excellente tâche Cowork\n\nRésumé opérationnel\n- Mentalité : déléguer la totalité d’un travail (collecte de contexte, exécution, production finale), pas juste poser des questions. Claude doit pouvoir rendre un livrable prêt à l’emploi.\n- Quand utiliser : tâches reproductibles ou en plusieurs étapes, nécessitant collecte d’informations, manipulation d’outils, et livrable final (rapports, présentations, listages, analyses, exports).\n\nChecklist d’une excellente tâche Cowork (à inclure dans la consigne)\n1. Objectif clair et mesurable (ex. : « rapport de 5 pages présentant X avec recommandation A/B »).\n2. Critères d’acceptation (format, longueur, ton, livrables, exemples).\n3. Entrées fournies + où les récupérer (liens, accès, fichiers).\n4. Contrainte temporelle et priorité.\n5. Outils autorisés et accès (Google Drive, base de données, APIs).\n6. Étapes attendues et degré d’autonomie (recherches, validation intermédiaire ou pas).\n7. Sorties exactes (nom de fichier, structure, métadonnées).\n8. Hypothèses autorisées et limites à ne pas dépasser.\n\nRisques concrets et mitigations\n- Fuite de données sensibles : anonymiser données, limiter accès.\n- Hypothèses erronées : demander validation sur points inconnus avant finalisation.\n- Scope creep : fixer bornes et exclure explicitement ce qui n’est pas demandé.\n- Output non conforme : fournir templates et exemples.\n- Dépendance aux outils externes : prévoir plan B si l’API/accès échoue.\n\nExemple de tâche Cowork (prête à envoyer)\n« Objectif : produire un rapport PDF de 4–6 pages synthétisant les performances Q2 des produits A/B et 3 actions recommandées. Entrées : dossier Google Drive /Projets/Q2 (donne-moi accès), base de données analytics (utiliser la requête fournie dans /queries/Q2.sql). Critères : structure (Résumé, Méthode, Résultats, Recommandations), ton exécutif, inclure 2 graphiques exportés en PNG, livrable nommé Rapport_Q2_ProduitsA_B.pdf. Contraintes : délai 48 h. Autonomie : tu peux exécuter les requêtes et corriger les graphiques ; si une donnée manque, pose 1 question ciblée. N’utilise pas les données personnelles brutes ; anonymise tout identifiant. »"
      }
    },
    "ex_claude_certified_architect_foundations__05_002": {
      "correction": {
        "en": "Chat — lightweight, turn-by-turn dialogue for quick exchanges:\n- Best for brainstorming, Q&A, short drafts, exploring ideas one tool at a time.\n- Strengths: fast, low context, immediate clarifications.\n- Limitations: not built to coordinate many tools or long-running artifacts.\n\nCowork — multi-tool, persistent workspace for sustained, multi-step work:\n- Best for projects needing multiple tools, document assembly, research over time, handoffs, and polished deliverables.\n- Strengths: stateful workspace, file/tool orchestration, repeated edits, long context.\n- Limitations: heavier setup than Chat.\n\nCode — full developer environment with terminal and git access:\n- Best for writing, running, testing, and shipping code inside a repo; refactors, CI fixes, debugging.\n- Strengths: executes code, commits, runs tests.\n- Limitations: developer-focused; not ideal for freeform brainstorming.\n\nSix-task mapping (example choices and why)\n1. Quick brainstorm for blog titles — Chat (fast, iterative ideas).\n2. Turn a research folder and notes into a formatted report — Cowork (orchestrate files, produce finished output).\n3. Fix failing unit tests and push a bugfix branch — Code (run tests, edit code, git).\n4. Long-term product roadmap with stakeholder comments and deliverables — Cowork (persistent context, versioned artifacts).\n5. Ask a math question or short factual lookup — Chat (single-turn answer).\n6. Implement a new API endpoint, run integration tests, and open PR — Code (dev environment + git).\n\nUse Chat for quick, isolated tasks; Cowork for multi-step, collaborative, persistent work; Code when you must run and ship code.",
        "fr": "Définition courte (pour décision)  \n- Chat : dialogue tour‑par‑tour, idéal pour questions rapides, brainstorming, clarification conceptuelle. Pas d’accès à terminal/git ni de persistance d’atelier.  \n- Cowork : espace de travail multi‑outils, long fil de travail, accès à fichiers et intégrations, produit des livrables finis (rapports, présentations, assets).  \n- Code : IDE + terminal + git, exécution de code et flux de travail de développement (tests, commits, déploiement).\n\nChecklist qu’une bonne réponse doit couvrir pour chaque scénario  \n1. Objectif concret (quel livrable ou résultat attendu).  \n2. Pourquoi un mode plutôt qu’un autre (outils requis, persistance, nécessité d’exécution ou d’accès au dépôt).  \n3. Critères temporels (interaction courte vs travail itératif).  \n4. Exemple d’action concrète réalisable dans ce mode (ex. exécuter test, générer slide, faire relecture rapide).  \n5. Risque/limitation si mauvais choix (perte de contexte, impossibilité d’exécuter, duplication de travail).\n\nExemples d’appariements types (6 scénarios)  \n1. Brainstorming noms produit — Chat (rapide, itératif).  \n2. Rédiger et livrer présentation finale pour la direction — Cowork (gestion de fichiers, rendu final).  \n3. Déboguer et exécuter tests unitaires localement, faire commit — Code (terminal + git).  \n4. Ébauche d’un plan de projet multi‑sprints avec dépendances — Cowork (suivi, versions).  \n5. Question technique spécifique / explication d’un algorithme — Chat (réponse concise).  \n6. Revue de PR avec exécution de correctifs et push — Code.\n\nUne bonne correction justifie chaque choix par les outils et le livrable attendu, et signale les risques opérationnels si on choisit le mauvais mode."
      }
    },
    "ex_claude_certified_architect_foundations__05_003": {
      "correction": {
        "en": "Three-way comparison (concise)\n- Chat — Best for conversational, open-ended tasks: drafting emails, brainstorming, asking follow-ups, exploring ideas. Strengths: fast back-and-forth, clarifying questions, creative/iterative thinking. Limitation: not optimized for complex code editing or multi-file workflows.\n- Cowork — Best for document- and task-focused collaboration: structured transforms, multi-step edits, templates, collaborative revision of long docs or slide decks. Strengths: preserves context across revisions, supports tracked edits and workflows. Limitation: not specialized for running or debugging code.\n- Code — Best for programming work: writing, refactoring, debugging, generating tests, and interpreting stack traces. Strengths: understands syntax, can produce runnable snippets and suggest fixes. Limitation: less suited for open-ended brainstorming or long-form collaborative document polish.\n\nSix-task challenge — right mode + brief rationale\n1. Draft a short professional decline email — Chat (quick, conversational drafting and tone-tuning).\n2. Refactor a 300-line Python function and add unit tests — Code (code-aware edits, test generation, syntax-correct suggestions).\n3. Convert a research doc into a slide deck with speaker notes — Cowork (structured transform, preserves document context, templating).\n4. Debug failing integration tests with stack trace analysis — Code (interpret traces, suggest concrete fixes and test cases).\n5. Co-edit a policy document with tracked changes and multiple rounds of revisions — Cowork (collaborative edits, iterative refinements).\n6. Brainstorm 20 product name ideas and group them by tone — Chat (fast ideation, grouping, and conversational refinement).",
        "fr": "Correction modèle — que doit contenir une bonne réponse\n\nRésumé des modes (1-2 phrases chacun)\n- Chat : conversation libre, Q&A, brainstorming et rédaction rapide. Utile pour idées, clarification et itérations textuelles courtes.\n- Cowork : travail collaboratif centré sur un document/projet en plusieurs étapes (révisions, structuration, style, commentaires). Utile pour co‑rédaction et tâches qui gardent du contexte.\n- Code : génération, débogage et explication de code, sorties structurées (tests, snippets), et recommandations techniques.\n\nExemples de correspondance (6 tâches typiques)\n1. Brainstormer idées de campagne marketing → Chat. Justification : réponses créatives rapides.\n2. Réviser et formater un rapport long avec sections à conserver → Cowork. Justification : maintien du contexte et édition structurée.\n3. Debug d’un test unitaire qui échoue → Code. Justification : besoin de logs, snippets et correction de code.\n4. Demander conseils stratégiques produit → Chat. Justification : discussion exploratoire.\n5. Fusionner commentaires d’équipe dans un document → Cowork. Justification : gestion de versions et commentaire ciblé.\n6. Générer une fonction et ses tests automatisés → Code. Justification : production et validation de code.\n\nPrompts d’exemple\n- Chat : « Aide-moi à trouver 8 accroches pour une campagne B2B (ton professionnel, 10–12 mots). »\n- Cowork : « Revois ce rapport (sections ci‑dessous) : uniformise le style, synthétise les points clés. »\n- Code : « Voici le test qui échoue et le fichier X.py. Explique l’erreur et propose un patch (diff). »\n\nChecklist d’évaluation\n- Choix du mode cohérent avec l’objectif et la justification claire.\n- Prompts adaptés au mode (concis pour Chat, document+instructions pour Cowork, code+tests pour Code).\n- Exemple concret (prompt ou snippet) et mention du besoin de contexte ou fichiers quand nécessaire."
      }
    },
    "ex_claude_certified_architect_foundations__05_004": {
      "correction": {
        "en": "Instruction to Cowork (what to do)\n- Open: /Proposals/templates/ClientProposal_Template.docx and /MeetingNotes/Acme_2026-07-18.txt\n- Create new file: /Proposals/Acme_Proposal_2026-07-25.docx using the template. Replace template fields (Client Name, Project Title, Background, Objectives, Deliverables, Timeline, Budget, Acceptance Criteria, Contact) with content pulled from the meeting notes. Run spell/format check and save a final PDF at /Proposals/Acme_Proposal_2026-07-25.pdf.\n\nKey replacements (exact pulls)\n- Background: pull lines describing current pain points.\n- Objective: use meeting sentence “reduce time-to-deploy by 40%” as the measurable objective.\n- Constraints: include “pilot duration: two weeks” and “budget cap: $50,000” verbatim from notes.\n- Contact: use the person and email in notes.\n\nExample final proposal content (student target)\n- Executive summary: We propose a two-week pilot to validate a deployment workflow that aims to reduce time-to-deploy by 40%. The pilot will focus on CI/CD automation and staging rollback.\n- Scope & Deliverables:\n  1) Assessment report (3 days) — baseline metrics collected.\n  2) CI/CD implementation (6 days) — scripts + docs; acceptance: successful rollback in <5 minutes.\n  3) Knowledge transfer session (1 day) — recorded.\n- Timeline: Week 1 — assessment + initial setup; Week 2 — implementation + handoff.\n- Price: $45,000 total (meets “budget cap: $50,000”).\n- Next steps: Sign & return by 2026-08-01; kick-off meeting scheduled within 5 business days.\n\nSave both .docx and .pdf, and attach meeting notes page as Appendix A.",
        "fr": "Une bonne correction modèle doit donner des instructions exploitables et reproductibles pour que Claude (Cowork) modifie directement vos fichiers. Points clés à couvrir :\n\n1) Objectif et critères d’acceptation\n- Définir le livrable exact (ex. “document Word / résumé thématique”, “Google Sheet pivot+graphiques”, “proposition client .docx formatée”).\n- Indicateurs de succès (ex. 5 thèmes, ≥3 citations par thème, totale et % par thème).\n\n2) Liste de fichiers et chemins\n- Fournir chemins exacts et formats (ex. /Documents/Feedback/feedback_week_2026-07-18.csv ; /Reports/Sales_Q2.pdf ; /Sheets/metrics.xlsx).\n\n3) Règles de traitement concrètes\n- Pour feedback : regrouper par thème via mots-clés + clustering, normaliser fautes, retirer doublons, inclure 3 citations représentatives et une statistique (count/%).\n- Pour dashboard : extraire chiffres des trois sources, normaliser noms de colonnes (Date, Source, Metric, Value), créer onglet “CombinedData” puis onglets de visualisation (pivots + graphique temporel interactif), ajouter filtres par Source/Region.\n- Pour proposition : appliquer le modèle template.docx, insérer sections issues des notes_meeting.docx, mettre en forme styles prédéfinis, enregistrer sous /Deliverables/Proposal_Client_X_v1.docx.\n\n4) Exemples et edge-cases\n- Fournir 1–2 lignes d’entrée et la sortie attendue.\n- Instructions sur données manquantes (ex. marquer “N/A”), format de date attendu.\n\n5) Validation, sauvegarde et versioning\n- Faire une copie avant modification (append _v1), lister checks automatiques (comptages, totaux, validation de somme).\n\n6) Risques et mitigations\n- Risques : écrasement de fichiers, mauvaise extraction OCR, perte de formatage, fuite de PII.\n- Mitigations : backup automatique, validation manuelle finale, masquer/anonimiser PII avant partage.\n\nExemple de prompt Cowork (à coller) :\n\"Travaille sur /Documents/Feedback/feedback_week_2026-07-18.csv. Regroupe feedbacks en thèmes (usage, prix, bugs, feature), normalise texte, enlève doublons, crée /Deliverables/Feedback_summary.docx avec : liste de thèmes, count, % et 3 citations par thème. Sauvegarde copie originale en /Archive/feedback_week_2026-07-18_backup.csv. Signale problèmes de parsing.\""
      }
    },
    "ex_claude_certified_architect_foundations__05_005": {
      "correction": {
        "en": "Key problems\n- Unescaped string concatenation into HTML (name, svg, ico, etc.) — allows stored/reflected XSS and HTML injection.\n- Injecting raw SVG into markup is especially dangerous (SVG can contain scripts).\n- Character-encoding corruption (â) indicates missing/incorrect UTF-8 headers, which can break parsing and filtering.\n- No input validation or content-security controls.\n\nConcrete risks\n- Remote attacker injects <script> or event handlers via “name” or file metadata.\n- Malicious SVG executes script or exfiltrates data.\n- Broken encoding bypasses filters or mangles UI.\n\nRemediations (actionable)\n1. Never build HTML with unescaped user data. Use textContent or createElement:\n   - Example:\n     const div = document.createElement('div');\n     div.className = 'name';\n     div.textContent = name; // safe\n     container.appendChild(div);\n2. If you must insert HTML, sanitize with a library (DOMPurify):\n   - Example:\n     const safeSvg = DOMPurify.sanitize(svg, {FORCE_BODY: true});\n     svgContainer.innerHTML = safeSvg;\n   - Prefer rendering untrusted SVG as an <img src=\"data:image/svg+xml;base64,...\" /> to avoid inline script execution.\n3. HTML-escape any string used inside innerHTML:\n   function escapeHtml(s) { return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;').replace(/'/g,'&#39;'); }\n4. Fix encoding: send Content-Type: text/html; charset=utf-8 and include <meta charset=\"utf-8\">.\n5. Add server-side validation, CSP (Content-Security-Policy: default-src 'self'; script-src 'nonce-...'), and output-encoding on all render paths.\n6. Review file handling to avoid path traversal and validate filenames.\n\nFollow these steps to eliminate XSS, SVG script risks, and encoding problems.",
        "fr": "Problèmes concrets et actions à prendre\n- Risques de sécurité : concaténation de chaînes pour produire du HTML crée des vulnérabilités XSS si name/svg proviennent d’utilisateurs.  \n- Robustesse : fonctions/utilitaires (step, row, variables PDF/XLS/DOC) non définis ; variables globales ; composants retournant des strings HTML difficiles à tester.  \n- Accessibilité & maintenance : balisage implicite, absence d’attributs ARIA, pas de séparation données/presentation.\n\nCorrections recommandées (actionnables)\n1. Ne pas construire de HTML par concaténation. Utiliser DOM APIs (createElement, textContent) ou templates sécurisés.  \n2. Valider/sanitiser les SVG entrants (parser et n’autoriser que <svg>) ou servir SVG comme fichier de confiance.  \n3. Factoriser helpers : implémenter step() et row() pour renvoyer des DocumentFragment ou éléments. Éviter variables globales.  \n4. Ajouter tests unitaires (ex. injection de script dans name/svg doit rester inactif), et tests d’accessibilité.\n\nExemple fonctionnel (pattern) — création sûre d’un élément “conn”\nfunction conn(name, on, svgString) {\n  const container = document.createElement('div');\n  container.className = 'conn';\n  // Label sûr\n  const label = document.createElement('span');\n  label.textContent = name || '';\n  container.appendChild(label);\n  // SVG : parser et n’autoriser que <svg>\n  if (svgString) {\n    const doc = new DOMParser().parseFromString(svgString, 'image/svg+xml');\n    const svg = doc.querySelector('svg');\n    if (svg) container.appendChild(svg);\n  }\n  // état\n  const state = document.createElement('span');\n  state.className = 'state';\n  state.textContent = on ? 'connecté' : 'déconnecté';\n  container.appendChild(state);\n  return container;\n}\n\nPoints de vérification qu’une bonne réponse couvrirait\n- Suppression de toute concaténation HTML non échappée ; usage de textContent.  \n- Validation stricte des SVG et autres fichiers.  \n- Implémentation claire de helpers (step/row) renvoyant des nœuds DOM/testables.  \n- Tests de sécurité et d’accessibilité, et gestion d’erreurs (fallbacks)."
      }
    },
    "ex_claude_certified_architect_foundations__05_006": {
      "correction": {
        "en": "Steps a student should follow to delegate an end-to-end task (prompt → finished deliverable), with a sample prompt template and validation checklist.\n\n1. Define scope and acceptance criteria\n- Objective, audience, format, length, tone\n- Success metrics (readability score, SEO keywords, no factual errors)\n- Non-goals and constraints (legal, privacy, sensitive topics)\n\n2. Prepare inputs and assets\n- Source documents, data, examples, brand guidelines, images\n\n3. Write the delegation prompt (system + user + examples)\n- Include output schema and one example result\n- Ask for a stepwise plan and confirmation before full generation\n\nSample prompt (use as system + user):\nSystem: “You are a professional content writer. Produce JSON: {title, outline[], content, word_count, citations[]}.”\nUser: “Write a 600-word blog post for small-business owners about customer churn reduction. Tone: practical, friendly. Include 3 actionable tactics and 2 short case-study bullets. Cite sources inline and in citations array. Produce an outline first; wait for my approval.”\n\n4. Iterative workflow\n- Approve outline → request draft → request targeted revisions (quote lines to change)\n- Use few-shot examples for style or formatting\n\n5. Validation checklist (manual + automated)\n- Factual accuracy (check citations)\n- Tone and audience fit\n- Structure and completeness vs acceptance criteria\n- Plagiarism check and legal/privacy compliance\n- Final formatting (file names, metadata, README)\n\n6. Handoff and record-keeping\n- Deliver final files, revision history, prompt log, and reviewer notes\n- Capture feedback for continuous improvement\n\nFollowing this makes delegation predictable, auditable, and easy to iterate.",
        "fr": "Objectif attendu — livrable de bout en bout\n1) Définition claire de la tâche (à inclure dans le prompt)\n- Rôle du modèle (ex. «Vous êtes un rédacteur technique senior»).\n- Produit attendu (format, sections obligatoires, longueur, style).\n- Contrainte temporelle et ressources autorisées.\n- Exemple d’entrée + sortie attendue.\n\nPrompt modèle (à copier / adapter)\n«Vous êtes [rôle]. Produisez [type de livrable] pour [audience] en [format: Markdown/PDF/etc.]. Doit contenir : 1) Résumé en 30–50 mots, 2) Objectifs, 3) Processus étape‑par‑étape, 4) Tests/Exemples, 5) Limitations. Ton : [formel/convivial]. Citez les sources et fournissez un changelog. Si incertitude, posez jusqu’à 3 questions de clarification.»\n\n2) Critères d’acceptation mesurables\n- Contenu présent : toutes les sections obligatoires.\n- Longueur dans la fourchette demandée.\n- 2 exemples tests fonctionnels fournis.\n- Aucun fait inventé sans balise «non vérifié».\n- Format correct (fichiers, noms, métadonnées).\n\n3) Processus d’itération\n- Générer → évaluer avec la grille ci‑dessus → corriger en citant modifications → régénérer.\n- Loguer les prompts successifs et les réponses dans un changelog.\n\n4) Handoff final\n- Fichier final + README expliquant usage, limitations, étapes pour reproduire.\n- Liste des risques concrets (biais, hallucinations, dépendances externes, données sensibles) et stratégies d’atténuation.\n- Estimation du temps et contacts pour clarifications.\n\nChecklist de remise\n- Prompt initial + 2 itérations et leur évaluation, livrable final, README, changelog, et formulaire de feedback rempli."
      }
    },
    "ex_claude_certified_architect_foundations__05_007": {
      "correction": {
        "en": "How I’m using course concepts\n- Implemented a standardized prompt-engineering workflow for our support bot: seed prompts + structured few-shot examples + critique loop. Result: 35% fewer user escalations and 20% faster response-template iteration.\n- Built a safety filter pipeline based on the course’s red-team checklist and classifier stacking; reduced toxic outputs in production by ~60% (A/B test).\n- Adopted evaluation metrics from the course (precision/recall for safety labels, human rating rubric for helpfulness) and added automated regression tests to catch quality drops.\n\nWhat worked well\n- Clear, actionable frameworks (prompt patterns, red-team methodology) that were easy to operationalize.\n- Practical evaluation templates and sample rubrics that helped align cross-functional reviewers.\n\nSuggestions for improvement\n- Add a hands-on lab that walks through end-to-end deployment: prompt → eval → monitoring → rollback, with code snippets for common SDKs.\n- Expand the section on multi-turn safety and escalation policies with real-world case studies.\n- Provide downloadable templates: prompt-version control format, evaluation spreadsheets, and a CI pipeline example for model changes.\n- Include a short module on cost/latency tradeoffs and strategies for constrained environments.\n\nOverall, the course materially improved our model safety and iteration speed. Thank you — I’d be happy to share anonymized before/after metrics or participate in a case study.",
        "fr": "Pour cet exercice, une bonne réponse doit décrire concrètement comment vous appliquez les concepts du cours et fournir un retour actionnable. Utilisez la structure suivante dans le formulaire de feedback :\n\n1) Contexte et rôle — indiquez votre poste, secteur, et cas d’usage (ex. assistant client, résumé de documents juridiques, classification).  \n2) Concepts appliqués — listez les modules et techniques utilisés (prompt engineering, évaluation des hallucinations, chaînes de pensée, limitations de sécurité, tests de robustesse). Pour chaque concept, donnez un exemple précis (prompt avant/après, métrique mesurée).  \n3) Impact mesurable — fournissez chiffres : réduction du temps de traitement, baisse du taux d’erreur/hallucination, amélioration NPS, latence et coût par requête.  \n4) Problèmes rencontrés — énumérez bugs ou limites reproduisibles (ex. comportements biaisés sur X données, fuites de contexte >2048 tokens) et étapes pour les reproduire.  \n5) Améliorations demandées — priorisez 2–4 changements (docs plus claires, exemples supplémentaires, outils d’évaluation, presets de sécurité).  \n6) Besoins en formation ou ressources — compétences manquantes, tutoriels souhaités, formats préférés (atelier, cheat-sheet).  \n7) Confidentialité — redigez les extraits partagés pour retirer données sensibles.  \n\nConseil pratique : soyez concret, quantitatif et fournissez extraits de prompts/inputs et outputs anonymisés pour faciliter l’action."
      }
    }
  },
  "claude_certified_architect_foundations__06": {
    "ex_claude_certified_architect_foundations__06_001": {
      "correction": {
        "en": "Benefits\n- More natural constraint enforcement: a concise system prompt describes style, scope, and constraints in human terms; the model internalizes these rather than following a brittle checklist of rules that can conflict or be forgotten.\n- Cleaner handling of edge cases: role-based system prompts (e.g., \"You are a safety-aware assistant...\") let the model generalize intent across unexpected inputs, reducing the need for exhaustive rule enumerations.\n- Easier maintenance: changing one system-role statement updates behavior globally instead of editing many rule entries.\n\nBest practice (make system prompt optional)\n- API design: make the system prompt parameter optional (default = null/None) so callers can reuse chat functions without always supplying one.\n- Avoid empty-string errors: treat empty-string as None. Example (Python-like):\n  def chat(messages, system_prompt=None):\n      if not system_prompt:  # None or '' -> no system role applied\n          system_prompt = None\n      if system_prompt:\n          messages.insert(0, {\"role\":\"system\",\"content\":system_prompt})\n      return send_to_model(messages)\n- Validation and safety: if provided, enforce max length, strip whitespace, and optionally run the prompt through a safety/format checker.\n- Documentation & examples: document that omitting system_prompt uses default behavior, show examples for with/without system prompts, and include tests to ensure empty strings don’t inject blank system messages.",
        "fr": "Avantages (précis)\n- Les contraintes exprimées via un rôle (p. ex. \"You are a concise medical assistant...\") sont plus naturelles et composables que de longues listes de règles : elles s’appliquent globalement, s’adaptent aux cas limites (ton/registre), et se combinent mieux avec historiques de conversation.\n- Moins de duplication et moins d’effets de bord quand on compose plusieurs prompts (les règles implicites d’un rôle s’imposent sans ré-écriture exhaustive).\n- Risques concrets : ambiguïtés si le rôle est trop vague ; conflit entre rôle et instructions utilisateur ; fuite d’instructions si on réutilise le même rôle pour usages différents.\n\nMeilleure pratique (implémentation)\n- Rendre system_prompt optionnel avec None par défaut ; traiter None comme “pas de message system” au lieu d’une chaîne vide.\n- Valider/normaliser l’entrée : None -> pas d’injection, \"\" -> considérer comme absence et logguer un avertissement.\n\nExemple Python fonctionnel :\ndef build_messages(user_prompt, system_prompt: Optional[str] = None):\n    messages = []\n    if system_prompt is not None and system_prompt.strip() != \"\":\n        messages.append({\"role\": \"system\", \"content\": system_prompt})\n    messages.append({\"role\": \"user\", \"content\": user_prompt})\n    return messages\n\nBonnes pratiques opérationnelles\n- Documenter clairement la différence None vs chaîne vide.\n- Fournir un test unitaire : vérifier qu’un system_prompt vide n’ajoute pas de message, que None non plus.\n- Logger quand system_prompt est vide pour éviter erreurs silencieuses.\n- Pour usages sensibles, ajouter une validation stricte du contenu du system_prompt (allowlist/length)."
      }
    },
    "ex_claude_certified_architect_foundations__06_002": {
      "correction": {
        "en": "Streaming design summary (what to build and why)\n- Goal: give users immediate, progressive feedback while model generates (reduce perceived latency).\n- Approach: use a Converse Stream between server and Bedrock; surface streamed “delta” events to the client as partial text.\n\nDetailed flow (server ↔ model ↔ client)\n1. Client POSTs question → server opens converse stream to Bedrock.\n2. Bedrock returns initial acknowledgment event (no text) → server immediately returns a short “thinking” ACK to client (e.g., show spinner/typing indicator).\n3. Bedrock emits a sequence of stream events (deltas). Server forwards them as they arrive.\n4. Client appends each delta to the visible message; when a final/end event arrives, mark the response complete.\n\nPractical implementation details\n- Event types: ack, delta (text fragment), choice/index (if multiple candidates), done/end, error.\n- Rendering: append deltas incrementally; flush either on punctuation or every X ms (e.g., 100–300 ms) to balance smoothness vs choppy updates.\n- Sanitization: escape/strip unsafe HTML/JS before inserting into DOM.\n- Cancellation & retry: expose “Stop” to cancel stream; on error offer “Retry” and log partial output for diagnostics.\n- Backpressure & buffering: limit client buffer (e.g., 1–2 MB); if client slow, pause server reads or drop oldest buffered deltas.\n- UX polish: show typing cursor, preserve caret, avoid layout shifts, make updates screen-reader friendly.\n- Metrics & monitoring: track time-to-first-delta, delta rate, total duration, error rate.\n- Security: never expose API keys to client; validate inputs and rate-limit requests.\n\nTesting checklist\n- Verify immediate ACK handling, ordered delta assembly, clean finalization, safe HTML rendering, cancel/retry behavior, and accurate metrics.",
        "fr": "Correction modèle — Streaming de conversation (Converse Stream)\n\nRésumé: implémentez un flux d’événements (SSE/WebSocket) qui renvoie immédiatement un ack puis des \"deltas\" textuels au fur et à mesure de la génération. Assurez cohérence UI+réseau, gestion d’erreurs, et indicateurs UX.\n\nChecklist d’implémentation (actionnable)\n- Protocole: utiliser WebSocket ou Server-Sent Events. Schéma minimal d’événements:\n  - ack: {type:\"ack\", requestId}\n  - delta: {type:\"delta\", text:\"...\", tokenIndex}\n  - done: {type:\"done\"}\n  - error: {type:\"error\", code, message}\n- Buffering/flush: agréger tokens puis flush toutes les 50–200 ms ou après une ponctuation/espaces pour éviter coupures de mots.\n- UI: afficher indicateur de \"typing\" dès l’ack; insérer deltas incrémentaux; permettre pause/annulation client-side.\n- Consistance: tamponner localement pour corriger réordonnements; appliquer idempotence via requestId.\n- Erreurs & reprise: gérer error + reconnection with resume tokenIndex; fallback non-stream si latence > 30s.\n- Sécurité & quotas: valider/authentifier chaque socket, limiter taux par utilisateur, chiffrer transport.\n- Mesures: TTFB, tokens/sec, taux d’erreur, time-to-first-delta, taux d’annulation.\n\nRisques concrets\n- Affichage de texte incomplet ou coupé en plein mot (UX brisée)\n- Requêtes dupliquées ou incohérence d’état sur reconnexion\n- Exposition d’informations sensibles dans flux non chiffrés\n- Surcharge serveur / DoS via sockets persistants\n\nBonnes pratiques finales: flush fréquent mais pas trop, heuristiques de ponctuation, affichage clair de l’état (streaming/terminé/erreur), tests de latence et de reprise sur réseau instable."
      }
    },
    "ex_claude_certified_architect_foundations__06_003": {
      "correction": {
        "en": "How streaming works (concise flow)\n1. Client submits question → server opens a Converse stream to Bedrock.\n2. Bedrock returns an immediate acknowledgment event (no text) — treat this as “response started”/typing indicator.\n3. Bedrock sends a sequence of events containing pieces of the generated response.\n4. Each text event contains a small chunk (token/delta) the client app appends to the visible output so the user sees the answer build in real time.\n5. When a final/end event arrives, mark the message complete and run any post-processing (e.g., full parsing, analytics).\n\nCommon event types and how to handle them\n- message_start: first event. Set UI to “typing” and initialize buffer/state.\n- token / text_chunk (delta): append chunk to buffer and render incrementally. Avoid re-rendering whole text; append only new bytes.\n- partial_metadata: update score/author/formatting info mid-stream; update UI metadata if present.\n- message_end / complete: stop typing indicator, finalize buffer, run safety checks and save transcript.\n- error / aborted: show friendly error, allow retry; do not display partial content as final.\n- heartbeat / ack: keep-alive; ignore for display but use to detect connection health.\n\nPractical tips\n- Keep an accumulating buffer and a displayed pointer to avoid duplicates.\n- Sanitize/escape any streamed HTML before inserting into DOM to prevent XSS.\n- Support cancel: send a stop signal to Bedrock and discard further chunks.\n- Handle reconnection by resuming with context or restarting the query; make responses idempotent.\n- Show progress affordances (spinner / “typing” + partial text) and only treat text as final after message_end.",
        "fr": "Une bonne correction doit lister précisément chaque type d’événement attendu, son contenu et comment le client/serveur doit y réagir. Points clés à couvrir :\n\n- Ack initial (response acknowledge) : payload minimal {request_id, status}. Action → montrer indicateur de “génération en cours”.\n- Début du message (message.start) : {message_id, role, model, timestamp}. Action → allouer buffer, verrou de rendu, initialiser métadonnées.\n- Delta / Token (message.delta ou token) : {message_id, text, token, index, is_final=false, partial_html=false}. Action → appender au buffer, échapper HTML pour éviter XSS, rendre par petits paquets (debounce 50–200ms) pour fluidité, conserver token-index pour idempotence.\n- Fin du message (message.end) : {message_id, finish_reason, total_tokens, usage}. Action → finaliser rendu, remplacer curseur, afficher metadata/usage.\n- Alternatives / choices : {choice_index, is_best}. Action → permettre affichage sélectif ou menu pour choisir alternative.\n- Erreur (error) : {code, message, recoverable}. Action → afficher message utile, proposer retry/cancel, log.\n- Métadonnées supplémentaires (meta) : {model_version, safety_labels}. Action → appliquer filtres UX (masquage si contenu bloqué).\n- Opérations usuelles : supporter reconnection/resume via last_event_id, déduplication par message_id+index, gestion d’interruption utilisateur (stop stream), backpressure côté client.\n\nExemples de champs utiles et règles de sécurité (HTML-escape, validation size, limites rate) doivent être inclus."
      }
    },
    "ex_claude_certified_architect_foundations__06_004": {
      "correction": {
        "en": "Use try/except around each tool call, JSON-serialize any successful output to a string, and return a structured result object with status \"ok\" or \"error\" and a content field that is a JSON string. Claude can read the returned error content and decide to retry with corrected arguments.\n\nExample implementation (Python):\n\nimport json, traceback\n\ndef run_tool(tool_func, *args, **kwargs):\n    try:\n        result = tool_func(*args, **kwargs)\n        # Ensure consistent string formatting of outputs\n        content = json.dumps(result, default=str)\n        return {\"tool\": tool_func.__name__, \"status\": \"ok\", \"content\": content}\n    except Exception as e:\n        tb = traceback.format_exc()\n        # Include error message (and optional traceback) in content for Claude to analyze\n        err_obj = {\"error\": str(e), \"traceback\": tb}\n        return {\"tool\": tool_func.__name__, \"status\": \"error\", \"content\": json.dumps(err_obj)}\n\ndef run_tools(tool_calls):\n    results = []\n    for func, args, kwargs in tool_calls:\n        results.append(run_tool(func, *args, **kwargs))\n    # Final step: return list of tool result parts to send back to Claude\n    return results\n\nExample usage:\n- tool_calls = [(parse_date, (\"2020-13-01\",), {}), (fetch_user, (123,), {})]\n- run_tools(tool_calls) -> [\n    {\"tool\":\"parse_date\",\"status\":\"error\",\"content\":\"{\\\"error\\\":\\\"...\\\",\\\"traceback\\\":\\\"...\\\"}\"},\n    {\"tool\":\"fetch_user\",\"status\":\"ok\",\"content\":\"{\\\"id\\\":123,\\\"name\\\":\\\"Alice\\\"}\"}\n  ]\n\nNotes:\n- JSON-serialize outputs to keep formatting consistent.\n- Include clear error messages so Claude can analyze and generate corrected arguments to retry.",
        "fr": "Correctif modèle (actionnable)\n\n1) Structure générale\n- Envelopper toute exécution d’outil dans try/except.\n- Toujours renvoyer un objet résultat avec un champ status (\"ok\" / \"error\"), input_args, output (JSON string), et error (message + type + traceback) si erreur.\n- Sérialiser output en JSON via json.dumps(obj, default=str) pour garantir un format constant.\n\n2) Exemple Python fonctionnel\n```python\nimport json, traceback\n\ndef run_tool_safe(tool_fn, *args, **kwargs):\n    result = {\"status\": None, \"input_args\": {\"args\": args, \"kwargs\": kwargs}, \"output\": None, \"error\": None}\n    try:\n        out = tool_fn(*args, **kwargs)\n        result[\"status\"] = \"ok\"\n        result[\"output\"] = json.dumps(out, default=str)\n    except Exception as e:\n        result[\"status\"] = \"error\"\n        result[\"error\"] = {\n            \"type\": type(e).__name__,\n            \"message\": str(e),\n            \"traceback\": traceback.format_exc()\n        }\n        # Optionnel: inclure diagnostic minimal pour éviter fuite d'infos sensibles\n        result[\"output\"] = json.dumps({\"note\": \"no output due to error\"})\n    return result\n\n# Exemple de retour final à envoyer à Claude\nparts = [{\"role\": \"tool_result\", \"content\": json.dumps(run_tool_safe(my_tool, 1, key=\"v\"))}]\n```\n\n3) Points clés à couvrir dans la réponse d’un étudiant\n- Champs précis du résultat (status, input_args, output JSON, error détaillé).\n- Utilisation de json.dumps(..., default=str) pour objets non sérialisables.\n- Ne pas exposer données sensibles dans error.traceback (ou masker).\n- Indiquer que Claude doit analyser error.message/type et proposer un nouvel appel avec arguments corrigés ou stratégies de retry."
      }
    },
    "ex_claude_certified_architect_foundations__06_005": {
      "correction": {
        "en": "Recommended solution: use RAG with a clear preprocessing + retrieval pipeline, because it scales, reduces cost/latency, and focuses Claude on relevant content.\n\nImplementation steps (concise)\n1. Preprocess: clean text, split into logical units, remove boilerplate, store source/offset metadata.\n2. Chunking: choose a strategy (see below), target 500–1,000 tokens per chunk with 100–200 token overlap.\n3. Indexing: compute embeddings for each chunk (OpenAI/FAISS/Annoy/Pinecone/Weaviate), store metadata.\n4. Retrieval: on a query, compute query embedding, retrieve top_k (3–8) candidates by similarity, optionally rerank with lexical score (BM25).\n5. Compose prompt: concatenate highest-relevance chunks (respect model token limit), add provenance metadata, then the user question and an instruction to cite sources.\n6. Answer & validate: run Claude, then optionally verify answer by re-checking cited chunks or running a secondary verification step.\n\nChunking strategies (choose one or combine)\n- Header-based: split at document headings (good structure)\n- Paragraph/section: natural language boundaries (best fidelity)\n- Fixed-size sliding window: consistent token size with overlap (robust)\n- Hybrid: header-based with fallback to fixed-size for long sections\n\nPrompt template (example)\nContext:\n[Chunk 1 — source A, p.10]\n[Chunk 2 — source B, p.42]\n---\nTask: Using only the context above, answer the question. Cite chunk sources and line ranges. Question: [user question]\n\nPractical notes & risks\n- Use top_k and token budget to avoid exceeding limits.\n- Add overlap to mitigate split-context loss.\n- Risk: missing context → hallucinations; mitigate by provenance, verification queries, and refusing to answer if evidence insufficient.\n- Maintain/update index for document changes and monitor latency/cost.\n\nEvaluation: track accuracy, recall of relevant chunks, latency, and cost per query. Use small pilot (10–50 queries) to tune chunk size, top_k, and reranker.",
        "fr": "Solution recommandée (approche RAG optimisée)\n\nRésumé opérationnel\n- Prétraitement : découper le document en blocs de 800–2 000 tokens avec recouvrement 150–300 tokens. Favoriser d’abord découpes par en-têtes/paragraphes ; si absent, découpe fixe.\n- Indexation : calculer embeddings pour chaque bloc (OpenAI/Anthropic ou autre), stocker dans un vector DB (FAISS, Weaviate, Pinecone, Chroma) + métadonnées (document, page/offset, en-tête).\n- Recherche : pipeline hybride BM25 (ElasticSearch) → top 50 → embeddings similarity → rerank Claude → retourner top 3–7 blocs. Ajuster K pour privilégier rappel (start higher).\n- Prompting : inclure uniquement les blocs rerankés, métadonnées, instruction claire et format de sortie (JSON, table), demander citations (document/page).\n\nRisques concrets et mitigations\n- Contexte manquant → augmenter K, recouvrement, ou faire recherche hiérarchique (chapitre → paragraphe).\n- Hallucinations / réponses non sourcées → exiger citations et score de confiance ; utiliser reranking humain/automatisé.\n- Latence/coût d’indexation → batch d’embeddings asynchrones, cache des embeddings.\n- Données sensibles → chiffrer index, filtrer métadonnées, contrôle d’accès.\n\nTemplate de prompt (exemple)\nTu es un assistant. Voici les blocs pertinents (chaque bloc = source, page): [BLOC1], [BLOC2]...\nTâche : extraire {champs précis} en format JSON. Pour chaque champ, fournir \"valeur\", \"source\" (document/page) et \"confiance\" (0-1). Si info manquante, mettre null et expliquer brièvement.\n\nMesures de qualité\n- Recall@K, précision des champs, latence p95, coût par requête, revue humaine sur échantillon."
      }
    },
    "ex_claude_certified_architect_foundations__06_006": {
      "correction": {
        "en": "When to use each\n- Direct approach: use when the document(s) are small (well under model context limits), you need full-document fidelity, latency/cost are not critical, and system simplicity is a priority.\n- RAG: use when documents are large or numerous, you need lower latency/cost at scale, or you want the model to focus only on relevant passages.\n\nConcrete RAG implementation checklist (actionable)\n1. Chunking\n   - Choose chunk size: 500–1,500 tokens is typical. Balance larger chunks (more context) vs. smaller (more precise retrieval).\n   - Add overlap: 50–200 tokens to preserve cross-boundary context.\n   - Prefer semantic boundaries when possible (sections, headings, paragraphs); fall back to fixed-size sliding windows.\n\n2. Indexing & search\n   - Precompute embeddings for each chunk (use a quality embedding model).\n   - Store embeddings in a vector DB (Pinecone, Milvus, Weaviate).\n   - Combine lexical (BM25) + semantic search or use re-ranking to improve precision.\n\n3. Retrieval strategy\n   - Retrieve top-K relevant chunks (K = 3–10). Tune K for recall vs. prompt length.\n   - Re-rank retrieved chunks by relevance/recency or similarity score.\n\n4. Prompt construction\n   - Include only retrieved chunks (with short citations), then the user question and clear instructions (e.g., “Answer concisely; cite chunk IDs”).\n   - If needed, ask the model to say “Insufficient information” when chunks don’t contain an answer.\n\n5. Robustness & maintenance\n   - Precompute and update embeddings on document change.\n   - Cache frequent queries and results.\n   - Monitor latency, token usage, and answer quality; tune chunk size/K accordingly.\n\n6. Handling missing context\n   - If answers require broader context, either increase chunk size/overlap or perform a secondary retrieval round (retrieve related chunks and synthesize).\n\nRisks/tradeoffs (brief)\n- More engineering complexity and operational overhead vs. scalability, cost, and focused responses. Use direct approach for simplicity; use RAG when scaling or token limits demand it.",
        "fr": "Réponse attendue (correction modèle)\n\nRésumé des choix\n- Approche directe : simple (mettre tout le texte dans le prompt) mais impraticable pour gros documents (limites de tokens, coût et latence élevés). À réserver pour documents courts (<~5k tokens).\n- RAG (recommandé pour documents volumineux) : découpage + indexation + récupération des blocs pertinents puis inclusion sélective dans le prompt.\n\nImplémentation RAG concrète\n1. Prétraitement : nettoyer le texte, normaliser métadonnées.\n2. Découpage : 500–1000 tokens par bloc avec chevauchement 50–200 tokens (sliding window) ; alternative : découpage par en-têtes + sous-sections pour préserver cohérence.\n3. Indexation : calculer embeddings pour chaque bloc, stocker dans vectordb (FAISS, Pinecone).\n4. Requête : encoder la question, récupérer top-k = 3–10. Si besoin, élargir k pour améliorer rappel.\n5. Reranking/compression : appliquer un reranker sémantique sur top-20 puis garder top-5 ; option : résumer chaque bloc avant inclusion.\n6. Prompting : inclure uniquement les blocs retenus avec balises \"Source #N (titre, position) :\" puis la question ; demander explicitement des citations et de la prudence (“réponds en citant les sources”).\n7. Post-traitement : vérifier cohérence, détecter contradictions, demander clarification si contexte manquant.\n\nRisques concrets et mitigations\n- Perte de contexte → chevauchement + augmenter k ou inclure sommaire.\n- Hallucinations → exiger citations, vérifier avec source.\n- Faille de pertinence → reranker et métriques de rappel/précision.\n- Coût/latence → ajuster taille des blocs et k.\n\nCritères de succès\n- Rappel de documents pertinents ≥ cible (ex. 90% dans tests),\n- Réponses précises avec citations, latence et coût dans budget."
      }
    },
    "ex_claude_certified_architect_foundations__06_007": {
      "correction": {
        "en": "Step 4–6 model answer (concise, actionable)\n\n- Storage (Step 4)\n  - Store: (embedding vector, source_id, text/snippet, metadata/timestamps, embedding_model_id).\n  - Use a vector DB or ANN index (FAISS, Annoy, Milvus, Pinecone) configured for cosine/inner-product search.\n  - Normalize vectors on write (L2 normalization) if using cosine as dot product; store normalized vectors to speed queries and avoid re-normalizing.\n\n- Query embedding (Step 5)\n  - Embed the user query with the exact same embedding model and version used for the stored vectors.\n  - Batch queries where possible and normalize the resulting vector.\n\n- Similarity search (Step 6)\n  - Compute cosine similarity = cosine(theta) = (v · u) / (||v|| ||u||). If vectors are pre-normalized, cosine similarity = dot(v, u).\n  - Cosine distance = 1 − cosine_similarity.\n  - Retrieve top-k results using the vector DB’s ANN or exact search; optionally filter by metadata.\n  - Use a similarity threshold (example heuristics: >0.8 highly similar, 0.6–0.8 possibly useful; tune on your data).\n\n- Practical tips\n  - Always record embedding_model_id and re-embed when the model changes.\n  - Persist raw text & ids so you can re-index or show provenance.\n  - Consider hybrid scoring: combine cosine score + metadata relevance for final ranking.\n\nExample pseudocode:\n  query_vec = embed(query); query_vec = normalize(query_vec)\n  results = index.search(query_vec, top_k=10)\n  ranked = sort_by_dot(query_vec, results)  # dot == cosine if normalized",
        "fr": "Correction modèle — points essentiels et actions concrètes\n\n1) Formules et normalisation\n- Cosine similarity : cos_sim(a,b) = (a · b) / (||a|| ||b||). Valeurs ∈ [-1, 1].\n- Cosine distance courant : cos_dist(a,b) = 1 − cos_sim(a,b). Valeurs ∈ [0, 2]; plus proche de 0 = plus similaire.\n- Astuce pratique : normalisez (unit vectors). Alors cos_sim = dot(a_norm, b_norm) et cos_dist = 1 − dot(...). Si votre vector DB ne supporte que inner product, stockez vecteurs normalisés.\n\n2) Étapes opérationnelles (Étape 4–6)\n- Stockage : pour chaque document stockez (id, texte original, métadonnées, embedding normalisé, chunk_id). Indiquez modèle & version de l’embedding.\n- Embedding de la requête : utiliser exactement le même modèle + même prétraitement (tokenisation, nettoyage) et normaliser.\n- Recherche : exécutez ANN (HNSW, IVF) pour top-k (k = 5–20), puis rechiffrez exactement les cos_sim des top-k pour rerank.\n- Seuils indicatifs : cos_sim > 0.8 = forte similarité; 0.6–0.8 = modérée; <0.4 = faible (valeurs à calibrer).\n\n3) Pièges et recommandations\n- Conserver version d’embedding pour reproductibilité.\n- Gérer la dimension fixe et rejeter embeddings invalides.\n- Attention aux définitions : certaines bibliothèques retournent une \"distance cosinus\" différente (ex. arccos normalisé). Vérifiez la doc.\n- Ajoutez métadonnées + score pour filtrage (date, source) et appliquer reranking/MMR si besoin.\n\n4) Exemple rapide (Python)\n- normaliser puis similarity = np.dot(a_norm, b_norm); distance = 1 - similarity."
      }
    },
    "ex_claude_certified_architect_foundations__06_008": {
      "correction": {
        "en": "Cosine distance = 1 − cosine similarity; values closer to 0 mean higher semantic similarity. Vector DBs compute cosine similarity between the query vector and document vectors to retrieve semantically related chunks even when keywords differ.\n\nStep 7 — Prompt construction (concrete template and example)\n- Include: top-N retrieved chunks (with id and similarity score), the original user query, and clear instructions for the model (scope, style, citation rules).\n- Example assembled prompt:\n\nContext (use only the information below):\n[Chunk A | score=0.08] “Inspect pump seals every 3 months; replace seals if wear >0.5 mm.”\n[Chunk B | score=0.12] “Lubricate bearings quarterly; full inspection annually.”\n[Chunk C | score=0.45] “Operate between 1500–1800 RPM for optimal life.”\n\nUser question:\nWhat is the recommended maintenance schedule for the XJ-200 pump?\n\nInstructions:\n1) Answer using only the Context above. If information is missing, say “insufficient information.”\n2) Provide a short actionable schedule and cite chunk ids inline (e.g., [Chunk A]).\n3) Be concise (≤120 words) and avoid adding facts not in the Context.\n\nStep 8 — LLM generation\n- Send the assembled prompt to the model. Recommended settings: low temperature (0–0.2) to reduce hallucination, appropriate max tokens for answer length.\n- Post-check: verify citations match context and mark “insufficient information” if claims exceed provided chunks.",
        "fr": "Cosine distance = 1 − cosine_similarity. Rappel utile : cosine_similarity ∈ [−1,1] (pour les embeddings texte pratiques elle est souvent dans [0,1]), donc cosine_distance ∈ [0,2] ; des valeurs proches de 0 indiquent une forte similarité. Remarques techniques : normalisez ou utilisez la même fonction d’embedding pour garantir comparaisons valides. L’angular distance (arccos(sim)/π) est une alternative métrique stricte si nécessaire.\n\nÉtape 7 — Construction du prompt (checklist actionnable)\n- Sélectionner top_k (recommandé 3–5) et/ou appliquer un seuil (ex. sim ≥ 0.7 ou distance ≤ 0.3).\n- Regrouper les morceaux par ordre décroissant de similarité.\n- Préfixer chaque chunk par un identifiant source (ex. [S1], [S2]) et séparer par ===.\n- Indiquer explicitement au LLM la contrainte : “Répondre en se basant uniquement sur les chunks fournis ; citer les sources entre crochets ; si insuffisant, répondre ‘Je ne sais pas’.”\n- Troncature : s’assurer que la concaténation tient dans la fenêtre du modèle (privilégier résumés ou reranking si dépassement).\n\nÉtape 8 — Exemple de prompt combiné (fonctionnel)\nSystem: Tu es un assistant qui répond strictement avec les sources fournies.\nUser: Question: «Comment fonctionne X?»\nChunks:\n[S1] === <texte 1>  \n[S2] === <texte 2>  \nInstruction: Utilise uniquement les chunks ci‑dessus, cite [S1]/[S2] pour chaque assertion, et dis «Je ne sais pas» si la réponse n’est pas couverte.\n\nRisques concrets et mitigations\n- Embeddings incompatibles → toujours réutiliser le même modèle/prétraitement.\n- Chunks hors-sujet retenus → ajouter reranking/score pondéré BM25+cosine.\n- Hallucinations (LLM invente) → demander citations et forcer “Je ne sais pas”.\n- Données sensibles exposées → filtrer avant indexation.\n\nParamètres pratiques recommandés : top_k=5, seuil sim≥0.7, temperature faible (0–0.2), demander citations explicites."
      }
    },
    "ex_claude_certified_architect_foundations__06_009": {
      "correction": {
        "en": "Example implementation and explanation (Python-like pseudocode):\n\n1) Create and populate the vector store\n- Compute embeddings for each chunk, then store embedding + original text together.\n\n```python\n# chunks: list[str] (document split into chunks)\nembeddings = [generate_embedding(chunk) for chunk in chunks]\n\n# store expects a list of dicts; use zip to pair\nitems = [{\"embedding\": emb, \"text\": chunk} for emb, chunk in zip(embeddings, chunks)]\n\nvector_index = VectorIndex()            # your vector store instance\nvector_index.store(items)               # bulk store\n```\n\n2) Query processing: generate embedding for user question\n```python\nquery = \"what did software entering depth do last year\"\nquery_emb = generate_embedding(query)\n```\n\n3) Similarity search: retrieve top-2 by cosine distance\n```python\nresults = vector_index.search(query_emb, top_k=2, distance_metric=\"cosine\")\n# results: list of {\"distance\": float, \"text\": str, ...}\nfor r in results:\n    print(r[\"distance\"], r[\"text\"][:200])\n```\n\n4) Expected outcome (from exercise)\n- Top results: Section 2 (distance 0.71) and Methodology section (distance 0.72).\n\nDesign rationale (concise)\n- Store original text with embeddings because embeddings are opaque vectors; storing \"text\" (or chunk_id + metadata) enables retrieval, ranking explanation, and downstream generation or citation.",
        "fr": "Correction modèle (exécutable et actionnable)\n\nExemple minimal en Python (interface générique) :\n\n# préparation et index\nvector_index = VectorIndex()  # implémentation du store (FAISS, Annoy, etc.)\nitems = []\nfor i, (emb, chunk) in enumerate(zip(embeddings, chunks)):\n    items.append({\n        \"id\": f\"chunk_{i}\",\n        \"vector\": emb.astype(float),      # numpy array ou list\n        \"text\": chunk,\n        \"metadata\": {\"source\": \"document.pdf\", \"chunk_index\": i}\n    })\nvector_index.add(items)  # ajoute une liste de dicts au store\n\n# requête : calculer l'embedding de la question\nquery = \"what did software entering depth do last year\"\nquery_emb = embedding_model.embed(query)  # renvoie un vecteur\n\n# recherche de similarité (cosine)\nresults = vector_index.search(query_emb, k=2, metric=\"cosine\")\n# résultats attendus (extraction et interprétation)\nfor r in results:\n    print(r[\"id\"], r[\"score\"], r[\"text\"][:200])  # score = distance/cosine-similarity\n\nExplication et points clés attendus\n- Utiliser zip(embeddings, chunks) pour garantir l'alignement embedding↔chunk.\n- Stocker à la fois le vecteur et le texte (ou au minimum l'ID du chunk) pour pouvoir reconstruire le contexte quand on récupère un match.\n- Normaliser les vecteurs si le store n'applique pas la normalisation avant la métrique cosinus.\n- Paramètres : k=2, métrique=cosine. Résultats mentionnés (Section 2 distance 0.71, Méthodologie 0.72) doivent correspondre aux scores retournés.\n- Ajouter metadata (source, index) pour traçabilité et filtrage futur."
      }
    },
    "ex_claude_certified_architect_foundations__06_010": {
      "correction": {
        "en": "Recommended automated fix-and-PR workflow (what a good solution should include)\n\n1) Detection & triage\n- Alert on error patterns (e.g., CloudWatch metric filter for \"invalid model identifier\"). Attach matching log lines, stack trace, request IDs, recent deploy hash, and affected service.\n- Enrich with repo link and candidate files (config, environment, client wrapper).\n\n2) AI diagnosis & patch generation\n- Provide AI assistant: repository snapshot, failing stack trace, relevant files, and tests. Prompt it to propose a minimal change and unit/integration tests that reproduce the error.\n- Example patch (commit message + diff):\n  Commit: fix(config): correct model id typo\n  Diff: config/models.yml: model_id: \"gpt-4o-mini\" -> \"gpt-4o-mini-v1\"\n\n3) Safety gates & automation\n- Create ephemeral branch and open a PR via VCS API with:\n  - Clear PR title and description: include error log snippet, root cause, changed files, tests added, how to roll back.\n  - Example PR body: \"Detected 'invalid model identifier' in production logs (2026-07-25). AI found typo in config/models.yml and corrected model_id from 'gpt-4o-min' to 'gpt-4o-mini'. Added unit test verifying client initialization. CI ran and passed. Please review.\"\n- Run CI: lint, unit tests, integration/staging deploy, and automated security/secret scanning.\n\n4) Human-in-loop & merge policy\n- Require at least one human reviewer (codeowners) and green CI before merging. Enforce branch protection and signed commits.\n- Maintain audit trail of AI suggestions and decisions.\n\n5) Operational safeguards & risk mitigation\n- Limit automation token scope; log all actions; limit automatic pushes to non-prod branches; rate-limit AI-initiated PRs.\n- Prevent sensitive changes (secrets, infra) from auto-patches; require manual triage.\n- Continuously monitor post-merge metrics and enable automatic rollback if regressions detected.\n\nThis design catches production-only issues, provides context and tests, but preserves human review and safety controls.",
        "fr": "Flux recommandé (concret et actionnable)\n1. Détection → alerte (CloudWatch/Splunk) déclenche job CI/CD contenant les logs pertinents.\n2. Analyse IA → job instancie l’assistant avec : extrait de logs, chemin(s) de fichier(s) modifiables, commit history minimum.\n3. Génération de correctif → IA propose patch minimal + message de commit + tests/unitaires si possible.\n4. Création automatique de PR → job crée une branche, applique le patch, pousse et ouvre une PR contenant : extrait de logs, explication, diff, checklist CI.\n5. Validation → CI exécute lint/tests; reviewer humain approuve; merge et déploiement.\n\nExemple minimal de script (GitHub CLI)\n- git checkout -b fix/invalid-model-id\n- apply_patch.sh  # script qui applique le diff généré par l'IA\n- git add .\n- git commit -m \"fix: correct model identifier (prod failure - invalid model identifier)\"\n- git push origin fix/invalid-model-id\n- gh pr create --title \"fix: correct model identifier\" --body \"@CI logs: <extrait>\\n\\nContexte: …\\nPatch: …\"\n\nPrompt modèle pour l’IA\n\"Voici les logs (extrait) et le repo (chemins permis). Indique l’emplacement probable du bug, propose un patch minimal (fichier + diff), justifie le changement, fournis tests unitaires si requis, et explique les risques.\"\n\nRisques concrets & mitigations\n- Commit contenant données sensibles → rediger/masquer logs avant PR.\n- Correctif erroné ou non régressif → exiger CI vert + tests + review codeowners.\n- Escalade de permissions via pipeline → tokens à portée limitée et rotation.\n- Injection de patch malveillant → audit des diffs, approbation humaine obligatoire.\n\nChecklist qu’une bonne implémentation couvre\n- découpage des permissions, journalisation des actions, template PR avec contexte/logs, tests automatiques, règle \"merge only after review\", capacité de rollback."
      }
    },
    "ex_claude_certified_architect_foundations__06_011": {
      "correction": {
        "en": "Architecture overview (what to build)\n- Containerized agent: single isolated Docker image running a controller service (FastAPI) + executor + vision stack + local LLM or secure API client.\n- Chat interface: WebSocket/REST endpoint in container that accepts human instructions (natural language) and returns streaming status & final reports.\n- Execution loop: planner → action executor → visual-sensor → verifier → planner (repeat until done or abort).\n- Visual feedback: periodic screenshots (Playwright for web, PyAutoGUI + Xvfb for desktop) → OpenCV preprocessing → OCR (Tesseract) + object/element detection (YOLO/Detectron or a small ViT) → structured state representation.\n- Action layer: simulated input (keyboard/mouse) via Playwright or PyAutoGUI; supports retries, timeouts, and conditional branching.\n\nConcrete implementation notes\n- Language: Python. Libraries: FastAPI, websockets, Playwright, PyAutoGUI, OpenCV, pytesseract, torch (for vision), Hugging Face or local LLM for planning.\n- Docker run example (secure): docker run --rm -it \\\n  --cap-drop ALL --security-opt no-new-privileges \\\n  --memory=2g --cpus=1 --pids-limit=200 \\\n  --read-only -v /host/artifacts:/artifacts:rw my-agent-image\n- Reports: produce JSON + human-readable markdown containing action trace, timestamps, success/fail flags, input screenshots, diff images, extracted text, and root-cause notes. Store artifacts under /artifacts.\n\nSafety & integration\n- No app code changes: interact via screen/events and network only.\n- Limit side effects: run in sandboxed environment, use least-privilege user, whitelist allowed host connections.\n- Failure modes: enforce max retries, escalation messages to human, and automatic rollback where possible.\n\nHow it meets benefits\n- Automates repetitive tests, surfaces edge cases via vision-based assertions, handles multi-step flows with planning and verification, and integrates without modifying target apps while keeping execution confined to an isolated container.",
        "fr": "Architecture et éléments clés attendus\n- Conteneur isolé : image Docker légère (Debian/Alpine + Chromium/Playwright), exécution non-root, limits (cgroups), seccomp/AppArmor, réseau sortant restreint. Montage de volumes en lecture seule si nécessaire.\n- Interface chat : API WebSocket/REST pour recevoir instructions, conserver contexte, auth (JWT) et permissions par rôle.\n- Orchestrateur d’actions : queue des étapes, mapping instruction ↦ séquence Playwright/CLI, module de planification (retries, timeouts).\n- Traitement visuel : captures écran via headless browser, comparaison image (SSIM/PSNR), OCR (Tesseract) pour texte, DOM diff pour changements structurels, modèles ML pour détection d’UI spécifiques.\n- Rapportage : JSON structuré avec traces (étapes), captures, métriques (SSIM, OCR text, durée), erreurs, suggestions correctives, et checksum pour audit.\n\nExemples concrets\n- Dockerfile minimal :\n  FROM mcr.microsoft.com/playwright:focal\n  RUN useradd -m runner && mkdir /work && chown runner:runner /work\n  USER runner\n  WORKDIR /work\n- Schéma de rapport (extrait) :\n  {\n    \"task_id\": \"...\",\n    \"steps\":[\n      {\"id\":1,\"action\":\"click\",\"#selector\",\"screenshot\":\"s1.png\",\"ssim\":0.98,\"ocr\":\"Connexion\",\"status\":\"ok\",\"duration_ms\":120}\n    ],\n    \"summary\":{\"passed\":9,\"failed\":1,\"suggestions\":[\"augmenter timeout sur étape 4\"]}\n  }\n\nRisques concrets et mitigations\n- Faux positifs visuels (tolérance SSIM, baseline multiple, retry + averaging).\n- Tests fragiles (utiliser DOM stable selectors, fallback strategies).\n- Fuite de données (chiffrement des logs, restreindre mounts, egress control).\n- Exécution de commandes non autorisées (whitelist d’actions, approbation H2I pour actions à risque).\n- Dérive des modèles d’analyse (recalibration périodique, versioning des modèles).\n\nPoints d’évaluation qu’une bonne réponse doit couvrir\n- isolation et sécurité Docker précisées, flux chat concret, méthode de traitement visuel (techniques et seuils), format de rapport reproductible, gestion des erreurs et stratégies de robustesse."
      }
    },
    "ex_claude_certified_architect_foundations__06_012": {
      "correction": {
        "en": "Key points a strong answer should cover — concise, actionable design and operational guidance:\n\n1. Tool usage pattern (concrete workflow)\n- Information gathering: always start by reading relevant files and capturing screenshots of the environment UI/state. Treat these tool outputs as the authoritative, single-source-of-truth.\n- Plan & modify: generate and write change files (with clear diff/patch format). Create automatic backups/versioned copies before any write.\n- Validation: run the test suite immediately after writes. Use both automated unit/integration tests and smoke checks that exercise changed behavior.\n- Iterate: if tests fail, read updated files and screenshots, repair, re-run tests; only commit changes after passing EVAL criteria.\n\n2. “Context is king” practices\n- Never assume world state without tool calls. Re-read files or request fresh screenshots before making decisions.\n- Maintain concise state summaries for prompt context; include file hashes, timestamps and a short diff to limit token usage while preserving accuracy.\n\n3. Evaluation (EVALS)\n- Use deterministic unit tests, end-to-end integration tests, and regression suites. Add human-reviewed checkpoints for ambiguous/high-risk changes.\n- Track coverage, flakiness rate, and test-to-failure mapping.\n\n4. Risk assessment & mitigations\n- Concrete risks: data leakage in screenshots, corrupting production files, flaky tests masking failures, stale context, unauthorized actions.\n- Mitigations: redact sensitive data from screenshots, require backups and atomic writes, sandbox first (staging), use approval gates for high-impact changes, audit logs of all tool calls.\n\n5. When to use this agent design\n- Best for high-value, moderate-risk tasks where automatic checks and rollbacks keep failure costs manageable (CI automation, config updates, refactors). Avoid for tasks with catastrophic failure modes unless strict human oversight is enforced.",
        "fr": "Réponse modèle (points clés et actions concrètes)\n\nPattern d’utilisation des outils\n- Claude Code : utiliser systématiquement pour lire les fichiers sources (collecte), produire des modifications dans des fichiers (écriture) et lancer une suite de tests automatisés (exécution). Exemple de flux : lire README + code, proposer patch, écrire patch dans un fichier .patch ou .py, exécuter pytest.\n- Utilisation de l’ordinateur : capturer des captures d’écran après actions critiques (build, UI, logs) pour validation visuelle automatique et traçabilité.\n- Source d’informations : fonder toutes les décisions sur les sorties d’outils (contenu de fichiers, résultats de tests, captures d’écran) — éviter toute hypothèse non vérifiée par des outils.\n\nPrincipes de conception (actions concrètes)\n- « Le contexte est roi » : avant chaque décision, agréger et résumer les sorties d’outils pertinentes (fichiers, logs, captures). Ne jamais agir sans ce résumé.\n- Évaluation critique (EVALS) : définir et automatiser tests unitaires, tests d’intégration, checks statiques (lint/format), diff de fichiers et comparaison d’écrans (pixel/DOM). Mesures d’acceptation claires (ex : 100% des tests passés, pas de nouvelles erreurs lint).\n- Évaluation des risques : pour tâches à haute valeur, exiger dry-run + revue humaine + sauvegarde/rollback automatique.\n\nRisques concrets et mitigations\n- Écriture destructive : utiliser commits/branches, sauvegardes, opérations idempotentes, dry-run.\n- Fausse confiance sans contexte : toujours valider par tests et captures.\n- Fuites de données sensibles : filtrer/sanitiser les sorties avant stockage.\n- Tests instables : fixer dépendances, mock services externes.\n\nChecklist avant exécution\n1) Lire et résumer fichiers pertinents. 2) Proposer plan/patch. 3) Dry-run + write commit sur branche. 4) Lancer tests + capturer écran/logs. 5) Si OK, merger; sinon revert et itérer."
      }
    }
  },
  "claude_certified_architect_foundations__07": {
    "ex_claude_certified_architect_foundations__07_001": {
      "correction": {
        "en": "Client responsibilities: receive real‑time messages from the MCP server, distinguish log vs progress notifications, render safely in the UI, and handle performance, reconnection, and multiple concurrent tools.\n\nMinimal message contract (example):\n- Log: { type: \"log\", tool_id: \"checksum\", level: \"info|warn|error\", text: \"…\" , ts: 167... }\n- Progress: { type: \"progress\", tool_id: \"checksum\", fraction: 0.42, step: \"Downloading\", details: \"…\" , ts: ... }\n\nWorking ES6 example (WebSocket + DOM updates):\n\nconst ws = new WebSocket(\"wss://mcp.example/ws\");\nconst containers = new Map(); // tool_id -> {logEl, progressEl}\n\nfunction ensureUI(tool_id) {\n  if (!containers.has(tool_id)) {\n    const root = document.createElement(\"div\");\n    root.innerHTML = `\n      <h4>${tool_id}</h4>\n      <div class=\"progress\"><div class=\"bar\" role=\"progressbar\" aria-valuemin=\"0\" aria-valuemax=\"1\"></div></div>\n      <pre class=\"log\"></pre>`;\n    document.querySelector(\"#tools\").appendChild(root);\n    containers.set(tool_id, {\n      progressEl: root.querySelector(\".bar\"),\n      logEl: root.querySelector(\".log\")\n    });\n  }\n  return containers.get(tool_id);\n}\n\nws.addEventListener(\"message\", ev => {\n  let msg;\n  try { msg = JSON.parse(ev.data); } catch { return; }\n  const ui = ensureUI(msg.tool_id || \"global\");\n  if (msg.type === \"log\") {\n    // sanitize simple text to avoid HTML injection\n    const line = document.createElement(\"div\");\n    line.textContent = `[${new Date(msg.ts).toLocaleTimeString()}] ${msg.level.toUpperCase()}: ${msg.text}`;\n    ui.logEl.appendChild(line);\n    ui.logEl.scrollTop = ui.logEl.scrollHeight;\n  } else if (msg.type === \"progress\") {\n    const frac = Math.max(0, Math.min(1, msg.fraction ?? 0));\n    ui.progressEl.style.width = `${frac * 100}%`;\n    ui.progressEl.setAttribute(\"aria-valuenow\", frac.toString());\n    ui.progressEl.title = `${Math.round(frac*100)}% — ${msg.step || \"\"}`;\n  }\n});\n\nRobustness & UX tips:\n- Throttle UI updates (e.g., requestAnimationFrame or 200ms) to avoid jank.\n- Support tool_id so multiple tools render independently.\n- Sanitize text to prevent XSS.\n- Buffer messages during reconnect; implement exponential backoff.\n- Show final completion state and allow log export for debugging.",
        "fr": "Client — implémentation attendue (points clés)\n- Transport : ouvrir une connexion persistante (WebSocket ou SSE) vers le serveur d’exécution pour recevoir en temps réel les événements de type log/progress.\n- Schéma de messages : gérer au minimum {type: \"log\" | \"progress\", id?, level?, message?, percent?, detail?, seq? , ts?}. Utiliser seq/ts pour ordonnancer/dédupliquer.\n- UI : afficher les logs (zone scrollable) et mettre à jour une barre de progression accessible (aria-valuenow, label). Ne bloquer pas le thread UI (DOM updates minimalistes).\n- Résilience : reconnexion exponentielle, heartbeat/ping-pong, buffer côté client et tamponnement de l’affichage pendant reconnexion.\n- Sécurité & hygiène : échapper le HTML dans les logs, filtrer/supprimer données sensibles, limiter taille et fréquence des messages.\n- Performance & limites : throttling d’affichage, agrégation de messages fréquents, backpressure côté serveur.\n- Tests : scénarios de perte de connexion, messages hors-ordre, messages volumineux, messages malformés.\n\nExemple minimal (WebSocket + mise à jour UI)\nconst ws = new WebSocket(\"wss://server/mcp/events\");\nws.onmessage = e => {\n  const m = JSON.parse(e.data);\n  if (m.type === \"log\") {\n    const li = document.createElement(\"div\");\n    li.textContent = `[${m.level||'info'}] ${m.message}`;\n    document.getElementById(\"logs\").appendChild(li);\n  } else if (m.type === \"progress\") {\n    const pb = document.getElementById(\"progress\");\n    pb.value = Math.min(100, Math.max(0, m.percent||0));\n    pb.setAttribute(\"aria-valuenow\", pb.value);\n  }\n};\nws.onclose = () => setTimeout(() => reconnect(), 1000);  // backoff simple\n\nRisques concrets et mitigations\n- Flooding : rate-limit + agrégation.\n- Perte/ordre des messages : seq + ack/retry.\n- Fuite d’infos sensibles : filtrage côté serveur + escape côté client.\n- UI freeze : throttle DOM updates."
      }
    },
    "ex_claude_certified_architect_foundations__07_002": {
      "correction": {
        "en": "Design goals: keep users informed (status, progress, ETA), avoid ambiguity about stalls/failures, and provide real-time, actionable feedback and controls.\n\nConcrete design and behavior\n- Structured status events: emit START, HEARTBEAT, PROGRESS, COMPLETE, FAILED with op_id, timestamp, tool_name, status_code, progress (0–100), message, optional link_to_logs.\n  Example JSON event:\n  {\"op_id\":\"abc123\",\"type\":\"HEARTBEAT\",\"tool\":\"pdf_convert\",\"ts\":\"...\",\"progress\":28,\"msg\":\"Converting page 3 of 12\",\"logs\":\"https://...\"}\n- Heartbeats every 5–15s so UI can show “still alive.” If no heartbeat within timeout T_dead (e.g., 30s) mark as “Possibly stalled.”\n- Progress reporting: stream percent, items_processed/total, ETA. If tool cannot compute percent, show activity indicator + number of retries or elapsed time.\n- Stalled detection & messaging: after thresholds (e.g., 30s slow, 2× expected time), show “This operation is taking longer than expected; still running. Options: Cancel | Wait | Run fallback.” Provide op_id and last known status.\n- Failure handling: show clear cause (error code + short human message), suggested next steps (Retry, Provide different input, Contact support) and link to logs. Auto-suggest fallback tool when available.\n- User controls: Cancel, Pause, Retry, Request verbose logs. Confirm cancel acknowledged by tool (CANCEL_ACK).\n- Telemetry & audit: persist events for debugging; surface link in UI.\n\nOptional UX enhancement (can be omitted): live streaming console or visual progress bar with per-step breakdown and estimated remaining time.",
        "fr": "Messages utilisateur clairs\n- Différencier états : \"En file d'attente\", \"En cours\", \"Bloqué (ressource manquante)\", \"Échec (code 502)\", \"Terminé\".\n- Pour les erreurs, expliquer cause probable + action suggérée (ex. : \"Erreur réseau temporaire — réessayer\" ou \"Clé manquante — renseigner la clé API\").\n- Toujours fournir bouton/action : Réessayer, Annuler, Voir détails, Contacter le support.\n\nDétection et gestion d'appels bloqués/échoués\n- Timeout explicite (p.ex. 30 s) + heartbeat. Si timeout atteint : marquer \"Bloqué\" et proposer actions.\n- Retry contrôlé : backoff exponentiel + max retries, idempotency token pour éviter doublons.\n- Circuit breaker pour éviter surcharge répétée.\n\nVisibilité opérations longues\n- Stades explicites (queued → started → processing step X → completed) et %/ETA si possible.\n- Keep-alive/heartbeat et timestamps pour dernière mise à jour.\n- Stocker état serveur (job id) et fournir lien de suivi/posteriori (job URL, webhook).\n\nRétroaction en temps réel\n- Mécanismes : Server-Sent Events ou WebSocket pour push ; polling raisonnable (p.ex. 1–5 s) si pas de push.\n- Événements structurés (JSON) : {jobId, state, progress, message, lastUpdated}.\n\nSécurité/robustesse & métriques\n- Logs d’audit, codes d’erreur standardisés, monitoring (latence, échecs, taux retries).\n- Tests de chaos pour simuler blocages et vérifier UX.\n\nCritères d’acceptation\n- L’utilisateur comprend l’état en <5s, peut annuler ou réessayer, reçoit updates réguliers et a un moyen de retrouver un job terminé."
      }
    },
    "ex_claude_certified_architect_foundations__07_003": {
      "correction": {
        "en": "Testing steps (concise workflow)\n- Preconditions: Inspector running against a test MCP server; test document(s) available. Note current Inspector build/version.\n- Select tool: choose the target tool from the Inspector UI (e.g., “Document Processor v2”).\n- Input parameters: fill required fields — Document ID (e.g., DOC-12345), optional flags (dryRun=true), auth token if needed.\n- Run: click Run Tool → observe immediate UI feedback.\n- Verify: confirm success/failure message and validate returned payload versus expected.\n\nConcrete verification checklist\n- Success case\n  - UI shows a success banner like “Tool run completed” and execution time.\n  - Response body contains expected fields (status: OK, processedId: DOC-12345).\n  - Server logs show 200/OK and expected processing steps.\n  - DB/state change or downstream event emitted (verify with DB query or event log).\n- Failure cases\n  - Missing param: error “Document ID required”.\n  - Invalid ID: “Document not found (404)”.\n  - Unauthorized: “403 Forbidden”.\n  - Simulated network timeout → timeout error message and retry behavior.\n- Edge/boundary tests: very large document, concurrent runs, malformed JSON.\n\nAutomation & robustness tips\n- Prefer calling the tool’s API (curl/Postman) for repeatable tests:\n  curl -X POST https://inspector.test/run -H \"Authorization: Bearer TOKEN\" -d '{\"tool\":\"docProc\",\"docId\":\"DOC-12345\"}'\n- Build checks to assert status codes, response schema, DB side effects.\n- Because UI may change, keep tests resilient by relying on stable API or semantic selectors and record Inspector build/version.",
        "fr": "Critère attendu — correction modèle (ce que l’étudiant doit fournir)\n\n1) Scénarios de test (au minimum)\n- Cas valide : ID existant (ex. doc-123) → cliquer Run → message de réussite contenant status: \"success\", document_id et un résumé de sortie ; code HTTP 200.  \n- ID absent/obligatoire manquant → message d’erreur explicite \"Missing document ID\" ; code 400.  \n- ID malformé (caractères invalides) → message \"Invalid document ID format\" ; code 422.  \n- ID inexistant → message \"Document not found\" ; code 404.  \n- Permissions insuffisantes → message \"Unauthorized/Forbidden\" ; code 401/403.  \n- Erreur runtime de l’outil → message \"Tool invocation failed\" avec raison (stack/err) ; code 500.  \n- Timeout / latence élevée → message \"Timeout after Xs\" ; comportement : annulation & code 504.  \n- Concurrence : lancer N runs simultanés pour vérifier verrouillage/consistence et consommation ressources.\n\n2) Détails opérationnels à vérifier\n- Champs de saisie acceptent/valident format rapidement (client-side + server-side).  \n- Bouton Run désactivé pendant exécution ; indicateur de progression visible.  \n- Message de sortie contient timestamp, tool_id, document_id, statut et résumé des résultats (ou lien vers logs).  \n- Logs serveur : requête reçue, payload, start/end timestamps, code retour, trace id pour corrélation.  \n- Reproductibilité : fournir steps précis pour reproduire chaque cas.\n\n3) Exemples d’appel automatisé (curl)\ncurl -X POST https://dev.example/api/run-tool -H \"Authorization: Bearer $TOKEN\" -H \"Content-Type: application/json\" -d '{\"tool\":\"analyzer\",\"document_id\":\"doc-123\"}'\nAttendu: HTTP 200 + {\"status\":\"success\",\"document_id\":\"doc-123\",\"output_summary\":\"...\",\"trace_id\":\"abc\"}\n\n4) Risques concrets et mitigations\n- Injection de param → valider et échapper, utiliser whitelist.  \n- Exposition d’erreurs sensibles → ne pas renvoyer stack en prod ; loguer en interne.  \n- Déni de service par exécution concurrente → quotas/limites/concurrency pool.  \n- Fuites de données via messages → filtrer/sanitiser sortie.\n\n4) Critères d’acceptation\n- Tous les scénarios ci‑dessus passent en dev ; logs corrélés et messages utilisateurs clairs ; tests automatisés couvrant erreurs et succès."
      }
    },
    "ex_claude_certified_architect_foundations__07_004": {
      "correction": {
        "en": "Wrap the Claude client in a small session class that manages initialization, reuse, cleanup, and exposes focused methods (e.g., ask_about_file). Provide a CLI entrypoint that extracts text from a PDF and passes it to Claude. Include basic retry, timeout, and error handling.\n\nExample (Python):\n\n```python\nimport argparse\nimport time\nfrom PyPDF2 import PdfReader\n\n# Replace with your real Claude client import\n# from claude_sdk import ClaudeClient\n\ndef pdf_to_text(path):\n    reader = PdfReader(path)\n    return \"\\n\".join(page.extract_text() or \"\" for page in reader.pages)\n\nclass ClaudeSession:\n    def __init__(self, api_key, timeout=30):\n        # self.client = ClaudeClient(api_key=api_key, timeout=timeout)\n        self.api_key = api_key\n        self.timeout = timeout\n        self.client = None\n\n    def __enter__(self):\n        # initialize client here\n        # self.client.connect()\n        self.client = \"CONNECTED_CLIENT_PLACEHOLDER\"\n        return self\n\n    def __exit__(self, exc_type, exc, tb):\n        # clean up: close sockets, flush logs, etc.\n        # self.client.close()\n        self.client = None\n\n    def _call_with_retries(self, prompt, retries=3, backoff=1.0):\n        for i in range(retries):\n            try:\n                # Replace with actual client call, e.g. self.client.generate(prompt=prompt)\n                return f\"[simulated response for prompt length {len(prompt)}]\"\n            except Exception as e:\n                if i == retries - 1:\n                    raise\n                time.sleep(backoff * (2 ** i))\n\n    def ask_about_file(self, filepath, question):\n        doc_text = pdf_to_text(filepath)\n        prompt = f\"Document:\\n{doc_text}\\n\\nQuestion: {question}\\nAnswer concisely.\"\n        return self._call_with_retries(prompt)\n\ndef main():\n    p = argparse.ArgumentParser(description=\"Ask Claude about a PDF\")\n    p.add_argument(\"file\", help=\"path to PDF\")\n    p.add_argument(\"question\", help=\"question about the document\")\n    args = p.parse_args()\n\n    api_key = \"YOUR_API_KEY\"  # or read from env/config\n    with ClaudeSession(api_key) as sess:\n        answer = sess.ask_about_file(args.file, args.question)\n        print(answer)\n\nif __name__ == \"__main__\":\n    main()\n```\n\nBest practices: reuse a single session, support context manager, sanitize/trim long documents (summarize before sending), enforce timeouts, add logging, and limit PII exposure.",
        "fr": "Exemple de correction modèle — encapsulation d’une session client et exécution sécurisée de CLI\n\nCode (Python, synchronique) :\nclass ClaudeToolSession:\n    ALLOWED_TOOLS = {\"pdftotext\", \"identify\"}  # whitelist\n    MAX_OUTPUT_BYTES = 200_000\n\n    def __init__(self, client):\n        self.client = client\n        self._open = False\n\n    def __enter__(self):\n        self.client.open_session()  # méthode hypothétique\n        self._open = True\n        return self\n\n    def __exit__(self, exc_type, exc, tb):\n        self.client.close_session()\n        self._open = False\n\n    def _limit_resources(self):\n        import resource, os\n        resource.setrlimit(resource.RLIMIT_CPU, (5, 5))        # CPU sec\n        resource.setrlimit(resource.RLIMIT_AS, (200_000_000, 200_000_000))  # mem\n\n    def run_cli(self, cmd_list, timeout=10):\n        if cmd_list[0] not in self.ALLOWED_TOOLS:\n            raise ValueError(\"Outil non autorisé\")\n        import subprocess\n        proc = subprocess.run(cmd_list, stdout=subprocess.PIPE, stderr=subprocess.PIPE,\n                              timeout=timeout, preexec_fn=self._limit_resources)\n        out = proc.stdout[:self.MAX_OUTPUT_BYTES].decode(errors=\"replace\")\n        return out, proc.returncode, proc.stderr.decode(errors=\"replace\")\n\n    def ask_claude_with_tool(self, user_question, tool_cmd):\n        out, rc, err = self.run_cli(tool_cmd)\n        prompt = f\"Tool output (rc={rc}):\\n{out}\\n\\nQuestion: {user_question}\"\n        return self.client.send_prompt(prompt)\n\nPoints clés qu’une bonne réponse couvre\n- Gestion de ressource: context manager, fermeture explicite, limites CPU/RAM, timeout.  \n- Sécurité: whitelist d’outils, pas de shell=True, validation des chemins, taille max de sortie, redaction des données sensibles.  \n- Robustesse: gestion des erreurs/retours non nuls, logs auditables, retry backoff sur échecs transient.  \n- Tests et observabilité: tests unitaires pour run_cli, simulation d’outils, métriques/alertes sur erreurs et latence.  \n- Risques concrets listés: exécution de commandes arbitraires, fuite de données sensibles, DoS via sorties volumineuses, corruption de fichiers -> mitiger comme ci‑dessus."
      }
    }
  }
};
